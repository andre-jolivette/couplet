# Couplet — Claude Code Reference

> **What this file is:** This is the context prompt used with [Claude Code](https://claude.ai/code) throughout the development of Couplet. It tells Claude what the codebase does, how it's structured, and what invariants must not be broken. It's committed to the repo as a transparency artifact — this is literally how the app was built.

## On Genre and Assumptions

**Do not use genre labels as design constraints.** The developer's personal library happens to contain a particular style of photography, but Couplet is designed for any single-photographer library. Genre vocabulary ("street photography," "documentary") is likely to pull reasoning in the wrong direction: it suggests a narrower set of subjects, aesthetics, and pairing expectations than the tool actually supports, and it may lead to calibration decisions that are only correct for one kind of library. When assessing scoring, surfacing, or design questions, reason from what is actually in the data and what the pairing theory says — not from assumptions about what "street photographers" typically shoot or value. See decision #79.

## Project Overview
Couplet is a macOS app for photographers. It discovers meaningful image pairs in a photo library — not duplicates or sequential shots, but conceptually resonant connections the photographer might not have noticed.

- **Bundle ID:** `com.toastingmachine.Couplet` (note: UserDefaults keys and the toolbar identifier in the app source still use the legacy `com.toastbrigade.Couplet.*` string prefix — these are plain strings, not the bundle ID, and renaming them would orphan stored preferences)
- **Deployment target:** macOS 14.0
- **Language:** Swift

## Directory Layout
```
_couplet/
├── ConjunctEngine/          # Swift Package — engine, scoring, DB
├── Couplet MacOS App/       # Xcode app project
│   └── Couplet/
├── clip-vit-base-patch32.mlpackage   # CLIP model (gitignored)
├── DECISIONS.md             # Full architectural decisions log
└── CLAUDE.md                # This file
```

The app is sandboxed, so all of these live inside the app container at `~/Library/Containers/com.toastingmachine.Couplet/Data/`:

Database: `~/Library/Containers/com.toastingmachine.Couplet/Data/Library/Application Support/Conjunct/conjunct.db`
Thumbnail cache: `~/Library/Containers/com.toastingmachine.Couplet/Data/Library/Caches/Conjunct/thumbnails/{imageID}.jpg`
Mid-res preview cache: `~/Library/Containers/com.toastingmachine.Couplet/Data/Library/Caches/Conjunct/previews/{imageID}.jpg`

**pairs table — ThematicV2 columns (v15 migration):**
- `thematicV2Score REAL` — derived score (confidence when connected, 0 when not). NULL = not yet scored.
- `thematicV2RelationshipType TEXT` — one of complementary / contrastive / echo / ironic / tonal / none.
- `thematicV2Rationale TEXT` — one-sentence LLM rationale (max 200 chars). NULL when thematicV2Score is NULL.
- `roleHypothesis TEXT` (v17, decision #102) — the role-join's proposed connection, fed to the validation judge. Non-NULL = role candidate (judged via `validate()` not cold `score()`). Set on new role pairs AND existing pairs a join also fires on.

**images table — RoleProfile column (v16 migration, decision #102):**
- `roleProfile TEXT` — JSON `RoleProfile` extracted from the caption by `qwen2.5:14b-instruct`. NULL = not yet extracted (mirrors `caption`; null it on caption change to re-extract).

## Architecture

### Indexing Pipeline (8 phases)
1. **Scan** — FileScanner reads EXIF captureDate, colorProfile from CGImageSource
2. **Duplicate detection** — dHash perceptual hashing, Hamming threshold=6; plus filename-variant rule: images whose filenames are numeric prefix/suffix export variants (`FilenameVariants.areVariants`) AND share an identical EXIF captureDate are unioned into the same duplicate group — catches crop/re-export copies that drift 7–20 Hamming bits past the dHash threshold (decision #94). Groups mark one hero (earliest captureDate, tie → lowest id); non-heroes stay `isActive=1` but are excluded from pair scoring via `loadHeroVectors()` (`WHERE isHero = 1 AND isActive = 1`). Stale `duplicateGroups` rows are deleted each run before groups are re-created.
3. **Thumbnails** — 512px via CGImageSourceCreateThumbnailAtIndex (prevents IOSurface exhaustion)
4. **CLIP extraction** — CLIPCoreMLEngine, 224px input, cosine similarity embeddings
5. **Captioning** — OllamaCaptioningEngine → `qwen2.5vl-caption` via localhost:11434; captions ALL uncaptioned active images each run. 768px input thumbnail (generated fresh — not the 512px cache), greedy decoding (temp 0, top_k 1), prompt v2: physical facts first, direction of action, emotion only from named visible evidence — decisions #50, #97
5.5. **Role extraction** — `OllamaRoleExtractionEngine` → `qwen2.5:14b-instruct` via localhost:11434; extracts a per-image `RoleProfile` (JSON in `images.roleProfile`) from the caption for all active images WHERE roleProfile IS NULL. Text-only (reads the caption — no image decode), sequential, blocking; skipped when all present so normal re-indexes aren't slowed. Feeds the role-join entry-gate candidate generator (Phase 8.5). RoleProfile = subjects, phenomena (source/receiver), claims (sign text normalized to meaning), enacts/subverts (concepts the subject embodies/blocks — **subverts also covers physical confinement: a cage/bars/fence/enclosure that prevents escape → `["escape"]`, a barrier blocking movement → `["movement"]`, #114**), objects (+register real/toy/depicted/costume + category hypernym; **loaded symbols preserved as their own object — flag/cross/rainbow, not the bag/tattoo carrying them, #111**), directed_at, stance — decisions #102, #114, #111. **#111 clauses** (same text-only prompt lever as #114): a sign may map to MORE THAN ONE claim concept when it implies several (`"SEE SOMETHING SAY SOMETHING" → ["danger","watch"]`); a gesture/posture carrying emotional meaning also emits the affect (`hold hands → ["hold hands","tenderness"]`); and loaded cultural/political/religious symbols are kept as objects. Content-coverage validated offline (G14 affect, G15 symbols land; G9 confinement realizes on re-extract); *surfacing* of G10/G14/G15 stays blocked by downstream join/cap gaps (#113 popular-concept cap, join-2 synonym bridge, same-register echo join) — #111 stays open as a partial win. **#114 caveat:** the confinement clause is a prompt change — it only affects newly-extracted profiles, so realizing it on the existing library needs a forced re-extract (null `roleProfile` for affected images, e.g. `WHERE caption LIKE '%cage%' OR '%fence%' OR '%bars%' OR '%enclos%' OR '%pen%'`, then re-index → re-judge). Validated offline: surfaces G9 (escape sign + caged dog, a *rare* claim so the cap admits it); no false-fire on incidental fences (lean/walk/stand) or anchors. See KNOWN_GOOD_PAIRS.md.
6. **Accent color extraction** — backfills `accentHue` / `accentSaturation` for all active images WHERE accentHue IS NULL; uses 256px downsample, 24 hue bins (15°), prominence = area × mean saturation, 5–40% pixel fraction window, saturation floor 0.25; NULL for B&W/neutral images — decision #54
7. **Saliency centroid + gaze extraction** — backfills `weightCentroidX`, `weightCentroidY`, `gazeDirectionX`, and the v20 subject-clarity signals `faceCount` / `humanCount` / `subjectDominance` for all active images WHERE weightCentroidX IS NULL OR gazeDirectionX IS NULL OR faceCount IS NULL (`faceCount IS NULL` is the v20 re-extract sentinel); three Vision requests in one pass: `VNDetectHumanRectanglesRequest` (primary centroid: area-weighted human bounding boxes, confidence ≥ 0.3; also yields `humanCount`), `VNGenerateAttentionBasedSaliencyImageRequest` (centroid fallback for non-human subjects; its heatmap also yields `subjectDominance` — saliency-weighted concentration ∈ [0,1], 1 = one tight subject → 0 = scattered), `VNDetectFaceLandmarksRequest` (gaze: pupil offset within eye contour → [-1=left, +1=right], no fallback — returns nil when pupils not detected; also yields `faceCount`); runs on cached 512px thumbnails — decisions #59, #64, #65, #69, #109
8. **Pair scoring** — PairScorer, four-pool topK (composite top-150 + thematic top-50 + geometric top-5 + aesthetic top-5); two-phase: intra-folder first (blocking), cross-folder in background (cancellable). **Burst near-duplicate guard (decision #103):** before pool selection, `allScores` is filtered through `IndexingEngine.isBurstNearDuplicate` (`captureDate` gap ≤ `kBurstGapSeconds = 300` OR `FilenameVariants.areVariants`) so same-session burst frames that caption alike never enter any pool — the thematic/aesthetic/geometric pools select on *raw* axis score where the temporal penalty (composite-only) can't demote them. Mirrors the ThematicV2 judge's `fetchCandidates` guard. Distinct from #94 dHash duplicate detection (true duplicates) — these are genuinely different frames. — decisions #34, #68, #75, #76, #103
8.5. **Role-join candidate generation** — `IndexingEngine.generateRoleCandidates()` runs in the cross-folder task after scoring (decision #102, cap redesign #113). Loads all hero `RoleProfile`s, runs the deterministic `RoleJoins` rules: **join 1** source/receiver (gaze-excluded; speech/music folded into `sound` via `normalizePhenomenon`; fires only when the **receiver actively strains** — non-empty `subverts` — to sparsify the dense sound graph, #113); **join 2** claim↔enact/subvert (claims un-gated; matched via `rawMatch` token overlap OR a small curated `claimEmbodimentBridge` — currently just `"miss"→"tenderness"` — for semantically-linked but lexically-disjoint pairs, decision #116); **join 3** object real-vs-alt with category whitelist; **join 4** claim↔object (a sign warns of/names a thing present in the other frame — "danger"↔real weapon; via `claimToCategory` + shared-noun token; **distinct from the retired directed-attention "join 4" of #112, which was never built — reuses the priority slot**); join 5 (stance) deferred. **Cap redesign (#113):** each `RoleJoins.Candidate` carries a `specificity` (IDF-style corpus rarity of the matched key, from `CorpusFreq.build`); candidates are admitted **greedily best-first** — sorted `(priority asc, specificity desc, idA, idB)` — under **per-join-type caps** (join1=8, join2=8, join3=5, join4=12), replacing the old fragile first-come-by-id cap of 4. This is effectively non-evicting (a good pair only loses its slot to an even better one for the same image) so altering a join can't silently drop anchors. For each: NEW pairs are INSERTed (`selectedFor='role'`, axis scores via PairScorer); pairs that already exist are UPDATEd to set `roleHypothesis` (clearing any cold ThematicV2 verdict) so a composite/aesthetic pair with a genuine role connection still gets validate()d. The proposed connection is stored in `pairs.roleHypothesis` (v17) and fed to the judge. Surfaces Mode-1/2 third-meaning pairs the four-pool topK never reaches (backlog #95). **Known limit (#113):** popular connecting concepts (sound/smile/bird/danger) can only be *sampled* — specificity is identical within a concept, so a specific intended pair among many equal matches isn't guaranteed (G5 musician+ears unreachable; G8/G11 hub-limited). Reliable for *rare* or rule-sparsified connections. See KNOWN_GOOD_PAIRS.md.
8.6. **Gaze (directed-attention) candidate generation** — `IndexingEngine.generateGazeCandidates()` runs in the cross-folder task right after `generateRoleCandidates` (decision #109, backlog #72). Nominates "call and response" pairs — a figure in one image looking toward something that is the **subject** of the other (a person looking off-frame + the thing the look lands on). **This signal is visual, not textual** (the caption can't name an off-frame target — an earlier caption-based attempt, reverted #108, was 91% "two dogs" internal-gaze), so these are nominated by geometry alone and **never go through the text ThematicV2 judge**. Pure nominator `GazeNominator.nominate`: **looker** = `|gazeDirectionX| ≥ 0.22` AND `faceCount == 1` AND `humanCount ≤ 1` — a clear lateral look from a single unambiguous subject (the human gate is decisive: in a crowd Vision detects only one *face* but many *humans*, so face-count alone lets crowds through; live-measured a 7-person scene as faceCount 1 / humanCount 7); **target** = a hero whose `weightCentroidX` sits toward the **gutter** side so the gaze lands on its subject (coherence ≥ 0.05) AND `subjectDominance ≥ 0.55` (one dominant subject, not a scattered crowd); orientation per the gaze convention (rightward-gazer = imageAID/left, decision #71); strongest-looker-first, caps 4/looker + 3/target for diversity; burst guard. **~87 candidates / 23 lookers** on the dev library (thresholds calibrated against hand-reviewed cases — #109). Inserts NEW pairs only (Phase 1) as `selectedFor='gaze'` with axis scores via PairScorer and `gazeJudgeScore` NULL (pending). **Re-runnable:** clears prior unjudged `selectedFor='gaze'` rows first, so nominator re-tuning regenerates without a full re-index (judged rows preserved). **VISION judge — `GazeVisionBackgroundPass` + `GazeVisionJudge`** (decision #109): a single-image **validity filter**, NOT a quality/aim judge. `qwen2.5vl:7b` at **1024px** (the resolution where it perceives the gaze target — 512px thumbnails rubber-stamp) decides per LOOKER (amortized, ~one call/looker): is this a real off-frame look, or internal gaze (a phone in hand) / facing-away? The model **never sees the target** — the nominator already aimed it geometrically, and the VLM's two-image aim reads were unreliable (a removed step). Valid → `gazeJudgeScore` = geometry-derived clarity (`clarityScore`: normalized gaze-strength + gutter coherence, [0.60, 0.95] — ranks how *clearly* it's a real look, NOT quality, which is the user's call); rejected → 0. Pass drains `selectedFor='gaze' AND gazeJudgeScore IS NULL`, sequential/cancellable, images via an injected `ImageProvider` (app: `EngineController.render1024JPEG` bookmark-resolving; harness: direct read). Wired in the app: `EngineController.startGazeVisionPass()` runs **first** and **chains** `startThematicV2Pass()` on completion — the two Ollama passes are serialized so the `qwen2.5vl`/`14b` models don't thrash swapping (decision #109). **Directed gaze is a GEOMETRIC submode, not a standalone score** (decision #109): for a valid gaze pair, `convertToPairFree`/`convertToPair` set `effectiveGeometric` = the clarity score mapped `[0.60,0.95]→[0.50,0.76]` (the library's geometric ceiling, so it competes fairly with other geometric submodes instead of leapfrogging them) and `geometricSubmode = "directed_gaze"` — mirrors `effectiveThematic` (#82). It flows into composite/axisScore via the geometric weight (0.20, so no over-inflation). The lightbox shows it under the geometric rationale ("Directed gaze" + the judge's one-line reading); no separate gaze sort. **At the model ceiling** (~59 valid / 9 rejected of 68; residuals: subtle internal gaze it can't spatially resolve, occasional phone false-reject — the phone-eyes distinction see-saws, so not worth tuning). See decisions #109, #72, #108.
9. **ThematicV2 background pass** — `ThematicV2BackgroundPass` (actor, ConjunctEngine) runs after each page-0 grid load, judges candidate pairs sequentially via `ThematicScorerV2` using **qwen2.5:14b-instruct** on Ollama (LIMIT 750). **Role candidates first (decision #102):** pairs WHERE `roleHypothesis IS NOT NULL AND thematicV2Score IS NULL` are judged via `validate(captionA:captionB:hypothesis:)` — the judge *confirms or rejects the proposed connection* (it scores 6/7,4/4 this way vs 0/8 cold). **Non-role pool (decisions #100/#95, stratified #115, fills remaining budget):** pure-color accent-echo pairs (`accent_echo AND thematicScore < 0.15 AND geometricScore < 0.30`) excluded; remaining candidates are **stratified by fixed compositeScore bands**, not a straight `compositeScore DESC` sort — that ordering is circular for an unjudged pair (composite falls back to the weak cluster `thematicScore`, the exact signal ThematicV2 exists to correct), which left several genuinely strong Mode-1 pairs unreachable at ranks 89k–108k of ~119k candidates (#90). 9 bands over `[0.10, 0.15, 0.20, 0.25, 0.30, 0.35, 0.40, 0.50]` (narrower where the pool is sparse) plus a dedicated `accent_echo` tail bucket each get a proportional, guaranteed-non-zero slot share (`nonRoleBucketBounds` / `nonRoleBucketWeights` / `nonRoleAccentEchoTailWeight`, tunable constants); within a band, order is `pairID ASC` (not score — score-ordering even within a narrow band still permanently starves whichever pair sorts lowest). Judged via cold `score()`. Writes `thematicV2Score / thematicV2RelationshipType / thematicV2Rationale` to DB. Triggered from `EngineController.startThematicV2Pass()` after `streamPage0Pairs` completes; cancelled when a new index starts. **Near-duplicate guard:** candidates with identical `captureDate` (burst shots) excluded by SQL; filename export variants (via `FilenameVariants.areVariants`) excluded in Swift after the fetch. See decisions #82, #83, #94, #100, #102, #115.

**Concurrency (decision #101):** Phases 1.5 (dHash compute), 3 (thumbnails), 4 (CLIP), 6 (accent), 7 (Vision saliency/gaze), and 8 (pair scoring) all run concurrently via the bounded `mapConcurrent(_:concurrency:work:consume:)` helper on `IndexingEngine` (Phase 4 CLIP uses its own equivalent task group). `work` is `@Sendable`/pure and runs on the task pool; `consume` (DB writes) runs serially on the engine actor. Caps auto-scale: `cpuConcurrency = processorCount` (1.5/3/6), `visionConcurrency = min(processorCount, 6)` (Phase 7), CLIP at `min(cores, 4)`. Pair-scoring parallelism is bit-faithful: `PairScorer.score` is pure, and the four-pool topK uses `orderedByScore(_:_:)` with a stable `(score, imageAID, imageBID)` tiebreaker so the stored set is reproducible regardless of completion order. Phase 1 file-scan colour-profile backfill and the Ollama phases (5 captioning, 9 ThematicV2) remain sequential. See decision #101.

**CLIP model (decision #106):** `clip-vit-base-patch32.mlpackage` is bundled inside the app (at `Couplet MacOS App/Couplet/clip-vit-base-patch32.mlpackage`, gitignored at that path). Xcode 16's `PBXFileSystemSynchronizedRootGroup` auto-discovers, compiles, and bundles it as `clip-vit-base-patch32.mlmodelc`. `EngineController.buildCLIPEngine()` loads from `Bundle.main.url(forResource:withExtension:)` — tries `.mlmodelc` first (compiled bundle), `.mlpackage` fallback (unarchived dev). No security-scoped `ModelBookmark`. `CLIPStatus` is now binary: `healthy` or `broken` (no `notConfigured`). `ConjunctEngine/Modelfile` is also bundled so the setup flow can run `ollama create` without the engine source tree.

**First-run setup flow (decision #106):** `CoupletApp` uses `AppPhase: .checking / .settingUp / .ready`. At launch it runs `OllamaInventory.check()` before showing content; a healthy machine advances directly to `.ready` (flow never appears). `SetupFlowView` is a 3-step rail: (1) Install Ollama (human-action, blue `setupAccent = #3b82f6` accent, ring glyph, "NEEDS YOU" badge), (2) Model downloads (app-driven, gray "AUTOMATIC" badge, real progress from streaming `POST /api/pull`; `qwen2.5vl-caption` has a "Configuring…" sub-phase for `ollama create`), (3) Done (green `setupSuccess = #22c55e`, auto-advancing 6s countdown). `OllamaSetupManager` (`@MainActor ObservableObject`). On advance to main window, `EngineController.checkDependencyHealth()` re-runs as final gate. `SetupFlowView` replaces `ContentView` on first launch; `SetupSheet` (add photo folder) remains in the main app. Blue/green theme colors are exclusive to the setup flow.

**Uninstall / Reset sheet (decision #107):** `UninstallResetSheet` is reached from Settings → "Uninstall / Reset Couplet…". Scope is controlled by two checkboxes — DB + caches (always, non-negotiable) and Ollama models (checked by default). A third section (remove Ollama itself) is shown only when `com.toastbrigade.Couplet.coupletInstalledOllama` UserDefaults flag is true — set by `OllamaSetupManager._check()` when Ollama is missing at first launch. Reset sequence: (1) `DELETE /api/delete` for each model if selected; (2) `EngineController.prepareForReset()` cancels all tasks and nils the GRDB `DatabasePool`; (3) remove `Application Support/Conjunct/conjunct.db`, `Caches/Conjunct/thumbnails/`, `Caches/Conjunct/previews/`; (4) `UserDefaults.standard.removePersistentDomain(forName: bundleID)` clears all app defaults; (5) `NSWorkspace.shared.open(Bundle.main.bundleURL)` + `exit(0)` relaunches. The Ollama-itself section shows two standalone paths: an `NSOpenPanel` picker pre-navigated to the Ollama.app location (`NSWorkspace.shared.urlForApplication(withBundleIdentifier: "ai.ollama.ollama")`) that calls `FileManager.trashItem` on the selected file (reversible), and a copy-to-clipboard Homebrew command. `SettingsView` now takes `engine: EngineController` as an additional parameter (passed from `CoupletApp`).

**Dependency health subsystem (decision #105):** `OllamaInventory.check()` in `ConjunctEngine/Health/DependencyHealth.swift` does a single `GET /api/tags` (5s timeout) to determine reachability and installed model names. `buildDependencyIssues(ollamaInventory:clipStatus:)` is a pure `nonisolated` function that maps the result to `[DependencyIssue]`. Two required models: `qwen2.5vl-caption` (captioning) and `qwen2.5:14b-instruct` (role extraction + ThematicV2 scoring). CLIP status is derived from `EngineController.hasRealCLIP` without a network call (no `ModelBookmark` since #106). `EngineController.checkDependencyHealth()` publishes `@Published var dependencyHealth`; fired after `startEngineBuild()` completes (so Ollama has already warmed up and locally-created models appear in `/api/tags`) and as a fire-and-forget Task at each ThematicV2 pass start. Firing at raw `initialize()` caused a false-positive flash because `qwen2.5vl-caption` (a locally-created model) isn't immediately listed in `/api/tags` at cold start. `DependencyHealthView` in `PairsGridView`'s `.bottomTrailing` overlay renders a warning capsule (hidden when `isHealthy`) that opens a popover listing issues + "Recheck Now" button. No background timer — event-triggered only.

### Three Scoring Axes (PairScorer.swift)
- **Aesthetic (weight 0.40)** — Three-way max: HSL histogram intersection (harmony), LAB palette contrast (contrast), or accent color echo (accent_echo). Echo score = `hueScore × √(satA × satB) × warmRamp` where hueScore ramps ≤10°→1.0, ≤30°→linear, >30°→0, and warmRamp suppresses skin-as-accent pairs: when either image's `accentHue` ∈ [15°, 35°] (the 22.5° extraction bin — skin tones; true reds land at 7.5° and are unaffected), the score is scaled by `clamp((min(satA,satB) − 0.45) / 0.20, 0, 1)` — 0 at minSat ≤ 0.45, 1 at ≥ 0.65 (decision #92). Winning pathway sets `aestheticSubmode`. B&W pairs: harmony and contrast are multiplied by 0.65 — the 8-bin lightness-only comparison is less discriminative than the full 1,152-bin colour histogram, so scores are discounted rather than suppressed; a genuinely tonal-resonant B&W pair (both high-key, both moody) should score ~0.55–0.70; echo unaffected since `accentHue` is nil for B&W. Contrast normalization: `/100` (was `/80`) — avg LAB distance 100 = score 1.0. `colorProfile` must be fetched in `imageMeta` and passed to `PairScorer.score()` for suppression to fire. See decisions #56, #77.
- **Geometric (weight 0.20)** — three-component formula: `structural×0.50 + directional×0.25 + breath×0.25`. Structural = (edge orientation cosine × edgeMult + grid cosine × varMult) / 2; directional = `max(centroidScore, gazeScore)` — centroidScore from `directionalComplementScore()` using per-image `weightCentroidX/Y` (Vision human-detection centroid, decision #64); gazeScore from `gazeConversationScore()` using per-image `gazeDirectionX` (VNDetectFaceLandmarksRequest pupil offset, normalized by dividing by 0.30 to fill [-1,+1] before scoring, decision #70); gaze scored symmetrically — both directions tried, max taken — so pairs aren't missed due to arbitrary DB ID ordering (decision #71); rightward-gazer is always stored as imageAID (left display) for correct diptych orientation; orientationScore from `orientationOppositionScore()` using stored 32-bin edge histogram — dominant bin folded to undirected [0,16), scored as `sin(dist×π/16) × √(normPeakA×normPeakB)` with a hard diagonalness gate: both images must have dominant direction in the 22.5°–67.5° zone (`|sin(undirBin×π/8)| ≥ 0.50`); bins within ~22.5° of horizontal or vertical return 0 (decisions #73, #74); `directional = max(centroidScore, gazeScore, orientationScore)`; breath = `abs(normVarA − normVarB)`. When directional > structural: `geometricSubmode` = `"gaze_conversation"` / `"opposing_diagonals"` / `"directional_complement"` based on which directional signal won. Rationale strings per submode. `geometricSubmode` stored per pair in DB (v13 migration). See decisions #59, #60, #64, #65, #67, #70, #71, #73, #74.
- **Thematic (weight 0.40, boosted to 0.60 when dominant)** — weighted Dice coefficient on ConceptClusters matched from qwen captions; CLIP cosine fallback when no captions. Boost fires when `thematic >= 0.20 && thematic >= max(aesthetic, geometric × 0.8)` — i.e., thematic must be the dominant axis. Before #75, the boost fired at `thematic >= 0.20` unconditionally, penalising strong two-axis pairs. See decision #75. **ThematicV2 override:** at display time, `effectiveThematic = thematicV2Score ?? thematicScore` — the LLM pair-level score replaces the cluster score when available. See decision #82.

### ConceptClusters
30 semantic clusters with three tiers:
- **Tier 1.0 (emotional/dramatic):** grief_sorrow, vulnerability_exposure, isolation_solitude, ritual_ceremony, tension_conflict, tenderness_care, devotion_belief, power_dominance, sensory_overwhelm, transformation_change, uncanny_ordinary, economic_precarity, solitude_in_crowd, domestic_intimacy
- **Tier 0.75 (contextual):** skilled_performance, sound_music, labor_effort, stillness_rest, waiting_anticipation, movement_energy, looking_watching, confinement_freedom, youth_age, joy_celebration, humor_absurdity
- **Tier 0.2 (ambient):** urban_street, nature_landscape, community_gathering, animal_presence, bodily_gesture

`bodily_gesture` was demoted 0.75 → 0.2 in #120 (fires on 94% of the corpus — near-universal bodily description, expected coincidence not resonance; à la #47's `animal_presence`). Keyword lists were exhaustively de-collided in #120 (corpus-wide firings cut ~44%; e.g. `sign`/`cross` removed from devotion_belief, the look/gaze vocabulary reallocated from waiting_anticipation to looking_watching) — see decision #120.

Five clusters use two-signal gating (require ≥1 keyword from each of two vocabulary groups): `humor_absurdity`, `uncanny_ordinary`, `solitude_in_crowd`, `domestic_intimacy`, `animal_presence`.

## Pairing Theory — Design Intent and Known Gaps

Full theory in PAIRING_THEORY.md. This section is the operational summary for implementation work.

### Three Pairing Modes
- **Mode 1 — Semantic arc:** Two images occupy complementary positions in the same human experience arc. Neither image alone names it; together they make it visible. Third meaning is an idea that can be articulated. *Primary carrier: thematic axis.*
- **Mode 2 — Slant rhyme:** Two images share one specific formal property (a color, a shape, a quality of light) while diverging on everything else. Third meaning is a perception felt before it's named. *Primary carrier: aesthetic (accent echo) and geometric axes.*
- **Mode 3 — Ambient existential register:** Shared quality of attention to the fragile and ordinary (Soth's towels). Out of scope currently.

### Third Meaning Test
A pair passes if it creates a meaning that exists in neither image alone. It fails if the best description of the pair just restates what each image independently contains. Two dogs = fail ("there are two dogs"). Musician + ears-woman = pass (sound as a force in the city — given by one, hungered for by another).

### Per-Axis Design Intent vs. Current State

**Thematic** — *should measure:* complementary positions in the same human experience arc. *Currently measures:* weighted Dice on cluster vocabulary — rewards shared clusters, not relational position. *Architectural ceiling reached:* can't distinguish source from receiver of the same phenomenon. Musician and ears-woman both fire `sound_music` + `sensory_overwhelm` via the word "ear" → Dice > ambient floor → axis bonus guard fires → pair below thematic topK cutoff. Caption redesign (#50) is the correct next lever. Do not add more clusters expecting this to improve.

**Aesthetic** — *should measure:* harmony (same visual world), complement (productive tonal contrast), or echo (one specific formal property rhyming across dissimilar images). *Currently measures:* three-way max of HSL histogram intersection (harmony), LAB palette contrast (complement), and accent hue echo (#56). *Missing:* light quality (deferred, hardest to compute reliably); tonal weight complementarity for breath pairs (#55, adds ~0.12 max composite lift when implemented).

**Geometric** — *should measure:* structural rhyme, directional complement (figures spatially facing each other / strong lines leading from one image to the next), or breath (dense image paired with spare/open image). *Currently measures:* three-component formula `structural×0.50 + directional×0.25 + breath×0.25` — structural via edge/grid cosine similarity, directional via `max(centroidScore, gazeScore)` where centroidScore uses Vision human-detection centroid opposition (`weightCentroidX/Y`, decision #64) and gazeScore uses face landmark pupil direction (`gazeDirectionX`, decision #65), breath via tonal weight differential. Geometric topK pool (top-5 per image with submode variety bonus) gives gaze_conversation and directional_complement pairs an escape hatch from composite dominance (#68). *Missing:* uniform area ratio for genuine sparseness detection; light quality.

### Known Scoring Failures
- **Musician + ears-woman:** Both captions share `sound_music` + `sensory_overwhelm` vocabulary → Dice > ambient floor → axis bonus guard fires. Pair scores via Dice but ranks below thematic topK cutoff. Only caption redesign (#50) fixes this.
- **Breath pairs:** Geometric differential ~0.07 composite max is below surfacing threshold. Aesthetic tonal weight complementarity (#55) needed to close the gap.
- **Mode 2 beyond color echo:** Light quality echo and gestural/energetic echo are unmeasured anywhere in the system.

## Known Gotchas

**Never run Ollama with a quantized KV cache (`OLLAMA_KV_CACHE_TYPE=q8_0`)** — it corrupts qwen2.5vl generation into systematic article/function-word garbling ("a aftermath", "He phone, held in his right hand, is a only object") even at greedy decoding. At the time of the bug (decision #97), llama3.2 was the ThematicV2 model and it tolerated q8_0, so the breakage was silent — captions degraded while ThematicV2 appeared to work. ThematicV2 now uses `qwen2.5:14b-instruct` (since #102); the q8_0 impact on that model is untested, but keep the plist clean regardless. The Homebrew service plist (`/opt/homebrew/opt/ollama/homebrew.mxcl.ollama.plist`) had this set; `OLLAMA_FLASH_ATTENTION=1` alone is fine and can stay. If garbled articles reappear in captions, check `ps eww $(pgrep ollama) | tr ' ' '\n' | grep OLLAMA` first. See decision #97.

**B&W aesthetic discount requires colorProfile in imageMeta** — `PairScorer.aestheticScore()` discounts harmony and contrast scores (×0.65) for B&W pairs via `colorProfileA/B` params. These are fetched from the `images` table into the `imageMeta` tuple in both `runIntraFolderScoring` and `runCrossFolderScoring`. If either SQL SELECT or `imageMeta` typealias is modified, keep `colorProfile` in the query. Without it, B&W pairs will revert to inflated aesthetic scores. See decision #77.

**Filename-variant matching must be asymmetric — use FilenameVariants, never normalize-both-and-compare** — stripping `^\d+-` from both filenames silently fails when the base name itself starts with digits: `00-20250504-_R017085.jpg` loses only `00-`, but the original `20250504-_R017085.jpg` loses its *date* prefix, so the two normalized names never match. This bug made `sharesBaseName` (PairScorer) and `normalizedBaseName` (ThematicV2BackgroundPass) no-ops for this library's `NN-DATE-NAME` naming — crop-duplicate pairs entered the DB through the aesthetic topK pool with harmony ≈ 0.94. All filename-variant checks now go through `FilenameVariants.areVariants()` (ConjunctEngine/Analysis), which compares each name's stripped forms against the *raw* other name. Do not reintroduce local normalize-both helpers. See decision #94.

**GRDB returns INTEGER columns as Int64, not Int** — `as? Int` silently returns nil on GRDB Row even on 64-bit macOS. Use the `intCol()` helper or explicit coercion: `(row["col"] as? Int) ?? (row["col"] as? Int64).map(Int.init) ?? 0`. Affects COUNT(*) results too.

**captureDate stored as INTEGER in SQLite** — GRDB's `as? Double` returns nil for Int64 column values. Explicit coercion required in `fetchRepresentativePairs` and `fetchPairs`: `(row["captureDateA"] as? Double) ?? (row["captureDateA"] as? Int64).map { Double($0) }`. Without this, temporal penalty never fires (all dates arrive as nil → penalty=1.0). See decision #26.

**Titlebar (NSTitlebarBackgroundView)** — the frosted-glass effect comes from a private `NSTitlebarBackgroundView` at subview index 0 of `NSTitlebarView`, NOT from an NSVisualEffectView. The fix is `SolidTitlebarCover` (a plain CALayer-backed NSView) inserted at subview index 1 in `CoupletTheme.swift → installSolidTitlebar`. Re-applies from `didBecomeKeyNotification` and `didBecomeMainNotification`. Read decision #30 in full before touching any titlebar rendering — prior approaches (targeting VEVs, setting layer.backgroundColor on NSThemeFrame) do not work.

**Tools bar (filter controls in titlebar)** — three approaches that do NOT work: (1) SwiftUI `.toolbar { ToolbarItem }` — macOS 15 applies per-item liquid glass capsules to NSToolbarItem containers that cannot be suppressed from inside; (2) `NSTitlebarAccessoryViewController` with `.bottom` — creates a visually separate second row with dead space between it and the traffic-light band; (3) `.windowToolbarStyle(.unified(showsTitle: true))` on the scene — causes the window title to reappear during state transitions. **What works:** empty `NSToolbar` (no items) with `window.toolbarStyle = .unified` expands `NSTitlebarView` to ~50px without producing NSToolbarItem containers; a `PassthroughHostingView<AnyView>` is inserted directly into `NSTitlebarView` at `leadingAnchor + 192` and updated each render cycle via `WindowConfigurator.updateNSView`. See decision #36 for full rationale and known limitations.

**weightedDice ambient floor** — `weightedDice()` in ConceptClusters requires ≥1 cluster in the shared intersection with weight ≥ 0.75; pairs sharing only ambient-tier clusters (urban_street/nature_landscape/community_gathering/animal_presence/bodily_gesture, all weight 0.2 — bodily_gesture demoted in #120) return `kAmbientFloor = 0.1`. The gate is a per-cluster threshold (`contains(where: weight ≥ 0.75)`), NOT a sum of ambient weights, so adding ambient clusters is safe; the ≤ 0.24 guidance below is a conceptual guard, not a hard sum constraint. See decisions #29, #120.

**Meaningful asymmetry gate requires weight ≥ 0.75 unique clusters per side** — `PairScorer.swift` filters `onlyA` and `onlyB` to clusters with weight ≥ 0.75 before checking asymmetry. A pair where one image uniquely has `urban_street` (weight 0.2) and the other has nothing unique at the meaningful tier fails the gate and returns ambient floor, even if both images have rich meaningful clusters they share. Changed in #49 from the original `!onlyA.isEmpty && !onlyB.isEmpty` which allowed ambient-only asymmetry to pass. Axis pairs are exempt from this gate — they fire via different clusters on each side by structural definition. See decision #49.

**Axis bonus only fires at ambient floor** — `ConceptClusters.axisPairs` defines 9 cluster-opposition relationships that reward complementary pairs (source ↔ receiver of the same phenomenon). The bonus fires ONLY when `clusterScore ≤ 0.10` (ambient floor) — the guard `if saturated || clusterScore > 0.10 { axisBonus = 0 }` prevents +0.35 additive inflation on pairs that already score above ambient floor via Dice. A pair like musician + ears-cupping woman scores via Dice rather than the axis bonus if both captions happen to share `sound_music` or `sensory_overwhelm` vocabulary — the axis bonus won't additionally reward them. See decision #48.

**Temporal penalty must be replayed in convertToPair — two implementations must stay in sync** — `EngineController.convertToPair` (used in `fetchPairs` for the anchor strip) and `convertToPairFree` in `PairHelpers.swift` (used in the grid via `streamPage0Pairs`) are separate implementations of the same formula. Both must use: `≤30s → 0.40 | ≤60s → 0.55 | ≤120s → 0.75 | ≤300s → 0.90 | >300s → 1.0`. If you update one, update the other — divergence causes the anchor strip to show different composite scores than the grid for the same pairs. Decision #85 originally updated only `convertToPairFree`; decision #86 synced `convertToPair`. See decisions #26, #85, #86.

**Anchor strip DB fetch must not LIMIT below the image's pair count** — `EngineController.fetchPairs` (the only live call site always passes `anchorImageID`, #110) uses `dbLimit = 500` as its sole cap — no further per-image cap or display trim is applied. Do not lower this below the expected hub-image pair count (407 in the test library); 500 gives headroom without hydrating an unbounded list. See decisions #86, #110.

**Orphan pair sweep runs before per-run DELETE** — `IndexingEngine.reindex()` sweeps pairs where either image `isActive=0` before the scoped per-run DELETE+INSERT. Preserve this ordering if touching `reindex()`. See decision #19.

**Two-phase scoring scope** — Phase 1 (blocking) scores batch × batch only; Phase 2 (background) scores batch × all-other-active. DELETE scoping is separate for each phase. `crossFolderTask` is cancelled before any new `index()` call. See decision #34.

**A full re-index wipes `thematicV2Score`/`thematicV2RelationshipType`/`thematicV2Rationale`/`gazeJudgeScore`/`gazeJudgeRationale` for every rewritten pair — this is NOT preserved across re-index** — the per-run DELETE+INSERT (decisions #19/#34 above) applies to every pair whose endpoints are in the re-indexed batch, judged or not; `PairRecord` has no fields for the three `thematicV2*` columns at all, so a freshly-inserted row cannot carry them forward even in principle, and they revert to their SQLite default (NULL). `roleHypothesis` is regenerated (not preserved) by `generateRoleCandidates()`'s guarded `UPDATE`, which only fires when its deterministic join recomputes a matching hypothesis — it does not restore a prior `thematicV2Score`. `generateGazeCandidates()`'s "(judged rows preserved)" comment is true only when that function runs in isolation (no preceding full re-index in the same session); during an actual full re-index, Phase 4's earlier DELETE has already destroyed any previously-judged gaze row before that function's own narrower unjudged-only DELETE even runs. None of this is triggered by cluster-keyword changes specifically — `ThematicScorerV2` reads only the two captions (never `clusterScore`/`matchedClusters`), so a cluster-vocabulary-only edit (like #96's four fixes) does not itself invalidate any existing verdict; only actually re-indexing a folder does, and it does so unconditionally regardless of what changed. See decision "Pair-row lifecycle during re-index" (2026-07-08) for the full trace and a concrete dev-DB-copy verification.

**`mapConcurrent` parallel phases must never write to the DB from `work`** — the parallel indexing phases (1.5/3/6/7/8) run `work` on the concurrent task pool and `consume` serially on the engine actor. GRDB has a single writer: all `db.write` calls belong in `consume`, never in `work`. `work` must be pure/`Sendable` (no actor state, no DB). If you add a new parallel phase or move a DB write into `work`, you reintroduce concurrent-writer contention. The pure analysis funcs are static for this reason (`writeThumbnailFile`, `SaliencyAnalyser.analyse`, `ColourAnalyser.extractAccentColor`, `PerceptualHasher.dHash`, `PairScorer.score`). See decision #101.

**Parallel pair scoring requires the `orderedByScore` tiebreaker** — `PairScorer.score` is pure, so parallel evaluation is bit-faithful, but the four-pool topK selection uses unstable `sorted` + `prefix`. Without a deterministic tiebreaker the stored pair set would shuffle at *exact* score ties depending on parallel completion order. `orderedByScore(_:_:)` sorts by `(score desc, imageAID, imageBID)` in all four pools of BOTH scoring blocks (intra + cross-folder) — keep it on any new pool sort. (Note: a small run-to-run pair-count drift remains from Vision saliency/gaze ANE float nondeterminism — pre-existing, present on `main` too, unrelated to parallelism.) See decision #101.

**Every `PairSortOrder.dbColumn` case needs a matching expression index, or the grid load silently reverts to a full-table sort** — migration `v21_pairSortIndexes` (`DatabaseManager.swift`, decision #117) adds one SQLite expression index per sort-order formula in `AppModels.swift` (`idx_pairs_axis`, `idx_pairs_compositeEffective`, `idx_pairs_thematicEffective`, `idx_pairs_geometricScore`, `idx_pairs_aestheticScore`) so `streamRepresentativePairs`'s `ORDER BY \(sortColumn) DESC` can be satisfied by an index scan instead of `USE TEMP B-TREE FOR ORDER BY` (which materializes and sorts the *entire* matching row set before row 1 can stream — this was true for every current sort order, including the default "Best"/axis, before #117). SQLite matches an expression index only if the query's ORDER BY text is structurally identical to the indexed expression (table alias differences are fine; wording differences are not). If `PairSortOrder.dbColumn` gains a new case or an existing formula changes, add/update the matching `CREATE INDEX` in a new migration or that sort order quietly falls back to the pre-#117 full-table-scan behavior — verify with `EXPLAIN QUERY PLAN` (expect `SCAN p USING INDEX ...`, not `USE TEMP B-TREE`). See decision #117.

**Double-onChange race in PairsGridView** — `LibraryViewModel.selectFolder` and `selectCollection` each set *both* `selectedFolderID` and `selectedCollectionID` in the same synchronous pass. If PairsGridView has separate `onChange(of: selectedFolderID)` and `onChange(of: selectedCollectionID)` handlers, both fire within milliseconds with different settled-state snapshots, launching two competing `loadPairs` calls. Fix: merge both handlers to call a single `reloadPairs()` helper that reads the *settled* `currentFolderID` / `currentCollectionID` published values (not the argument to `onChange`). Any future state mutations that touch both properties will hit the same race if separate observers are used. See decision #39.

**QueryService.fetchRepresentativePairs and fetchImagePairCounts are nonisolated** — both methods are `nonisolated` synchronous functions. Calling them directly from `@MainActor` context (e.g. inside `EngineController`) runs them synchronously on the main thread and blocks the UI for the duration of the SQL query. Always call them from a `Task.detached` block. The existing call site in `EngineController.fetchRepresentativePairs` already does this correctly — any new call sites must follow the same pattern. See decision #40.

**PairHelpers.swift functions must stay nonisolated** — `adjustedGeometricFree`, `convertToPairFree`, `pairSortComparator`, `applyCap2Free`, `applyPass2Free` are free functions in `PairHelpers.swift` marked `nonisolated` to prevent Swift 6.3 from inferring `@MainActor` on them. The inference chain is: `DisplayPair.colorA/colorB` use `NSColor` (which is `@MainActor` in the macOS 14+ SDK) → Swift infers `@MainActor` on the `DisplayPair.init` → infers it on any function that constructs a `DisplayPair`. The `nonisolated` keyword on the helpers and `nonisolated init` on `DisplayPair` together break this chain. Do not remove these annotations. See decision #41.

**streamPage0Pairs populates representativePairsCache on completion** — `EngineController.streamPage0Pairs` runs DB fetching and cap-2 in a `Task.detached`, yields accepted batches through `AsyncStream<[DisplayPair]>`, then updates `representativePairsCache` via `await MainActor.run` (with generation check) after the last batch. `PairsGridViewModel.loadPairs` consumes the stream and appends batches directly to `allPairs`. `loadMorePairs` still slices from the cache as before. Do not skip the `MainActor.run` cache update at the end of the inner detached task or `loadMorePairs` will return empty results. See decision #41.

**ThematicScorerV2.score() throws on connection error, returns nil on LLM parse failure** — `score()` is `async throws`. A throw indicates a network or HTTP error (Ollama unreachable, non-200 status) and the caller (`ThematicV2BackgroundPass.run()`) increments the consecutive-connection-failure counter toward the abort. A nil return means the LLM produced unparseable JSON (unescaped quotes in rationale when the model quotes sign/text content verbatim, or a truncated response). Nil resets the connection counter and skips the pair without aborting. Do not collapse these two paths back to a single `return nil` — that would cause parse failures to abort the pass after 3 consecutive malformed responses. See decision #89.

**startThematicV2Pass must set isThematicV2Running = true synchronously** — `isThematicV2Running = true` is assigned directly in `startThematicV2Pass()` (which runs on `@MainActor`) before `Task.detached` is called, NOT inside the task body. If the assignment were moved into the task's first `await MainActor.run`, a race opens: the `.background`-priority task may not execute before the next `onChange` handler fires and calls `startThematicV2Pass()` again. Both calls would see `isThematicV2Running == false`, the second cancels the first, and the first task's deferred cleanup sets the flag back to `false` while the second task is still initialising — silently killing the pass. Keep `isThematicV2Running = true` (and the `thematicV2Scored`/`thematicV2Total` resets) as synchronous assignments. See decision #88.

**Role pipeline requires `qwen2.5:14b-instruct` pulled in Ollama** — both role extraction (`OllamaRoleExtractionEngine`, Phase 5.5) and the validation judge (`ThematicScorerV2`, now default model) call `qwen2.5:14b-instruct` via localhost:11434. Run `ollama pull qwen2.5:14b-instruct`. If absent, extraction throws (roleProfiles stay NULL → no role candidates) and the judge falls back to errors. The `#97` KV-cache gotcha (`OLLAMA_KV_CACHE_TYPE=q8_0`) was a qwen2.5**vl** issue; the text qwen2.5 path hasn't shown it, but keep the plist clean regardless. See decision #102.

**Role candidates judged via `validate()`, keyed on `roleHypothesis` not `selectedFor`** — a role candidate is any pair with `roleHypothesis IS NOT NULL` (set by `generateRoleCandidates` on both new pairs AND existing composite/aesthetic pairs a join fires on). `ThematicV2BackgroundPass` judges these FIRST via `validate(hypothesis:)` (confirm/reject the proposed connection — works locally where cold `score()` does not), then fills the budget with the non-role pool via cold `score()`. Do NOT switch the role-candidate predicate back to `selectedFor='role'` — that misses the tagged existing pairs (the bug live-verification caught: 3 of 4 golden pairs were already in the table as composite/aesthetic and would otherwise be cold-judged). When tagging an existing pair, `generateRoleCandidates` clears its prior ThematicV2 verdict so it gets re-judged via validate — but ONLY when the hypothesis is new/changed (`WHERE roleHypothesis IS NULL OR != ?`); do not drop that guard or every re-index re-nulls + re-judges the same surviving pairs (budget thrash). Other invariants to preserve: Phase 3.55 skips writing empty profiles (`RoleProfile.isEmpty`) so a Mock/no-op engine can't poison the `roleProfile IS NULL` sentinel; the judge reserves a non-role floor (`roleBudget` < `budget`) so the #100 pool isn't starved; the V2 pass is gated on `ThematicScorerV2.isAvailable()`; captioning nulls `roleProfile` alongside the caption write; and a rejected role hypothesis (`thematicV2Score == 0 && roleHypothesis != nil`) falls back to cluster `thematicScore` in `effectiveThematic` (both PairHelpers and EngineController) and the DB Best-sort ORDER BY, so a rejected role connection never demotes an otherwise-good pair to 0. The app (`EngineController`) must inject `OllamaRoleExtractionEngine` — the engine default is the Mock (for tests/bench). See decision #102.

**Never pick a single display value from a `Set` via `.first` — it's stable within a process but not across app launches** — `Set<String>`'s iteration order depends on Swift's hash seed, which is randomized once per process launch and held fixed for that run. Confirmed empirically (decision #118): the same input produced identical output across 2000 repeated calls in one process, but a *different* result on each of 5 separate `swift test` launches. Any UI text built by picking a representative element from a `Set` (e.g. a cluster label for a rationale sentence) must use a content-based deterministic tie-break — `ConceptClusters.representativeCluster(in:)` sorts by weight desc then name asc — never bare `.first`/`.randomElement()` on a `Set`. This bit the lightbox's live-computed `thematicRationale` (fixed in #118); the same pattern in `PairScorer.swift`'s two stored-at-index-time rationale builders (`meaningfulShared.first` in `score()`, `shared.first!` in `captionRationale`, decision #48) was the deferred sibling — **now fixed in #120**, both call sites use `representativeCluster`. Keep any new representative-cluster pick on `representativeCluster`, never bare `.first`.

## Workflow

When completing any feature or fix, follow these steps in order before considering the task done:

1. **Branch** — work on a branch named `[type]/[decision-id]-[short-description]` (e.g. `feat/54-accent-color`, `fix/26-temporal-penalty`). Create it at the start, not the end.
2. **Code complete** — implement and verify the change works.
3. **Docs** — update `CLAUDE.md` and `DECISIONS.md` to reflect the change. Check both files deliberately: not just the obvious section, but anything touched by the change (Known Gotchas, Open Backlog, Architecture overview). When closing a backlog item in `DECISIONS.md`, the status update must include the date closed in ISO format and a one-line verification note confirming what was checked (e.g. "DB query confirmed", "unit tests green", "validated against golden pairs"). Example Notes field close-out: `Closed 2026-06-23. [existing notes] Verified: [what was checked to confirm completion].` Existing done items do not need to be retrofitted — this applies to items closed from this session forward.
4. **Commit** — use `#ID` prefix convention. Doc updates go in the same commit or a follow-up commit on the same branch. Do not leave doc updates uncommitted.
5. **Merge check** — confirm all commits are on the branch and nothing is dangling or uncommitted.
6. **PR ready** — confirm the branch is pushed. State the suggested PR title and a one-paragraph description summarising what changed and why.
7. **Branch cleanup** — after the PR is merged, delete the remote branch.

Do not report the task as complete until all 7 steps are done.

## Open Backlog

| # | Title | Notes |
|---|-------|-------|
| 7 | Settings that require re-index | dHash threshold + CLIP similarity ceiling affect data written to DB; UI should distinguish these from cheap runtime settings. See decision #7. |
| 11 | FileAccessCoordinator centralization | Centralise security-scoped bookmark management; detect invalid bookmarks; clean up stale entries on `removeFolder`; handle offline volumes. See decision #11. |
| 14 | Continue geometric scorer tuning | Step distinctiveness multiplier exponent down from 0.4; add multiplier-strength slider; evaluate normalization anchors. See decision #14. |
| 16 | Center-cell discount | Slider + Apply button for peripheral composition cell weighting. Requires re-score (not full re-index). See decision #16. |
| 23 | Revisit dot badge on pair grid tiles | Recalibrate threshold + improve visual legibility after topK and scoring changes settle. See decision #23. |
| 35 | "Find Pairs For…" — explore or retire | Button removed (no implementation); evaluate vs. existing anchor/filmstrip flow before building. See decision #35. |
| 43 | Layout recursion warning | Console: "-layoutSubtreeIfNeeded on a view which is already being laid out" — once per launch, likely PassthroughHostingView insertion timing. See decision #43. |
| 55 | Breath pairs — aesthetic axis tonal weight complementarity | Geometric axis contributes `abs(normVarA − normVarB)` differential but max composite lift is ~0.07 — not enough to surface breath pairs reliably. Next step: add tonal weight complementarity as Component 3 of the aesthetic axis (weight 0.30 within aesthetic, ~0.12 max composite lift). Prerequisite: visually confirm `20250426-_R016343.jpg` and `20210313-L1001045.jpg` as genuine open/spare breath-pair candidates. See decision #55 and PAIRING_THEORY.md §Aesthetic axis redesign. |
| 57 | Accent echo — desaturated reds under shadow | Under low-light or shadow, photographic reds maintain hue but lose saturation. `√(satA × satB)` penalises these even when the pair is a genuine echo. Not solvable at pixel-statistics level without scene context. Backlog until a test case justifies the complexity. |
| 58 | Accent echo — ambient hue exclusion (foliage green, sky blue) | Reassessed after #92 (2026-06-11): stays backlogged. Sky blue needs no exclusion (prominence weighting already handles washed-out sky); green bins hold only ~30 images — no failing pairs to calibrate against. Revisit only if a different library surfaces spurious green echo pairs. See decision #92. |
| 61 | Directional complement — dominant orientation opposition | Use the existing 32-bin `edgeOrientation` histogram (stored per image in `featureVectors`). Extract dominant bin angle per image; score opposition as angular distance mapped so 180° → 1.0. No new data or re-index. Would detect opposing diagonals (DSCF3269-positive). |
| 62 | Directional complement — regional orientation histogram | Per-cell dominant edge direction from an 8×8 grid. Would capture "subject on left facing right" as rightward edges in left cells. Requires new extraction phase + DB schema. Long-term. |
| 80 | B&W aesthetic tuning — lighter and lower-contrast pairs | High-contrast B&W pairs surface well after #77 calibration. Lower-contrast and high-key B&W pairs are under-represented — 8-bin lightness histogram may not distinguish tonal styles within the low-contrast range. Geometric axis can also produce false-positive B&W pairs via structural edge similarity (e.g. tree silhouette + scooter chaos) — noted 2026-06-09. Revisit when the library includes more varied B&W work or specific failing pairs can be identified. |
| 84 | ThematicV2BackgroundPass near-duplicate guard — validate against other libraries | Selection-side gap closed via #103. Judge-side guard still library-specific: `normalizedBaseName()` strips leading numeric prefixes — will silently miss variants in other naming conventions (`foo_edit.jpg`, `foo-crop.jpg`, `foo (1).jpg`). When testing against a second library, check the thematic-sorted grid for near-duplicate pairs near the top. If they slip through, consider multi-signal conjunction guard (`aestheticScore > 0.90 AND geometricScore > 0.85 AND thematicScore > 0.90`) as naming-agnostic fallback. See decisions #83, #103. |
| 93 | ThematicV2 performance — investigate batching and parallelism | Sequential scoring, ~3–4s/call warm, ~45–50 min for 750 pairs. **Candidate-ordering axis closed via #115 (2026-07-07)** — the non-role pool's circular `compositeScore DESC` ordering (sharpened to a confirmed finding by #90: 4 golden Mode-1 pairs unreachable at ranks 89k–108k of ~119k) was replaced with fixed-band stratified sampling; see #115. **Concurrency + KV-cache axes investigated and closed 2026-07-07 (see decision #93 "ThematicV2 throughput" entry):** `OLLAMA_NUM_PARALLEL` benchmarked on the dev M1 Max (real `qwen2.5:14b-instruct` calls, live-DB captions) — flat-to-worse than sequential (24.4s vs 25.7s for 10 calls), confirming #101's original caution that a single GPU is compute-bound, not queue-bound; **not adopted**. KV-cache reuse for the constant system prompt was checked and found to **already happen automatically** in Ollama (measured ~2× latency difference between same-system-prompt and varying-system-prompt calls; confirmed by Ollama's own release notes on prompt-cache/KV-reuse improvements) — and `ThematicV2BackgroundPass`'s existing role-then-non-role candidate ordering already keeps the system prompt constant within each block, so no code change was needed. Remaining open axes: (1) streaming mode for incremental JSON parsing; (2) model quantization Q4 vs Q8 quality/speed tradeoff (needs a quality-regression pass, not just a speed benchmark). |
| 95 | ThematicV2 candidate selection — remaining golden pair gaps | Entry gate addressed via #102 role pipeline (4/8 golden pairs now surface end-to-end). Remaining gaps: smile+womanS hypothesis specificity; sunscreen+see needs join-4; smile+hoop extraction quality. Caption-embedding kNN was falsified — similarity measures aboutness, not role. See decisions #102, #99. |
| 104 | Crop-duplicate defense-in-depth in #94 detectDuplicates | Backlog (not currently needed). If a crop/re-export ever leaks past #94's dHash + `FilenameVariants` grouping, fix in `detectDuplicates` using CLIP cosine ≥ ~0.96 + same `captureDate` — NOT the #84 conjunction guard (geometric ceiling ~0.76, never fires). See decision #104. |
| 111 | Role-join entry-gate quality — claims / receiver coverage | **Partial win via #111 (2026-07-07).** Extraction-prompt pass added three clauses (multi-concept claims, affect on gestures, loaded-symbol preservation) — see decision #111. **Content coverage delivered & validated offline** (live production captions + `RoleJoins.join` + the real `generateRoleCandidates` cap): G14 → 587 now `["hold hands","tenderness"]`; G15 → 61 `{flag}`, 564 `{cross}`+`{rainbow flag}`; G9 → the #114 confinement clause realizes on re-extract (367/153 `subverts:["escape"]`) → join2 admitted. Anchors G4/G6/G7/G16 undisturbed (a first-draft symbol clause primed clothing enumeration and evicted G4's pigeon object — fixed with an anti-enumeration guard). **What remains open — surfacing, not content:** G10 is architecturally unreachable via extraction (the watching eyes are a secondary *object*, not the human subject, and the claim side is popular-concept cap-limited like G8/G11, #113); G14 needs a join-2 synonym bridge (`miss`↔`tenderness`); G15 needs a same-register conceptual-echo join (lever 6, not built). So richer extraction is necessary but not sufficient — the residual levers are downstream joins/cap (#113) and are out of this item's (extraction-only) scope. `directed_at` in-frame/absent flag investigated and declined (no consumer post-#112). Realized on the live library only after a forced re-extract of the affected subset. See decision #111, KNOWN_GOOD_PAIRS.md, backlog #95/#113. |
| 113 | Role joins — popular-concept recall (the "#2" lever) | The #113 cap redesign (greedy-by-specificity + per-type caps) made candidate selection non-evicting and surfaced G16, but popular connecting concepts (sound/smile/bird/danger) can still only be *sampled*: specificity is identical within a concept, so a specific intended pair among many equal matches isn't guaranteed. G5 (musician+ears) is unreachable (sound source-side density); G8/G11 are hub-limited (smile sign matches 148 imgs; weapon image matched by 49 danger-claims). Reliably surfacing these needs a *different* lever than the cap: per-concept rarity sub-signals (generalize `strainrcv`), a higher judge budget with smarter ordering, or moving a signal to another modality (cf. gaze→geometry #109). Gated on whether popular-concept recall is worth the judge cost. See decision #113, KNOWN_GOOD_PAIRS.md. |
| 119 | Propose new thematic clusters — theory + data + external research | #96's passes 1–3 mined judged pairs, rationales, and caption vocabulary for evidence of a missing cluster and found none that cleared both a recurrence and enrichment bar — every recurring un-clustered concept either showed null pairing signal or belongs to a different mechanism entirely (aesthetic axis: pattern/light; role pipeline: object-register, source/receiver). That closes "is there already latent evidence of a missing cluster in current data" — it does not answer "what new cluster concepts would make the system better," which is a generative question, not a mining one. This item is that generative complement. Approach is intentionally open-ended, but should draw on more than one source so the candidate concepts aren't bottlenecked by a single person's intuition: (1) PAIRING_THEORY.md's three pairing modes and the third-meaning test as a grounding framework; (2) the app's own corpus/extraction data, mined differently than #96's keyword-frequency approach (e.g. embedding-based caption clustering, an LLM cold-reading a caption sample and proposing concepts, RoleProfile field patterns); (3) external research outside the developer's own intuition — photography/visual-art criticism, semiotics, montage/juxtaposition theory, curatorial frameworks — specifically so the system's concept vocabulary isn't limited to one person's gut sense of what's missing. Treat this as its own scoped planning exercise (design-session-first, per the project's convention for complex/creative work) before any implementation, not a mechanical mining pass. See #96. |
| 121 | Caption accuracy defect log — hallucinated subjects & unwarranted gender assumptions | Running log of qwen2.5vl caption errors observed in the app, to accumulate over time and decide if a prompt revision (cf. #50/#97 caption prompt v2) is warranted. This is a **captioning-subsystem** issue (Phase 5, `OllamaCaptioningEngine`), NOT a cluster/scoring issue — clusters faithfully fire on whatever the caption says, so a wrong caption propagates to wrong tags/pairs downstream. Two failure modes seen so far (2026-07-08, post-#120 re-index): (1) **Hallucinated subject** — `20250319-_DSF1049.jpg`: no person in the photo (just a small blue R/C toy truck in front of a full-size blue truck with doors open), but the caption opens "A woman, likely in her 30s, stands in the doorway of a blue van, her hands resting on the frame as she looks down at a small toy truck…" — invents a woman and a pose. (2) **Unwarranted gender assumption** — `20250515-_DSF2444.jpg`: a cowboy (man) riding a white horse in a rodeo arena, captioned "A woman, likely in her twenties, is riding a white horse…". Guidance for a future prompt fix: when the subject's gender isn't clearly determinable at caption resolution, don't guess — describe without gendering (prompt v2 already says "emotion only from named visible evidence"; extend the same principle to gender/subject-existence). Add rows here as more are spotted; escalate to a prompt-revision decision once a pattern is clear. See decisions #50, #97, #99. |
| 122 | Role-join / judge — same-subject pairs pass the third-meaning test too easily | The role pipeline + ThematicV2 `validate()` judge can confirm a "source-receiver" (or generic complementary) connection at a high score for a pair whose third meaning just **restates shared content** — failing PAIRING_THEORY's third-meaning test ("two dogs = fail"). Concrete case (2026-07-08): `20250125-_DSF1060.jpg` ↔ `20240505-R0006402.jpg`, judged complementary — "Both images depict individuals carrying animals in a casual outdoor setting, with a clear source-receiver relationship between the caretaker and the animal being cared for." Each image just has a person holding a pet; the pair is a *category* ("people with pets"), not a resonance — it should score materially lower. This is distinct from the *recall* gaps in #95/#113 (getting good rare pairs to surface); this is a *precision* gap (a weak same-subject pair scoring too high). Candidate levers: a third-meaning rubric clause in the `validate()`/`score()` judge prompt that penalises rationales which only restate a shared subject/activity; or a same-subject guard analogous to the burst/near-duplicate guards. Needs a small labeled set of "category, not resonance" pairs first. See PAIRING_THEORY.md §Third Meaning Test, decisions #95, #102, #113. |
| 123 | Quiet/sparse photos under-surface vs event photos in top pairs | Observed 2026-07-08 (post-#120 re-index): the top-ranked pairs skew heavily toward events/protests/rodeos and away from sparse, quiet urban photos. This is *expected given the current design* — event photos fire multiple rich emotional clusters (tension_conflict, joy_celebration, community_gathering, power_dominance…), so their thematic axis scores high; quiet urban photos fire mostly ambient-tier clusters (urban_street 0.2) and so under-rank on thematic. **Raising ambient cluster weights is the wrong fix** (Andre's own instinct): it would make "both urban" a sufficient match, reintroducing the same-same problem #47/#120 fought. The correct lever is surfacing quiet photos through the axes designed for them, not thematic: Mode 2 slant-rhyme via the **aesthetic axis** (accent echo #56, and the unbuilt breath-pair tonal-weight complementarity #55 — the canonical breath candidates `20250426-_R016343.jpg`/`20210313-L1001045.jpg`) and **geometric** breath (dense↔spare), plus Mode 3 (ambient existential register — Soth's towels, currently out of scope in PAIRING_THEORY). Net: quiet photos need a *non-thematic* on-ramp with enough composite lift to compete, not a thematic boost. Scope this as: (1) confirm the breath-pair candidates visually, (2) build #55's aesthetic tonal-weight component, (3) evaluate whether Mode 3 warrants a dedicated signal. See PAIRING_THEORY.md (three modes), backlog #55, decisions #47, #56, #120. |
| — | KNOWN_GOOD_PAIRS.md — reference test set | Ground-truth list of Andre-identified good/reference pairs, tagged by mode/axis + DB status. Maintained continuously: add a row whenever a good pair is named. Use it to keep mode labels straight (#90 went sideways by filing aesthetic/geometric pairs as "thematic not surfacing"). |

## Closed Backlog

Completed items with one-line resolution. Full rationale in `DECISIONS.md`.

| # | Title | Resolution |
|---|-------|------------|
| 120 | Cluster keyword audit — exhaustive per-keyword collision review | Closed 2026-07-08. Ran the audit #96 never did — read every keyword in all 30 clusters against corpus-wide caption usage via an extended `ClusterDiag` harness (per (cluster, keyword): corpus firings, % of cluster, sole-trigger removal delta, raw-form inventory for collision detection, reachability sweep, thematicV2 enrichment; `diagnostics/120-keyword-audit-harness.swift`). A consolidated verdict table (all 30 clusters) was signed off before any edit. **Landed 62 collision removals + ~30 reachability fixes, one commit per cluster, each verified exact against the production-DB-copy harness (zero collateral deltas), 107 tests green, golden pairs G4/G6/G7/G16 undisturbed.** Corpus-wide firings cut 7,753 → 4,374 (−44%). Worst offenders: `waiting_anticipation` 755→76 (look/gaze/observ/watch reallocated to `looking_watching`), `confinement_freedom` 550→41 (`behind` 63% preposition + scenery), `tenderness_care` 662→133 (`hold` 85% instrumental), `devotion_belief` 373→114 (`sign`/`cross`; `flag` kept for enrichment), `stillness_rest` 590→285, `youth_age` 415→174 (`small` size adjective), `labor_effort` 273→96 (`build` background architecture). Reachability fixes *raised* genuine coverage: `joy_celebration` 292→330, `movement_energy` 86→95. **Tier:** `bodily_gesture` demoted 0.75→0.2 (94% corpus ubiquity, ~3% enrichment ≈ base — à la #47's `animal_presence`); `looking_watching` kept at 0.75. **Phase 0:** fixed the deferred #48/#118 sibling — `PairScorer.swift`'s two stored-rationale sites (`meaningfulShared.first`, `shared.first!`) now use deterministic `representativeCluster`. Edits change stored pair *scores* only on the next re-index (pure function of caption); close-out stays **amendable pending Andre's post-merge re-index spot check** (the real-world test #96 failed). **Re-index review done 2026-07-08:** confirmed the cleanup, and surfaced one more ambient collision now fixed on-branch (`public` removed from `community_gathering`, 504→454); the other observations were caption-subsystem or judge-precision issues outside cluster scope, tracked as new backlog #121/#122/#123. See decision #120. |
| 96 | Use ThematicV2 reasoning to improve cluster quality | Closed 2026-07-08. Four passes of read-only diagnostics (per-cluster distribution, rationale mining, blind-spot analysis + specificity correction, caption-first vocabulary mining, judge-fabrication recheck) found no evidence for a new cluster or vocabulary addition — every recurring un-clustered concept either showed null pairing-enrichment signal or belongs to the aesthetic axis or role pipeline instead. Landed 4 keyword edits: removed `observ` from `ritual_ceremony` and `strip` from `transformation_change` (stem collisions), fixed `"obscure"`→`"obscur"` and `"raise"`→`"rais"` (keyword-reachability bugs — literal dictionary words that never matched their own stemmed inflections). Verified: 106/106 ConjunctEngine tests green; dev-DB-copy re-score confirmed exact predicted corpus-firing deltas (`ritual_ceremony` 142→39, `transformation_change` 96→21, `bodily_gesture` 965→968, `joy_celebration` 174→292, `uncanny_ordinary` unchanged at 0); KNOWN_GOOD_PAIRS golden pairs G4/G6/G7/G16 confirmed structurally unaffected. See decision #96. Generative follow-up (proposing genuinely new clusters, not mining for evidence of missing ones) opened separately as #119. **Amendment 2026-07-08 (real-world validation, post re-index):** Andre reviewed actual re-indexed pairs and found the improvement was NOT appreciable end-to-end — 3 concrete pairs cited where `devotion_belief`/`tenderness_care`/`confinement_freedom` tags made no sense against the captions/images. Investigated and confirmed as genuine, additional over-broad-keyword collisions #96's methodology never surfaced: `sign` (65% of `devotion_belief`'s 373 corpus firings — literal bus-stop/storefront signs, no religious/political content), `cross` (23% — "arms **crossed**" body posture, not a religious cross), `lean` (15% of `tenderness_care` — physical leaning-against-an-object, not emotional support), `behind` (63% of `confinement_freedom` — a bare spatial preposition, already flagged in pass 2d as reconnaissance but never fixed within #96's scope). #96's passes mined either FROM judged-pair signal or FROM caption vocabulary the clusters don't yet cover — none of them exhaustively re-examined vocabulary the clusters ALREADY claim against real caption usage, which is why these went undetected. The 4 landed edits were real and correctly verified but represent a small fraction of this class of problem. See decision "#96 real-world validation gap" (2026-07-08) for full corpus-frequency evidence. Follow-up audit opened as **#120**. |
| 46 | Complementary role scoring — two sides of the same phenomenon | Superseded by #113 (popular-concept recall) / #95 (candidate-selection gaps). Root cause re-diagnosed 2026-06-27 as join-1 cap density on a popular phenomenon, architecturally distinct from original Dice/axis-bonus theory. Canonical test case tracked going forward as G5 in KNOWN_GOOD_PAIRS.md — do not lose this pointer. |
| 112 | Join 4 (directed-attention role join) | Retired 2026-06-25 diagnostic. `directed_at` is 90%-populated but mostly camera/viewer/vague/in-frame — not a nameable off-frame subject. Directed attention is a visual signal (handled by #109's geometry-based gaze), not a text-join signal. No coverage gap remains. Joins 1–3 stay; join 5 remains deferred. |
| 117 | Grid load — unbounded full-table scan on every load | Closed 2026-07-07. Root cause was different from what the backlog assumed: none of the 5 `PairSortOrder.dbColumn` expressions (including the default "Best"/axis sort) is a bare indexed column, so every grid load forced SQLite into `USE TEMP B-TREE FOR ORDER BY` — full materialize-and-sort of the whole matching row set before row 1 could stream, contradicting the "streams via idx_pairs_score" doc comments (now corrected). Both candidate fixes from the backlog were evaluated and rejected: an early-exit on cap-2 coverage never fires on real data (7 of 973 pairable images are only ever covered by pass-2, whose candidates require scanning the full rejected set); a `PARTITION BY imageID` window-function LIMIT is provably unsafe since cap-2 is a global greedy matching, not per-image top-K, and truncation could silently change which pairs win. Fix shipped instead: migration `v21_pairSortIndexes` adds one SQLite expression index per sort-order formula (auto-maintained by SQLite, no app-level sync) — `EXPLAIN QUERY PLAN` now shows `SCAN p USING INDEX ...` with no temp b-tree for all 5 sort orders. Measured on the production DB (1,028 images, 121,589 pairs): first-row latency 606–955ms → 0.6–1.8ms (~500–1000×), full drain 1.7–2.1s → 1.3–1.5s (~25–35%); at synthetic 3× scale (3,084 images, 364,767 pairs) first-row stays flat (0.6–5.3ms) instead of growing with library size, closing the "gets slower as libraries grow" concern for time-to-first-paint specifically (full drain remains O(N), dominated by per-row Swift-side conversion, out of scope here). Cap-2 selection logic untouched; anchor-strip (`dbLimit=500`) verified unaffected. See decision #117, backlog row removed. |
| 118 | Lightbox thematic rationale — flickering cluster label | Closed 2026-07-07. User's hypothesis (hover-driven re-render + non-deterministic `Set.first` tie-break) was half right: recompute-on-hover was confirmed real, but a 2000-call same-process test showed the tie-break is actually stable *within* a run — the real mechanism is Swift's per-process hash seed, confirmed by 5 separate `swift test` launches picking 5 different clusters for identical input. Fixed by (1) adding `ConceptClusters.representativeCluster(in:)` — deterministic weight-desc/name-asc tie-break — and (2) turning `DisplayPair.thematicRationale` from a computed property into a stored `let` built once in `init`, so hover no longer re-triggers the caption-parsing work at all. Scoring code (`PairScorer.swift`) intentionally untouched — same `.first` pattern exists there (decision #48) but is out of scope (stored-at-index-time, not display). See decision #118. |
| 110 | Raise grid page size / display limit | Closed 2026-07-07 — investigated, found moot. `pageSize=150`/`displayLimit=750` don't gate the main grid: `streamPage0Pairs`'s underlying query has no SQL LIMIT and already streams the entire `pairs` table every load (measured 490 displayed pairs post cap-2 on the dev library, already under the old 750 ceiling). The `displayLimit`/`dbLimit` constants live in `EngineController.fetchPairs`, which turned out to be dead code for that branch — the function is only ever called with `anchorImageID` set, so the `anchorImageID == nil` branch containing them never executes. Removed that dead branch; anchor-strip behavior (`dbLimit=500`) and cap-2 are unchanged. Verified: 104/104 ConjunctEngine tests green, Xcode build green, live measurement against a copy of the production DB (121,589-row pairs table, 1.7s full drain, 490 displayed pairs). See decision #110, backlog #117 (new item opened for the real cost driver this surfaced). |
| 8 | Double CLIP build on launch | Closed 2026-07-07. Not a race in `engineBuildTask?.cancel()` (original guess was wrong) — two independent, always-firing call sites to `EngineController.initialize()`: `CoupletApp.swift`'s launch `.task` (added by #106, calls it unconditionally so the engine is ready during setup) and `ContentView.swift`'s pre-existing `.onAppear` (present since the initial commit). On a machine that's already set up (the common dev case), both fire within the same launch — `.task` first, then `ContentView.onAppear` moments later once `phase` flips to `.ready`. Confirmed live: built and ran the app directly (bypassing Xcode) 3 times pre-fix, each launch logged `CLIP: loaded from app bundle` twice plus a `CAPTION`/`ROLE` "not available"→"available" flicker (the two `startEngineBuild` calls racing Ollama's `/api/tags` warm-up) — added temporary diagnostics confirming a single `EngineController` instance had `initialize()` entered twice from the two call sites, not two engine instances or two windows. Impact was hygiene/perf only, not correctness: `engineBuildTask?.cancel()` does correctly gate the final `self.indexingEngine =` assignment (line ~733), so only the second, uncancelled call's engine ever gets wired up — but the first call's CLIP/captioning/role-availability work still runs to completion and is thrown away. Fix: removed the redundant `engine.initialize()` call from `ContentView.onAppear`, leaving `CoupletApp`'s `.task` as the sole call site. Verified: 3 more fresh launches post-fix, each showing exactly one `CLIP: loaded` line and no CAPTION/ROLE flicker; all 104 ConjunctEngine tests green. See #106. |
| 47 | animal_presence demoted to ambient tier | Done. 0.75 → 0.2; dog+dog pairs reduced but still fire via shared 0.75-tier behavior clusters. See #47. |
| 48 | Complementary axis pair bonus | Done. 9 axis pairs in `ConceptClusters.axisPairs`; bonus fires only at `clusterScore ≤ 0.10`. See #48. |
| 49 | Meaningful asymmetry gate — weight ≥ 0.75 unique clusters per side | Done. Replaced `!onlyA.isEmpty` check with meaningful-tier filter in `PairScorer.swift`. See #49. |
| 56 | Accent color echo — pair scoring + info panel | Done. `accentEchoScore = hueScore × √(satA × satB)`; three-way max with harmony/contrast; lightbox "Color echo" swatch display. See #56. |
| 59 | Directional complement scoring — geometric axis | Superseded by #64. Edge-energy centroid proven ineffective (stdev=0.046). Infrastructure retained; computation replaced with Vision saliency. See #59. |
| 60 | Restore geometric structural weight | Done. Superseded by #67 (current: `structural×0.50 + directional×0.25 + breath×0.25`). See #60. |
| 63 | Gaze / body direction detection | Done. `gazeDirectionX` via `VNDetectFaceLandmarksRequest`. Pupil-only; head-yaw fallback removed in #69. See #65. |
| 64 | Vision saliency centroid — replace edge-energy centroid | Done 2026-05-17. stdev_x = 0.141, coverage 99.8%. See #64. |
| 65 | Gaze direction scoring | Done. ~15–20% pupil-only coverage; false positives from tilted faces eliminated. See #65, #69. |
| 66 | Composite scoring rethink — per-axis surfacing | Done via #75 (thematic boost fix + aesthetic pool) + #76 (axisScore "Best" sort) + #77+#78 (calibration). See #75–#78. |
| 67 | Directional weight rebalance | Done. `structural×0.50 + directional×0.25 + breath×0.25`. See #67. |
| 68 | Geometric topK pool — per-submode surfacing escape hatch | Done. Top-5/image by geometricScore with submode variety bonus; `selectedFor = "geometric"`. See #68. |
| 69 | Drop head-yaw gaze fallback — pupil-only detection | Done. v14 migration nulls existing readings; re-extracted pupil-only on next re-index. See #69. |
| 70 | Gaze score normalization | Done. `kGazeScale = 1.0 / 0.30` in `gazeConversationScore()`. See #70. |
| 71 | Symmetric gaze scoring + canonical display order | Done. Max(forward, reversed); rightward-gazer always stored as imageAID. See #71. |
| 72 | Directed-attention / looking-toward pairs | Done 2026-06-25. Geometric nominator → `GazeVisionJudge` (`qwen2.5vl:7b` @1024px) → clarity score → `directed_gaze` geometric submode. ~59 valid / 9 rejected of 68; shipped at model ceiling. See #109. |
| 73 | Dominant orientation opposition — opposing_diagonals submode | Done. `orientationOppositionScore()` using 32-bin edge histogram; new submode `"opposing_diagonals"`. See #73. |
| 74 | Opposing diagonals — diagonalness gate + submode display | Done. Hard gate: both images must be in 22.5°–67.5° zone. Pool: 75 pairs. Per-submode lightbox labels. See #74. |
| 75 | Thematic boost fix + aesthetic topK escape hatch | Done. Boost condition tightened to dominant-axis only; aesthetic pool added (top-5/image, ≥0.55 floor). See #75. |
| 76 | axisScore display field + "Best" default sort | Done. `axisScore` field added; default sort changed from "Composite" to "Best". See #76, #78. |
| 77 | B&W aesthetic discount + contrast ceiling recalibration | Done. Harmony/contrast ×0.65 for B&W pairs; contrast divisor `/80` → `/100`. See #77. |
| 78 | axisScore blend formula | Done. `axisScore = 0.6×peakScore + 0.4×displayComposite`. See #78. |
| 81 | Caption correction for known-failing thematic pairs | Closed 2026-06-23. Manual caption override retired; gun and ears-woman misreads fixed automatically by prompt v2. Preserve-list IDs (2018/3439/2038/2072) are stale — DB rebuilt, those IDs no longer exist. See #99, #50. |
| 85 | ThematicV2 — evaluate llama3.2:11b for better discrimination | Closed 2026-06-23. Resolved by #102: default model switched to `qwen2.5:14b-instruct`. Switch + `validate()` judging achieved 6/7 recall, 4/4 precision. See #102, #99. |
| 90 | ThematicV2 — golden thematic pairs surfacing | Closed 2026-07-07. Live-DB diagnostic against all 14 Mode-1/dual-mode pairs in KNOWN_GOOD_PAIRS.md: the scorer and display pipeline are healthy — 4 judged pairs (G4/G6/G7/G16, all 0.95) all rank top-0.3% under Best sort and survive cap-2 within the first few grid pages (also surfaced: G16 was independently confirmed to have flipped from absent to judged since #113 shipped). Every residual gap is already tracked, not a #90 defect: 4 pairs (G8/G10/G14/G15) sit in the DB unjudged, ranked 89k–108k of ~119k eligible non-role candidates — budget/ordering, folded into #93; 4 pairs (G1/G5/G11/G13) never enter the pairs table at all — join/extraction capability gaps tracked via #58/#113/#111; 1 pair (G9) is pending the #114 forced re-extract (code merged, DB not yet re-indexed against it — expected, not a bug); 1 pair (G12) is an extraction register-mislabeling gap. See #90. |
| 91 | ThematicV2 candidate selection — exclude color echo pairs | Done. Superseded by #100 (2026-06-15): blanket exclusion replaced with narrow pure-color guard + tail-deprioritisation. See #91, #100. |
| 92 | Color echo — warm-band saturation ramp for skin-as-accent pairs | Done. Saturation ramp (0 at sat ≤ 0.45 → 1 at sat ≥ 0.65) when hue ∈ [15°, 35°]. 18,857 echo pairs zeroed. See #92. |
| 94 | Duplicate detection — group crop/re-export filename variants | Done. `FilenameVariants.areVariants` rule added to `detectDuplicates`; broken normalize-both regex fixed. See #94. |
| 99 | Oracle investigation — three-layer diagnosis | Done 2026-06-14. Three serial gates diagnosed: caption accuracy, selection, scorer. Led to #50, #95, #85. See #99. |
| 101 | Parallelize indexing & scoring phases | Done 2026-06-15. Phases 1.5/3/6/7/8 parallelized; 2.1× overall speedup. Ollama phases remain sequential (see #93). See #101. |
| 103 | Burst near-duplicate guard in four-pool topK selection | Done 2026-06-17. `isBurstNearDuplicate` filters `allScores` before pool selection. Unit tests added. See #103. |
| 105 | Dependency status indicator — Ollama / model / CLIP health | Done 2026-06-17. `OllamaInventory.check()` + `DependencyHealthView` capsule + "Recheck Now". See #105. |
| 106 | First-run setup assistant + bundled CLIP model | Done 2026-06-18. `SetupFlowView` 3-step rail; CLIP bundled; `ModelBookmark` removed. See #106. |
| 107 | Uninstall / Reset Couplet | Done 2026-06-18. `UninstallResetSheet` from Settings; DB/cache/prefs clear + relaunch. See #107. |
| 98 | Caption gaze-direction errors — diagnostic close-out | Done 2026-07-07. 0 of 686 judged pairs touching flagged captions "lean on" a hallucinated gaze-target claim; 12 of 14 gaze-vocabulary rationale hits trace to role-join real-vs-depicted logic instead. No prompt change warranted. See #98. |
| 116 | Join-2 synonym bridge — claim↔embodiment matching | Closed 2026-07-07. Added a small curated `claimEmbodimentBridge` (currently one entry, `"miss"→"tenderness"`) to `RoleJoins`, matched on the WHOLE claim/embodiment string (exact, case-insensitive) rather than token overlap. Verified: offline harness over live production profiles (10 golden/anchor images freshly re-extracted with the current #111 prompt to correct DB staleness) — **G14 now fires and is admitted** (587↔645). **G4/G6/G7/G16 anchors undisturbed.** A `smile→speak` bridge for G8 was tested and rejected: it raw-fires (259↔598) but never survives the cap-8 race (598 still loses to other "smile" matches) while adding 145 extra raw join-2 candidates — flood for zero gain, so G8 stays unreached (confirms #113's "structurally hub-limited" finding). One genuine false positive was caught and fixed during validation: token-level matching let a `"Miss Rodeo"` pageant-sash claim spuriously bridge to an unrelated "tenderness" embodiment via the shared "miss" token (958↔174, 958↔587) — switching to whole-string exact match eliminated it without losing G14. Net effect: 1221→1223 admitted candidates (content-patch baseline), i.e. +2, both real (587↔645 and a second plausible "miss"→174's genuine tenderness embodiment, not evicting anything). 2 new unit tests added (`RoleJoinsTests`); all 104 tests green. See decision #116, KNOWN_GOOD_PAIRS.md. |

## Commit Convention
Use `#ID` prefix matching the decisions log:
```
fix(#26): replay temporal penalty in convertToPair
feat(#21): add collapsed caption display to info rail
chore: initial commit
```
