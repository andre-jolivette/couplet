# Couplet — Architectural Decisions & Current State

## Project Brief
Couplet is a macOS app (bundle ID `com.toastingmachine.Couplet`, deployment target macOS 14.0) for photographers. It discovers meaningful image pairs in a photo library — not duplicates or sequential shots, but conceptually resonant connections the photographer might not have noticed. The serendipity machine goal: surface pairs that reward a second look.

**On genre and assumptions:** The developer's library happens to contain a particular style of photography, but Couplet is not designed for a specific genre. Genre labels are likely to impose unhelpful constraints — they narrow the expected subject range, aesthetic, and pairing logic in ways that may only be correct for one kind of library. Where decisions reference the library's characteristics (e.g. "urban_street fires on most images"), treat these as calibration observations about one dataset, not design axioms. See decision #79.

## Repository Layout
- Engine (Swift Package): `~/Documents/00-projects/_couplet/ConjunctEngine/`
- App: `~/Documents/00-projects/_couplet/Couplet MacOS App/Couplet/`
- CLIP model: `~/Documents/00-projects/_couplet/clip-vit-base-patch32.mlpackage`
- Database: `~/Library/Containers/com.toastingmachine.Couplet/Data/Library/Application Support/Conjunct/conjunct.db` (app is sandboxed — all Conjunct data lives inside the app container)

---

## Engine Architecture

### Indexing Pipeline (5 phases)
1. **Scan** — FileScanner reads EXIF captureDate, colorProfile (color/bw) from CGImageSource
2. **Duplicate detection** — dHash perceptual hashing, Hamming threshold=6, hero image selection
3. **Thumbnails** — CGImageSourceCreateThumbnailAtIndex at 512px max (prevents IOSurface exhaustion)
4. **CLIP extraction** — CLIPCoreMLEngine, 224px input, cosine similarity embeddings
5. **Captioning** — OllamaCaptioningEngine → moondream via localhost:11434. Captions ALL uncaptioned active images in DB (not just current scan batch). Clears malformed captions with label prefixes on each run.
6. **Pair scoring** — PairScorer with dual topK (composite + thematic), then DELETE+INSERT to replace stale pairs

### Scoring (PairScorer.swift)
- **Aesthetic** — HSL histogram intersection (harmony) vs LAB palette contrast
- **Geometric** — edge orientation cosine similarity + composition grid cosine similarity
- **Thematic** — ConceptClusters vocabulary matching on moondream captions; Dice coefficient on matched cluster sets; falls back to CLIP cosine similarity if no captions
- **Composite weights** — aesthetic×0.40 + geometric×0.20 + thematic×0.40 (thematic boosted to 0.60 when score ≥0.20)
- **Penalties** — sequential shots (≤30s: ×0.40, ≤60s: ×0.55, ≤5min: ×0.85), filename-base duplicates (zeroed), CLIP similarity ceiling >0.88 (×0.40), cluster saturation: weight-based gate (weightedSharedSum > 5.0 → zeroed; previously count > 3)
- **Dual topK** — top-50 by composite per image + top-10 by thematic score per image (minimum 0.20)

### ConceptClusters (ConceptClusters.swift)
29 semantic clusters: skilled_performance, sound_music, sensory_overwhelm, bodily_gesture, tenderness_care, isolation_solitude, community_gathering, tension_conflict, joy_celebration, grief_sorrow, labor_effort, movement_energy, stillness_rest, ritual_ceremony, urban_street, vulnerability_exposure, power_dominance, looking_watching, devotion_belief, confinement_freedom, youth_age, waiting_anticipation, transformation_change, nature_landscape, humor_absurdity, uncanny_ordinary, economic_precarity, solitude_in_crowd, domestic_intimacy

Tokenizer strips common suffixes (ation, tion, ance, ence, ing, ed, er, etc.) before matching.
Thematic score uses **Dice coefficient** (not Jaccard) — less harsh when one image has many clusters.
Two-signal clusters (require ≥1 keyword hit from each of two vocabulary groups): humor_absurdity, uncanny_ordinary, solitude_in_crowd, domestic_intimacy, economic_precarity.

### Database Schema (GRDB, SQLite WAL)
- `images` — path, contentHash, filename, folderID, captureDate (Double, Unix timestamp), colorProfile ("color"/"bw"), caption (TEXT), dHash, duplicateGroupID, isHero, isActive
- `pairs` — imageAID, imageBID, aestheticScore, geometricScore, thematicScore, compositeScore, rationale, rawEdgeSim, rawGridSim, maxEdgePeakedness, maxGridVariance, edgePeakednessMult, gridVarianceMult
- Migrations: v1_initial, v2_duplicates, v3_captions, v4_colorProfile, v5_geometricStats, v6_distinctivenessMultipliers

---

## App Architecture

### Key files
- `EngineController.swift` — @MainActor bridge. Builds CLIP + captioning engines in cancellable Task. fetchPairs: fetches 2000 from DB, applies per-image cap of 15, trims to 750
- `PairScorer` modality labeling — thematic if thematicScore ≥0.25 AND thematicScore > geometricScore; otherwise geometric if geometric ≥ aesthetic
- `LightboxInfoRail.swift` — right rail showing captions, cluster breakdown, score bars, composite breakdown. Opened by clicking score block in lightbox top bar

### UI State
- Grid: 750 pairs displayed, per-image cap of 15
- Filters: All/Aesthetic/Geometric/Thematic pills, Sort dropdown (Composite/Thematic/Geometric/Aesthetic), All tones/Color/B&W/Mixed picker, Show hidden toggle, Search
- Sort is display-time only — independent of modality filter pills; combine freely (e.g. filter to Thematic, sort by geometric score)
- Hide sequential pairs: moved to Settings (Display section), persisted via UserDefaults, synced into PairsGridViewModel at display time
- Lightbox: score block (A/G/T + composite) clickable → slides in info rail
- Score pills: opacity scales with score value; color-coded (blue=aesthetic, green=geometric, orange=thematic)

---

## Known Issues / Active Work

### Title bar frosted glass — RESOLVED (see backlog #30)

---

### Thematic scoring problems
- **Labeling bias**: geometric scores (0.80+) dominate because most street photography has strong structural similarity. Thematic scores (0.20–0.45) rarely beat geometric despite being meaningful. Needs Option B (store selection reason at index time) and Option D (improve geometric scorer)
- **Score ceiling**: confirmed Jaccard hard-caps at 0.60 given the asymmetry + saturation gates. Switched to Dice — new ceiling 0.75. Existing DB pairs retain old Jaccard scores until re-indexed.
- **Cluster weight equity**: all cluster matches currently count equally, but `urban_street` overlap is far less meaningful than `grief_sorrow` overlap. Weighted Dice is the next step (see backlog #17).
- **Vocabulary gaps**: clusters covering sound/music and sensory experience added; violin+ear-covering pair now scores thematic=0.25. More gaps likely exist
- **Caption quality**: moondream (1.8B) describes surface action accurately but misses subtext, irony, cultural references. Acceptable for cluster matching; not good enough for nuanced semantic understanding

### Pending improvements (prioritised)
1. **Option B** — store `selected_for` flag ('thematic'/'composite') in pairs table at scoring time; use for labeling instead of post-hoc score comparison
2. **Option C** — already done (three score pills + info rail)
3. **Option D** — improve geometric scorer to be more selective; currently rewards any two street photos with similar subject scale
4. **Color tone filter** — ✅ Fixed. Three-tier detection: Gray color model → ICC profile name → pixel-level chroma sampling (catches RGB B&W JPEG exports from Lightroom). Backfill runs for all active images on every re-index (same pattern as caption backfill), so a single re-index of any folder corrects the whole library.
5. **Collections** — not yet wired to database
6. **Diptych export** — not yet implemented
7. **Settings view** — ✅ modality weight triangle picker + thematic threshold slider implemented and wired. Duplicate threshold not yet UI-accessible.

### Double CLIP build issue
On some launches two engine builds start simultaneously (one finds ollama unavailable, one finds it available). Root cause: `initialize()` and sometimes `storeModelBookmark()` both trigger `startEngineBuild`. The `engineBuildTask?.cancel()` pattern should prevent this but the timing window is narrow. Monitor — hasn't caused crashes recently.

---

## Ollama / Captioning Setup
- Install: `brew install ollama`, then `ollama pull qwen2.5vl:7b`
- Register the trimmed context variant: `ollama create qwen2.5vl-caption -f ConjunctEngine/Modelfile`
- Run: `ollama serve` (or `brew services start ollama` for auto-start)
- Current model: `qwen2.5vl-caption` — qwen2.5vl:7b with num_ctx capped at 2048 (see ConjunctEngine/Modelfile). Prompt v2 (#50/#97): single flowing paragraph, physical facts first (hands, feet, gaze target, precise objects, apparent gender), direction of every significant action, emotional register only as a conclusion from named visible evidence, ban on freestanding mood phrases. No structured scaffolding.
- Input: 768px max-dimension thumbnail (raised from 512px in #97 — small subject-defining details were missed at 512px). Decoding: greedy (temperature 0, top_k 1) for determinism and to suppress run-to-run fabrication; repeat_penalty 1.15 retained as loop insurance.
- Timeout: 120s per image; URLSession ceiling 150s. Actual caption time ~3–6s/image on M1 Max.
- num_predict: 500 tokens (raised 250 → 400 → 500; see decisions log). The v2 prompt typically completes in 140–190 tokens; 500 is headroom. Budget: image ≤750 + prompt ≈460 + output 500 ≈ 1,710 of num_ctx 2048.
- **Do NOT run the Ollama server with `OLLAMA_KV_CACHE_TYPE=q8_0` (or any quantized KV cache)** — it corrupts qwen2.5vl generation into systematic article/function-word garbling ("a aftermath", "He phone") even at greedy decoding, while llama3.2 tolerates it, so the breakage is silent and model-specific. `OLLAMA_FLASH_ATTENTION=1` alone is fine. The Homebrew service plist (`/opt/homebrew/opt/ollama/homebrew.mxcl.ollama.plist`) had both set — remove the KV cache line if it reappears (e.g. after editing the plist or reinstalling). See decision #97.

---

## Decisions Log

| Decision | Rationale |
|----------|-----------|
| Security-scoped bookmark for CLIP model | App Sandbox requires it; model lives outside app bundle |
| 512px thumbnail for CLIP input | Full-res caused IOSurface exhaustion during concurrent extraction |
| Soft temporal falloff (not hard cliff) | Hard 30s cliff was too aggressive for street photography shot quickly |
| Dice coefficient for thematic | Jaccard penalised protest photos (6 clusters) pairing with portraits (2 clusters) too harshly. Dice penalises each unique cluster once (denominator only) vs. Jaccard's double-penalty (numerator + denominator). Both asymmetry gate (≥1 unique per side) and saturation gate (>3 shared → zero) are preserved. Ceiling: 3 shared + 1 unique each → 6/8 = 0.75, comparable to aesthetic (0.84) and geometric (0.79) maxima in the current library. |
| 2000 DB fetch → cap → 750 display | Per-image cap + thematic re-weighting shrank 750 fetch to ~380 displayed |
| Per-image cap of 15 | Car interior image had 117 pairs, crowding out rest of library |
| topK=50 + thematic topK=10 | Thematic pairs never made composite top-50; separate selection ensures they appear |
| DELETE+reINSERT pairs on re-index | record.save() hit UNIQUE constraint silently; old scores were never updated |
| Caption all active images (not just scan batch) | Images from previous scans weren't getting captioned on re-index |
| colorProfile: pixel-level chroma sampling threshold=15 | `kCGImagePropertyColorModel == "Gray"` misses B&W JPEG exports from Lightroom/Capture One (stored as RGB, zero chroma). 32×32 downsample + max per-channel diff ≤ 15/255 catches these while absorbing JPEG ringing artifacts. |
| colorProfile backfill on every re-index | Migration default "color" affected all pre-v4 images. Running `refreshAllColorProfiles` after `upsertImages` (skipping the current scan batch) corrects the whole library from a single folder re-index. |
| Structured moondream prompt with Sentence 1/2 labels | Freeform prompts return empty string for ~90% of images; label scaffolding in instructions is required for reliable output even when asking moondream not to emit the labels. Retired — qwen2.5vl:7b (#20) uses a freeform paragraph prompt with no scaffolding. |
| Settings wired into EngineController at init | SettingsStore is @Observable (not ObservableObject); passed in at construction so fetchPairs can read live weights and threshold without async bridging |
| Thematic filter + live weight re-sort are display-time only | Scores stored in DB use default weights; re-sort on fetch avoids re-indexing on every settings change |
| TriangleWeightPicker takes three Binding<Double> params | Float↔Double bridging done at call site via computed Binding properties in SettingsView |
| Edge magnitude threshold raised 0.05 → 0.15 | Fine texture (brick, fabric, skin pores) was polluting the orientation histogram. Higher threshold filters these pixels before they vote, leaving only genuine structural edges. Baked-in constant in GeometricAnalyser — not a slider. |
| Geometric sub-scores stored per pair (v5) | rawEdgeSim, rawGridSim, maxEdgePeakedness, maxGridVariance persisted at score time. Enables display-time gating and re-weighting without re-score or re-index. Nil for pre-v5 pairs (fallback to stored geometricScore). |
| Display-time gating via two sliders | "Line direction strength" (edgePeakednessFloor, default 2.2) and "Composition structure floor" (gridVarianceFloor, default 0.12) apply binary discounts (40%/50%) at display time. Calibrated against library debug output: p10≈2.16 for peakedness, p10≈0.116 for variance. Floor discounts apply after the distinctiveness multiplier. |
| Distinctiveness multiplier (v6) | edgePeakednessMult = pow(normPeakA × normPeakB, 0.4); gridVarianceMult = pow(normVarA × normVarB, 0.4). Both images must be geometrically interesting for a pair to earn full credit — one flat image drags the score down proportionally. Normalization anchors at p90: kEdgePeakedNorm=4.0, kGridVarianceNorm=0.20. Exponent 0.4 (reduced from sqrt=0.5 — one calibration step toward 0 to soften the penalty). Multipliers stored per pair for display-time re-application. |
| Multiplier and floor gate are complementary, not alternatives | Multiplier: continuous, uses product of both images (both must be interesting). Floor gate: binary, uses max of both images (if even the stronger image doesn't clear the bar, discard). The multiplier fires first; the floor gate catches any remaining cases where the max is below the user's chosen threshold. |
| Orphan pair cleanup on every re-index | The per-run `DELETE FROM pairs WHERE imageAID IN (:ids) OR imageBID IN (:ids)` only covers images active in the current scan. Pairs for images deactivated via `removeFolder` were never removed, accumulating indefinitely (40,848 orphaned rows found — half the table). Added a sweep `DELETE FROM pairs WHERE imageAID NOT IN (active) OR imageBID NOT IN (active)` before the per-run DELETE. Runs unconditionally each re-index; cost is negligible on SQLite with indexed foreign keys. |
| Strip moondream label prefix at generation time | The structured prompt uses "Sentence 1:" scaffolding in its instructions; moondream (1.8B) sometimes echoes the label in output despite "No sentence labels" in rules. Previously: cleanup SQL nullified these captions, triggering a re-caption that often produced the same prefix again — an infinite cycle wasting ~152 ollama calls per run. Fix: (a) cleanup SQL now strips the prefix in-place using `TRIM(SUBSTR(caption, INSTR(caption, ': ') + 2))` instead of `SET caption = NULL`, preserving caption content; (b) `OllamaCaptioningEngine.caption()` strips the prefix from the raw response before storing. WHERE patterns unchanged (`'Sentence 1:%'` and `'First sentence:%'` only). Retired — both the cleanup SQL and the prefix-stripping code were removed when moondream was replaced by qwen2.5vl:7b (#20). |
| Weighted Dice for thematic scoring | All 24 clusters now carry a pairing weight (1.0 / 0.75 / 0.5). Score = 2×Σweights(A∩B) / (Σweights(A)+Σweights(B)). Tier 1.0: grief_sorrow, vulnerability_exposure, isolation_solitude, ritual_ceremony, tension_conflict, tenderness_care, devotion_belief, power_dominance, sensory_overwhelm. Tier 0.75: transformation_change, skilled_performance, labor_effort, stillness_rest, waiting_anticipation, movement_energy, bodily_gesture, looking_watching, confinement_freedom, youth_age, sound_music, joy_celebration. Tier 0.5: urban_street, nature_landscape, community_gathering. Weights calibrated against library frequency data (rationale field) — high-frequency ambient clusters scored lower. Known follow-on: a pair matching only a single 0.5-weight cluster still computes Dice=1.0 when neither image has any other clusters; a minimum weighted-shared floor is deferred to a subsequent session. |
| qwen2.5vl-caption: num_ctx capped at 2048; images resized to 512px before encoding | qwen2.5vl:7b defaults to a 128k context window. Ollama allocates KV cache proportional to num_ctx at load time — 128k costs ~2GB extra RAM and dominates inference time. Full-resolution camera files (12–24MP) encode to ~2,677 prompt tokens via qwen2.5vl's tiled patch encoder, which exceeds 2048 and causes degenerate output. Fix: OllamaCaptioningEngine now resizes every image to 512px using CGImageSourceCreateThumbnailAtIndex (same pipeline as CLIP) before base64-encoding. Measured token budget at 512px: ~247 vision + ~270 prompt + 250 response cap = ~767 total. num_ctx 2048 gives ~2.5× headroom. The Modelfile lives at `ConjunctEngine/Modelfile` and registers the model as `qwen2.5vl-caption`. Do not raise num_ctx casually — even 4096 doubles the KV cache cost for no benefit at these input sizes. |
| qwen2.5vl tokenizer special-token cleanup | qwen2.5vl:7b occasionally leaks tokenizer control tokens (`<|im_start|>`, `<|im_end|>`) into the generated response when generation is cut off at the num_predict boundary. `OllamaCaptioningEngine.caption()` truncates the response at the first occurrence of `<|im_` before storing. This is preferred over a post-hoc SQL cleanup — the DB never receives a malformed caption in the first place. |
| Captioning pipeline uses cached thumbnails | The captioning phase previously passed original image URLs to `OllamaCaptioningEngine`, which then re-decoded the full-res file (typically 12MB) to generate a 512px thumbnail on each call. Phase 2.5 (Thumbnails) runs before captioning and already writes 512px JPEGs to `~/Library/Containers/com.toastingmachine.Couplet/Data/Library/Caches/Conjunct/thumbnails/{imageID}.jpg`, recorded in `images.thumbnailPath`. The captioning SELECT was extended to fetch `thumbnailPath` and resolve it to a file URL; when the cached file exists it is passed directly to `caption()`, bypassing the re-decode. `thumbnailData(from:)` in `CaptioningEngine.swift` remains as a fallback for any image whose cache file is absent (e.g. OS cleared Caches). Measured saving: ~80ms per image (~90s across a 1,100-image library). |
| Five new clusters + two-signal Cluster struct (#15) | Clusters designed through photographer-influence research (Paul Graham, Rebecca Norris Webb, Daniel Arnold, Jeremy Paige, Melissa O'Shaughnessy, Andre Wagner, Rosalind Fox Solomon, Sage Sohier). Validated against 1,080 live qwen2.5vl captions before implementation. **Architecture**: Added `requiredGroups: [Set<String>]?` to the `Cluster` struct (default `nil` — zero behavior change to existing 24 clusters). When set, `matchedClusters()` requires ≥1 keyword hit from *each* group rather than any keyword. Two-signal gating was necessary for three clusters: `absurd`/`juxtaposition` each appear 121× in the corpus and `surreal` 59× — without gating these would fire on nearly every street photo. **humor_absurdity** (tier 0.75): G1 = comic/absurd register (absurd, humor, irony, juxtaposi, whimsic, comic); G2 = visible reaction (laugh, amuse, delight, play, cheer, glee). Tier 0.75 because humor is meaningful but less emotionally weighty than grief/tenderness. **uncanny_ordinary** (tier 1.0): G1 = eerie/dreamlike register (surreal, unsettl, ghost, haunt, dreamlike, mysterio); G2 = specific visual mechanism (mirror, echo, obscure, oblivio, unaware, double). Tier 1.0 because when both signals fire together the pairing is rare and significant. **economic_precarity** (tier 1.0): single-signal; condition vocabulary (marginalize, inequalit, precario, systemic) plus material evidence (worn, weather, dishevel, makeshift, tatter). Note: poverty/homeless/destitute validated as zero-occurrence in qwen captions — excluded. `weather` is broad but anchored by the social-framing terms. **solitude_in_crowd** (tier 1.0): two-signal to distinguish from `isolation_solitude`; G1 = crowd context (crowd, bustl, amid, pedestrian, surround, throng); G2 = disconnection (alone, solitary, detach, withdrawn, invisible). Both clusters can fire for the same image — correct behavior; the pair scoring values the overlap. **domestic_intimacy** (tier 1.0): single-signal, conservative vocabulary; bedroom/kitchen/household/nurturing validated as zero-occurrence — excluded. Uses domestic, household, interior, cozy, curtain, famili, belonging, nurtur, groom, tend, nest. Low expected frequency is intentional: tier 1.0 reflects that rare matches are high-confidence. |
| Weighted Dice ambient floor (kMinWeightedShared=0.75, kAmbientFloor=0.1) | A pair matching only a single tier-0.5 cluster (urban_street, nature_landscape, community_gathering) computes weightedShared=0.5, the only possible value below 0.75 in the discrete weight set {0.5, 0.75, 1.0}. Rather than returning 0, such pairs return kAmbientFloor=0.1 — a weak signal, not thematic resonance. The threshold 0.75 matches the lowest non-ambient tier weight so that a single bodily_gesture/looking_watching/etc. match still scores normally. The asymmetry gate in PairScorer.swift already prevents pure-symmetric single-cluster pairs (both images having only that one cluster) from scoring at all; this floor handles the residual case where each image has the ambient cluster plus unique non-overlapping clusters. If a new tier weight below 0.75 is added, kMinWeightedShared must be lowered to match. Calibrated against library: 0 pairs had thematicScore=1.0; up to ~2,109 cluster-scored pairs (6.2%) are affected on re-index. |
| Five new clusters designed from photographer-influence research (#15) | Clusters designed by researching the work and aesthetic concerns of: Paul Graham, Rebecca Norris Webb, Daniel Arnold, Jeremy Paige, Melissa O'Shaughnessy, Andre Wagner, Rosalind Fox Solomon, Sage Sohier — the humanist street and documentary tradition. Real library images used to validate cluster boundaries and sharpen vocabulary during design. **`humor_absurdity`** (tier 0.75): strangeness that resolves into a punchline or visible delight. Distinguished from `uncanny_ordinary` by the presence of a visible reaction (laughter, amusement, complicity) or a structurally resolved comedic contrast. Requires two signals — at least one register/reaction word plus one structural word — to avoid false positives from loose descriptors like `incongruous` alone. **`uncanny_ordinary`** (tier 1.0): the ordinary tips into strangeness without resolving. Defining quality is category slippage — things appearing where they don't belong, or unconsciously becoming something unexpected. The subject is typically unaware of the meaning layer the camera found. Also requires two signals from different vocabulary groups; `surreal` alone is insufficient given qwen's tendency to use it loosely for vivid lighting. **`economic_precarity`** (tier 1.0): the person for whom the social contract is visibly not working, in any setting. Vocabulary is intentionally setting-neutral — fires for street work, indoor protests, institutions equally. Should co-occur naturally with `solitude_in_crowd`; that's correct behavior. **`solitude_in_crowd`** (tier 1.0): presence without participation. Distinct from `isolation_solitude` by requiring crowd context (at least one crowd-context term must be present). Distinct from `waiting_anticipation` by the absence of forward orientation. Two modes: still figure in motion (stationary while the world flows past) and moving figure in a crowd (in motion but sealed off). **`domestic_intimacy`** (tier 1.0): the private world made briefly visible. Fires for domestic objects carried into public space, domestic gestures performed outside, or interiors where the living space functions as psychological portrait. Low expected frequency — when it fires, trust it; tier 1.0 reflects that rare high-confidence matches deserve full weight. Full vocabulary for all five clusters lives in the Claude Code session notes for #15 and in ConceptClusters.swift once implemented. |
| Post-#15 cluster calibration: saturation gate, vocabulary pruning, num_predict (#25) | After adding 5 clusters (29 total), three structural problems surfaced. **(1) Saturation gate**: original threshold `shared.count > 3` was calibrated for 24 clusters. With 29, genuine cross-context resonant pairs routinely share 4–6 clusters across different emotional registers and are zeroed. Replaced with weight-based gate: `weightedSharedSum > 5.0` (PairScorer.swift). 4×tier-0.75 = 3.0 (passes), 5×tier-1.0 = 5.0 (borderline), 8–10×mixed same-event = 8.0+ (correctly zeroed). **(2) False-positive keywords**: `sound_music` contained `play` (fires on "play of light"), `press`/`cover`/`plug`/`shield`/`block` (duplicate `sensory_overwhelm` vocabulary). Removed; kept `ear`/`hear`/`listen` because ear-cupping legitimately belongs in sound_music, sensory_overwhelm, and bodily_gesture simultaneously — clusters are not exclusive. `domestic_intimacy` contained `tend` (stem("tender") = "tend" after stripping "er"; "tender" is ubiquitous in humanist captions, fires alongside tenderness_care inflating shared count). Removed `tend`, `window`, `settled`, `lived`. Converted to two-signal: G1 = enclosed personal space (domestic/household/interior/cozy/curtain/famili/belonging/doorstep/windshield/dashboard/backseat/tent/camper/room); G2 = domestic behavioral register (nurtur/groom/nest/habitual/intimate/caring). `economic_precarity` converted to two-signal (G1=social framing, G2=material evidence); added `struggle` as explicit keyword (stem("struggle")="struggle", no suffix match; "struggl" only caught progressive form). `uncanny_ordinary` added "surrealism" to G1 (stem("surrealism")="surrealism"; qwen writes "a touch of surrealism" not "surreal" for dreamlike/halo effects). **(3) Ambient clusters over-firing**: `nature_landscape` fired on urban greenery (tree trunk on sidewalk) — removed `tree`/`light`/`shadow`/`open`/`vast`, added `foliag`/`canopi`/`grove`. `looking_watching` fired on nearly every pair — removed `look`/`eye`/`focus`/`attention`/`direct`/`aim`/`point`/`camera`/`lens`/`glass`/`window`/`reflect`/`reveal`/`expos`/`frame`; retained only deliberate-gaze vocabulary: watch/stare/gaze/peer/squint/glance/glimps/observ/witness/behold/survey/scan/scrutin/seen/noticed/caught/regard. **(4) Caption truncation**: 613/1,083 captions (57%) stored truncated at ~1,200 chars, ending mid-sentence. Root cause: num_predict=250 tokens (≈1,100–1,200 chars) cuts qwen's output before the interpretive second half. Raised to 400. Existing captions require a full re-caption pass; re-score alone does not fix already-truncated captions in the DB. |



| Visual diversity multiplier on thematic score + expanded thematic topK (#45) | **Problem:** Cluster scoring rewards shared cluster volume. Same-event pairs (protest+protest, festival+festival) share many clusters → high Dice → dominate thematic topK-30. Cross-context resonant pairs (musician+ears, grief in different settings) share fewer clusters → lower Dice → excluded from topK entirely. Diagnostic confirmed: musician hero (2045) + ears hero (1523) are active, have captions, have feature vectors, but never made either image's composite topK-150 or thematic topK-30. **Root cause — topK density:** With 1,057 active images and topK=150, the 150th composite pair scores ~0.44–0.47. A cross-context pair with mismatched visual aesthetics scores below this threshold. The thematic topK-30 cutoff for the musician was ~0.46; musician+ears (estimated 0.37–0.42 thematic) fell outside. **Dead-end documented:** Caption embeddings (nomic-embed-text) tested as primary signal. Reverted: general-purpose text embeddings measure same-domain similarity, not cross-context resonance. Two festival images share festival vocabulary → high cosine → crowded out cross-context pairs further. **Fix 1 — Visual diversity multiplier (PairScorer.swift):** CLIP cosine is already computed at scoring time. Applied as a multiplier to `clusterScore` before the thematic-boost check: CLIP < 0.30 → ×1.35 (cross-context bonus); CLIP 0.30–0.60 → ×1.0 (neutral); CLIP 0.60–0.88 → ×0.75 (same-context penalty); CLIP > 0.88 handled by existing composite ceiling. **Effect:** protest+protest (CLIP ~0.65) drops from ~0.73 to ~0.55 thematic. Musician+ears (CLIP ~0.15–0.20) rises from ~0.38 to ~0.51. The multiplier applies to all pairs universally — no enumerated special cases. **Why better than complementarity bonus:** Does not require enumerating role relationships (sender/receiver). Any visually-distinct pair with cluster overlap gets rewarded regardless of the nature of the relationship. **Fix 2 — Thematic topK expansion (IndexingEngine.swift):** `thematicK = max(10, topK/5) = 30` → `max(30, topK/3) = 50` with topK=150. Applies to both Phase 1 and Phase 2 scoring. Combined with Fix 1, musician+ears thematic ~0.51 now fits within the top-50 thematic for the musician. **Pair count impact:** ~10–15% more pairs stored (~112K → ~130K expected). **Asymmetry gate confirmed not the culprit:** Ears has `isolation_solitude`/`sensory_overwhelm`/`grief_sorrow` that musician lacks; musician has `skilled_performance` that ears lacks. Both images have unique clusters; gate passes. Do not weaken or remove it. **Existing penalties preserved:** CLIP > 0.88 → composite ×0.40; CLIP > 0.75 && thematic < 0.20 → composite ×0.65. These remain unchanged — the diversity multiplier and composite ceiling are independent. |

| animal_presence demoted to ambient tier (#47) | **Diagnostic (2026-05-06, post-#45 re-index):** Dog+dog pairs ranked #7 globally (images 1447+1624, composite 0.623, thematic 0.547). Multiple other animal-mention pairs in the 0.58–0.62 composite range. The `animal_presence` cluster at weight 0.75 was the cause — it gave "two dogs" a meaningful thematic signal, exactly the same-same pattern the system is supposed to reduce. The visual diversity multiplier from #45 didn't help: two different dogs in different urban settings score CLIP cosine ~0.35–0.50 (neutral zone, ×1.0 multiplier). **Fix:** `animal_presence` weight changed from 0.75 → 0.2 in `ConceptClusters.swift`. At 0.2, it is now an ambient-tier cluster. Pairs sharing only animal_presence (and other ambient clusters) return the ambient floor (0.1) from `weightedDice` and score ≤ 0.1 thematic. Dog pairs that share a genuine emotional cluster (tenderness_care for a touching scene, isolation_solitude for a lonely stray) still score meaningfully via those clusters. The two-signal gate on `animal_presence` is preserved — it still requires both a specific animal subject noun AND a behavioral/relational context word to fire. This prevents incidental background animal mentions from contributing even the 0.2 ambient weight. **Why not remove the cluster entirely:** Removing it would cause animal pairs with no other cluster overlap to contribute nothing at all to the Dice denominator, inflating scores for pairs that DO share clusters. Keeping it at ambient weight preserves denominator contribution while preventing same-subject rewards. |

| Complementary axis pair bonus (#48) | **Problem:** The thematic Dice score rewards SHARED clusters — it measures "what do these images have in common?" Cross-register complementary pairs (one image is the source of a phenomenon, the other is the receiver) share NO clusters because they fire DIFFERENT clusters on each side. Musician fires `sound_music`, `skilled_performance`; ears fires `sensory_overwhelm`, `isolation_solitude`. Weighted Dice = ambient floor (0.1). After visual diversity multiplier (~1.35 for visually distinct images): thematic ≈ 0.135. Below the 0.20 boost threshold. The pair never enters DB. **Diagnostic confirmation:** Post-#45 re-index, musician (2045) + ears (1523) completely absent from the pairs table (123,326 rows total). Not low-ranked — absent. **Fix — axis pairs:** Added `ConceptClusters.axisPairs: [(a: String, b: String, bonus: Float)]` defining 9 cluster-opposition relationships: `sound_music↔sensory_overwhelm` (+0.35), `power_dominance↔vulnerability_exposure` (+0.35), `joy_celebration↔grief_sorrow` (+0.30), `skilled_performance↔looking_watching` (+0.25), `tenderness_care↔isolation_solitude` (+0.25), `power_dominance↔confinement_freedom` (+0.25), `labor_effort↔stillness_rest` (+0.20), `movement_energy↔stillness_rest` (+0.20), `devotion_belief↔tension_conflict` (+0.20). **Scoring integration (PairScorer.swift):** After computing `clusterScore` via Dice, compute `axisBonus = max(bonuses for any firing axis pair)`. The axis bonus fires ONLY when `clusterScore ≤ 0.10` (ambient floor) — the code guards `if saturated || clusterScore > 0.10 { axisBonus = 0 }`. This prevents additive inflation on top of a real Dice score (e.g. a pair that already scores 0.65 via Dice would reach T=1.35 capped at 1.0 without the guard). The saturation gate is also respected — same-event pairs (weightedSharedSum > 5.0) don't earn the bonus. The asymmetry gate is NOT required for axis pairs — the nature of axis pairs is that each image fires a DIFFERENT cluster, so asymmetry is structural. Apply: `thematic = min(1.0, (clusterScore + axisBonus) × diversityMult)`. **Post-implementation diagnostic (2026-05-06):** After re-index with changes #47, #48, and #49 active, musician (2045) + ears (1523) remains absent from the DB. Root cause: `"ear"` is present in BOTH `sound_music` and `sensory_overwhelm` keyword vocabularies (ears are both a musical instrument component and a sensory receiver). Additionally, `"intensity"` stems to `"intens"` which matches sensory_overwhelm. Both images therefore fire BOTH clusters and share them as a meaningful cluster intersection — Dice > ambient floor, so axisBonus = 0 (guard fires). The pair scores via Dice (~0.45–0.51 thematic) but ranks below the thematic topK-50 cutoff (~0.55–0.70) for this library. The axis pairs approach works correctly for pairs where images genuinely fire opposite-only clusters; it cannot help pairs where both images share both clusters due to overlapping vocabulary. Lesson: the keyword cluster system cannot distinguish "musician playing" from "listener overwhelmed" when both captions use the word "ear" or musical vocabulary. Caption prompt redesign is the recommended next step — see backlog #50. **Rationale string update:** When thematic ≥ 0.20 but no meaningful shared cluster exists, the rationale now checks axis pairs and emits "Complementary resonance — [A] meets [B]" instead of the generic "Conceptual contrast" fallback. Also fixed a pre-existing bug where the rationale picked `shared.first` which could surface ambient clusters ("urban street") — now filters to `meaningfulShared` (weight ≥ 0.75) before selecting the label. **Why axis pairs over role annotations or LLM scoring:** Axis pairs are enumerable, deterministic, and zero-cost at scoring time. Role annotations require per-cluster schema changes and still reduce to enumerating relationships. LLM scoring would require an inference call per candidate pair, expensive at 123K+ pairs. Axis pairs scale to any future relationship type by adding a single line to the array. |

| Meaningful asymmetry gate (#49) | **Problem:** The old asymmetry gate (`!onlyA.isEmpty && !onlyB.isEmpty`) was trivially satisfied by ambient-tier clusters. Two dog photos differing only by `urban_street` vs. `community_gathering` (both ambient, weight 0.2) passed the gate, allowing the Dice computation to proceed and producing inflated thematic scores via their shared tier-0.75 animal behavior clusters (`movement_energy`, `bodily_gesture`, `waiting_anticipation`). **Fix:** Changed the asymmetry gate in `PairScorer.swift` to require at least one meaningful cluster (weight ≥ 0.75) unique to each side: `let meaningfulOnlyA = onlyA.filter { (ConceptClusters.weights[$0] ?? 0) >= 0.75 }` and same for B; gate passes only when both are non-empty. Ambient-only asymmetry (one image has `urban_street`, the other doesn't) no longer counts as meaningful differentiation. **Effect:** Pairs where both images have identical meaningful clusters plus different ambient clusters no longer receive an asymmetry pass — they either score via shared meaningful Dice or return ambient floor. Pairs where each image has at least one unique meaningful cluster continue to score normally. **Interaction with axis pairs:** The axis bonus is not gated by the asymmetry check — when axis bonus fires, the pair is structurally asymmetric by definition (each image fires a DIFFERENT cluster on its side of the axis). **Calibration result:** Dog pairs that previously passed via ambient-only asymmetry now correctly return ambient floor or score only via shared tier-0.75 behavior clusters they genuinely share. |

| Caption embedding infrastructure removed (#51) | **What was built:** During backlog item #9 / #44, a caption-embedding pipeline was added to complement cluster scoring. Components: (1) `CaptionEmbeddingEngine.swift` — a `CaptionEmbeddingEngine` protocol plus `OllamaEmbeddingEngine` (POST `/api/embeddings` to nomic-embed-text, 768-dim L2-normalised float vectors) and `MockEmbeddingEngine`; (2) `v9_caption_embeddings` DB migration — `captionEmbedding BLOB` column on the images table (3,072 bytes/image); (3) Phase 3.6 in `IndexingEngine` — backfill loop for uncaptioned images; (4) `PairScorer.captionEmbeddingCosineSim` — raw cosine on float vectors, unused after revert; (5) `IndexingEngine` stores `embeddingEngine: any CaptionEmbeddingEngine` and fetched `captionEmbedding` in both intra- and cross-folder `imageMeta` queries; (6) `EngineController.buildEmbeddingEngine()` — availability check + engine construction, with `embeddingAvailable` published property; (7) `IndexingProgress.Phase.embedding` case. **What was tried:** nomic-embed-text cosines on qwen2.5vl captions live in [0.55, 0.95] not [0, 1]. After floor normalisation (`max(0, (cosine − 0.50) / 0.50)`), a 65/35 blend with cluster score was applied. This gave zero-cluster pairs like protest+ritual a thematic of ~0.205 — enough to pass the 0.20 boost threshold. The approach was implemented, tested, and partially integrated. **Why it failed:** The embedding measures same-domain similarity, not cross-context resonance. Two festival captions score high cosine because they use the same vocabulary, not because they are conceptually complementary. The same-domain pairs that were already crowding out cross-context pairs via cluster scoring crowded them out further via embedding. An embedding ceiling was added (cap thematic at 0.10 when raw cosine > 0.85) which helped with obvious same-event pairs but the fundamental problem remained: the embedding rewards proximity in language space, not complementarity in human experience. The revert commit (`revert(#9): remove embedding from thematic scoring, disable phase 3.6`) removed the scoring use while leaving infrastructure in place. A subsequent prompt-testing session (2026-05-09) confirmed that the underlying problem is the caption vocabulary, not the scoring mechanism — qwen2.5vl-caption describes what is visible, not what is felt. Embeddings of description-language captions cannot capture emotional register or positional relationship between images. **Why infrastructure was removed:** The v9 column was unused (all values NULL after revert), adding 3KB/image of dead storage. The engine, protocol, and DB query overhead remained even though nothing read from them. The `buildEmbeddingEngine()` call ran on every launch to check nomic-embed-text availability — an unnecessary Ollama request. Phase 3.6 was a no-op comment. None of the 7 infrastructure touchpoints provided value. **What is preserved in this record:** The scoring formula (floor normalisation + blend), the calibration data (7 representative pair cosines), the embedding space shape (0.55–0.95 range for paragraph-length captions), and the failure mode (same-domain amplification). If caption quality improves (via prompt redesign, see #50), a text-embedding approach could be re-evaluated. The implementation was straightforward and the infrastructure took ~1 day to build; rebuilding it is not expensive if the input quality problem is solved first. |

| Caption embeddings (nomic-embed-text) as primary thematic signal (#44) | **Architectural diagnosis:** ConceptClusters vocabulary-matching hit a ceiling at 18% zero-overlap pairs (31,755 pairs both captioned, neither scoring). The problem is conceptual analogy — protest crowd + religious procession, two isolated figures described in different words — invisible to keyword matching regardless of vocabulary depth. Diagnosis from backlog item #9. **CLIP text tower inspection:** `clip-vit-base-patch32.mlpackage` contains only the visual encoder (576 vision_model weight references, zero text_model references). No text encoder is present. **Embedding approach:** nomic-embed-text via Ollama (`ollama pull nomic-embed-text`, 274MB). POST `/api/embeddings` with `{"model": "nomic-embed-text", "prompt": "<caption text>"}` returns a 768-dim L2-normalised float vector. Storage: raw bytes (`[Float].withUnsafeBufferPointer { Data(buffer:) }`) in `images.captionEmbedding BLOB`; 768 × 4 bytes = 3,072 bytes/image, ~3.3MB for 1,100 images. **Calibration — embedding space shape:** nomic-embed-text cosines for real qwen2.5vl captions (150–250 word paragraphs) live in [~0.55, ~0.95], not [0, 1]. Tested against 7 representative pairs: grief-grief=0.732, urban-urban=0.660, protest-ritual=0.658 (the key zero-cluster case), grief-protest=0.611, grief-joy=0.568, grief-urban=0.551. Raw cosine cannot be used directly — unrelated pairs (grief-urban=0.551) sit too close to the target pairs (protest-ritual=0.658). **Floor normalisation:** applied `normSim = max(0, (cosine − 0.50) / 0.50)` to expand the discriminative range. After flooring: grief-grief=0.464, protest-ritual=0.316, grief-joy=0.136, grief-urban=0.102. **Blend formula:** `thematic = 0.65 × normSim + 0.35 × clusterScore`. With no cluster overlap: protest-ritual→0.205 (passes 0.20 boost threshold); grief-joy→0.088 (does not pass). With cluster overlap, the blend averages embedding and cluster evidence. **What is unchanged:** asymmetry gate (both images must have a cluster the other lacks); saturation gate (weightedSharedSum > 5.0 zeroes cluster component); meaningful-tier gate inside weightedDice (shared must contain ≥1 cluster with weight ≥ 0.75); thematic boost (composite = 0.25A + 0.15G + 0.60T when thematic ≥ 0.20); dual topK thresholds; CLIP image ceiling/same-subject penalties. **Cluster system role preserved:** clusters remain the exclusive source of filter-pill labels and info-rail display. Embedding scores are opaque; clusters still name the connection. **Fallback chain:** (1) captions + embeddings → blend formula; (2) captions only → weighted Dice; (3) no captions → CLIP image cosine. **Backfill:** Phase 3.6 in IndexingEngine queries `WHERE caption IS NOT NULL AND captionEmbedding IS NULL` on every re-index, same pattern as caption backfill. **v9 DB migration:** `ALTER TABLE images ADD COLUMN captionEmbedding BLOB` — lives on images table alongside caption, no JOIN needed at scoring time. |

## Backlog

<!-- Convention: update Status in place as work progresses (backlog → in progress → done).
     Claude Code should update this file directly when an item is completed or started.
     Add new items at the next available number. Do not remove done items — they serve as a log. -->

| # | Title | Type | Status | Notes |
|---|-------|------|--------|-------|
| 1 | Option B: store selected_for flag at score time | improvement | done | Add selected_for column (thematic/composite) to pairs table at scoring time. Use for modality labeling instead of post-hoc score comparison. Fixes labeling bias where geometric dominates. **Implementation (2026-05-05):** DB migration v8 adds nullable `selectedFor TEXT` to pairs. `IndexingEngine` tracks composite-topK keys in a `Set<String>` alongside `toInsert`; each `PairRecord` is stamped `selectedFor = compositeKeys.contains(key) ? "composite" : "thematic"` at INSERT time. `QueryService` SELECTs the column in both query paths and passes it through `PairQueryResult`. `convertToPairFree` and `convertToPair` use `selectedFor == "thematic"` as the primary modality signal; NULL rows (pre-v8) fall back to the post-hoc score comparison. A full re-index populates `selectedFor` for all pairs. |
| 2 | Option D: tighten geometric scorer | improvement | in progress | Magnitude threshold raised (0.05→0.15). Distinctiveness multiplier added (exponent 0.4). Display-time floor sliders implemented. More tuning likely needed — see item #14. |
| 3 | Fix color tone filter (B&W + Mixed) | bug | done | colorProfile column populated via migration default only. Needs real re-index to get actual values. B&W and Mixed filters currently show nothing. |
| 4 | Wire Collections to database | feature | done | See decision #38. |
| 5 | Diptych export | feature | done | Side-by-side export as JPEG or PDF. Equal-slot canvas with letterboxing (never crops). Outer padding 4% of slot height, gap between images 2.5%. Optional filename label under each image (left-aligned, 75% gray). Suggested filename: `stemA-(^_^)-stemB_diptych.jpg/pdf`. Entry point: lightbox action bar button (⌘E). Security-scoped folder access via FolderBookmarks; first export from a folder that predates bookmark support triggers a one-time Locate Folder panel. |
| 6 | Settings view: runtime weights + knobs | feature | done | ⌘, sheet exposing modality weights (aesthetic/geometric/thematic), topK, caption timeout. Persisted via UserDefaults, read in EngineController. No re-index required. |
| 7 | Settings that require re-index | feature | backlog | Duplicate threshold (dHash phase) and CLIP similarity ceiling affect data written to DB. Modality weights are a middle case — re-scorable from stored component scores without full re-index. UI should distinguish these from cheap runtime settings. |
| 8 | Double CLIP build on launch | bug | backlog | On some launches two engine builds start simultaneously due to ollama timing window. engineBuildTask?.cancel() pattern should prevent it but race is narrow. Monitor — no crashes recently. |
| 9 | ConceptClusters vocabulary audit + animal_presence cluster + caption embeddings | improvement | done | **Diagnostic (2026-05-05):** Ran differential word frequency analysis against 32,862 low-thematic pairs (thematicScore < 0.20, both captioned) vs 1,504 high-thematic pairs (> 0.65). Original query (compositeScore > 0.45, thematicScore < 0.15) returned 0 rows — after the full re-caption pass, all high-composite pairs already have meaningful thematic scores. The real gap is in the 31,755 pairs (18%) with thematicScore < 0.10 (no cluster overlap). **Key findings:** (1) "gazing" stems fixed in looking_watching. (2) animal_presence cluster added (tier 0.75, two-signal). (3) The 18% zero-overlap gap is structural — conceptual analogy invisible to keyword matching — addressed by caption embeddings (see #44). Full re-index required to update stored thematicScores with embedding-based scoring. |
| 10 | Caption quality ceiling (moondream) | improvement | superseded by #20 | moondream 1.8B describes surface action but misses subtext, irony, cultural references. Acceptable for cluster matching today but may need upgrade path. |
| 11 | FileAccessCoordinator: centralise security-scoped bookmark management | improvement | backlog | Current fix stores per-folder bookmarks in UserDefaults and resolves them at export time. Long-term: (a) extract into a dedicated FileAccessCoordinator so any future feature (RAW processing, full-res preview, metadata re-sync) calls one place instead of solving this independently; (b) detect permanently-invalid bookmarks (renamed/moved volumes) and offer the Locate Folder panel proactively rather than surfacing a generic error; (c) call FolderBookmarks.remove(folderPath:) from removeFolder so dead entries don't accumulate; (d) check FolderItem.driveType before resolving — show "drive not connected" rather than the locate panel when an external/NAS volume is simply offline. |
| 12 | Address visually dominant images with many pairs | improvement | done | SQL redesigned: full pairs table fetched ordered by sort column (no SQL pre-filtering — top-N CTE approaches left hub-heavy libraries with insufficient fallback diversity). Pagination moved to in-memory cache (150/page, infinite scroll). Dot badge, lightbox pair count, anchor strip count, and anchor filmstrip fresh-fetch from DB all implemented. Grid uses two-pass greedy: pass 1 = strict (both images unrepresented, ~431 pairs, 862 images); pass 2 = covers remaining unrepresented images with their best available pair even if the partner is already seen (~166 additional pairs). Result: 597 pairs, all 1,028 active images represented. topK raised 50→150 at index time to give peripheral images enough diverse candidates for the greedy to work. |
| 24 | Pair grid: two-pass greedy matching | decision | done | Explored cap-2 (~127 pairs), cap-N, and single-pass greedy with various topK values. Single-pass greedy at topK=150 covered 862/1,028 images; 218 images were structural orphans (all their scored pairs pointed to already-claimed hubs). Two-pass greedy: pass 2 re-iterates pairs in score order and adds any pair where at least one image is still unrepresented, with each image used at most once per pass. This covers all images while keeping hub appearances bounded (most appear once; the ~166 pass-2 pairs allow their partners to appear twice). |
| 23 | Revisit dot badge on pair grid tiles | improvement | backlog | Dot badge currently triggers at >100 pairs (static threshold, top 14% of images by pair count at time of calibration; library distribution: p50=53, p75=63, p90=145). With topK raised to 150 and scoring improvements planned, both the threshold and the meaning of "hub image" will shift — recalibrate after #15/#14 are settled. Separately, the dot itself (6pt white circle, 0.55 opacity) is easy to miss on busy thumbnails. Visual improvements to consider: larger size, higher opacity, a filled ring or pill shape, a contrasting outline, or a corner badge with a count rather than a plain dot. The signal ("this image has many pairs — explore it") should be immediately legible without hovering. |
| 13 | Investigate caption persistence across imports | investigation | done | Caption coverage is 100% (1094/1094 active images captioned). 152 images had malformed "Sentence 1:" prefix captions that were cleared and re-captioned each run — fixed by stripping the prefix in-place (IndexingEngine.swift) and at generation time (CaptioningEngine.swift). Root cause of the apparent CLIP-fallback problem was orphaned pairs, not caption quality — see #19. |
| 14 | Continue geometric scorer tuning | improvement | backlog | Distinctiveness multiplier exponent currently at 0.4 (one step from sqrt=0.5). Remaining work: (a) continue stepping exponent down in 0.1 increments until balance feels right; (b) add a "multiplier strength" slider (0–1 blend between raw similarity and fully-multiplied score) as a runtime control — no re-score needed when moved; (c) evaluate whether normalization anchors (currently p90: 4.0/0.20) should be lowered toward p50–p70 to concentrate the penalty on the bottom quartile. Revisit after thematic cluster work since relative geometric weight matters less once thematic improves. |
| 16 | Center-cell discount | improvement | backlog | Slider + Apply button (re-score only, no re-index). Weights peripheral 4×4 composition grid cells more heavily so unusual/asymmetric framing scores higher relative to centered-subject default. Cannot be derived from stored scalars — requires recomputing grid cosine similarity with a spatial weight mask applied to the raw vectors, so a re-score is unavoidable when the slider moves. Apply button pattern used to make the cost explicit. |
| 15 | Thematic cluster vocabulary expansion | improvement | done | Five new clusters implemented and vocabulary calibrated. The two outstanding structural fixes — (1) meaningful-tier gate (require ≥1 cluster with weight ≥0.75 in shared intersection) and (2) ambient weight reduction (urban_street / nature_landscape / community_gathering 0.5 → 0.2) — were both implemented in #29. |
| 18 | Weighted Dice minimum-threshold | improvement | done | A pair matching only a single low-weight cluster (e.g. urban_street, weight 0.5) with no other clusters computes Dice=1.0 when neither image has any other clusters. A minimum weighted-shared floor (e.g. require weightedShared ≥ 0.75 before the score is non-zero, or ≥ 1.0 for a pair to be considered thematically linked) would suppress these degenerate cases. Needs calibration against the library — floor value should be chosen so that a single tier-0.5 cluster is excluded but a single tier-1.0 cluster is not. Implement in ConceptClusters.thematicScore alongside the weights dict. |
| 19 | CLIP-fallback scores dominate thematic sort | improvement | Deferred | Root cause identified: the 11,233 CLIP-fallback pairs all referenced deactivated images (IDs below the current active range 980–2073). They were orphaned pairs from previous imports never cleaned up by the per-run DELETE. fetchPairs already filtered them via isActive=1 JOIN, so they were not visible in the UI — but accumulated in the DB. Fix: added an orphan-cleanup DELETE (pairs where either image isActive=0) before the per-run DELETE+reINSERT in IndexingEngine.swift. No scoring algorithm change needed; all 40,160 active pairs are cluster-scored with 0 CLIP-fallback. |
| 17 | Weighted Dice for thematic scoring | improvement | done | All cluster matches currently count equally. Setting/context clusters (urban_street, nature_landscape, community_gathering) are ambient and coincidental; emotional/dramatic clusters (grief_sorrow, vulnerability_exposure, tenderness_care) signal genuine thematic connection. Proposed: assign a pairing weight per cluster (0.0–1.0) and compute weighted Dice: weighted_shared = Σ weight(c) for c in (A∩B); score = 2×weighted_shared / (Σ weights(A) + Σ weights(B)). Draft tiers: 1.0 = grief_sorrow, tenderness_care, tension_conflict, joy_celebration, vulnerability_exposure, isolation_solitude, ritual_ceremony, devotion_belief; 0.75 = labor_effort, stillness_rest, transformation_change, waiting_anticipation, skilled_performance, power_dominance; 0.5 = urban_street, nature_landscape, community_gathering, youth_age, movement_energy, looking_watching, bodily_gesture, sound_music, sensory_overwhelm, confinement_freedom. Note: a minimum weighted-shared threshold is also needed — a pair sharing only a 0.5-weight cluster with no other clusters still computes Dice=1.0 when neither image has any other clusters. Design this alongside the weight calibration. Weights live in ConceptClusters.swift alongside the cluster definitions. Do this before vocabulary expansion (#15). |
| 20 | Switch captioning model to qwen2.5vl:7b + redesign prompt | improvement | done | Replaced moondream 1.8B with qwen2.5vl:7b via Ollama. Retired the Sentence 1/2 prompt scaffolding entirely — replaced with a single flowing paragraph prompt covering scene context, social situation, emotional register, symbolism, irony/humour, and meaningful contrasts. num_predict raised 80→250; timeout raised 90s→120s; URLSession ceiling raised 120→150s. Added cleanup for qwen tokenizer special tokens (<\|im_start\|> etc.) that can leak into output at token-budget boundaries. v7 DB migration clears all moondream captions on first run so the full library is re-captioned. Validated on 30-image sample before committing — clear wins on protest/event context, symbol reading, emotional depth, and irony detection. Supersedes #10. |
| 22 | Dual-resolution image loading (grid vs lightbox) | improvement | done | Grid thumbnails (512px) unchanged. Lightbox now loads a ~2048px mid-res preview on demand via MidResLoader actor. The 512px thumbnail renders immediately (no blank state); mid-res crossfades in when ready. Cache at `~/Library/Containers/com.toastingmachine.Couplet/Data/Library/Caches/Conjunct/previews/{imageID}.jpg` with count-based LRU eviction (high: 200, target: 150). Security-scoped access via existing FolderBookmarks system — folderPath now stored on DisplayPair (sourced from `folders.path` in the query). No DB migration needed. |
| 21 | Caption UI improvements in lightbox info rail | improvement | done | qwen2.5vl:7b captions are rich paragraphs (~150–250 words) vs moondream's two sentences. Three areas of work: (1) Collapsed-by-default display — captions render in a collapsed state (7-line limit) so A/G/T scores and cluster breakdown are visible without scrolling; a "Show more"/"Show less" button per caption expands to full text on demand. Short captions (≤280 chars) show no toggle. A gradient fade at the bottom of collapsed cards signals there is more text. (2) Redundant openers — post-processing deferred; `strippingCaptionOpener()` called at display time to strip common qwen lead-ins without touching stored text. (3) Mid-sentence truncation — num_predict raised to 400 in #25. Implementation in `LightboxInfoRail.swift → captionBlock()`. **Layout bug fix (2026-06-09):** Root cause: `.textSelection(.enabled)` backs the `Text` with an `NSTextView`. On first click, `NSTextView` activates and renders at its full intrinsic height, overriding SwiftUI's `lineLimit` clipping — the caption visually overflowed into the card below while the layout frame stayed at 7-line height. Fix: `.allowsHitTesting(isExpanded.wrappedValue || !needsToggle)` on the caption text blocks mouse events when collapsed, preventing `NSTextView` from activating. Hit testing re-enables once expanded (or for short captions never truncated). Card body tap removed as an affordance: `onTapGesture` does not flush ScrollView content size on macOS; `Button` wrapper fixes reflow but blocks text selection — not worth the complexity. "Show more"/"Show less" is the only expand/collapse control; caption text has `.textSelection(.enabled)` for copy. Container-level `.animation(.easeInOut, value:)` on `captionsSection` animates button-triggered expansion. |
| 25 | Full re-caption pass for pre-400-token captions | maintenance | done | Re-caption pass completed. All captions are now full-length at the 400-token budget. |
| 26 | Temporal penalty bypass via NULL-captureDate duplicates | bug | done | **Confirmed root cause (three bugs).** (1) `EngineController.convertToPair` recomputed `displayComposite` from raw component scores without replaying the temporal penalty — the comment acknowledged this as an "approximation." Sequential pairs enter via thematic topK-10 (high thematic score bypasses composite filter) and inflate to the top since display score ≈ 0.78 vs. intended ≈ 0.31. Fix: temporal penalty replayed in `convertToPair` using already-fetched `captureDateA/B`. (2) `QueryService` stored `captureDate` as INTEGER in SQLite but read it with `row["captureDateA"] as? Double`, which GRDB silently coerces to nil for int64 column values — so all dates arrived as nil at the penalty replay site, leaving penalty=1.0 for every pair. Fix: explicit int64→Double coercion `(row["captureDateA"] as? Double) ?? (row["captureDateA"] as? Int64).map { Double($0) }` in both `fetchRepresentativePairs` and `fetchPairs`. This was the reason the first fix had no visible effect despite being logically correct. (3) Null-captureDate duplicate records exist because `upsertImages` keys on `path`: folder remove+re-add from a new path creates a new row. Fix: filename+contentHash check in INSERT branch coalesces moved files onto existing records; inactive image cleanup added alongside the orphan-pair sweep in `reindex()`. Null-dated duplicates confirmed inactive with 0 pairs — no active penalty bypass was occurring at the DB level. Verified: after fix, pair 2382788 (R016467+R016468, 3s gap) shows penalty=0.40 → displayComposite=0.311 instead of 0.778. |
| 27 | Investigate aesthetic score inflation (0.968 for weak pair) | investigation | done | Investigated. Issue no longer significant — high-aesthetic/low-thematic pairs surface around composite 0.49–0.53, below the grid's practical visibility threshold. No action needed. |
| 28 | Same-subject type pairing (dogs, cars, etc.) | improvement | done | Implemented via secondary CLIP tier in PairScorer.swift (see #42). Diagnostic confirmed almost no pairs in the severe zone (composite>0.50, thematic<0.20, geometric<0.60) post re-caption pass. |
| 30 | Solid-colour title bar — suppress NSTitlebarBackgroundView | bug | done | Title bar rendered with frosted-glass / wallpaper bleed on macOS 14–15. **Actual root cause (confirmed via view hierarchy dump):** the effect comes from `NSTitlebarBackgroundView`, a private AppKit class at subview index 0 of `NSTitlebarView` — NOT from an `NSVisualEffectView`. All prior attempts failed because they targeted a VEV that does not exist in the hierarchy on this macOS version. **Fix:** navigate to `NSTitlebarView` via `window.standardWindowButton(.closeButton)?.superview`, then insert a `SolidTitlebarCover: NSView` (wantsLayer=true, makeBackingLayer returns CALayer with backgroundColor=#0e0e10) at subview index 1 — just above `NSTitlebarBackgroundView`. All controls (traffic lights at [5][6][7], accessory button at [3]) are at higher subview indices and remain visible without any re-stacking. Also set `titlebarAppearsTransparent = true`, `titleVisibility = .hidden`, and `window.backgroundColor = #0e0e10`. Re-apply from `didBecomeKeyNotification` and `didBecomeMainNotification` to survive AppKit focus transitions. Call via `DispatchQueue.main.async` from `viewDidMoveToWindow` to let the hierarchy fully assemble. Implementation: `CoupletTheme.swift`, `installSolidTitlebar` + `SolidTitlebarCover`. **What does NOT work and why:** setting `titlebarAppearsTransparent` alone, walking VEVs and changing `blendingMode/material`, setting `layer.backgroundColor` on `NSThemeFrame` — none of these touch `NSTitlebarBackgroundView`. Also: inserting a cover view inside the VEV produces #1e1e1e not #0e0e10 because the VEV's material compositing lightens any content inside it. |
| 34 | Two-phase scoring: intra-folder first, cross-folder in background | improvement | done | **Phase 1 (blocking, fast):** `IndexingEngine.runPipeline` scores only the scan batch against itself (batch × batch). Scoped DELETE removes only intra-folder pairs for the batch before re-inserting. Yields `.complete` when done; `EngineController` sets `isIndexing = false` and calls `refreshFolders()` — folder is immediately browsable. **Phase 2 (background, cancellable):** `IndexingEngine.runCrossFolderScoring` scores batch × (all other active) at `.background` priority. Launched from `index()` as a stored `Task<Void, Never>` (`crossFolderTask`) after phase 1 returns. Scoped DELETE removes only cross-folder pairs for the batch (one image inside, one outside) before re-inserting. Phase 2 calls `Task.checkCancellation()` between batch images. **Cancellation:** `index()` cancels any running `crossFolderTask` before starting a new one — so re-indexing a folder while phase 2 is in flight cancels cleanly. Phase 2 always yields `.backgroundScoringComplete` before closing the stream (even on cancellation), so `EngineController` gets the signal to call `refreshFolders()` and break the loop. **Progress:** Phase 2 is silent — `.backgroundScoring` and `.backgroundScoringComplete` are not published to the UI progress card. Phase 1 progress reports `itemsTotal = batchVectors.count` (e.g. 66 for a 66-image folder), not the full library size. **DB:** No schema changes. Intra vs cross-folder origin is implicit from `folderID` on images; existing query scoping from #33 handles folder view filtering. **No changes to PairScorer** — all scope control is in the IndexingEngine orchestration loop. Supersedes #31. |
| 31 | Scope pair scoring to current scan batch only | bug | superseded by #34 | Superseded by #34, which provides the full two-phase solution. |
| 32 | Folder pair counts show 0 in sidebar | bug | done | `FolderItem` had no `pairCount` field; sidebar showed `imageCount` instead. Fix: added `pairCount: Int` to `FolderQueryResult` and `FolderItem`; `fetchFolders()` now computes intra-folder pair count via a correlated subquery (`ia.folderID = f.id AND ib.folderID = f.id`); `refreshFolders()` threads it through; `SidebarView` shows `folder.pairCount`. Counts match #33's scope exactly. **Secondary bug:** GRDB returns SQLite INTEGER columns (including `COUNT(*)` from correlated subqueries) as `Int64`, not `Int` — `as? Int` silently returns nil even on 64-bit macOS. Both `imageCount` and `pairCount` were affected. Fix: `intCol()` helper in `fetchFolders()` coerces via `Int64` as canonical fallback. Pattern to use for any future integer column read from GRDB Row: `(row["col"] as? Int) ?? (row["col"] as? Int64).map(Int.init) ?? 0`. |
| 33 | Folder filter shows cross-folder pairs instead of intra-folder pairs | bug | done | `fetchRepresentativePairs`, `fetchPairs`, and `fetchImagePairCounts` all used `(a.folderID = ? OR b.folderID = ?)`, returning cross-folder pairs in folder views. Changed to `a.folderID = ? AND b.folderID = ?` in all three methods. The All view (folderID = nil) continues to show all pairs unchanged. Verified against DB: folder 2 has 106,477 intra-folder pairs + 13,739 cross-folder; folder 3 has 399 intra + 13,739 cross — those 13,739 now only appear in the All view. |
| 35 | "Find Pairs For…" — explore or retire | exploration | backlog | The "Find Pairs For…" button was removed from the filter bar (2026-05-01) as it had no implementation and its design intent is unclear. The original idea: let the user pick a specific image from a folder and surface all pairs that include it, even if they didn't surface in the main grid. **Open questions:** (a) does this add value over clicking an image in the lightbox and using the anchor/filmstrip flow, which already filters to all pairs for that image? (b) if the user wants to explore pairs for an image they haven't yet seen paired, there's no way to browse to it — they'd need an image browser, which is a significant feature. (c) street/documentary photographers rarely start from a specific image; they start from the pair grid. Consider retiring permanently rather than building. Revisit if user research suggests a strong need. |
| 36 | Tools bar chrome: current approach and known limitation | architecture | done | The tools bar (filter controls) lives in the AppKit titlebar layer, not in SwiftUI content. SwiftUI `.toolbar { ToolbarItem }` was rejected because macOS 15 applies per-item "liquid glass" capsule treatment to NSToolbarItem containers that cannot be suppressed from inside. `NSTitlebarAccessoryViewController` with `.bottom` was rejected because it creates a visually separate second row below the 28px traffic-light band with dead space between. **Current approach (2026-05-01):** An empty `NSToolbar` (no items, so no NSToolbarItem glass) with `window.toolbarStyle = .unified` causes `NSTitlebarView` to expand to a single ~50px band. A `SolidTitlebarCover` NSView fills the band with zinc-950. A `PassthroughHostingView<AnyView>` (NSHostingView subclass with `mouseDownCanMoveWindow = true`) is inserted directly into `NSTitlebarView` at `leadingAnchor + 192`, above the cover, and updated via `WindowConfigurator.updateNSView` on every SwiftUI re-render. **Known limitation:** When the sidebar is hidden, the filter controls span the full window width but there is a ~80px leading gap (the width reserved for traffic lights + sidebar toggle accessory). This gap is always present because the accessory view has a fixed leading width even when no sidebar-related controls are visible there. Not worth fixing until sidebar-hide UX is redesigned. **Border alignment:** vertical sidebar border is an AppKit `TitlebarSidebarBorderLine` at `leadingAnchor + 191` (1pt wide, occupies x=191–192, matching SwiftUI overlay at the same x). Bottom border is `TitlebarToolsBottomBorder` at `leadingAnchor + 192`, full width, 1pt tall. |
| 37 | Filter bar: left-divider-to-Composite gap | bug | done | The gap between the left vertical Divider and the "Composite" Sort picker appeared wider than the symmetric right-side gap (All Tones → right Divider), even though both sides use the same outer `HStack(spacing: 12)`. **Root cause:** SwiftUI's `.menu` Picker on macOS is backed by `NSPopUpButton`, which allocates its layout frame symmetrically (`.frame(width: 150)` is honoured) but places the label text with ~8pt of internal leading whitespace — because the dropdown arrow occupies the far-right of the cell, the visible text content starts several points inside the left frame edge, creating an optical gap that cannot be corrected by adjusting `HStack(spacing:)` or `Divider` padding (those moves affect both sides equally). **Fix:** Applied `.padding(.leading, -8)` directly to the Sort Picker in `FilterBarView.swift`. This shifts the picker's rendered position 8pt leftward inside its layout frame without affecting its declared width or the spacing of any surrounding element. The Tone picker is unchanged (the dropdown arrow near its right edge naturally compensates the right-side gap). Value -14pt was chosen empirically after visual verification. **What was not changed:** `HStack(spacing: -8)` inner group, outer `HStack(spacing: 12)`, Divider layout, `.frame(width: 150)` on the Sort picker. |
| 38 | Collections: add/remove pairs, DB-backed, drag-and-drop | feature | done | Wired the Collections UI to the database. **DB:** `collections` table (id, name, pairCount) and `collectionPairs` junction table (collectionID, pairID, UNIQUE constraint). `QueryService.addPairToCollection` uses `INSERT OR IGNORE` and returns `db.changesCount > 0` so callers can tell whether the row was actually inserted (as opposed to already existing). `QueryService.removePairFromCollection` deletes the junction row. **EngineController:** `addPairToCollection(pairID:collectionID:) async -> Bool` and `removePairFromCollection` bridge these to the main actor. **Count tracking:** `LibraryViewModel.refreshPairCount(forCollection:delta:)` increments/decrements the sidebar count in memory only when the DB actually inserted/deleted (i.e., when the Bool return is true). This prevents overcounting on repeated drops of the same pair. **Drag-and-drop:** `PairsGridView` provides an `onDrag` modifier on each tile that vends a `UTType.data` item (JSON-encoded pair ID); `SidebarView` collection rows have an `onDrop` handler that decodes the pair ID and calls `addPairToCollection`. **Drag preview:** Static `PairDragPreview` struct renders two thumbnail panes side-by-side at a fixed 126×50pt — animation was attempted (`DispatchQueue.main.asyncAfter` + `withAnimation`, then declarative `.animation(.easeInOut, value:)`) but both approaches were inconsistent because `NSEventTrackingRunLoopMode` blocks GCD main queue timers during drag sessions. Static sizing is reliable and visually clean. **Remove from collection:** Swipe-to-delete on rows in the collection grid view calls `removePairFromCollection`; count decremented by 1 on success. **Lightbox:** "Add to collection" button in the action bar presents a sheet with all collections; selecting one calls `addPairToCollection` and shows a confirmation badge. |
| 39 | Grid load race condition — double-onChange, unkeyed cache, Task debounce | bug | done | Rapid folder/collection navigation caused stale results, flickering, or delayed loads. Three root causes identified and fixed. **(1) Double-onChange race:** `LibraryViewModel.selectFolder` sets both `selectedFolderID` and `selectedCollectionID = nil` in the same synchronous pass. PairsGridView had separate `onChange(of: selectedFolderID)` and `onChange(of: selectedCollectionID)` handlers, each firing within milliseconds and each launching a `loadPairs` call with different settled state snapshots. Fix: merged both handlers to call a single `reloadPairs()` helper that reads `currentFolderID` and `currentCollectionID` from settled published state, so both observers always start the same query. **(2) Unkeyed representative-pairs cache:** `EngineController.representativePairsCache` was a plain `[DisplayPair]` array. Page > 0 requests could receive slices from a cache built for a different folder or collection. Fix: added `representativePairsCacheKey: (folderID: Int64?, collectionID: Int64?, sortColumn: String)` set on every page-0 fill; page > 0 requests validate the key and return `[]` on mismatch. **(3) Task debounce + cancellation:** `PairsGridViewModel.loadPairs` now stores the launched `Task` in `loadTask`. On each new call, `loadTask?.cancel()` is called first, then a new Task sleeps 250ms (`try? await Task.sleep(for: .milliseconds(250))`). If the sleep is cancelled (a newer navigation arrived), the Task exits without querying. A `guard !Task.isCancelled` check at the top of `fetchRepresentativePairs` provides a second exit point before entering the `QueryService` actor. **Remaining limitation (addressed in #40):** Once a Task passes the cancellation guard and suspends on `await queryService.fetchRepresentativePairs(...)`, it cannot be interrupted — `QueryService` is a Swift actor that serializes all requests. Clicks > 250ms apart can still queue multiple queries that drain serially. Fixed in #40 via nonisolated DB reads + generation counter + collection fast-path. |
| 40 | Grid load — nonisolated DB reads, generation counter, collection fast-path | bug | done | Follow-on to #39. Three fixes that together eliminate blocking on rapid folder/collection navigation. **(1) nonisolated QueryService reads:** `fetchRepresentativePairs` and `fetchImagePairCounts` made `nonisolated` — both only access `self.db` (a `let DatabaseManager: Sendable`), so Swift 6 permits this. Calling them no longer acquires the actor's serial executor. **(2) Task.detached in EngineController:** both DB calls combined into a single `Task.detached(priority: .userInitiated)` so they run on a background thread via `DatabasePool` WAL concurrent reads. A collection query is never blocked behind an in-flight folder query. **(3) Generation counter:** `loadGeneration: Int` incremented at the top of `fetchRepresentativePairs`; checked after the detached task returns. Stale results from queued-but-superseded queries are silently discarded. **(4) Collection fast-path:** when `collectionID != nil` the two-pass cap-2 greedy deduplication is skipped — collection pairs are already curated, so deduplication serves no purpose. **Critical:** `fetchRepresentativePairs` and `fetchImagePairCounts` are now `nonisolated` synchronous functions. Calling them from `@MainActor` context without a `Task.detached` wrapper runs them on the main thread and blocks the UI. Always wrap in `Task.detached` when calling from `@MainActor`. Remaining: cap-2 still runs on `@MainActor` for folder views — see #41. |
| 41 | Move cap-2 off @MainActor + progressive grid rendering | improvement | done | Two related fixes. **(1) Cap-2 off @MainActor:** The entire map + sort + two-pass cap-2 deduplication is now inside the `Task.detached` block in `fetchRepresentativePairs`. All `@MainActor`-isolated values (`settings.weights`, floor settings, `thumbnailBaseURL`, `imagePairCounts` built from DB counts) are captured as local constants before the closure; `self` is never touched inside the detached block. Pure helpers (`adjustedGeometricFree`, `convertToPairFree`, `pairSortComparator`, `applyCap2Free`) live in `PairHelpers.swift` with explicit `nonisolated` keyword — required because Swift 6.3 otherwise infers `@MainActor` on free functions in files that use `@MainActor`-associated types (NSColor via `DisplayPair.colorA/colorB`). Also added `nonisolated init` to `DisplayPair` and `@preconcurrency import AppKit` in AppModels.swift to prevent NSColor SDK annotations from propagating `@MainActor` onto the struct init. **(2) Progressive rendering:** Added `streamPage0Pairs(folderID:collectionID:sortOrder:)` which fetches DB rows in 20-row chunks via a new `LIMIT/OFFSET` overload on `QueryService.fetchRepresentativePairs`, runs incremental pass-1 cap-2 greedy deduplication across chunks, and yields each accepted batch through `AsyncStream<[DisplayPair]>`. Pass-2 orphan-cover pairs arrive in a final batch. `PairsGridViewModel.loadPairs` now consumes the stream, appending batches to `allPairs` immediately so `LazyVGrid` starts rendering within ~100ms of the 250ms debounce expiring. `representativePairsCache` is populated on stream completion (via `await MainActor.run` with generation check) for `loadMorePairs` compatibility. `continuation.onTermination` cancels the inner detached task when the consumer task is cancelled. |
| 43 | Layout recursion warning in titlebar/toolbar area | bug | backlog | Console emits "It's not legal to call -layoutSubtreeIfNeeded on a view which is already being laid out" (logged once per launch). Likely triggered by the PassthroughHostingView insertion into NSTitlebarView during a layout pass — see decision #36. No visible breakage observed yet but AppKit logs it as a future-breaking condition. Investigate whether the PassthroughHostingView constraints or the WindowConfigurator.updateNSView call site can be deferred until after the current layout pass completes (e.g. via DispatchQueue.main.async). |
| 44 | Caption embeddings — thematic scoring coverage | improvement | done | See decisions log entry above. Caption embeddings address the 18% zero-cluster-overlap gap using nomic-embed-text via Ollama. Requires full re-index to compute and store embeddings and re-score all pairs with the new blend formula. |
| 45 | Visual diversity multiplier + thematic topK expansion | improvement | done | See decisions log entry #45. Two-part fix: (1) clusterScore multiplied by CLIP-based diversity factor before thematic-boost check; (2) thematicK raised from max(10,topK/5) to max(30,topK/3). Requires full re-index. |
| 46 | Complementary role scoring — two sides of the same phenomenon | exploration | done | Implemented via axis pairs approach (#48). Prerequisite diagnostic confirmed: musician+ears absent from DB post-#45 re-index; dog+dog pairs at rank #7 globally. Two fixes implemented: (1) animal_presence demoted to ambient tier (#47); (2) axis pairs bonus added (#48). See decisions log entries for both. |
| 47 | animal_presence demoted to ambient tier | improvement | done | See decisions log entry #47. |
| 48 | Complementary axis pair bonus | improvement | done | See decisions log entry #48. Requires full re-index. |
| 49 | Meaningful asymmetry gate — require weight ≥ 0.75 unique clusters per side | improvement | done | See decisions log entry #49. |
| 50 | Caption prompt redesign — emotional register over scene description | exploration | in-progress | Keyword cluster system confirmed at architectural ceiling after four implemented changes (#45, #47, #48, #49). Root cause: qwen2.5vl-caption captions describe what is visible, not what is felt. Two strategies: (a) redesign the qwen prompt to request emotional register, lighting mood, human condition, and relational context rather than scene inventory — small change, high leverage; (b) post-process captions with a second LLM call to extract emotional register labels. Option (a) is lower-cost and should be tried first. Prerequisite: define 3–5 test pairs that the current system fails (musician+ears is one; identify others) and measure whether a new prompt resolves them before committing to a full re-caption pass. **Progress (2026-06-10):** Revised prompt validated on 3 images (`06-20250802-_DSF4201.jpg` violinist, `07-20250405-_R014662.jpg` woman on steps, `04-20160709-IMG_8931.jpg` punk singer). Two additions: (1) direction-of-action language in paragraph 3 — "aimed at or toward", "reaching outward", "straining toward something just out of reach"; (2) new paragraph 4 explicitly asking about transmission/exchange acts and whether they find their recipient. Validated results: violinist caption now names the sound "carrying through the air, connecting the performers to an audience just out of frame"; punk singer caption now reads "engaging with them directly, perhaps even challenging them". Woman-on-steps correctly produced no false transmission reading (model ignores the paragraph when there's nothing to transmit). Prompt committed to `CaptioningEngine.swift`; three pilot captions written to DB. Full library re-caption pending — will null all existing captions and trigger Phase 5 on next re-index. **Progress (2026-06-12, #97):** Output garbling root-caused to `OLLAMA_KV_CACHE_TYPE=q8_0` (server-level, not our code — see #97). Prompt rewritten (v2): physical-facts-first ordering, every emotional word tied to named visible evidence, explicit ban on hub-forming mood phrases, direction-of-action retained, apparent gender stated when clearly visible. Caption input raised 512px → 768px and decoding made greedy (temp 0, top_k 1). Validated against the 5-image known-bad pilot set: 4 of 6 problem captions auto-fixed (rose-in-cup, joy-jump, hoop-over-mouth, hub-vocabulary image); fight and punk-provocation remain model perception failures, manually corrected under #81. Six pilot captions written to DB; 105 stale ThematicV2 scores on their pairs nulled. Full library re-caption still pending. |

| 97 | Caption garbling root cause (q8_0 KV cache) + prompt v2 + 768px greedy captioning | fix | done | **Problem 1 — systematic output garbling:** Every sampled caption had article/function-word corruption ("captures a aftermath", "He phone, held in his right hand, is a only object", "a essence of camaraderie"). Initially suspected `repeat_penalty 1.15` suppressing the high-frequency token "the" over long generations — disproven: garbling persisted at `repeat_penalty 1.0` and even at greedy decoding (temp 0, top_k 1), and appeared in **text-only** generation on the base `qwen2.5vl:7b`, ruling out the vision path, our Modelfile (whose template is byte-identical to the stock one), and all request options. Control: llama3.2 on the same server was grammatically perfect. **Root cause:** the Homebrew Ollama service runs with `OLLAMA_KV_CACHE_TYPE=q8_0` (+ `OLLAMA_FLASH_ATTENTION=1`, set in `/opt/homebrew/opt/ollama/homebrew.mxcl.ollama.plist`). Reproduction triangle at greedy decoding: FA=1+q8_0 → garbled; FA=1+f16 → clean; FA=0 (KV forced f16) → clean, with FA=1+q8_0 re-enabled reproducing the exact same corrupted tokens. Quantized KV cache corrupts qwen2.5vl's attention enough to flip articles and function words; llama3.2 tolerates it. Related: ollama/ollama#11230 (FA misbehaviour with Qwen 2.5 VL). **Fix:** remove `OLLAMA_KV_CACHE_TYPE` from the service plist (keep `OLLAMA_FLASH_ATTENTION=1` — proven innocent). Plist edit pending user action (outside repo); a manually-started `ollama serve` without the var was used for this session. The old repeat_penalty rationale ("the the the" loops on the 400-token pass) was almost certainly this same corruption; 1.15 kept as harmless loop insurance. **Problem 2 — factual misreads:** with the server fixed, re-tested the #50 prompt on the 5-image known-bad pilot set; grammar fixed but misreads persisted (rose-in-cup read as "potted plant"+"red bandana", fight read as care, joy-jump read as "quiet tension… unease"). **Prompt v2** (`CaptioningEngine.swift`): physical-facts-first (hands, feet-on-ground-or-air, gaze target, precise objects, transcribe legible text, state apparent gender/age when clearly visible), then direction of every significant action (who does what to whom), then emotional register **only as a conclusion from named evidence**, with an explicit ban on freestanding mood phrases ("quiet introspection", "the weight of the moment", "lost in thought") — the vocabulary that made `07-20190120-_G130022.jpg` a 27-of-500-pairs hub in V2 scoring. Positive opening instruction ("begin with the main subject") replaced the ineffective "no preamble" ban. **768px input** (was 512px): the rose-in-cup was invisible at 512px, correctly read at 768px ("a white cup with orange stripes, which contains a single red rose"). Token budget: image ≤750 + prompt ≈460 + output 500 ≈ 1,710 of num_ctx 2048. **Greedy decoding** (temp 0 → was 0.2, top_k 1): at 0.2, captions fabricated run-to-run (a nonexistent second figure "mimicking" the subject); greedy removed every observed fabrication and makes captions reproducible. **Pilot results:** 4 of 6 auto-fixed (rose, joy-jump, hoop-net-over-mouth explicit + "little tikes" transcribed, hub image now grounded); 2 model perception failures (fight at any resolution flips between care/play/intimacy; punk's directed provocation never fires) manually captioned under #81. Resolution is not monotone — 768px once hallucinated where 512px didn't at temp 0.2; greedy is what stabilised it. **DB writes:** 6 pilot captions (4 greedy@768 outputs verbatim, 2 manual), 105 stale `thematicV2Score` rows on pairs touching those images nulled for re-judging. Backup at `~/conjunct-backup-pre97.db`. **Files:** `ConjunctEngine/Sources/ConjunctEngine/Inference/CaptioningEngine.swift` (prompt, 768px, options, comments). **Follow-up (same day):** the prompt's full example sentences leaked verbatim into thematically-adjacent captions — a laughing (not jumping) woman in `20240727-R0007888.jpg` was captioned "her arms flung above her head mid-jump, exuberant", copied from the grounding example. At library scale this would clone identical phrases across images and manufacture a fake hub. Examples replaced with schematic noun-phrase guidance ("the gesture, the grip on an object, the angle of the mouth, where the weight of the body is") — leak gone on retest, no regression on hoop/hub/rose pilots; the joy-jump caption reads slightly more conservative (the example had been nudging it), acceptable precision-over-recall trade. Lesson: never put complete copyable sentences in a qwen captioning prompt. On the eventual full re-caption, do NOT null the 6 validated pilot captions (2 are manual, and `20-20240909-_DSF9023.jpg`'s v2 caption is more mouth-explicit than what the current prompt regenerates). Plist fixed same day (`OLLAMA_KV_CACHE_TYPE` removed via PlistBuddy, brew service restarted clean, smoke test passing) — full re-caption no longer gated. |
| 51 | Remove caption embedding infrastructure | cleanup | done | Removed all infrastructure added during #9/#44 after confirming it was dead weight. See decisions log entry #51 for full account of what was built, tried, and why removed. |
| 52 | Path B classifier — known-pair validation round | validation | backlog | The Path B direction classifier (outward/inward word lists in `test_pair_vlm_v2.sh`) was validated on a single pair (musician + ears woman). Before any Phase 3 pipeline is built around this classifier, it needs to be tested against a broader set of known pairs across different domains. **Process:** (1) Identify 4–6 known pairs from the library that should score COMPLEMENTARY — covering different arc types beyond sound/music. Candidates: a care/offering pair (one image tending, one image isolated), a power/witness pair (one image figure of authority, one image bystander), a movement/stillness pair, and at least one non-human arc pair if available. (2) For each pair, retrieve actual captions from the DB (`SELECT caption FROM images WHERE id = X`). (3) Run `test_pair_vlm_v2.sh` with those captions substituted in place of the musician/ears test captions. (4) Record which trials fire COMPLEMENTARY and which fire UNCLEAR or PARALLEL. (5) For any UNCLEAR result, inspect model output — if the action description is correct but the word is missing from the classifier lists, add it; if the model output is wrong register (description rather than action), note it as a prompt gap. (6) Also run 2–3 known-bad pairs (two dogs, a same-event pair) and confirm they do NOT fire COMPLEMENTARY. (7) After the round, update the word lists in the classifier and note any recurring model output patterns that suggest prompt refinement. **Goal:** A classifier that fires correctly on known-good pairs and stays quiet on known-bad pairs before any scoring changes are committed to the main codebase. |
| 42 | Same-subject CLIP secondary tier | improvement | done | clipCosineSimilarity is not stored in pairs — penalty applied at score time in PairScorer.swift. Condition: clipSim > 0.75 AND thematicScore < 0.20 → composite ×0.65. The thematic guard prevents penalising visually-similar pairs with genuine cross-context emotional connection (e.g. two protest images that share bodily_gesture + tension_conflict). The 0.88 ceiling (×0.40, near-duplicates) fires first on the same `clipSim` value; the 0.75 tier is a second, softer band below it. Thresholds calibrated against diagnostic output: post-re-caption, only 1 pair sat in the composite>0.50/thematic<0.20/geometric<0.60 zone, confirming the problem is well-contained. Existing pairs require a full re-index to reflect the penalty. |
| 29 | Ambient cluster weight reduction + meaningful-tier gate | improvement | done | In a street photography library, `urban_street` fires on virtually every image. At weight 0.5 it routinely appeared in shared-cluster intersections alongside real emotional clusters, inflating scores for pairs whose primary connection was "both are street photos." Two changes implemented: **(1) Lower ambient weights** — urban_street / nature_landscape / community_gathering dropped from 0.5 → 0.2. At 0.2, even all three ambient clusters shared = 0.6, well below the meaningful threshold. **(2) Meaningful-tier gate** — in `ConceptClusters.weightedDice()`, replaced the old numeric `kMinWeightedShared >= 0.75` guard with an explicit check: the shared intersection must contain ≥1 cluster with weight ≥ 0.75 before a real Dice score is computed. Pairs sharing only ambient-tier clusters return `kAmbientFloor` (0.1) regardless of how many ambient clusters are shared. The old `kMinWeightedShared` constant was removed; the comment now warns that new ambient clusters must stay ≤ weight 0.24 (so that all ambient-only sums remain below 0.75). Ambient clusters still participate in the Dice denominator when present in only one image, lightly penalising asymmetry. After re-score: check (a) DSCF7816 + DSF9179 (gas station + tattooed man) which was 0.000 — may now score if the saturation gate was the culprit; (b) single meaningful-tier cluster pairs still score ≥ 0.20. |

| 67 | Directional weight rebalance — centroid + gaze validation | improvement | done | See decisions log entry #67. |
| 68 | Geometric topK pool — per-submode surfacing escape hatch | improvement | done | See decisions log entry #68. |
| 53 | Breath-pair exception + tonal weight differential (geometric axis) | improvement | done | Two changes to `PairScorer.geometricScore()` addressing breath pairs — compositionally dense image paired with intentionally open/spare image (PAIRING_THEORY.md §Geometric axis redesign, "Breath" mode). **Change 1 — edge peakedness exception:** When `abs(normPeakA − normPeakB) > 0.5`, `edgeMult` is floored at `pow(max(normPeakA, normPeakB), 0.4) × 0.6` rather than allowing the product form to suppress the pair. Starting calibration: threshold=0.5, factor=0.6. **Diagnostic finding:** In a typical street photography library, this exception rarely fires and never changes scores — the "sparsest" images by edge peakedness are directionally diffuse chaotic scenes (foliage, crowds) rather than genuinely open compositions. Edge peakedness measures angular concentration of edges, not visual sparseness. A plain wall with strong panel lines scores HIGH peakedness; a festival crowd scene scores LOW. **Change 2 — tonal weight differential (the actual breath-pair signal):** Added `breathScore = abs(normVarA − normVarB)` as a third term: `score = (rawEdge × edgeMult + rawGrid × varMult + breathScore × 0.4) / 2.4`. Grid variance correctly captures compositional openness: a plain wall has low variance (~0.067, normVar≈0.33); a dense layered scene is near 1.0. The differential peaks at ~0.67 for the wall + dense scene pair, contributing ~0.11 to geometric score. Unlike the similarity terms (rawEdgeSim, rawGridSim), this term rewards complementarity — it is highest when the two images are most different in tonal complexity. Two similarly dense or similarly flat images contribute near zero. **Why the similarity terms alone cannot rescue breath pairs:** rawEdgeSim and rawGridSim are inherently low for structurally different images; the multiplier exceptions only reduce suppression, not add positive signal. The differential term is the first positive signal for breath pairs in the geometric axis. **Limitation:** geometric has 0.20 composite weight; max breath contribution is ~0.07 to composite. Full breath-pair surfacing likely requires the aesthetic axis tonal-weight complementarity component (PAIRING_THEORY.md §Aesthetic axis redesign, Component 3) — deferred. |

| 54 | Per-image accent color extraction (index-time) | improvement | done | **Motivation:** PAIRING_THEORY.md §"A Redesigned Aesthetic Axis" defines Component 2 (accent color echo) for Mode 2 slant-rhyme pairs — two images sharing a specific dominant-but-not-overwhelming hue while diverging in overall palette. The pair-level scoring is a follow-on task; this decision covers extraction and storage only. **Definition:** The accent color is the hue cluster with the highest mean saturation that accounts for ≥5% and ≤40% of the image's pixels, after excluding pixels with saturation < 0.25. NULL when no qualifying accent exists. **DB migration:** `v9_accentColors` adds two nullable REAL columns to the images table: `accentHue` (0–360°, HSL hue angle of the accent color) and `accentSaturation` (0–1, mean saturation of the accent cluster). Both default to NULL implicitly. **Algorithm (`ColourAnalyser.accentColor(from:)`):** (1) Downsample image to 256×256 via existing `extractRGBPixels`. (2) Convert each pixel to HSL via existing `rgbToHSL` (returns h in [0, 1)). (3) Exclude pixels where s < 0.25. (4) Bucket remaining pixels into 24 hue bins (15° each): bin = min(Int(h × 24), 23). (5) For each bin, compute fraction of total image pixels and mean saturation. (6) Select the bin with highest **prominence** (fraction × mean saturation) where fraction is in [0.05, 0.40]. Using area × saturation rather than saturation alone ensures pale-but-widespread colors can win over small-but-vivid ones: a pale blue at 30% / s=0.35 scores 0.105 vs. a red at 7% / s=0.75 scoring 0.053. (7) Return bin center as accentHue in degrees = (bin + 0.5) × 15°; mean saturation of the winning bin as accentSaturation. Return nil if no bin qualifies. **Threshold rationale:** 5% floor prevents a small accent element (one person's red hat at distance) from dominating; lower to 0.03 if validation shows meaningful accents being missed. 40% ceiling prevents dominant colors from being treated as accents; raise to 0.45 if validation shows the ceiling cutting off legitimate accents. 0.25 saturation floor excludes grey concrete, asphalt, and skin tones endemic to street photography; raise to 0.30 if near-neutral accents are sneaking through. **Backfill (IndexingEngine Phase 3.6):** Queries `WHERE isActive = 1 AND accentHue IS NULL` on every re-index. Uses cached 512px thumbnail when available (same pattern as caption backfill). Images where extraction returns nil (B&W, neutral-heavy) remain NULL and are re-attempted on the next re-index — same trade-off as caption backfill. **NULL semantics:** accentHue = NULL means either "not yet processed" or "processed but no qualifying accent exists." Both are indistinguishable from the DB's perspective; this is acceptable given re-attempt cost is negligible. **Files changed:** `ColourAnalyser.swift` (new `extractAccentColor(image:)` public + `accentColor(from:)` internal), `DatabaseManager.swift` (v9 migration), `IndexingProgress.swift` (`.accentExtraction` phase case), `IndexingEngine.swift` (Phase 3.6 block). **Pair-level scoring** using accentHue/accentSaturation is a follow-on task — see PAIRING_THEORY.md §Component 2 for the proposed ±20° match window and contextual-dissimilarity weighting. |
| 56 | Accent color echo — pair scoring + info panel | improvement | done | **Motivation:** `accentHue` / `accentSaturation` (stored per image since decision #54) were fetched at index time but ignored at score time. This decision implements Component 2 of the redesigned aesthetic axis (PAIRING_THEORY.md §"A Redesigned Aesthetic Axis"): pairs that share a specific accent hue while diverging in overall palette. **Formula:** `accentEchoScore = hueScore × √(satA × satB)`. Hue ramp: angular distance ≤10° → hueScore=1.0; ≤30° → linear 1.0→0.0; >30° → returns 0. Angular distance is circular: `min(|hA−hB|, 360−|hA−hB|)`. Geometric mean saturation (`√(satA × satB)`) scales the result — pairs where both images carry vivid accents score higher than pairs where one accent is dull. No hard saturation gate or hue-range exclusions. **Three-way max:** `aestheticScore()` now computes harmony, contrast, and echo; the winning pathway is returned along with its submode string (`"harmony"`, `"contrast"`, or `"accent_echo"`). Echo wins only when its score strictly exceeds both alternatives. **Calibration history:** v1 formula used `hueScore × contextDissim` (1 − histogram intersection of non-accent bands). Abandoned: both images in the canonical test pair (`_R017085` + `R0024458`, both dark urban reds) have similar non-accent palettes → low contextDissim → echo lost to harmony every time. v2 added hard saturation gate (≥0.55) and hue-range exclusions for greens/blues — rejected: low saturation should still participate (soft geometric mean is sufficient); hue exclusions require scene context, not pixel statistics. v3 (final): `hueScore × √(satA × satB)`, tightened ramp (±10°/±30° instead of ±20°/±40°). **Canonical pair validation:** `_R017085.jpg` (accentHue=7.5°, sat=0.61) + `R0024458.jpg` (accentHue=7.5°, sat=0.71): echo score ≈ 0.656, beats harmony (0.531) → pair surfaces with `aestheticSubmode = "accent_echo"`. **UI:** `LightboxInfoRail.aestheticDetail` changed to `@ViewBuilder`; `accent_echo` branch shows "Color echo" label + two `RoundedRectangle` hue swatches (12×12pt) using `Color(hue: h/360, saturation: 1.0, brightness: 0.85)`. **Files changed (8):** `PairScorer.swift` (accentEchoScore, three-way aesthetic max, rationale case), `IndexingEngine.swift` (both score sites: ImageMeta tuple + SQL + PairScorer call), `PairQueryResult.swift` (4 optional fields), `QueryService.swift` (both SELECT paths), `AppModels.swift` (DisplayPair 4 optional fields + init), `EngineController.swift` (convertToPair), `PairHelpers.swift` (convertToPairFree), `LightboxInfoRail.swift` (@ViewBuilder detail + accentEchoDetail view). **Backlog follow-ons:** #57 desaturated shadow reds (hue maintained, saturation lost → echo penalised despite genuine match); #58 ambient hue exclusion (billboard green vs foliage green requires scene context, cannot distinguish at pixel level). |
| 59 | Directional complement scoring — geometric axis | improvement | superseded by #64 | **Motivation:** The geometric axis (decision #53) rewards structural similarity (edge orientation + composition grid cosine) and breath pairs (tonal weight differential). PAIRING_THEORY.md §Geometric axis redesign names a third valid mode — directional complement: two images whose visual weight sits on opposite sides of the frame, creating a spatial conversation rather than structural echo. Currently penalised as dissimilarity; should be rewarded. **Architecture:** Per-image storage of edge-energy-weighted visual centroid (`weightCentroidX`, `weightCentroidY`), same pattern as `accentHue/accentSaturation` (decision #54). The full 32-float composition grid is already in `featureVectors.compositionGrid`; centroid computation at extraction time reads the stored blob — no extra image decode. **Centroid formula:** Uses the edge-density half of the composition grid (indices 16–31, 4×4 grid): `centroidX = Σ(edgeDensity[row×4+col] × col) / (totalEnergy × 3)`. Returns nil when total edge energy < 1e-6 (featureless/uniform image). Normalized to [0,1]: left=0/right=1 (X), top=0/bottom=1 (Y). **Scoring formula:** `directionalComplementScore = (hDist×0.70 + vDist×0.30) × √(normVarA × normVarB)`. Horizontal opposition primary (0.70). Gate: `max(normVarA, normVarB) ≥ kGridVarianceFloor=0.1`. When either centroid is NULL, returns 0. **Original geometric axis formula:** `structural×0.40 + directional×0.35 + breath×0.25`. **Post-implementation finding — signal ineffective:** Library-wide stats on 1,057 images: centX stdev=0.046, 96.1% of images in centX [0.40, 0.60]. Even strongly asymmetric compositions (DSCF3336: man's torso filling left half of frame, arm cropped at left edge) return centX=0.497 because background edges (door frame, curtain on right) exactly counteract foreground subject energy. Left-column edge total: 26.6%, right-column: 28.2% — right scores higher despite the subject being on the left. The 4×4 grid (32×32px per cell at 128px analysis size) is too coarse and edge density is not a semantic signal. The directional component approaches zero for nearly every pair, deflating top structural pairs from geo≈0.72 to geo≈0.44. Structural weight partially restored in #60; centroid computation replaced with Vision attention saliency in #64. **DB migration:** `v10_weightCentroids` adds two nullable REAL columns to images: `weightCentroidX`, `weightCentroidY`. NULL = not yet processed or featureless image; re-attempted on next re-index. **Files changed:** `GeometricAnalyser.swift` (added `weightCentroid(from:)`, removed in #64), `DatabaseManager.swift` (v10 migration), `IndexingProgress.swift` (`.centroidExtraction` phase case), `IndexingEngine.swift` (Phase 3.7 block + both ImageMeta tuples + both PairScorer call sites), `PairScorer.swift` (`directionalComplementScore()`, updated `geometricScore()` signature/formula/return, updated `score()` signature, updated `rationale()`). |
| 60 | Restore geometric structural weight after centroid failure | improvement | done | **Motivation:** With `directional ≈ 0` for 96.1% of image pairs (see decision #59 post-implementation finding), the formula `structural×0.40 + 0 + breath×0.25` deflates structural pairs by ~35% relative to the old formula. Top geometric pairs dropped from geo≈0.72 to geo≈0.44. The 0.35 directional slot was dead weight. **Change:** Updated formula weights in `PairScorer.geometricScore()` from `structural×0.40 + directional×0.35 + breath×0.25` to `structural×0.65 + directional×0.10 + breath×0.25`. The 0.10 directional slot is retained as a placeholder — the scoring infrastructure from #59 remains in place; only the weight is reduced. **No re-index required** — formula change only; scores update on next scoring run. **Superseded by #64** — when Vision saliency centroid produces a real signal, the formula weights will be re-calibrated. |
| 64 | Vision saliency centroid — replacing edge-energy centroid | improvement | done | **Motivation:** The edge-energy centroid (#59) is proven ineffective as a proxy for subject position. Replaced with `VNGenerateAttentionBasedSaliencyImageRequest` (Apple Vision framework), which produces salient object bounding boxes with confidence scores from the cached 512px thumbnails. A confidence-weighted centroid of those bounding-box centers gives "where is the subject" without being confused by background texture. **Why Vision saliency is better:** The attention model is trained on visual salience (where human eyes focus), not edge density. It treats a man's torso as salient and a door frame behind him as background — the correct split that edge density could not make. **Computation:** Load 512px thumbnail from cache → `VNImageRequestHandler(cgImage:)` → `VNGenerateAttentionBasedSaliencyImageRequest` → `VNSaliencyImageObservation.salientObjects` (array of `VNRectangleObservation` with bounding box + confidence) → confidence-weighted mean of `boundingBox.midX / (1 - boundingBox.midY)` (Vision uses bottom-left origin; Y is flipped to top-left convention). Returns nil when no salient objects found. **No Package.swift change needed** — Vision is a system framework on macOS; `import Vision` is sufficient. **DB migration:** `v11_saliencyCentroids` nulls out all existing `weightCentroidX/Y` values written by the edge-energy approach, so Phase 3.7 re-populates them with saliency values on next re-index. Column names unchanged (they still semantically mean "visual weight centroid"; the implementation now uses a better signal). **Phase 3.7:** Replaced JOIN on `featureVectors` + blob read + `GeometricAnalyser.weightCentroid(from:)` with a simple `SELECT id FROM images WHERE isActive=1 AND weightCentroidX IS NULL`, then `SaliencyAnalyser.attentionCentroid(thumbnailURL:)` per image. **Code removed:** `GeometricAnalyser.weightCentroid(from:)` deleted (no longer called anywhere). **New file:** `Analysis/SaliencyAnalyser.swift`. **Files changed:** `DatabaseManager.swift` (v11 migration), `Analysis/SaliencyAnalyser.swift` (new), `IndexingEngine.swift` (Phase 3.7 body), `Models/IndexingProgress.swift` (phase description string "Extracting saliency centroids"), `Analysis/GeometricAnalyser.swift` (removed `weightCentroid(from:)`). **Deferred:** Formula weight re-calibration (directional from 0.10 back toward 0.35) is deferred until post-re-index validation confirms saliency centroids are non-trivially distributed (stdev > 0.10) and DSCF3336/DSCF3269-positive register centX < 0.45. |
| 65 | Gaze direction scoring — face landmarks + max(centroid, gaze) directional | improvement | done | **Motivation:** Human-detection saliency centroid (#64) gives a real subject-position signal (stdev_x=0.141) but fails for centered compositions with strong gaze direction — a person facing forward but looking sharply left scores centroid≈0.5 even though their gaze creates a strong directional implication. The correct signal for this case is where the *eyes are pointing*, not where the body sits in the frame. **What this adds:** Per-image `gazeDirectionX` [-1.0=looking left, +1.0=looking right], extracted from `VNDetectFaceLandmarksRequest` pupil positions, run alongside the existing human/saliency detection in a single thumbnail pass. Three Vision requests in one `handler.perform()` call: human rectangles (centroid primary), attention saliency (centroid fallback), face landmarks (gaze). **Gaze computation:** Primary: pupil offset within eye contour. Each pupil landmark's x position is normalized within its eye contour's x-extent [0,1] (0=leftmost edge, 1=rightmost); both eyes averaged and mapped to [-1,+1]. Pupils toward right of their eye openings = looking right. Requires leftPupil + rightPupil landmarks and eye contour width > 5% of face bounding box. Fallback: head yaw from nose crest vs eye midpoint — nose right of eye midpoint → head turned right, scaled to ~[-1,+1]. **Scoring:** `gazeConversationScore(gazeA, gazeB) = max(0, (gazeA - gazeB) / 2)`. Maximum 1.0 when A looks right (+1) and B looks left (-1), facing each other across the diptych. Zero for parallel gaze or gaze away from each other. `directional = max(centroidScore, gazeScore)` — whichever directional signal is stronger for that pair wins; handles centered compositions (gaze fires) and off-center compositions (centroid fires) independently. **Formula:** `structural×0.55 + directional×0.20 + breath×0.25` — weights unchanged from #64. **New geometricSubmode:** `"gaze_conversation"` when `gazeScore >= centroidScore` and `directional > structural`. Rationale string: `"Eyes in conversation — each image completes the other's look."` **DB migration:** `v12_gazeDirection` adds `gazeDirectionX REAL` nullable column to images. NULL when no face detected or landmarks unreliable. **Phase 3.7 query:** Changed to `WHERE isActive=1 AND (weightCentroidX IS NULL OR gazeDirectionX IS NULL)` — triggers backfill when either value is missing. UPDATE writes centroid x/y and gaze direction in the same row. **Error isolation:** Centroid and gaze are computed independently; a face-detection failure returns `gazeDirectionX: nil` but still returns the centroid, and vice versa. **API change:** `SaliencyAnalyser.attentionCentroid()` renamed to `SaliencyAnalyser.analyse()`, returns new `ImageSpatialFeatures` struct (`centroid: (x:y:)?`, `gazeDirectionX: Float?`). **Files changed:** `DatabaseManager.swift` (v12 migration), `Analysis/SaliencyAnalyser.swift` (new struct + face request + gaze computation), `IndexingEngine.swift` (Phase 3.7 query + UPDATE + both ImageMeta tuples + both SQL SELECTs + both call sites), `Scoring/PairScorer.swift` (`gazeConversationScore()`, `geometricScore()` signature + max logic + submode, `score()` signature, `rationale()` new case). **Verification:** After re-index, query `SELECT count(*) total, count(gazeDirectionX) with_gaze, round(avg(gazeDirectionX),3) mean, round(sqrt(avg(gazeDirectionX*gazeDirectionX) - avg(gazeDirectionX)*avg(gazeDirectionX)),3) stdev FROM images WHERE isActive=1`. Expect ~30–50% coverage, stdev > 0.30. Gaze_conversation pairs: `SELECT p.geometricScore, a.filename, b.filename, a.gazeDirectionX gazeA, b.gazeDirectionX gazeB FROM pairs p JOIN images a ON a.id=p.imageAID JOIN images b ON b.id=p.imageBID WHERE p.rationale LIKE '%conversation%' ORDER BY p.geometricScore DESC LIMIT 10`. |
| 55 | Breath pairs — aesthetic axis tonal weight complementarity | improvement | backlog | Geometric axis (#53) now contributes a tonal weight differential term (`breathScore = abs(normVarA − normVarB)`, weight 0.4 in a 2.4 denominator) that rewards compositional density asymmetry. Diagnostic post-#53: `20250426-_R016343.jpg` (normVar≈0.18, very tonally flat) is the sparsest-by-grid-variance image in the current library; it generates breathScore≈0.82 in its top pairs, contributing ~0.137 to geometric score. Wall image `20200717-L1008954.jpg` gets modest geometric uplift but low raw similarity to dense street scenes limits the total signal. **Why geometric alone is insufficient:** geometric has 0.20 composite weight; max breath contribution to composite is ~0.07. A genuinely compelling breath pair (Soth towels-style open image alongside a charged portrait) would need 0.15–0.25 composite lift to surface reliably. **Diagnostic finding — L1001045 and _R016343:** Post-#53 diagnostic confirmed `_R016343.jpg` (Waymo interior, flash-lit) is tonally flat for the wrong reason — dark uniform car interior, not open\/spare in the Graham\/Soth sense. `L1001045.jpg` (foliage-buried subject) is also tonally flat but ambiguous: the metric cannot distinguish a simple subject buried in grass from a complex object buried in foliage. Both have low grid variance for structural reasons unrelated to visual sparseness. **Revised understanding — subject complexity, not background type:** Foliage images CAN work as breath pair companions. A half-eaten apple in an overgrown field of grass is genuinely spare: simple isolated subject, uniform surround, low grid variance, almost no thematic clusters. The same foliage with people or complex objects is NOT spare despite identical grid variance — it is visually loaded with internal structure. This matches Graham's and Soth's haiku cutaway: the open image is informationally sparse, not merely tonally flat. Grid variance measures tonal uniformity, not edge density or subject complexity. Dense foliage has HIGH edge density uniformly distributed across the frame — low orientation peakedness AND low grid variance — but is not spare. The metric cannot distinguish "simple subject in uniform surround" from "complex subject in uniform surround." **Proxy available now:** Low grid variance + few meaningful thematic clusters is the best combined signal with current measurements. A genuinely sparse image fires almost no thematic clusters by definition (apple in grass → at most `nature_landscape` ambient). Complex objects in foliage fire more. The thematic axis already partially captures this; the combination is more reliable than grid variance alone. **Correct long-term geometric signal — uniform area ratio:** Total edge energy per region (L2 magnitude, not directional orientation) distinguishes genuinely open images (large regions with near-zero edge density) from uniform-but-busy images (foliage, dark interiors — high edge density, uniformly oriented but not sparse). Requires a new measurement in `GeometricAnalyser.swift` alongside the existing edge orientation histogram and grid brightness variance. **Calibration gradient from visual review (2026-05-10):** Four foliage\/grass images ranked as breath pair companions: (1) `20250722-R0026827.jpg` — fallen dove flag in a bush, best candidate in the library, closest to Soth's folded towels; (2) `20240731-_DSF7940.jpg` — orange mailbox enveloped in green leaves, close second; (3) `20210313-L1001045.jpg` — tiger logo lost in overgrown foliage, acceptable, more right than wrong; (4) `20250419-_DSF2906.jpg` — group of people lying on a blanket at a ren fair, catastrophically wrong — no breathing room, image is entirely about the human activity. The critical gap is between L1001045 and _DSF2906, not between the top images. **What this means for signal design:** The catastrophic failure case (_DSF2906 as a breath pair companion) is already handled by the thematic axis — a ren fair crowd fires joy_celebration, bodily_gesture, community_gathering, movement_energy, likely 4–5 high-weight clusters, placing it firmly on the dense side of pairs. Geometric signals do not need to prevent the worst cases; thematic already does. L1001045 being "acceptable" means the threshold does not need to discriminate between logos-in-foliage and simple-objects-in-foliage — it just needs to sit comfortably below _DSF2906. The geometric signals are for PROMOTING genuinely sparse images (R0026827, _DSF7940) so they surface alongside charged counterparts, not for rejecting the wrong images. **Next step:** R0026827 is the primary calibration anchor. Check whether it surfaces a charged counterpart (strong portrait, dense street scene) among its top pairs in the current DB. If not, that is the failure mode to fix — not misidentification of _DSF2906. Uniform area ratio in GeometricAnalyser remains the correct long-term signal for promoting genuine sparseness; implement it before tuning aesthetic axis weights. **Library gap — calibration requires more breather images:** R0026827 is the named anchor for now, but the current library has very few genuinely sparse images (open sky, fog, spare architectural scenes, isolated objects in open surrounds). Calibration of weights and thresholds will need to be revisited once the library includes more examples. Before tuning the aesthetic axis component, add a set of breather images — skies, fog, minimalist scenes — and verify that the combined signal correctly promotes them relative to dense street scenes. These images may also trigger more ambient-tier clusters (nature_landscape, community_gathering) which is expected and acceptable. |
| 67 | Directional weight rebalance after centroid + gaze validation | improvement | done | **Validation results (2026-05-17):** Centroid stdev_x = 0.141 (threshold > 0.10 — PASS). Gaze coverage = 46.8% (within expected 30–50% — PASS). Gaze stdev = 0.208 (below threshold of 0.30 — PARTIAL). Gaze signal is real but weaker than hoped; most detected faces look roughly forward. No gaze_conversation pairs visible in DB: `geometricSubmode` is not stored as a DB column (only computed locally in `geometricScore()` and surfaced via the `rationale` string when geometric wins the whole pair — rare given thematic dominance). Strong gaze-opposing pairs do exist: top computed gazeScore = 0.659 (R0007428 + _R008826) but compositeScore = 0.405, well below the composite top-150 ceiling. **Weight rebalance:** `structural×0.55 + directional×0.20 + breath×0.25` → `structural×0.50 + directional×0.25 + breath×0.25`. Conservative given gaze stdev below target; the real surfacing fix is the geometric topK pool (#68). **Files changed:** `Scoring/PairScorer.swift` (formula weights + comment). |
| 68 | Geometric topK pool — per-submode surfacing escape hatch | improvement | done | **Problem:** No geometric escape hatch exists in the topK pools. Composite top-150 + thematic top-50 both favour high-thematic pairs; a gaze_conversation pair with geo=0.56 but low thematic never surfaces because its composite is too low. 26,652 pairs in DB where geometric is the highest component, but none reach the grid. **Solution:** Add a geometric topK pool (top-5 per image by geometricScore) alongside the existing composite and thematic pools in `IndexingEngine.runIntraFolderScoring` and `runCrossFolderScoring`. Includes a submode variety bonus: if the top-5 are all structural, the best non-structural pair (gaze_conversation or directional_complement) gets one extra slot. Pairs in this pool are marked `selectedFor = "geometric"`. **geometricSubmode storage:** Added `geometricSubmode: String` to `PairScore` struct (PairScorer.swift). Added `geometricSubmode: String?` to `PairRecord` and `PairQueryResult` models. DB migration `v13_geometricSubmode` adds a nullable `geometricSubmode TEXT` column to the pairs table. Written at INSERT time from `PairScore.geometricSubmode`. NULL for pre-v13 rows; variety logic gracefully skips NULL rows at selection time (selection happens in-memory at score time where the value is always populated). **selectedFor values:** "composite" (unchanged), "thematic" (unchanged), "geometric" (new). Display modality is still determined by score comparison when `selectedFor == "geometric"`, same as the fallback path for NULL rows — no change to `PairHelpers.convertToPairFree` or `EngineController.convertToPair`. **Verification:** After re-score, query `SELECT selectedFor, count(*) FROM pairs GROUP BY selectedFor` — expect a "geometric" bucket. Check `SELECT p.geometricScore, p.selectedFor, p.geometricSubmode, a.filename, b.filename FROM pairs p JOIN images a ON a.id=p.imageAID JOIN images b ON b.id=p.imageBID WHERE p.selectedFor = 'geometric' AND p.geometricSubmode != 'structural' ORDER BY p.geometricScore DESC LIMIT 10` for gaze_conversation and directional_complement pairs. **Files changed:** `Scoring/PairScorer.swift` (geometricSubmode added to PairScore struct + passed through from geometricScore return tuple), `Models/PairRecord.swift` (geometricSubmode field + init), `Models/PairQueryResult.swift` (geometricSubmode field), `Database/DatabaseManager.swift` (v13 migration), `IndexingEngine.swift` (geometric topK pool in Phase 1 + Phase 2 + PairRecord INSERT + selectedFor logic), `QueryService.swift` (both SELECT paths + both PairQueryResult init calls). |
| 69 | Drop head-yaw gaze fallback — pupil-only detection | fix | done | **Problem:** The head-yaw fallback in `gazeFromLandmarks()` computed gaze from nose-crest vs eye-midpoint geometry when pupils weren't detected. This produced false positives on tilted/recumbent faces: a passed-out man (face tilted back, nose pointing skyward), a woman being dipped back in a hug, a man face-down on the floor. All scored gaze > 0.78 despite having no meaningful lateral gaze direction. These dominated the gaze_conversation topK pool. **Fix:** Remove the fallback entirely. `gazeFromLandmarks()` now returns nil when `leftPupil`/`rightPupil` landmarks are absent or eyes are too small. Coverage drops from ~47% to ~15–20% but the remaining readings are genuine pupil-based gaze. **DB migration:** `v14_regaze` nulls all existing `gazeDirectionX` values so Phase 3.7 re-extracts them pupil-only on next re-index. **Files changed:** `Analysis/SaliencyAnalyser.swift` (removed `headYawFallback()`, collapsed `gazeFromLandmarks()` to pupil-only path), `Database/DatabaseManager.swift` (v14 migration). |
| 70 | Gaze score normalization — rescale pupil signal to fill [-1, +1] | fix | done | See decisions log entry #70. |
| 70 | Gaze score normalization — rescale pupil signal to fill [-1, +1] | fix | done | **Problem:** Raw `gazeDirectionX` values from `VNDetectFaceLandmarksRequest` pupil offsets are compressed near zero. Library-wide distribution after #69 shows p90 of abs(gazeDirectionX) ≈ 0.28–0.30; only 2 images in the library score ≥ +0.6. A punk photo with a clearly strong lateral gaze (`04-20160709-IMG_8931.jpg`) registers -0.238. Without normalization, `gazeConversationScore` compresses the actual gaze contrast — even genuinely opposing gazes score only 0.2–0.35 out of a possible 1.0, making them weakly competitive in the directional slot. **Root cause:** Pupil offset within the eye contour is measured in normalized face-landmark space. The eye contour points represent the outline of the visible eye opening, not the full orbital range. Pupils rarely touch the extreme corners of the eye — so the [-1, +1] theoretical range is never reached in practice. The actual distribution has most readings in [-0.30, +0.30]. **Fix:** In `gazeConversationScore()`, divide raw gaze values by `kGazeScale = 0.30` and clamp to [-1, +1] before computing the score. This anchors the p90 of the distribution to ±1.0. The punk photo at -0.238 rescales to -0.793 (correctly signaling strong leftward gaze); a reading of ±0.30+ clamps to ±1.0. **No re-index required** — normalization happens at score time inside `PairScorer.gazeConversationScore()`; existing `gazeDirectionX` DB values are unchanged. Re-score on next `index()` call. **Files changed:** `Scoring/PairScorer.swift` (`gazeConversationScore()` applies `kGazeScale = 1.0 / 0.30` before computing score). |
| 71 | Symmetric gaze scoring + canonical display order | fix | done | **Problem 1 — missing pairs:** `gazeConversationScore(gazeA, gazeB)` is directional — only rewards A-looks-right + B-looks-left. Which image becomes gazeA is determined by DB image ID ordering (lower ID = imageA), which is arbitrary. Post-#70 query showed 1,648 pairs where the left-looker happened to be imageA, scoring 0 for gaze and never entering the gaze_conversation pool. The punk photo + L1007802 pair was one of these. **Problem 2 — wrong display order:** Even when the pair scored correctly, ~50% of gaze_conversation pairs would have the left-looker displayed on the left, defeating the visual effect. **Root cause — gaze params not reordered with ID canonicalization:** `score()` reorders vectorA/vectorB when `imageAID > imageBID` (lines 64-67), but `gazeDirectionXA/XB` and `weightCentroidXA/XB` were still passed in the caller's original order — meaning gazeXA sometimes referred to the higher-ID image after the swap. **Fix 1 — param reordering:** In `score()`, reorder gaze and centroid params alongside the vector swap so `gazeA` always refers to the canonical `aID` (smaller ID). **Fix 2 — symmetrize:** In `geometricScore()`, compute `max(fwd, reversed)` gaze scores and return `gazeFlipped: Bool` indicating which direction scored higher. **Fix 3 — canonical display order:** In `score()`, when `geo.gazeFlipped && geo.geometricSubmode == "gaze_conversation"`, swap `aID`/`bID` in the returned `PairScore` so the rightward-gazer is always `imageAID` (displayed left). This overrides the ID-based canonical ordering for gaze pairs — safe because the DELETE queries use `IN (batch)` checks on both image IDs, covering both A/B orderings. **Result:** ~3,400 genuine gaze-conversation pairs expected (vs 1,810 previously). All display correctly with rightward-gazer on left. **Files changed:** `Scoring/PairScorer.swift` (param reordering in `score()`, symmetrized gaze + `gazeFlipped` in `geometricScore()`, canonical ID swap in `PairScore` construction). |
| 73 | Dominant orientation opposition — opposing_diagonals geometric submode | improvement | done | **Motivation:** The directional slot in the geometric formula (`max(centroidScore, gazeScore) × 0.25`) only captures human-centric signals (subject position, face gaze). PAIRING_THEORY.md names "structural rhyme" and "directional complement" as geometric modes — the latter includes opposing diagonals: two images whose dominant compositional lines run perpendicular to each other, creating visual tension across the diptych. No new data needed — the 32-bin edge orientation histogram (`featureVectors.edgeOrientation`) is already computed per image. **Algorithm:** (1) Find dominant directed bin [0, 32) from each image's L1-normalised edge histogram. (2) Fold to undirected [0, 16) by `bin % 16` — bins 180° apart are the same undirected line direction. (3) Compute undirected angular distance: `dist = min(|undirA - undirB|, 16 - |undirA - undirB|)` in [0, 8] bins = [0°, 90°]. (4) `scoreBase = sin(dist × π / 16)` — 0 at 0° (parallel), 0.71 at 45°, 1.0 at 90° (perpendicular). (5) Scale by `√(normPeakA × normPeakB)` so weak-lined images earn less credit. Gate: both images require `normPeak ≥ 0.25` (raw peakedness ≥ 1.0). **Integration:** `directional = max(centroidScore, gazeScore, orientationScore)`. New `geometricSubmode = "opposing_diagonals"` when orientationScore dominates directional. Rationale: "Diagonal tension — lines cut across each other through the diptych." **Note on symmetry:** orientationOppositionScore is symmetric (no flip needed — perpendicular lines are perpendicular regardless of display order). **Verification:** After re-score, `SELECT count(*) FROM pairs WHERE geometricSubmode='opposing_diagonals'` and check DSCF3269-positive's top pairs shift to opposing_diagonals submode. **Files changed:** `Scoring/PairScorer.swift` (new `orientationOppositionScore()`, wired into `geometricScore()` directional max + submode logic, new rationale string). **Post-implementation calibration finding (2026-05-24):** `kEdgePeakedNorm = 4.0` was set to p90 of the library's raw peakedness distribution, so ~10% of images saturate to normPeak=1.0. Once an image hits normPeak=1.0, `orientationOppositionScore` can't distinguish it from a stronger diagonal — both contribute `sqrt(1.0 × partnerNormPeak)`. `_DSF2811` (peakedness=4.25, normPeak=1.0) scores geo≈0.782 via near-90° opposition with partners, appearing 385 times in the geometric pool. Visual assessment: `_DSF2811`'s diagonal "not a particularly good example" while `_DSF1497` is "great." However, per-pair analysis shows `_DSF1497` has nearly identical peakedness (≤4.25, also normPeak=1.0). Any threshold that would filter `_DSF2811` would also filter `_DSF1497`. The edge histogram measures concentration of edges in one direction but cannot judge aesthetic quality of the angle or compositional salience of the line. This is a known ceiling of the approach — not fixable with thresholding. The 385 pool appearances are a DB hub-image artifact; cap-2 at display time limits actual grid appearances to 2. Score and surfacing are correct; the limitation is in what the histogram signal can distinguish. _DSF2811_ (near-horizontal dominant direction) scored geo≈0.782 despite user assessment of "not a particularly good example." Root cause: scoring only rewarded opposition between two directions, not whether those directions were actually diagonal. A near-horizontal + near-vertical pair scored max on opposition even though neither is a compositional diagonal. Fixed in decision #74. |
| 74 | Opposing diagonals — diagonalness factor to penalise horizontal/vertical lines | fix | done | **Problem:** `orientationOppositionScore` rewarded angular distance between dominant bins but not whether those directions were actually diagonal. A near-horizontal image (dominant bin ≈ 0°) paired with a near-vertical image (dominant bin ≈ 90°) scored maximum opposition (1.0) even though neither contains a compositional diagonal. `_DSF2811` dominated the geometric pool despite its horizontal line. **Root cause:** The metric was perpendicularity, not diagonality. "Opposing diagonals" requires both lines to actually be diagonal. **Fix:** Add a `diagonalness` factor: `diag = |sin(undirBin × π/8)|` — 1.0 at undirBin 4 (45°) and 12 (135°), 0.0 at undirBin 0 (horizontal) and 8 (vertical). Smooth and continuous: a 20° line scores 0.71, a 10° line scores 0.38, a 0° line scores 0. New formula: `sin(dist×π/16) × √(normPeakA×normPeakB) × √(diagA×diagB)`. **Expected impact:** `_DSF2811` at ~10° from horizontal: diag≈0.38, score drops from ~0.78 to ~0.30 — no longer wins directional slot. `_DSF1497` at ~35°: diag≈0.92, retains high score (~0.85) with a genuinely opposing partner. `DSCF3269` (validated positive) at genuine diagonal: retains high score. **No re-index required** — purely a scoring formula change; takes effect on next `index()` call. **Files changed:** `Scoring/PairScorer.swift` (updated `orientationOppositionScore()` comment and return expression). **Post-implementation recalibration (2026-05-24):** Initial approach used a continuous multiplicative factor `√(diagA×diagB)` — too aggressive. Pool dropped from 2,487 → 16 pairs; DSCF3269 and _DSF1497 (validated positives) shifted back to structural because even a 0.71 diagonalness factor was enough to push orientationScore below their high structural scores (rawEdgeSim 0.87–0.97 with top partners). Fixed by replacing continuous scaling with a **hard gate**: `guard diagA >= 0.50, diagB >= 0.50` (equivalent to ≥ 22.5° from nearest axis). Bins 0–1 and 7–9 and 15 are filtered (near horizontal/vertical); bins 2–6 and 10–14 (22.5°–67.5° diagonal zone) get full credit, same as #73. Expected pool: significantly smaller than 2,487 but much larger than 16 — both DSCF3269 and _DSF1497 restored to opposing_diagonals. **Display:** `LightboxInfoRail.geometricDetail` updated to show submode-specific label and description: `opposing_diagonals` → "Diagonal tension", `gaze_conversation` → "Eyes in conversation", `directional_complement` → "Spatial tension", structural (default) → "Compositional structure" (unchanged). `geometricSubmode` threaded from `PairQueryResult` → `DisplayPair` (new `String?` field in `AppModels.swift` + explicit `nonisolated init`) → both `convertToPairFree` and `convertToPair`. **Additional files changed:** `Couplet/Models/AppModels.swift`, `Couplet/Engine/PairHelpers.swift`, `Couplet/Engine/EngineController.swift`, `Couplet/Views/Lightbox/LightboxInfoRail.swift`. |

| 80 | B&W aesthetic tuning — lighter and lower-contrast pairs | improvement | backlog | High-contrast B&W pairs surface well after #77 calibration. Lower-contrast and high-key B&W pairs are under-represented: fewer such images exist in the test library, and the 8-bin lightness histogram may not distinguish tonal styles within a narrow low-contrast range. Additionally, geometric structural scoring can produce false-positive B&W pairs via edge-orientation coincidence — observed 2026-06-09: `20250329-_DSF6016.jpg + 20210327-L1001391.jpg` (tree silhouette + scooter scene) surfaces as top B&W geometric pair despite no meaningful connection; both images share high-contrast diagonal edge structure. This is a known limitation of using edge orientation as a proxy for compositional structure. Revisit when the library includes more varied B&W work or when specific failing pairs can be documented. See decision #77. |

| 81 | Caption correction for known-failing thematic pairs | improvement | in-progress | **Progress (2026-06-12, #97):** hoop caption auto-fixed by prompt v2 at 768px/greedy — now reads "his face is framed by the netting of the hoop, which is positioned in front of his mouth… as a makeshift mask" with "little tikes" transcribed. Punk caption manually written (directed provocation explicit: "glares sideways, directly into the camera… daring the viewer, singling them out and demanding a reaction"). Of the three images added from the ThematicV2 review: `20240824-_R008884.jpg` (fight) manually captioned — model perception failure at every resolution (read variously as care, play, or intimacy; never as a fight); `20240216-R0004921.jpg` (joy-jump) and `20250504-_R017085.jpg` (rose-in-cup) auto-fixed by v2. All six written to DB; pair re-score still pending to confirm downstream improvement. **Update (2026-06-14, #99):** two more object-misread anchors found and manually re-captioned during the oracle investigation — image 2038 (`73-20250606-_R019980.jpg`, holstered gun qwen read as "a camera") and image 2072 (`07-20250405-_R014662.jpg`, hands-over-ears read as "temples/distress"). Both flip their pairs from unrecoverable/weak to recoverable. Full manual-caption preserve-list is now image IDs **2018, 3439, 2038, 2072** (see #99). Original analysis: Two pairs in the thematic scoring test set fail despite correct prompt architecture and a capable pair-level scorer. Root cause in both cases is caption quality — the captions don't surface the language needed for the connection to be recognizable. **Smile text / basketball hoop man** (`96-20250823-_DSF6565.jpg` + `20-20240909-_DSF9023.jpg`): The connection is visual/textual irony — "SMILE you're Beautiful" is painted on concrete; the companion image shows a man whose mouth is physically obscured by a basketball hoop net. The scorer needs the hoop caption to make the mouth-covering explicit ("the netting covers his mouth, making a smile impossible"). Currently the caption describes the hoop as "a comical mask" with no reference to the mouth or what it prevents. **Holstered gun / punk singer** (`73-20250606-_R019980.jpg` + `04-20160709-IMG_8931.jpg`): The connection is performed transgression aimed at an audience — both images carry a specific edge of defiance directed outward. The punk caption reads as concert coverage ("raw emotion," "passion and intensity") with no language around provocation or challenge directed at a witness. The gun image reads as costume irony. Neither caption surfaces the shared attitudinal register. **Approach:** Manually write corrected captions for `20-20240909-_DSF9023.jpg` and `04-20160709-IMG_8931.jpg`, update the DB directly, re-run the pair scorer to confirm both pairs now score correctly. This validates that the architecture works given sufficient caption quality, and informs what the qwen prompt still can't reliably produce — which feeds back into #50. |

| 94 | Duplicate detection — group crop/re-export filename variants missed by dHash; fix broken sharesBaseName regex | fix | done | **Problem:** ~50 pairs of prefix-copy images (manually prepped pairs folder exports like `00-20250504-_R017085.jpg` alongside the original `20250504-_R017085.jpg`) were both `isActive=1`, and 6 of those pairs had rows in the `pairs` table — e.g. pair 1977×4017 with harmony 0.944, occupying one of `_R017085`'s five aesthetic topK pool slots and crowding genuine pairs out of the surfaced pool. **Root-cause analysis (three layers, none a comparison-scope bug):** (1) *Dedup scope is correct* — `detectDuplicates` compares all active images globally. 42 of the 50 pairs (Hamming ≤ 6) were correctly grouped, their non-hero copies excluded from pairing via `loadHeroVectors()`; none had pairs rows. (2) *The 8 missed pairs are crops/re-exports, not exact duplicates* — different pixel dimensions (5671×3781 vs 6240×4160; one pair even 3:2 vs 4:3), Hamming 7–20. dHash cannot catch crops, and **no pixel-level rule can**: a genuine same-second burst pair in this library (`_DSF2488`/`_DSF2489`) measures Hamming 19 / CLIP cosine 0.964 — *more similar* than the crop copy `_DSF0572` at Hamming 20 / CLIP 0.934. Any threshold (dHash, CLIP, or conjunction) that catches the crops would also stack genuine burst frames. (3) *The existing filename mitigation was a no-op due to a regex bug* — `sharesBaseName` (PairScorer) normalized BOTH filenames with `^\d+-` strip: the prefixed copy loses only `00-` → `20250504-_r017085.jpg`, but the unprefixed original loses its *date* → `_r017085.jpg`; the normalized names never match, so composite was never zeroed and the pair entered the DB through the aesthetic pool (pools select on raw per-axis scores; the `compositeScore > 0` insert filter would have excluded it had the flag fired). `normalizedBaseName` in ThematicV2BackgroundPass (#83) had the identical bug, masked there by the captureDate SQL guard. **Fix:** (1) New `FilenameVariants.areVariants()` (ConjunctEngine/Analysis) — compares each filename's stripped forms (numeric prefix `^\d+-`, numeric suffix `-\d+(\.\w+)$`, both) against the raw *and* stripped forms of the other, so one-sided prefixes match; identical filenames return false. `sharesBaseName` and the ThematicV2 guard now delegate to it. (2) **Filename-variant rule in `detectDuplicates` — documented last resort per decision #79's no-library-specific-hacks principle:** after the dHash union-find pass, images whose filenames are export variants AND whose EXIF `captureDate` is identical (non-null) are unioned into the same duplicate group. The captureDate gate makes it burst-safe: a re-export keeps its capture timestamp; camera counter rollover collisions (`20210101-_DSF0001.jpg` vs `20250504-_DSF0001.jpg`) do not. Genuine same-second different shots (`_R038330`/`_R038332`) don't match because their stripped names differ. (3) Hero selection unchanged — earliest captureDate, tie → lowest id — consistent with the 42 dHash-caught groups, where the prefixed copy (lower id) is already the hero. Note dedup marks non-heroes `isHero=0`, NOT `isActive=0`; pairs involving non-heroes are removed by the per-run DELETE+INSERT (hero-only vectors), not by the orphan sweep (which only covers `isActive=0`). (4) Hygiene: `DELETE FROM duplicateGroups` before re-creating groups each run — the table had accumulated 11,099 rows with only 98 referenced. **Validation (re-index of the 1,047-image library, 2026-06-12):** 50/50 prefix-copy pairs grouped with exactly one hero (heroes 998 → 990, exactly the 8 crop copies demoted); all 6 lingering pairs rows (incl. 1977×4017, harmony 0.944) gone; active image count unchanged at 1,047 (dedup demotes via `isHero`, never `isActive`); zero burst-frame false positives — every genuine same-second different-shot pair remains ungrouped (`_DSF8862`/`_DSF8863` shares a group but was already dHash-grouped pre-fix at Hamming 2); `duplicateGroups` 11,099 → 57 rows. Canonical accent-echo pair `_R017085`+`R0024458` (echo 0.656) did NOT re-enter the DB — legitimate competition, not pollution: both images now hold 8+ genuine echo pairs at 0.658–0.771 (largely the same warm-accent partners), so 0.656 ranks ~9th on both sides, below the top-5 aesthetic pool cutoff. **Files changed:** `ConjunctEngine/Sources/ConjunctEngine/Analysis/FilenameVariants.swift` (new), `IndexingEngine.swift` (`detectDuplicates`), `Scoring/PairScorer.swift` (`sharesBaseName`), `Scoring/ThematicV2BackgroundPass.swift` (guard), `Tests/ConjunctEngineTests` (FilenameVariantsTests). |

| 91 | ThematicV2 candidate selection — exclude accent_echo pairs from the candidate pool | fix | done | **Problem:** A read-only DB diagnostic (2026-06-12, FINDINGS-cluster-vs-thematicv2.md) found that 495 of the first 498 pairs scored by `ThematicV2BackgroundPass` (99.4%) had `aestheticSubmode = 'accent_echo'`. The candidate query orders by `MAX(aestheticScore, geometricScore) DESC`, and color echo pairs dominate the high-aesthetic end of the table (9,239 accent_echo pairs total), so the pass spent its entire budget re-judging color relationships the aesthetic axis already captures — instead of evaluating pairs where an LLM thematic judgment adds new information. Worse, the LLM tends to hallucinate thematic connections driven by shared color when captions mention it (the system prompt tries to prevent this but doesn't fully eliminate it — see #85), inflating accent_echo pairs in the thematic sort. **Fix (approach 1 of the three in the backlog note):** Added `AND (p.aestheticSubmode IS NULL OR p.aestheticSubmode != 'accent_echo')` to the WHERE clause in `fetchCandidates()`. Everything else (ordering, LIMIT 500, captureDate/filename-variant guards from #83/#94) unchanged. Approaches (2) (prioritize geometric/cluster-thematic pairs in the ordering) and (3) (post-scoring penalty on `accent_echo` + `relationshipType = 'echo'`) were considered but are not needed once accent_echo is excluded at the source — the remaining pool is already ordered by the existing MAX() and contains no color echo pairs to penalize. **Existing scores left as-is:** the 495 already-scored accent_echo pairs keep their `thematicV2Score` — sunk cost but not actively harmful, and a small accent_echo slice may be worth revisiting later. This change only affects future candidate selection; no rows were nulled or re-scored. **Interim decision, not architectural:** some color echo pairs may have genuine thematic resonance beyond color (the tradeoff noted in the backlog); a small accent_echo carve-out may return once #58/#92 (ambient hue handling) settle. **Validation (read-only queries against the live DB):** candidate pool 127,307 → 118,578 (−8,729; the remaining accent_echo pairs of the 9,239 total were already scored or fail other guards). Top-50 by `MAX(aestheticScore, geometricScore)`: 43 contrast + 7 harmony, 0 accent_echo (previously dominated by accent_echo). **Files changed:** `ConjunctEngine/Sources/ConjunctEngine/Scoring/ThematicV2BackgroundPass.swift` (`fetchCandidates()` WHERE clause + doc comment). **Follow-up (2026-06-12):** candidate cap raised 500 → 750 (LIMIT in `fetchCandidates()`). Results improve with successive passes — each pass scores the next tranche down the `MAX(aesthetic, geometric)` ordering, and genuinely thematic pairs sit deeper in that ordering than aesthetically-strong ones — so a larger batch per pass surfaces them sooner. Also noted: the #94 validation re-index recreated the `pairs` table, wiping all pre-#91 V2 scores (including the 495 accent_echo rows), so the "existing scores left as-is" point above is moot — the current scored set is 100% non-accent_echo (418 contrast / 82 harmony after the first post-#91 pass). |

| 82 | ThematicScorerV2 — LLM pair-level thematic scorer via llama3.2 | feature | done | **Motivation:** The cluster-based ThematicScorer (decision #46 ff.) operates on single-image captions and measures shared vocabulary, not relational position. It cannot distinguish a pair where both images share `sound_music` from a pair where one image *produces* sound and the other *receives* it. A pair-level scorer that sees both captions simultaneously can reason about this directly. **Architecture:** Three new components in ConjunctEngine. `ThematicScorerV2` (actor) calls `llama3.2` via the local Ollama HTTP API (`/api/generate`, `stream: false`). The system prompt instructs the model to evaluate five connection dimensions — shared subject/domain, complementary/opposing actions, tonal resonance, text-as-message, and spatial/temporal context — and return a structured JSON object: `{connected, confidence, shared_context, relationship_type, rationale}`. `ThematicV2Result.score` = confidence when connected, 0 when not. `ThematicV2BackgroundPass` (actor) runs after each page-0 grid load, pulls a candidate batch (pairs where `thematicV2Score IS NULL` AND at least one of aesthetic/geometric > 0.3 AND both images captioned), scores sequentially, writes results back to DB. Sequentail execution is intentional: Ollama handles one request at a time. **DB schema:** v15 migration adds three nullable columns to `pairs`: `thematicV2Score REAL`, `thematicV2RelationshipType TEXT`, `thematicV2Rationale TEXT`. Null = not yet scored. **Display integration:** `PairQueryResult` carries the new fields. `convertToPairFree` (PairHelpers) and `convertToPair` (EngineController) compute `effectiveThematic = thematicV2Score ?? thematicScore` and use it for `displayComposite`, `peakScore`, `axisScore`, and `thematicScore` on `DisplayPair`. Fallback to cluster score when null means pre-pass pairs are unaffected. **Trigger:** `EngineController.startThematicV2Pass()` is called at the end of the `streamPage0Pairs` completion block (inside `await MainActor.run`, after the cache is populated). This starts a background-priority `Task.detached` that runs `ThematicV2BackgroundPass.run()`. The pass task is cancelled when a new index starts. Scores take effect on the next natural grid load — the grid does not observe DB changes reactively, so re-ranking requires a navigation. **Validation:** 9/10 test pairs correctly evaluated by llama3.2 3B in pre-implementation testing. Warm inference ~1–2s per pair; cold start ~26s. **Constraints:** Sequential only (no concurrent Ollama calls). Returns nil on connection failure without crashing — pass aborts rather than burning through candidates against a dead server. **Files changed:** `Database/DatabaseManager.swift` (v15 migration), `Scoring/ThematicScorerV2.swift` (new), `Scoring/ThematicV2BackgroundPass.swift` (new), `Models/PairQueryResult.swift` (+3 fields), `QueryService.swift` (SELECT + mapping in both `streamRepresentativePairs` and `fetchPairs`), `Couplet/Engine/PairHelpers.swift` (effectiveThematic fallback), `Couplet/Engine/EngineController.swift` (effectiveThematic fallback, `thematicV2PassTask`, `startThematicV2Pass()`, cancel on re-index, trigger in `streamPage0Pairs`). |

| 83 | ThematicV2BackgroundPass — skip near-duplicate pairs | fix | done | **Problem:** Near-duplicate pairs (numeric-prefix variants and burst shots) were receiving inflated `thematicV2Score` values (0.8–0.9) because their nearly-identical captions look like a strong thematic connection to the LLM. When sorted by thematic score, these floated to the top despite having compositeScore ≈ 0.07–0.10. Two failure modes: (1) Numeric-prefix variants: `63-foo.jpg` and `foo.jpg` are the same image with a numbering prefix. `PairScorer.sharesBaseName` already zeros composite score for these, but ThematicV2BackgroundPass had no equivalent guard. (2) Burst shots: `_DSF2488` and `_DSF2489` are sequential frames shot within the same second (identical captureDate); dHash doesn't catch them if framing differs enough. **Fix:** (1) Added `AND (a.captureDate IS NULL OR b.captureDate IS NULL OR a.captureDate != b.captureDate)` to the SQL WHERE clause in `fetchCandidates()` — identical captureDates mean burst/sequential shots. (2) Added a `normalizedBaseName()` free function (mirrors PairScorer's strip-prefix/suffix logic) and applied it as a Swift-side filter after the SQL fetch (SQLite has no REGEXP by default). (3) Nulled all existing bad scores via `UPDATE pairs SET thematicV2Score = NULL ... WHERE i1.captureDate = i2.captureDate` — cleared 13 rows. **Same-captureDate pairs are not deleted** — their compositeScore is already 0 from PairScorer's `sharesBaseName` guard, so they won't surface by composite sort; they'll simply never receive a ThematicV2 score. **Files changed:** `ConjunctEngine/Sources/ConjunctEngine/Scoring/ThematicV2BackgroundPass.swift`. |

| 87 | ThematicV2BackgroundPass — GRDB priority inversion warning; priority raise reverted, warning accepted | fix | reverted | **Problem:** Xcode Thread Performance Checker reported a priority inversion at `GRDB/Utils/Pool.swift:80`: a `.userInitiated` thread (grid DB read via `streamPage0Pairs`) blocked on the ThematicV2BackgroundPass task. The checker flags ANY QoS mismatch, not just large gaps — raising to `.utility` still produced the same warning (`.utility` vs `.userInitiated`). **Fix attempted:** Raised task priority `.background` → `.userInitiated`. This eliminated the GRDB warning but introduced an `isThematicV2Running` synchronization race (see #88) when the commit also restructured the task body. **Revert:** The priority raise (and the concurrent body restructure) was reverted in commit `7004afa` to restore a known-working state. Priority remains `.background`. The GRDB warning is accepted — the actual long-running work (Ollama inference) happens outside any GRDB lock. Fixing cleanly requires GRDB's async-await API, which is a larger refactor than the inversion warrants. The synchronization race was fixed separately in #88 without touching priority. |
| 89 | ThematicScorerV2 — distinguish connection failure from LLM parse failure in abort logic | fix | done | **Problem:** `score()` returned `nil` for both network failures (connection refused, timeout) and JSON parse failures (model output contained unescaped quotes or truncated strings). `ThematicV2BackgroundPass.run()` had a single 3-consecutive-nil abort guard, treating both failure types identically. In practice the first three candidates in the scoring pool had captions referencing protest sign text — the LLM quoted the sign verbatim with unescaped double-quotes in the rationale, producing invalid JSON every time. Three consecutive nil returns triggered the abort, and the pass ended after 0 scored pairs without ever being able to surface "N/500" in the pill. The pill appeared (isThematicV2Running = true) then vanished (abort → isThematicV2Running = false) with thematicV2Total staying 0. **Root cause of the JSON failures:** The LLM includes quoted sign/text content from the captions verbatim in the rationale — e.g. `"rationale": "...activism ("We are NOT" vs. "Say Something")..."` — with unescaped inner double-quotes. Standard JSON parsing fails on these. Also seen: truncated response (model ran out of tokens mid-sentence, missing closing `"`). **Fix:** Changed `score()` from `async -> ThematicV2Result?` to `async throws -> ThematicV2Result?`. Network errors and non-200 HTTP now throw; task cancellation still returns nil (caller checks `Task.isCancelled`); LLM parse failures still return nil. Updated `ThematicV2BackgroundPass.run()` to use `do/catch`: the catch block tracks `consecutiveConnectionFailures` (abort after 3); the nil-return branch resets the counter and continues (pair stays unscored in DB, retried in next pass). **Result:** Parse failures no longer abort the pass; the scorer works through the 500-pair batch, skipping any pair whose rationale output is malformed. **Files changed:** `ConjunctEngine/Sources/ConjunctEngine/Scoring/ThematicScorerV2.swift`, `ConjunctEngine/Sources/ConjunctEngine/Scoring/ThematicV2BackgroundPass.swift`. |
| 88 | startThematicV2Pass — set isThematicV2Running synchronously to close guard race | fix | done | **Problem:** `isThematicV2Running = true` was set inside the detached task's first `await MainActor.run { ... }` body rather than synchronously in `startThematicV2Pass()` itself. The guard `guard !isThematicV2Running else { return }` at the top of `startThematicV2Pass()` runs on the main actor, but the task that sets the flag to `true` is a `.background`-priority detached task. Between when the task is created and when its first `await MainActor.run` executes, `isThematicV2Running` remains `false`. Any `onChange` handler that fires during that window (e.g. `isBackgroundScoring`, `folders.count`, settings changes) can trigger `reloadPairs()` → `streamPage0Pairs` → a second `startThematicV2Pass()` call. The guard passes (flag still false), the first task is cancelled, a second task is created. The first task's already-enqueued `await MainActor.run { isThematicV2Running = true }` still fires (Swift does not cancel `MainActor.run` when the parent task is cancelled), then the first task detects `Task.isCancelled` in `pass.run`, exits quickly, and runs `await MainActor.run { isThematicV2Running = false }`. This sets the flag false while the second task may still be initialising — causing the pill to vanish and `thematicV2Total` to remain 0 since `isThematicV2Running = false` means the pill never shows progress even if the second task is scoring. **Fix:** Move `isThematicV2Running = true`, `thematicV2Scored = 0`, and `thematicV2Total = 0` to synchronous assignments in `startThematicV2Pass()` before `Task.detached` is called, and remove the corresponding inner `await MainActor.run` block from the task body. Since `startThematicV2Pass()` is `@MainActor`, setting `isThematicV2Running = true` here is atomic with the guard check — any subsequent call will see the flag immediately. **Files changed:** `Couplet MacOS App/Couplet/Engine/EngineController.swift` (`startThematicV2Pass()`). |

| 86 | Anchor strip surfacing — fix LIMIT 200 and sync temporal penalty in convertToPair | fix | done | **Problem 1 — Anchor DB LIMIT too small:** `EngineController.fetchPairs` with `anchorImageID != nil` used `dbLimit = 200`. The `displayLimit = 200` alongside it was never applied — the trim code is inside `if anchorImageID == nil`. So the DB LIMIT was the sole cap for anchor queries. Hub images can have 400+ pairs; LIMIT 200 silently dropped the bottom half. The V2-aware ORDER BY (`MAX(compositeScore, 0.6 × peakScore + 0.4 × composite)`) correctly floats V2-scored pairs up, but once enough other pairs get high V2 scores, a pair ranked 11th today could slip past 200. **Fix:** Anchor queries use `dbLimit = 500`; non-anchor keeps 2000. 500 covers the realistic hub-image ceiling (407 in the test library) without hydrating an unbounded list in the anchor strip UI. The `displayLimit` trim only applies inside `if anchorImageID == nil`, so the DB limit is the sole cap for anchor queries. **Problem 2 — Temporal penalty divergence (missed in #85):** `EngineController.convertToPair` (used in `fetchPairs` for the anchor strip) retained the old 4-step formula: `≤30s → 0.40 | ≤60s → 0.55 | ≤300s → 0.85 | >300s → 1.0`. Decision #85 updated `convertToPairFree` (PairHelpers.swift) and PairScorer.swift but missed this function. The anchor strip was computing different composite scores than the grid for the same pairs. **Fix:** Synced `EngineController.convertToPair` to the canonical 5-step formula: `≤30s → 0.40 | ≤60s → 0.55 | ≤120s → 0.75 | ≤300s → 0.90 | >300s → 1.0`. **Verification:** The canonical test pair (R0004921 + L1009811, pairID=7880084: aesthetic=0.398, geometric=0.731, V2=0.8 once scored, dates 4yr apart → penalty=1.0) would compute: displayComposite=0.625, peakScore=0.800, axisScore=0.730. It ranks #1 among all 407 pairs for that image under the V2-aware ORDER BY, and is now reachable. Currently (V2=NULL) it ranks 11th (sort score=0.533) — well within LIMIT 2000. It is candidate #255 in the ThematicV2BackgroundPass pool (MAX(aesthetic,geometric)=0.731) and will be scored on the next pass. **Files changed:** `Couplet MacOS App/Couplet/Engine/EngineController.swift` (dbLimit unified to 2000; temporal penalty 5-step). |

| 85 | Temporal penalty — add 120s intermediate step, soften 300s step | fix | done | **Problem:** The four-step penalty table (`≤30s → 0.40`, `≤60s → 0.55`, `≤300s → 0.85`, `>300s → 1.0`) had a gap between the 60s and 300s bands. Pairs shot 60–120 seconds apart — often the same scene revisited briefly — received a penalty of only 0.85, nearly identical to pairs shot up to 5 minutes apart. In practice this was too permissive: same-session pairs within 2 minutes are rarely interesting pairings. **Fix:** Added a new 120s step at 0.75 (`≤120s → 0.75`) and softened the 300s step from 0.85 to 0.90 (`≤300s → 0.90`). The 30s and 60s steps are unchanged. The `>300s → 1.0` band is unchanged. Updated formula: `≤30s → 0.40 | ≤60s → 0.55 | ≤120s → 0.75 | ≤300s → 0.90 | >300s → 1.0`. **No re-index required** — the penalty is applied at display time in `convertToPairFree` (PairHelpers.swift) and also at score time in PairScorer.swift; both sites updated. `ThematicV2BackgroundPass.fetchCandidates()` uses `ABS(captureDate diff) > 300` to exclude candidates that would receive any penalty — this threshold remains `> 300` and is unaffected. **Files changed:** `Couplet/Engine/PairHelpers.swift` (display-time penalty), `ConjunctEngine/Sources/ConjunctEngine/Scoring/PairScorer.swift` (score-time penalty). Note: `EngineController.convertToPair` was not updated in #85 — synced in #86. |

| 79 | Genre framing — do not use genre labels as design constraints | principle | permanent | Couplet is designed for any single-photographer library. The developer's personal library is a data source, not a design specification. Genre vocabulary ("street photography," "documentary") imposes an artificially narrow set of subject, aesthetic, and pairing expectations. Where historical decisions reference library-specific observations (e.g. "urban_street fires on most images," "street photography libraries tend toward mid-tonal B&W"), treat these as observations about one dataset at a point in time, not stable axioms. Calibration values derived from a single library should be revisited if the library changes significantly or if the tool is used with a different library. When assessing scoring or surfacing problems: reason from the data and the pairing theory, not from assumptions about what photographers of a particular genre value or shoot. |

| 78 | axisScore blend — 0.6 × peakScore + 0.4 × displayComposite | improvement | done | **Problem:** Pure `max()` axisScore (`max(aesthetic, geometric×0.8, thematic) × temporalPenalty`, decision #76) meant a pair exceptional on one axis but weak elsewhere (e.g. aesthetic=0.90, geometric=0.10, thematic=0.05) ranked identically to a pair excellent on two or three axes. Single-axis pairs monopolised the top of the "Best" sort. **Fix:** Changed axisScore to a blended formula: `0.6 × peakScore + 0.4 × displayComposite`, where `peakScore` is the pure max formula. Effect: single-axis pair (0.90, 0.10, 0.05) → axisScore≈0.70; two-axis pair (0.80, 0.10, 0.75) → axisScore≈0.74; three-axis pair (0.75, 0.70, 0.72) → axisScore≈0.74; mediocre-everywhere (0.55, 0.50, 0.52) → axisScore≈0.54. Multi-axis pairs pull above single-axis; both beat mediocre-everywhere. **Display-time only. No re-score needed.** **Files changed:** `Couplet/Engine/PairHelpers.swift` (`convertToPairFree`: renamed intermediate to `peakScore`, blend formula for `axisScore`), `Couplet/Engine/EngineController.swift` (`convertToPair`: identical change). |

| 77 | B&W aesthetic discount + contrast ceiling recalibration | fix | done | **Problem 1 — B&W tonal inflation:** Two B&W images with similar contrast scored ~0.9+ on the aesthetic axis. The HSL histogram for B&W images concentrates all mass in 8 lightness bins (all pixels map to h=0, s=0 in `rgbToHSL` when r=g=b). The intersection of two 8-bin distributions is less discriminative than the full 1,152-bin colour intersection — even moderately different B&W images score high because photographic libraries tend to cluster in mid-tones. **Problem 2 — Contrast ceiling:** `paletteContrastScore()` normalised by `/80`, allowing extreme colour-palette pairs to reach 0.941 and dominate the top of the "Best" sort. **Fix 1 — B&W discount (initial 0.35, revised to 0.65):** Added `colorProfileA/B: String = "color"` parameters to `PairScorer.score()` and `aestheticScore()`. When `bothBW == true`, harmony and contrast are multiplied by a discount factor. Initial value 0.35 was too aggressive — it brought the B&W ceiling to ~0.35, below almost every colour pair, and suppressed genuinely tonal-resonant B&W pairs (both high-key, both moody). Revised to **0.65** after validating that B&W harmony IS measuring real lightness similarity (not artificial bin inflation) — it just has lower discriminative power due to 8 vs 1,152 bins. At 0.65, a strong tonal match (pre-discount ~0.85) scores ~0.55; a weak match (~0.43) scores ~0.28 — comparable range to colour pairs. Accent echo is unaffected (`accentHue` is nil for B&W images). **Fix 2 — Contrast ceiling:** Changed normalisation divisor in `paletteContrastScore()` from `/ 80` to `/ 100`. The 0.941 pair drops to 0.753. **No DB migration needed. Requires re-score.** **Files changed:** `Scoring/PairScorer.swift` (`score()` signature, `aestheticScore()` signature + B&W discount block + comment, `paletteContrastScore()` divisor), `IndexingEngine.swift` (both scoring phases: `imageMeta` typealias + SQL SELECT + construction + `PairScorer.score()` call). |

| 76 | axisScore display field + "Best" default sort | improvement | done | **Problem:** The display grid sorted by `displayComposite` (weighted average), which ranked mediocre-everywhere pairs above one-axis-exceptional pairs. #75 ensured such pairs entered the DB; they still ranked 400th in the grid. **Fix:** Added `axisScore: Float = max(aestheticScore, geometricScore × 0.8, thematicScore) × temporalPenalty` to `DisplayPair`. The × 0.8 geometric scalar accounts for geometric's lower composite weight (0.20 vs 0.40 for the others) so all three axes compete on equal footing. `temporalPenalty` is computed once alongside `displayComposite` and reused — never recomputed separately (decision #26). Changed default `sortOrder` in `PairsGridViewModel` from `.composite` to `.axis`. Added `.axis = "Best"` case to `PairSortOrder`; renamed `.composite = "Balanced"`. `dbColumn` for `.axis` returns `"compositeScore"` (proxy for the DB-level query ordering; the cap-2 greedy then re-sorts the in-memory results by axisScore via `pairSortComparator`). Added `.axis` case to `pairSortComparator` and `displayedPairs` sort switch. `axisScore` defaults to `0` in the `nonisolated init` so preview/SampleData call sites that don't pass it continue to compile. Also fixed `convertToPair` in `EngineController` to recognise `selectedFor == "aesthetic"` for modality (missed in #75, present in `convertToPairFree` but not the main-actor path). **No DB schema changes. No re-score required.** Takes effect immediately. **Files changed:** `AppModels.swift` (`axisScore` field, `PairSortOrder` cases, `nonisolated init`), `PairHelpers.swift` (`axisScore` computation, `pairSortComparator` `.axis` case), `EngineController.swift` (`axisScore` computation, `selectedFor == "aesthetic"` fix), `PairsGridViewModel.swift` (default sort, `displayedPairs` switch, `clearFilters`). FilterBarView picks up the "Best"/"Balanced" label rename from the `rawValue` change in AppModels — no code change needed there. |

| 75 | Thematic boost fix + aesthetic topK escape hatch (first step toward #66) | improvement | done | **Problem addressed:** (1) The thematic boost fired at `thematic >= 0.20` unconditionally, reweighting toward thematic even when it was the weakest axis. L1007802+punk (aesthetic=0.737, geometric=0.707, thematic=0.369) triggered it and scored composite=0.511 instead of 0.584. (2) No aesthetic escape-hatch pool existed — geometric and thematic had topK pools; aesthetic didn't. **Fix 1 — Thematic boost condition (PairScorer.swift):** Changed guard to `if hasCaptions && thematic >= 0.20 && thematic >= max(aesthetic, geometric * 0.8)`. Boost fires only when thematic is dominant. L1007802+punk: composite rises to 0.584. **Fix 2 — Aesthetic topK pool (IndexingEngine.swift, both phases):** Top-5 per image by `aestheticScore`, minimum 0.55. `selectedFor = "aesthetic"`. **Fix 3 — Modality display (PairHelpers.swift):** `selectedFor == "aesthetic"` → `modality = .aesthetic`. **What this does NOT fix:** The display grid sorts by `displayComposite` — a simple weighted average of component scores with no specialization reward. A pair with aesthetic=0.80 + thematic=0.10 has displayComposite=0.40×0.80 + 0.20×G + 0.40×0.10 = ~0.38, ranking below pairs that score 0.50 on everything. Getting into the DB via the aesthetic pool doesn't help if the pair then ranks 400th in the sorted grid. The systemic fix — replacing or supplementing displayComposite with something like `max(aesthetic, 0.8×geometric, thematic)` — remains to be designed in backlog #66. **No DB migration required.** Takes effect on next re-score. **Files changed:** `Scoring/PairScorer.swift`, `IndexingEngine.swift` (both phases), `Couplet/Engine/PairHelpers.swift`. |

| 98 | Caption gaze-direction errors — permission-to-omit on next re-caption | improvement | open | qwen2.5vl gets gaze direction wrong in some captions (e.g. "looking down" when the subject looks ahead). **Exposure (DB query, 2026-06-12 post full re-caption):** 666/1047 captions mention gaze; 383 claim a direction (down/up/ahead/away — wrong tilt is mostly cosmetic, the LLM has little to grab onto); 159 claim a target ("looking at X" — the dangerous variant, a wrong target fabricates a relational connection). **Not redundant with geometric gaze:** caption gaze feeds the thematic/ThematicV2 LLM axis; geometric `gazeDirectionX` (Vision pupil offset) feeds the geometric axis, measures horizontal only ([-1,+1] left/right), and has ~15-20% coverage (pupil-only since #69). The vertical "down vs ahead" errors are on a dimension the geometric signal does not measure, so the two do not cover for each other. **Do NOT fix by demanding more precision:** tested mid-session (#97) — adding "name what the gaze lands on" made qwen confidently invent targets (reassigned the rose-woman gaze to the bus). Forcing precision converts "missing" into "fabricated". **Correct fix:** soften the prompt gaze clause toward permission-to-omit gaze when it is not clearly readable — but that requires another full ~1047-image re-caption, so do NOT re-run for this alone. **Gating:** let the post-re-caption ThematicV2 pass be the judge. Watch for V2 pairs whose rationale leans on a gaze *target*; if those come back hallucinated, batch the permission-to-omit prompt change into the next re-caption alongside any other refinements. If V2 results are clean despite occasional wrong tilt, it never mattered. Single-frame cousin of #72 (directed-attention pairs). See #50, #97. |

| 99 | Oracle investigation — three-layer diagnosis of why curated thematic pairs don't surface | diagnostic | done | **Method:** Andre supplied 8 hand-curated "golden" thematic pairs that should surface (violinist+ears-woman, pigeons+peacock, sunscreen-eyes+see-something-bus, see-something+holstered-gun, gun+water-gun-game, gun+punk, smile-text+hoop-over-mouth, smile-text+smiling-woman) plus 3 known-bad pairs. Checked each against the live DB (`check_golden.py`), then forced all 11 directly through `ThematicScorerV2`'s exact call (system prompt + IMAGE A/B format, llama3.2 3B, temp 0) bypassing candidate selection (`force_v2.py`), then ran a frontier-model oracle (Opus 4.8, judging **from caption text only**, images and ground-truth descriptions withheld) on the same inputs. **Finding 1 — selection drops everything:** 0 of 8 golden pairs reached the live V2 scorer. 5 of 8 have no pairs-table row at all (below every four-pool topK cutoff — they're Mode 1/Mode 2 pairs that score low on aesthetic/geometric/cluster-thematic, exactly backlog #95 thread 4); 2 of 8 are in the DB but excluded from V2 by the #91 accent_echo carve-out (see-something+gun, gun+water-gun — the #58/#92 tradeoff biting real pairs); 1 (pigeons+peacock) is a valid candidate just not yet reached in the MAX(aesthetic,geometric) ordering. **Finding 2 — the 3B scorer can't reason these even when forced:** of 8 golden pairs handed directly to it, 6 rejected with near-identical boilerplate (*"depict different subjects/settings, lacking a clear thematic connection"* at flat 0.32) and 2 "accepted" only via hallucinated `echo` rationales (smile+hoop invented "the woman's shadow and the man's beard"; smile+smiling-woman called a concrete sign "a woman smiling"). It also false-accepted known-bad BAD-3 via lazy echo. The 3B never fires `complementary` (source/receiver) or `ironic` (sign-vs-image) — the two categories your pairs need — despite both being defined in its prompt. **Finding 3 — a frontier model recovers most from the SAME captions:** the oracle recovered 5 of 8 (pigeons+peacock contrastive ~0.7; sunscreen+see-something ironic ~0.6; both smile pairs ironic ~0.7; violinist+ears complementary, weak ~0.5 then ~0.7 after caption fix). So the LLM-judge architecture is **not at a capability floor** — the connections are present in the captions; the 3B reasoner just can't see them. **Finding 4 — caption accuracy is a hard upstream gate:** 3 of 8 (all gun-anchored) were unrecoverable by ANY model because the gun's caption contained no gun — qwen misread an openly holstered semi-automatic pistol as *"a black device, possibly a camera."* A single misread image silently killed 3 pairs. An anchor-misread sweep of the 6 load-bearing images found a second: image 07 (ears-woman) read hands-cupped-over-ears as *"hands pressed against her temples… distress,"* erasing the sound-reception gesture the flagship pair depends on. The other 4 anchors (smile-path text, see-something LED, pigeons, peacock) were correct on their load-bearing elements (only cosmetic embellishments). Misreads cluster on **ambiguous physical objects qwen resolves to the wrong everyday referent** (a holstered gun → camera; ears-covering → temples). **Actions taken:** manually re-captioned image 2038 (73, gun) and image 2072 (07, ears) in the v3 physical-facts style; both flip from unrecoverable/weak to clearly recoverable by the oracle (gun+water-gun contrastive ~0.75, see-something+gun ironic ~0.72, violinist+ears complementary ~0.7) — but the 3B still rejects the corrected violinist+ears with the same 0.32 boilerplate, confirming the scorer (not the caption) is now the binding constraint for that pair. V2 scores nulled on the 9 pairs touching the two corrected images. **PRESERVE-LIST (critical):** the irreplaceable manual captions are image IDs **2018** (punk), **3439** (fight), **2038** (gun), **2072** (ears) — a future full re-caption that nulls all captions MUST exclude these IDs or qwen will overwrite them with its misreads. (The 4 auto-fixed pilots 2026/2048/3835/4017 are greedy@768 outputs, regenerable.) **Conclusion — the three layers gate in series and all three need work:** (1) caption accuracy — targeted #81-style fixes for object-misread anchors; (2) selection (#95/#66) — the hard gate, Mode 1/2 pairs never enter the pool; the #91 accent_echo exclusion also cuts genuine pairs; (3) scorer (#85) — the 3B cannot do source/receiver or sign-vs-image irony; a bigger model is validated as the lever for pairs that do surface. Scripts in `/tmp/caption-test/` (check_golden.py, force_v2.py). |
