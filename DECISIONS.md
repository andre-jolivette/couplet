# Couplet — Architectural Decisions & Current State

> **What this file is:** A running log of every significant architectural decision made during development, in order. If you're trying to understand why something is built the way it is, start here. The [CLAUDE.md](CLAUDE.md) file has the current operational summary; individual decision entries in this log are the authoritative record of what was built and why.

## Project Brief
Couplet is a macOS app for photographers. It discovers meaningful image pairs in a photo library — not duplicates or sequential shots, but conceptually resonant connections the photographer might not have noticed.

---

## Architecture

Couplet is built as a sandboxed macOS app (Swift, macOS 14.0) with a Swift package (`ConjunctEngine`) handling all indexing, scoring, and database work, wired to a SwiftUI front-end via an `@MainActor` `EngineController` bridge. The core pipeline is an 8-phase indexing engine (scan → duplicates → thumbnails → CLIP → captioning → role extraction → accent/saliency → pair scoring) feeding a SQLite/GRDB database, with LLM background passes (`GazeVisionBackgroundPass`, `ThematicV2BackgroundPass`) that run after the grid loads. Three scoring axes — aesthetic (0.40), geometric (0.20), and thematic (0.40) — drive pair selection via a four-pool topK (composite top-150 + thematic top-50 + geometric top-5 + aesthetic top-5).

Current architecture is documented in [CLAUDE.md](CLAUDE.md). Individual decision entries in the log below are the authoritative record of what was built and why.

---

## Decisions Log

| Decision | Rationale |
|----------|-----------|
| Security-scoped bookmark for CLIP model | App Sandbox requires it; model lives outside app bundle |
| 512px thumbnail for CLIP input | Full-res caused IOSurface exhaustion during concurrent extraction |
| Soft temporal falloff (not hard cliff) | Hard 30s cliff was too aggressive for street photography shot quickly |
| Dice coefficient for thematic | Jaccard penalised protest photos (6 clusters) pairing with portraits (2 clusters) too harshly. Dice penalises each unique cluster once (denominator only) vs. Jaccard's double-penalty (numerator + denominator). Both asymmetry gate (≥1 unique per side) and saturation gate (>3 shared → zero) are preserved. Ceiling: 3 shared + 1 unique each → 6/8 = 0.75, comparable to aesthetic (0.84) and geometric (0.79) maxima in the current library. |
| 2000 DB fetch → cap → 750 display | Per-image cap + thematic re-weighting shrank 750 fetch to ~380 displayed |
| Per-image cap of 15 | Car interior image had 117 pairs, crowding out rest of library |
| topK=50 + thematic topK=10 | Thematic pairs never made composite top-50; separate selection ensures they appear |
| DELETE+reINSERT pairs on re-index | record.save() hit UNIQUE constraint silently; old scores were never updated |
| Caption all active images (not just scan batch) | Images from previous scans weren't getting captioned on re-index |
| colorProfile: pixel-level chroma sampling threshold=15 | `kCGImagePropertyColorModel == "Gray"` misses B&W JPEG exports from Lightroom/Capture One (stored as RGB, zero chroma). 32×32 downsample + max per-channel diff ≤ 15/255 catches these while absorbing JPEG ringing artifacts. |
| colorProfile backfill on every re-index | Migration default "color" affected all pre-v4 images. Running `refreshAllColorProfiles` after `upsertImages` (skipping the current scan batch) corrects the whole library from a single folder re-index. |
| Structured moondream prompt with Sentence 1/2 labels | Freeform prompts return empty string for ~90% of images; label scaffolding in instructions is required for reliable output even when asking moondream not to emit the labels. Retired — qwen2.5vl:7b (#20) uses a freeform paragraph prompt with no scaffolding. |
| Settings wired into EngineController at init | SettingsStore is @Observable (not ObservableObject); passed in at construction so fetchPairs can read live weights and threshold without async bridging |
| Thematic filter + live weight re-sort are display-time only | Scores stored in DB use default weights; re-sort on fetch avoids re-indexing on every settings change |
| TriangleWeightPicker takes three Binding<Double> params | Float↔Double bridging done at call site via computed Binding properties in SettingsView |
| Edge magnitude threshold raised 0.05 → 0.15 | Fine texture (brick, fabric, skin pores) was polluting the orientation histogram. Higher threshold filters these pixels before they vote, leaving only genuine structural edges. Baked-in constant in GeometricAnalyser — not a slider. |
| Geometric sub-scores stored per pair (v5) | rawEdgeSim, rawGridSim, maxEdgePeakedness, maxGridVariance persisted at score time. Enables display-time gating and re-weighting without re-score or re-index. Nil for pre-v5 pairs (fallback to stored geometricScore). |
| Display-time gating via two sliders | "Line direction strength" (edgePeakednessFloor, default 2.2) and "Composition structure floor" (gridVarianceFloor, default 0.12) apply binary discounts (40%/50%) at display time. Calibrated against library debug output: p10≈2.16 for peakedness, p10≈0.116 for variance. Floor discounts apply after the distinctiveness multiplier. |
| Distinctiveness multiplier (v6) | edgePeakednessMult = pow(normPeakA × normPeakB, 0.4); gridVarianceMult = pow(normVarA × normVarB, 0.4). Both images must be geometrically interesting for a pair to earn full credit — one flat image drags the score down proportionally. Normalization anchors at p90: kEdgePeakedNorm=4.0, kGridVarianceNorm=0.20. Exponent 0.4 (reduced from sqrt=0.5 — one calibration step toward 0 to soften the penalty). Multipliers stored per pair for display-time re-application. |
| Multiplier and floor gate are complementary, not alternatives | Multiplier: continuous, uses product of both images (both must be interesting). Floor gate: binary, uses max of both images (if even the stronger image doesn't clear the bar, discard). The multiplier fires first; the floor gate catches any remaining cases where the max is below the user's chosen threshold. |
| Orphan pair cleanup on every re-index | The per-run `DELETE FROM pairs WHERE imageAID IN (:ids) OR imageBID IN (:ids)` only covers images active in the current scan. Pairs for images deactivated via `removeFolder` were never removed, accumulating indefinitely (40,848 orphaned rows found — half the table). Added a sweep `DELETE FROM pairs WHERE imageAID NOT IN (active) OR imageBID NOT IN (active)` before the per-run DELETE. Runs unconditionally each re-index; cost is negligible on SQLite with indexed foreign keys. |
| Strip moondream label prefix at generation time | The structured prompt uses "Sentence 1:" scaffolding in its instructions; moondream (1.8B) sometimes echoes the label in output despite "No sentence labels" in rules. Previously: cleanup SQL nullified these captions, triggering a re-caption that often produced the same prefix again — an infinite cycle wasting ~152 ollama calls per run. Fix: (a) cleanup SQL now strips the prefix in-place using `TRIM(SUBSTR(caption, INSTR(caption, ': ') + 2))` instead of `SET caption = NULL`, preserving caption content; (b) `OllamaCaptioningEngine.caption()` strips the prefix from the raw response before storing. WHERE patterns unchanged (`'Sentence 1:%'` and `'First sentence:%'` only). Retired — both the cleanup SQL and the prefix-stripping code were removed when moondream was replaced by qwen2.5vl:7b (#20). |
| Weighted Dice for thematic scoring | All 24 clusters now carry a pairing weight (1.0 / 0.75 / 0.5). Score = 2×Σweights(A∩B) / (Σweights(A)+Σweights(B)). Tier 1.0: grief_sorrow, vulnerability_exposure, isolation_solitude, ritual_ceremony, tension_conflict, tenderness_care, devotion_belief, power_dominance, sensory_overwhelm. Tier 0.75: transformation_change, skilled_performance, labor_effort, stillness_rest, waiting_anticipation, movement_energy, bodily_gesture, looking_watching, confinement_freedom, youth_age, sound_music, joy_celebration. Tier 0.5: urban_street, nature_landscape, community_gathering. Weights calibrated against library frequency data (rationale field) — high-frequency ambient clusters scored lower. Known follow-on: a pair matching only a single 0.5-weight cluster still computes Dice=1.0 when neither image has any other clusters; a minimum weighted-shared floor is deferred to a subsequent session. |
| qwen2.5vl-caption: num_ctx capped at 2048; images resized to 512px before encoding | qwen2.5vl:7b defaults to a 128k context window. Ollama allocates KV cache proportional to num_ctx at load time — 128k costs ~2GB extra RAM and dominates inference time. Full-resolution camera files (12–24MP) encode to ~2,677 prompt tokens via qwen2.5vl's tiled patch encoder, which exceeds 2048 and causes degenerate output. Fix: OllamaCaptioningEngine now resizes every image to 512px using CGImageSourceCreateThumbnailAtIndex (same pipeline as CLIP) before base64-encoding. Measured token budget at 512px: ~247 vision + ~270 prompt + 250 response cap = ~767 total. num_ctx 2048 gives ~2.5× headroom. The Modelfile lives at `ConjunctEngine/Modelfile` and registers the model as `qwen2.5vl-caption`. Do not raise num_ctx casually — even 4096 doubles the KV cache cost for no benefit at these input sizes. |
| qwen2.5vl tokenizer special-token cleanup | qwen2.5vl:7b occasionally leaks tokenizer control tokens (`<|im_start|>`, `<|im_end|>`) into the generated response when generation is cut off at the num_predict boundary. `OllamaCaptioningEngine.caption()` truncates the response at the first occurrence of `<|im_` before storing. This is preferred over a post-hoc SQL cleanup — the DB never receives a malformed caption in the first place. |
| Captioning pipeline uses cached thumbnails | The captioning phase previously passed original image URLs to `OllamaCaptioningEngine`, which then re-decoded the full-res file (typically 12MB) to generate a 512px thumbnail on each call. Phase 2.5 (Thumbnails) runs before captioning and already writes 512px JPEGs to `~/Library/Containers/com.toastingmachine.Couplet/Data/Library/Caches/Conjunct/thumbnails/{imageID}.jpg`, recorded in `images.thumbnailPath`. The captioning SELECT was extended to fetch `thumbnailPath` and resolve it to a file URL; when the cached file exists it is passed directly to `caption()`, bypassing the re-decode. `thumbnailData(from:)` in `CaptioningEngine.swift` remains as a fallback for any image whose cache file is absent (e.g. OS cleared Caches). Measured saving: ~80ms per image (~90s across a 1,100-image library). |
| Five new clusters + two-signal Cluster struct (#15) | Clusters designed through photographer-influence research (Paul Graham, Rebecca Norris Webb, Daniel Arnold, Jeremy Paige, Melissa O'Shaughnessy, Andre Wagner, Rosalind Fox Solomon, Sage Sohier). Validated against 1,080 live qwen2.5vl captions before implementation. **Architecture**: Added `requiredGroups: [Set<String>]?` to the `Cluster` struct (default `nil` — zero behavior change to existing 24 clusters). When set, `matchedClusters()` requires ≥1 keyword hit from *each* group rather than any keyword. Two-signal gating was necessary for three clusters: `absurd`/`juxtaposition` each appear 121× in the corpus and `surreal` 59× — without gating these would fire on nearly every street photo. **humor_absurdity** (tier 0.75): G1 = comic/absurd register (absurd, humor, irony, juxtaposi, whimsic, comic); G2 = visible reaction (laugh, amuse, delight, play, cheer, glee). Tier 0.75 because humor is meaningful but less emotionally weighty than grief/tenderness. **uncanny_ordinary** (tier 1.0): G1 = eerie/dreamlike register (surreal, unsettl, ghost, haunt, dreamlike, mysterio); G2 = specific visual mechanism (mirror, echo, obscure, oblivio, unaware, double). Tier 1.0 because when both signals fire together the pairing is rare and significant. **economic_precarity** (tier 1.0): single-signal; condition vocabulary (marginalize, inequalit, precario, systemic) plus material evidence (worn, weather, dishevel, makeshift, tatter). Note: poverty/homeless/destitute validated as zero-occurrence in qwen captions — excluded. `weather` is broad but anchored by the social-framing terms. **solitude_in_crowd** (tier 1.0): two-signal to distinguish from `isolation_solitude`; G1 = crowd context (crowd, bustl, amid, pedestrian, surround, throng); G2 = disconnection (alone, solitary, detach, withdrawn, invisible). Both clusters can fire for the same image — correct behavior; the pair scoring values the overlap. **domestic_intimacy** (tier 1.0): single-signal, conservative vocabulary; bedroom/kitchen/household/nurturing validated as zero-occurrence — excluded. Uses domestic, household, interior, cozy, curtain, famili, belonging, nurtur, groom, tend, nest. Low expected frequency is intentional: tier 1.0 reflects that rare matches are high-confidence. |
| Weighted Dice ambient floor (kMinWeightedShared=0.75, kAmbientFloor=0.1) | A pair matching only a single tier-0.5 cluster (urban_street, nature_landscape, community_gathering) computes weightedShared=0.5, the only possible value below 0.75 in the discrete weight set {0.5, 0.75, 1.0}. Rather than returning 0, such pairs return kAmbientFloor=0.1 — a weak signal, not thematic resonance. The threshold 0.75 matches the lowest non-ambient tier weight so that a single bodily_gesture/looking_watching/etc. match still scores normally. The asymmetry gate in PairScorer.swift already prevents pure-symmetric single-cluster pairs (both images having only that one cluster) from scoring at all; this floor handles the residual case where each image has the ambient cluster plus unique non-overlapping clusters. If a new tier weight below 0.75 is added, kMinWeightedShared must be lowered to match. Calibrated against library: 0 pairs had thematicScore=1.0; up to ~2,109 cluster-scored pairs (6.2%) are affected on re-index. |
| Five new clusters designed from photographer-influence research (#15) | Clusters designed by researching the work and aesthetic concerns of: Paul Graham, Rebecca Norris Webb, Daniel Arnold, Jeremy Paige, Melissa O'Shaughnessy, Andre Wagner, Rosalind Fox Solomon, Sage Sohier — the humanist street and documentary tradition. Real library images used to validate cluster boundaries and sharpen vocabulary during design. **`humor_absurdity`** (tier 0.75): strangeness that resolves into a punchline or visible delight. Distinguished from `uncanny_ordinary` by the presence of a visible reaction (laughter, amusement, complicity) or a structurally resolved comedic contrast. Requires two signals — at least one register/reaction word plus one structural word — to avoid false positives from loose descriptors like `incongruous` alone. **`uncanny_ordinary`** (tier 1.0): the ordinary tips into strangeness without resolving. Defining quality is category slippage — things appearing where they don't belong, or unconsciously becoming something unexpected. The subject is typically unaware of the meaning layer the camera found. Also requires two signals from different vocabulary groups; `surreal` alone is insufficient given qwen's tendency to use it loosely for vivid lighting. **`economic_precarity`** (tier 1.0): the person for whom the social contract is visibly not working, in any setting. Vocabulary is intentionally setting-neutral — fires for street work, indoor protests, institutions equally. Should co-occur naturally with `solitude_in_crowd`; that's correct behavior. **`solitude_in_crowd`** (tier 1.0): presence without participation. Distinct from `isolation_solitude` by requiring crowd context (at least one crowd-context term must be present). Distinct from `waiting_anticipation` by the absence of forward orientation. Two modes: still figure in motion (stationary while the world flows past) and moving figure in a crowd (in motion but sealed off). **`domestic_intimacy`** (tier 1.0): the private world made briefly visible. Fires for domestic objects carried into public space, domestic gestures performed outside, or interiors where the living space functions as psychological portrait. Low expected frequency — when it fires, trust it; tier 1.0 reflects that rare high-confidence matches deserve full weight. Full vocabulary for all five clusters lives in the Claude Code session notes for #15 and in ConceptClusters.swift once implemented. |
| Post-#15 cluster calibration: saturation gate, vocabulary pruning, num_predict (#25) | After adding 5 clusters (29 total), three structural problems surfaced. **(1) Saturation gate**: original threshold `shared.count > 3` was calibrated for 24 clusters. With 29, genuine cross-context resonant pairs routinely share 4–6 clusters across different emotional registers and are zeroed. Replaced with weight-based gate: `weightedSharedSum > 5.0` (PairScorer.swift). 4×tier-0.75 = 3.0 (passes), 5×tier-1.0 = 5.0 (borderline), 8–10×mixed same-event = 8.0+ (correctly zeroed). **(2) False-positive keywords**: `sound_music` contained `play` (fires on "play of light"), `press`/`cover`/`plug`/`shield`/`block` (duplicate `sensory_overwhelm` vocabulary). Removed; kept `ear`/`hear`/`listen` because ear-cupping legitimately belongs in sound_music, sensory_overwhelm, and bodily_gesture simultaneously — clusters are not exclusive. `domestic_intimacy` contained `tend` (stem("tender") = "tend" after stripping "er"; "tender" is ubiquitous in humanist captions, fires alongside tenderness_care inflating shared count). Removed `tend`, `window`, `settled`, `lived`. Converted to two-signal: G1 = enclosed personal space (domestic/household/interior/cozy/curtain/famili/belonging/doorstep/windshield/dashboard/backseat/tent/camper/room); G2 = domestic behavioral register (nurtur/groom/nest/habitual/intimate/caring). `economic_precarity` converted to two-signal (G1=social framing, G2=material evidence); added `struggle` as explicit keyword (stem("struggle")="struggle", no suffix match; "struggl" only caught progressive form). `uncanny_ordinary` added "surrealism" to G1 (stem("surrealism")="surrealism"; qwen writes "a touch of surrealism" not "surreal" for dreamlike/halo effects). **(3) Ambient clusters over-firing**: `nature_landscape` fired on urban greenery (tree trunk on sidewalk) — removed `tree`/`light`/`shadow`/`open`/`vast`, added `foliag`/`canopi`/`grove`. `looking_watching` fired on nearly every pair — removed `look`/`eye`/`focus`/`attention`/`direct`/`aim`/`point`/`camera`/`lens`/`glass`/`window`/`reflect`/`reveal`/`expos`/`frame`; retained only deliberate-gaze vocabulary: watch/stare/gaze/peer/squint/glance/glimps/observ/witness/behold/survey/scan/scrutin/seen/noticed/caught/regard. **(4) Caption truncation**: 613/1,083 captions (57%) stored truncated at ~1,200 chars, ending mid-sentence. Root cause: num_predict=250 tokens (≈1,100–1,200 chars) cuts qwen's output before the interpretive second half. Raised to 400. Existing captions require a full re-caption pass; re-score alone does not fix already-truncated captions in the DB. |



| Visual diversity multiplier on thematic score + expanded thematic topK (#45) | **Problem:** Cluster scoring rewards shared cluster volume. Same-event pairs (protest+protest, festival+festival) share many clusters → high Dice → dominate thematic topK-30. Cross-context resonant pairs (musician+ears, grief in different settings) share fewer clusters → lower Dice → excluded from topK entirely. Diagnostic confirmed: musician hero (2045) + ears hero (1523) are active, have captions, have feature vectors, but never made either image's composite topK-150 or thematic topK-30. **Root cause — topK density:** With 1,057 active images and topK=150, the 150th composite pair scores ~0.44–0.47. A cross-context pair with mismatched visual aesthetics scores below this threshold. The thematic topK-30 cutoff for the musician was ~0.46; musician+ears (estimated 0.37–0.42 thematic) fell outside. **Dead-end documented:** Caption embeddings (nomic-embed-text) tested as primary signal. Reverted: general-purpose text embeddings measure same-domain similarity, not cross-context resonance. Two festival images share festival vocabulary → high cosine → crowded out cross-context pairs further. **Fix 1 — Visual diversity multiplier (PairScorer.swift):** CLIP cosine is already computed at scoring time. Applied as a multiplier to `clusterScore` before the thematic-boost check: CLIP < 0.30 → ×1.35 (cross-context bonus); CLIP 0.30–0.60 → ×1.0 (neutral); CLIP 0.60–0.88 → ×0.75 (same-context penalty); CLIP > 0.88 handled by existing composite ceiling. **Effect:** protest+protest (CLIP ~0.65) drops from ~0.73 to ~0.55 thematic. Musician+ears (CLIP ~0.15–0.20) rises from ~0.38 to ~0.51. The multiplier applies to all pairs universally — no enumerated special cases. **Why better than complementarity bonus:** Does not require enumerating role relationships (sender/receiver). Any visually-distinct pair with cluster overlap gets rewarded regardless of the nature of the relationship. **Fix 2 — Thematic topK expansion (IndexingEngine.swift):** `thematicK = max(10, topK/5) = 30` → `max(30, topK/3) = 50` with topK=150. Applies to both Phase 1 and Phase 2 scoring. Combined with Fix 1, musician+ears thematic ~0.51 now fits within the top-50 thematic for the musician. **Pair count impact:** ~10–15% more pairs stored (~112K → ~130K expected). **Asymmetry gate confirmed not the culprit:** Ears has `isolation_solitude`/`sensory_overwhelm`/`grief_sorrow` that musician lacks; musician has `skilled_performance` that ears lacks. Both images have unique clusters; gate passes. Do not weaken or remove it. **Existing penalties preserved:** CLIP > 0.88 → composite ×0.40; CLIP > 0.75 && thematic < 0.20 → composite ×0.65. These remain unchanged — the diversity multiplier and composite ceiling are independent. |

| animal_presence demoted to ambient tier (#47) | **Diagnostic (2026-05-06, post-#45 re-index):** Dog+dog pairs ranked #7 globally (images 1447+1624, composite 0.623, thematic 0.547). Multiple other animal-mention pairs in the 0.58–0.62 composite range. The `animal_presence` cluster at weight 0.75 was the cause — it gave "two dogs" a meaningful thematic signal, exactly the same-same pattern the system is supposed to reduce. The visual diversity multiplier from #45 didn't help: two different dogs in different urban settings score CLIP cosine ~0.35–0.50 (neutral zone, ×1.0 multiplier). **Fix:** `animal_presence` weight changed from 0.75 → 0.2 in `ConceptClusters.swift`. At 0.2, it is now an ambient-tier cluster. Pairs sharing only animal_presence (and other ambient clusters) return the ambient floor (0.1) from `weightedDice` and score ≤ 0.1 thematic. Dog pairs that share a genuine emotional cluster (tenderness_care for a touching scene, isolation_solitude for a lonely stray) still score meaningfully via those clusters. The two-signal gate on `animal_presence` is preserved — it still requires both a specific animal subject noun AND a behavioral/relational context word to fire. This prevents incidental background animal mentions from contributing even the 0.2 ambient weight. **Why not remove the cluster entirely:** Removing it would cause animal pairs with no other cluster overlap to contribute nothing at all to the Dice denominator, inflating scores for pairs that DO share clusters. Keeping it at ambient weight preserves denominator contribution while preventing same-subject rewards. |

| Complementary axis pair bonus (#48) | **Problem:** The thematic Dice score rewards SHARED clusters — it measures "what do these images have in common?" Cross-register complementary pairs (one image is the source of a phenomenon, the other is the receiver) share NO clusters because they fire DIFFERENT clusters on each side. Musician fires `sound_music`, `skilled_performance`; ears fires `sensory_overwhelm`, `isolation_solitude`. Weighted Dice = ambient floor (0.1). After visual diversity multiplier (~1.35 for visually distinct images): thematic ≈ 0.135. Below the 0.20 boost threshold. The pair never enters DB. **Diagnostic confirmation:** Post-#45 re-index, musician (2045) + ears (1523) completely absent from the pairs table (123,326 rows total). Not low-ranked — absent. **Fix — axis pairs:** Added `ConceptClusters.axisPairs: [(a: String, b: String, bonus: Float)]` defining 9 cluster-opposition relationships: `sound_music↔sensory_overwhelm` (+0.35), `power_dominance↔vulnerability_exposure` (+0.35), `joy_celebration↔grief_sorrow` (+0.30), `skilled_performance↔looking_watching` (+0.25), `tenderness_care↔isolation_solitude` (+0.25), `power_dominance↔confinement_freedom` (+0.25), `labor_effort↔stillness_rest` (+0.20), `movement_energy↔stillness_rest` (+0.20), `devotion_belief↔tension_conflict` (+0.20). **Scoring integration (PairScorer.swift):** After computing `clusterScore` via Dice, compute `axisBonus = max(bonuses for any firing axis pair)`. The axis bonus fires ONLY when `clusterScore ≤ 0.10` (ambient floor) — the code guards `if saturated || clusterScore > 0.10 { axisBonus = 0 }`. This prevents additive inflation on top of a real Dice score (e.g. a pair that already scores 0.65 via Dice would reach T=1.35 capped at 1.0 without the guard). The saturation gate is also respected — same-event pairs (weightedSharedSum > 5.0) don't earn the bonus. The asymmetry gate is NOT required for axis pairs — the nature of axis pairs is that each image fires a DIFFERENT cluster, so asymmetry is structural. Apply: `thematic = min(1.0, (clusterScore + axisBonus) × diversityMult)`. **Post-implementation diagnostic (2026-05-06):** After re-index with changes #47, #48, and #49 active, musician (2045) + ears (1523) remains absent from the DB. Root cause: `"ear"` is present in BOTH `sound_music` and `sensory_overwhelm` keyword vocabularies (ears are both a musical instrument component and a sensory receiver). Additionally, `"intensity"` stems to `"intens"` which matches sensory_overwhelm. Both images therefore fire BOTH clusters and share them as a meaningful cluster intersection — Dice > ambient floor, so axisBonus = 0 (guard fires). The pair scores via Dice (~0.45–0.51 thematic) but ranks below the thematic topK-50 cutoff (~0.55–0.70) for this library. The axis pairs approach works correctly for pairs where images genuinely fire opposite-only clusters; it cannot help pairs where both images share both clusters due to overlapping vocabulary. Lesson: the keyword cluster system cannot distinguish "musician playing" from "listener overwhelmed" when both captions use the word "ear" or musical vocabulary. Caption prompt redesign is the recommended next step — see backlog #50. **Rationale string update:** When thematic ≥ 0.20 but no meaningful shared cluster exists, the rationale now checks axis pairs and emits "Complementary resonance — [A] meets [B]" instead of the generic "Conceptual contrast" fallback. Also fixed a pre-existing bug where the rationale picked `shared.first` which could surface ambient clusters ("urban street") — now filters to `meaningfulShared` (weight ≥ 0.75) before selecting the label. **Why axis pairs over role annotations or LLM scoring:** Axis pairs are enumerable, deterministic, and zero-cost at scoring time. Role annotations require per-cluster schema changes and still reduce to enumerating relationships. LLM scoring would require an inference call per candidate pair, expensive at 123K+ pairs. Axis pairs scale to any future relationship type by adding a single line to the array. |

| Meaningful asymmetry gate (#49) | **Problem:** The old asymmetry gate (`!onlyA.isEmpty && !onlyB.isEmpty`) was trivially satisfied by ambient-tier clusters. Two dog photos differing only by `urban_street` vs. `community_gathering` (both ambient, weight 0.2) passed the gate, allowing the Dice computation to proceed and producing inflated thematic scores via their shared tier-0.75 animal behavior clusters (`movement_energy`, `bodily_gesture`, `waiting_anticipation`). **Fix:** Changed the asymmetry gate in `PairScorer.swift` to require at least one meaningful cluster (weight ≥ 0.75) unique to each side: `let meaningfulOnlyA = onlyA.filter { (ConceptClusters.weights[$0] ?? 0) >= 0.75 }` and same for B; gate passes only when both are non-empty. Ambient-only asymmetry (one image has `urban_street`, the other doesn't) no longer counts as meaningful differentiation. **Effect:** Pairs where both images have identical meaningful clusters plus different ambient clusters no longer receive an asymmetry pass — they either score via shared meaningful Dice or return ambient floor. Pairs where each image has at least one unique meaningful cluster continue to score normally. **Interaction with axis pairs:** The axis bonus is not gated by the asymmetry check — when axis bonus fires, the pair is structurally asymmetric by definition (each image fires a DIFFERENT cluster on its side of the axis). **Calibration result:** Dog pairs that previously passed via ambient-only asymmetry now correctly return ambient floor or score only via shared tier-0.75 behavior clusters they genuinely share. |

| Caption embedding infrastructure removed (#51) | **What was built:** During backlog item #9 / #44, a caption-embedding pipeline was added to complement cluster scoring. Components: (1) `CaptionEmbeddingEngine.swift` — a `CaptionEmbeddingEngine` protocol plus `OllamaEmbeddingEngine` (POST `/api/embeddings` to nomic-embed-text, 768-dim L2-normalised float vectors) and `MockEmbeddingEngine`; (2) `v9_caption_embeddings` DB migration — `captionEmbedding BLOB` column on the images table (3,072 bytes/image); (3) Phase 3.6 in `IndexingEngine` — backfill loop for uncaptioned images; (4) `PairScorer.captionEmbeddingCosineSim` — raw cosine on float vectors, unused after revert; (5) `IndexingEngine` stores `embeddingEngine: any CaptionEmbeddingEngine` and fetched `captionEmbedding` in both intra- and cross-folder `imageMeta` queries; (6) `EngineController.buildEmbeddingEngine()` — availability check + engine construction, with `embeddingAvailable` published property; (7) `IndexingProgress.Phase.embedding` case. **What was tried:** nomic-embed-text cosines on qwen2.5vl captions live in [0.55, 0.95] not [0, 1]. After floor normalisation (`max(0, (cosine − 0.50) / 0.50)`), a 65/35 blend with cluster score was applied. This gave zero-cluster pairs like protest+ritual a thematic of ~0.205 — enough to pass the 0.20 boost threshold. The approach was implemented, tested, and partially integrated. **Why it failed:** The embedding measures same-domain similarity, not cross-context resonance. Two festival captions score high cosine because they use the same vocabulary, not because they are conceptually complementary. The same-domain pairs that were already crowding out cross-context pairs via cluster scoring crowded them out further via embedding. An embedding ceiling was added (cap thematic at 0.10 when raw cosine > 0.85) which helped with obvious same-event pairs but the fundamental problem remained: the embedding rewards proximity in language space, not complementarity in human experience. The revert commit (`revert(#9): remove embedding from thematic scoring, disable phase 3.6`) removed the scoring use while leaving infrastructure in place. A subsequent prompt-testing session (2026-05-09) confirmed that the underlying problem is the caption vocabulary, not the scoring mechanism — qwen2.5vl-caption describes what is visible, not what is felt. Embeddings of description-language captions cannot capture emotional register or positional relationship between images. **Why infrastructure was removed:** The v9 column was unused (all values NULL after revert), adding 3KB/image of dead storage. The engine, protocol, and DB query overhead remained even though nothing read from them. The `buildEmbeddingEngine()` call ran on every launch to check nomic-embed-text availability — an unnecessary Ollama request. Phase 3.6 was a no-op comment. None of the 7 infrastructure touchpoints provided value. **What is preserved in this record:** The scoring formula (floor normalisation + blend), the calibration data (7 representative pair cosines), the embedding space shape (0.55–0.95 range for paragraph-length captions), and the failure mode (same-domain amplification). If caption quality improves (via prompt redesign, see #50), a text-embedding approach could be re-evaluated. The implementation was straightforward and the infrastructure took ~1 day to build; rebuilding it is not expensive if the input quality problem is solved first. |

| Caption embeddings (nomic-embed-text) as primary thematic signal (#44) | **Architectural diagnosis:** ConceptClusters vocabulary-matching hit a ceiling at 18% zero-overlap pairs (31,755 pairs both captioned, neither scoring). The problem is conceptual analogy — protest crowd + religious procession, two isolated figures described in different words — invisible to keyword matching regardless of vocabulary depth. Diagnosis from backlog item #9. **CLIP text tower inspection:** `clip-vit-base-patch32.mlpackage` contains only the visual encoder (576 vision_model weight references, zero text_model references). No text encoder is present. **Embedding approach:** nomic-embed-text via Ollama (`ollama pull nomic-embed-text`, 274MB). POST `/api/embeddings` with `{"model": "nomic-embed-text", "prompt": "<caption text>"}` returns a 768-dim L2-normalised float vector. Storage: raw bytes (`[Float].withUnsafeBufferPointer { Data(buffer:) }`) in `images.captionEmbedding BLOB`; 768 × 4 bytes = 3,072 bytes/image, ~3.3MB for 1,100 images. **Calibration — embedding space shape:** nomic-embed-text cosines for real qwen2.5vl captions (150–250 word paragraphs) live in [~0.55, ~0.95], not [0, 1]. Tested against 7 representative pairs: grief-grief=0.732, urban-urban=0.660, protest-ritual=0.658 (the key zero-cluster case), grief-protest=0.611, grief-joy=0.568, grief-urban=0.551. Raw cosine cannot be used directly — unrelated pairs (grief-urban=0.551) sit too close to the target pairs (protest-ritual=0.658). **Floor normalisation:** applied `normSim = max(0, (cosine − 0.50) / 0.50)` to expand the discriminative range. After flooring: grief-grief=0.464, protest-ritual=0.316, grief-joy=0.136, grief-urban=0.102. **Blend formula:** `thematic = 0.65 × normSim + 0.35 × clusterScore`. With no cluster overlap: protest-ritual→0.205 (passes 0.20 boost threshold); grief-joy→0.088 (does not pass). With cluster overlap, the blend averages embedding and cluster evidence. **What is unchanged:** asymmetry gate (both images must have a cluster the other lacks); saturation gate (weightedSharedSum > 5.0 zeroes cluster component); meaningful-tier gate inside weightedDice (shared must contain ≥1 cluster with weight ≥ 0.75); thematic boost (composite = 0.25A + 0.15G + 0.60T when thematic ≥ 0.20); dual topK thresholds; CLIP image ceiling/same-subject penalties. **Cluster system role preserved:** clusters remain the exclusive source of filter-pill labels and info-rail display. Embedding scores are opaque; clusters still name the connection. **Fallback chain:** (1) captions + embeddings → blend formula; (2) captions only → weighted Dice; (3) no captions → CLIP image cosine. **Backfill:** Phase 3.6 in IndexingEngine queries `WHERE caption IS NOT NULL AND captionEmbedding IS NULL` on every re-index, same pattern as caption backfill. **v9 DB migration:** `ALTER TABLE images ADD COLUMN captionEmbedding BLOB` — lives on images table alongside caption, no JOIN needed at scoring time. |

| Role extraction — claims/receiver content coverage (#111) | **Scope:** an extraction-*prompt* pass (text-only, in the spirit of #114), not a join or scoring change. Backlog #111 diagnosed the role-join entry gate's real blocker as role *content* (not presence — 1029/1029 images already carry a `roleProfile`): the golden Mode-1 pairs' images under-populated `claims`, never marked `receiver`, and flattened loaded/symbolic content to bare nouns, so joins 1/2 structurally couldn't fire. **Three clauses added** to `OllamaRoleExtractionEngine.systemPrompt`: (A) *claims may yield multiple concepts* — a surveillance/vigilance sign implies both alarm and watching (`"SEE SOMETHING SAY SOMETHING" → ["danger","watch"]`), where before it collapsed to `["danger"]` alone and lost the watching link; (B) *affect on gestures* — a gesture/posture carrying emotional meaning also emits the affect (`two figures holding hands → ["hold hands","tenderness"]`), not just the literal verb; (C) *preserve loaded symbols* — a culturally/politically/religiously loaded symbol is recorded as its own object rather than the plain carrier (`American-flag bag → {flag, depicted, symbol}`, `cross tattoo → {cross, ...}`, `rainbow flag → {rainbow flag, ...}`), with an explicit "do NOT start enumerating ordinary clothing" guard. **Validation (offline harness over the live production DB's captions + `RoleJoins.join` + the exact `generateRoleCandidates` greedy-by-specificity + per-type-cap admission; qwen2.5:14b, deterministic run-to-run, no production-DB mutation).** Baseline vs revised on the golden + anchor subset: **G9** (367 caged dog ↔ 153 "CAN YOU ESCAPE?") — the #114 confinement clause **realizes on re-extract of the actual production captions** (367 → `subverts:["escape"]`, 153 → `claims+subverts:["escape"]`) → join2 fires + admitted (rare concept). **G14** — 587 now `enacts:["hold hands","tenderness"]` (affect landed) but the join still does NOT fire: no lexical bridge `miss`↔`tenderness` (needs a join-2 synonym bridge, out of scope; a deliberate *slant* tier anyway). **G15** — 61 → `{flag,symbol}`, 564 → `{cross}`+`{rainbow flag}` (loaded descriptors now preserved) but the join still does NOT fire: no same-register conceptual-echo join exists (KNOWN_GOOD_PAIRS lever 6, not built). **G10** — 474 now reliably `claims:["danger","watch"]`, but the pair is architecturally unreachable via extraction: the watching eyes are on a *car* (a secondary object), not the human *subject*, so `enacts` (subject-embodiment) is the wrong field for them, and even when 513 did emit a `watch` enact the join was **capped out** — "watch"/"danger" are popular concepts, same structural cap limit as G8/G11 (#113). **Iteration finding (regression caught + fixed):** the first draft of clause C ("…not just the shirt, bag, or skin it appears on") **primed clothing enumeration** — 245 (a G4 anchor) deterministically dropped its `pigeons(real,bird)` object in favor of listing garments, breaking join3 and evicting G4. Removing the carrier-enumerating sentence and adding the anti-enumeration guard restored 245's bird object (first in the list) and G4. **Regression check (final prompt, two bit-identical runs):** anchors **G4/G6/G7/G16 all still fire and stay admitted** (join3/join2/join2/join1); total admitted stable 1242 vs 1202 baseline — no flood. **Net:** the *content-coverage* half of #111 is delivered and validated (receiver/watcher expressible, affect tagged, symbols preserved, confinement realizes); the *surfacing* half is blocked downstream by already-tracked join/cap gaps (#113 popular-concept cap for G10; a join-2 synonym bridge for G14; a same-register echo join for G15). Only G9 newly surfaces reliably, and it rides #114's confinement clause on a rare concept. Backlog #111 stays open as a partial win (Notes updated). **`directed_at` structural change investigated and declined:** #112 already ruled the off-frame-target route a dead end and retired the directed-attention join; with no consumer, adding an in-frame/absent flag to `directed_at` would be speculative — left untouched. **Realization caveat (same as #114):** affects only newly-extracted profiles; realize on the live library via a forced re-extract (null `roleProfile` for the affected image subset, then re-index → re-judge). Validated purely offline; no production DB was mutated this session. See KNOWN_GOOD_PAIRS.md (G9/G10/G14/G15 rows updated). |
| Role extraction — encode physical confinement as a subvert (#114) | **Problem:** the `subverts` slot captured body-part blocks (covered mouth → "smile") but not *environmental* confinement, so a caged/fenced subject carried no "cannot escape" signal — the "CAN YOU ESCAPE?" sign (153) + dog-behind-barred-doghouse (367) pair (G9) couldn't fire join 2. **Fix:** one clause added to the extraction prompt — "when a cage, bars, fence, pen, enclosure, or barrier physically CONFINES the subject or prevents it from leaving, emit the prevented action → ['escape']; a wall/barrier blocking movement → ['movement']" — plus a "CAN YOU ESCAPE?" → ["escape"] claim example. **Why this works where popular-concept pairs don't:** "escape" is a *rare* concept, so once both sides carry it the #113 greedy-by-specificity cap admits the pair (the cap can't surface popular concepts like smile/sound, but rare ones surface reliably). **Validated offline (qwen2.5:14b, no re-extract):** 367 → subverts ["escape"], 153 → claims+subverts ["escape"]; anchors (598/80/245/176) and incidental-fence probes (lean/walk/stand on a fence, ids 48/49/76) all → []. Iteration note: every *explanatory guard sentence* tried ("an incidental fence … is not confinement") perturbed the 14B into FALSE-firing 49 — the bare clause is cleaner; less is more. **Realization caveat:** prompt change affects only newly-extracted profiles; realize on the current library via a forced re-extract (null `roleProfile` for confinement-word captions, then re-index → re-judge). G15 symbol-preservation deferred (hallucinates without a deterministic gate, and wouldn't surface without a new join). See KNOWN_GOOD_PAIRS.md. |
| Role-join cap redesign — greedy-by-specificity + per-type caps (#113) | **Problem:** `generateRoleCandidates` capped role candidates at 4 per image per join-type, admitted **first-come by image-id order**. This was (a) *fragile* — any change to a join's matching reshuffled which 4 won and silently evicted working pairs (validated in an offline harness over the live profiles: a synonym tweak evicted the smile anchors), and (b) *arbitrary* — the meaningful partner often lost its slot to a lower-id one (the violinist is the 148th sound-source for the straining-ears woman by id). **Fix:** every `RoleJoins.Candidate` now carries a `specificity` (IDF-style corpus rarity of the matched key, from a one-pass `CorpusFreq.build`); `generateRoleCandidates` collects *all* candidates, sorts globally by `(priority asc, specificity desc, idA, idB)`, then admits greedily under **per-join-type caps** (join1=8, join2=8, join3=5, join4=12). This is effectively non-evicting — a good pair only loses its slot to an *even better* pair for the same image, never an arbitrary lower-id one — and adding/altering a join can't silently drop anchors. **Join 1 also** (a) folds speech/music into `sound` (`normalizePhenomenon`) so a megaphone speaker completes a sound receiver, and (b) fires only when the **receiver actively strains** (non-empty `subverts`), which sparsifies the otherwise near-complete sound graph (one receiver had 189 source partners → flood). **New join 4 — claim↔object** (`claimToCategory` map + shared-noun token): a sign/text that warns of or names a thing physically present in the other frame ("danger" ↔ a real weapon). **Distinct from the retired directed-attention "join 4" (#112), which was never built — this reuses the free priority slot.** **Harness-validated (live profiles, no re-extract):** surfaces G16 (megaphone+ears), holds the G4/G6/G7 anchors, ~1.2k candidates. **Structural limit recorded:** popular connecting concepts (sound/smile/bird/danger) can only be *sampled* — specificity is identical within a concept, so the intended pair among many equal matches isn't guaranteed (G5 musician+ears unreachable, G8/G11 hub-limited). The mechanism is reliable for *rare* connections or rule-sparsified ones (strainrcv). Joins 4-directed-attention/5 stay retired/deferred. Realized on the next full re-index. See KNOWN_GOOD_PAIRS.md (join-side + cap-redesign findings). |
| Parallelize indexing & scoring phases (#101) | **Problem:** Indexing/scoring were slow on the M1 Max because most pipeline phases processed one image (or one pair) at a time on a single core, even though the work is independent. Only Phase 3 (CLIP extraction) was parallel (a bounded `withThrowingTaskGroup` producer-consumer at `min(cores, 4)`). **Fix:** Generalised that pattern into a private `mapConcurrent(_:concurrency:work:consume:)` helper on `IndexingEngine` — `work` is `@Sendable`/pure and runs on the concurrent task pool, `consume` runs serially on the engine actor so every `db.write` stays single-writer (GRDB requirement). Applied to: Phase 1.5 dHash compute (the per-image "already hashed?" check hoisted into one read first), Phase 3 thumbnails (`generateThumbnail` split into the pure static `writeThumbnailFile` + a consumer DB write), Phase 6 accent, Phase 7 Vision saliency/gaze, and the Phase 8 pair-scoring loops (intra- and cross-folder; the outer row loop parallelized, each task returning that row's `[PairScore]`, merged in `consume`). **Concurrency formulas (auto-scaling, no UI):** added `cpuConcurrency = processorCount` (thumbnails/accent/dHash) and `visionConcurrency = min(processorCount, 6)` (Vision is ANE/GPU-bound, diminishing returns past a handful); CLIP left at `min(cores, 4)`. Replaces the flat cap so a 16-core Mac uses 16 — important for distributing to other machines. **Determinism:** `PairScorer.score` is a pure static function, so the scored set and every score are identical regardless of evaluation order. The only order-sensitivity was at *exact* score ties in the four-pool topK selection (unstable `sorted` + `prefix`); added `orderedByScore(_:_:)` with a stable `(score desc, imageAID, imageBID)` tiebreaker applied to all four pools in both scoring blocks, so the stored pair set is fully reproducible regardless of parallel completion order. **Validation (`conjunct-bench --mock`, 115-image dev folder, fresh thumbnail cache):** parallel vs. `main` produced **identical top-5 pairs and scores** and identical duplicate stacks; per-phase speedups — duplicate detection 10.2s→1.4s (7.3×), thumbnails 10.6s→1.4s (7.6×), saliency 3.8s→0.9s (4.2×), total 40.8s→19.5s (2.1×). The residual run-to-run pair-*count* variance (~0.1–0.3%, e.g. main 3300–3310 across 5 runs) is **pre-existing** Vision (saliency/gaze) ANE float nondeterminism flipping borderline pairs across the `compositeScore > 0` / topK boundaries — present identically on `main`, not introduced here. **Not done:** Phase 1 file-scan color-profile backfill left sequential (low per-image cost; deferred). Ollama captioning/ThematicV2 concurrency left sequential — uncertain gain (one VLM can saturate the GPU) and requires `OLLAMA_NUM_PARALLEL` server config; benchmark-gated follow-on. No new network/file APIs; all work remains in-sandbox. |
| Golden thematic pairs surfacing — diagnostic close-out (#90) | **Premise:** backlog #90 was opened before the #50 caption redesign and #102 role pipeline, on the belief that pairs the ThematicV2 judge scored well weren't reaching the grid. KNOWN_GOOD_PAIRS.md's own notes already suspected the premise didn't hold (mislabeled G2/G3 as thematic when they're aesthetic/geometric); this pass re-verified against the live production DB (`~/Library/Containers/com.toastingmachine.Couplet/.../conjunct.db`, 2026-06-27) instead of relying on that prior read. **Method:** resolved all 14 Mode-1/dual-mode pairs from KNOWN_GOOD_PAIRS.md to current hero image IDs (all unchanged from the doc), queried `pairs` for each, and for judged pairs replicated the `PairSortOrder.axis` ("Best" sort) SQL proxy plus the exact two-pass `applyCap2Free` algorithm in a Python simulation over the live DB export to get true display rank, not just raw score. **Result:** the scorer/display pipeline is healthy — G4 (245↔246, 0.95 contrastive), G6 (874↔80, 0.95 ironic), G7 (259↔80, 0.95 ironic) all rank in the top ~110 of 121,589 active pairs pre-cap and land at cap-2 ranks 6/21/324 respectively (pages 0–3 of the grid) — no eviction, no display-layer bug. **New finding:** G16 (50↔572, megaphone+ears) is documented as ABSENT in KNOWN_GOOD_PAIRS.md, but the live DB shows it present, role-hypothesis-tagged, and judged 0.95 complementary (cap-2 rank 40) — direct production confirmation that the #113 cap redesign's `strainrcv`+taxonomy fix, previously validated only in an offline harness, now surfaces this pair for real. (KNOWN_GOOD_PAIRS.md's G16 row is stale as a result — not updated in this pass, out of scope for a docs-only diagnostic branch.) **Residual gaps, all pre-existing and tracked:** G8/G10/G14/G15 are present in `pairs` but unjudged — each passes all three ThematicV2 candidate-SQL gates (axis score > 0.3, both captioned, captureDate gap > 300s) individually verified, but none has a `roleHypothesis`, so each competes in the non-role pool ordered by `compositeScore DESC` against ~119,354 eligible rows with only ~250 judge slots/pass; measured ranks 89,340 / 108,257 / 98,745 / 102,975 — sharpens #93's "smarter candidate ordering" axis into a concrete, confirmed finding: ordering by `compositeScore` is circular for these pairs, since an unjudged pair's composite falls back to the same weak cluster `thematicScore` the ThematicV2 pass exists to correct. G1/G5/G11/G13 never entered the `pairs` table at all — confirmed absent, all previously diagnosed as join/extraction capability gaps (#58 accent-hue extraction, #113's hub-density structural limit, no same-register symbolic-echo join). G9 is pending — #114 (confinement subvert) merged in code (`bc5a292`, 2026-06-27 14:10) after the DB's last index run (2026-06-27 11:26), so the fix hasn't been realized on the live library yet; this is the expected pending-re-extract state documented in #114's own caveat, not a new defect. G12 is a register-mislabeling extraction gap (water gun logged as generic "device"). **Conclusion:** no new backlog item warranted — every gap folds into #58, #93, #111, #113, or #114. Closed 2026-07-07. Verified: live DB query against all 14 KNOWN_GOOD_PAIRS.md Mode-1/dual-mode entries, cap-2 simulation for all 4 judged pairs, individual gate verification for all 4 unjudged-in-table pairs. |
| ThematicV2 non-role pool — stratified ordering fix (#115) | **Scope note:** this closes only the "smarter candidate ordering" axis of backlog #93 (sharpened to a concrete finding by #90); the other four #93 axes (Ollama concurrency, streaming, quantization, KV caching) are untouched and remain open. **Problem, reproduced live before touching code:** the non-role pool in `ThematicV2BackgroundPass.fetchCandidates()` ordered candidates by `compositeScore DESC`, but for an unjudged pair `compositeScore` falls back to the weak cluster `thematicScore` — the exact signal ThematicV2 exists to correct. Re-ran the #90 diagnostic against the same live DB snapshot (`conjunct.db`, 2026-06-27 vintage) by resolving G8/G10/G14/G15's current pair IDs from image-ID pairs (stored pair IDs in KNOWN_GOOD_PAIRS.md were stale — pairs get DELETE+reINSERTed every re-index): ranks reproduced exactly as documented — 89,340 / 108,257 / 98,745 / 102,975 of 119,354 eligible, confirming no drift since #90. **Design space explored:** equal-population rank quartiles (the originally-suggested starting point) were tried first and rejected — even the bottom quartile holds ~30k candidates, still unreachable at ~250 slots/pass in a realistic number of passes. The live `compositeScore` distribution turned out heavily non-uniform: ~62% of the eligible pool clusters in compositeScore [0.40, 0.50) alone, while pairs whose *only* weakness is cluster-Dice thinness (G8/G10/G14/G15's actual failure mode, composite 0.158–0.346) sit in a comparatively sparse [0.15, 0.40) band. Switching to **fixed compositeScore-value bins** (not population-equal) exploits this: bins are narrow where the pool is sparse (where the golden pairs live) and wide where it's dense, so the golden pairs' bins hold thousands, not tens of thousands. **Second finding, non-obvious:** even within a narrow fixed-score bin, secondary-sorting by `compositeScore DESC` still permanently starves whichever candidate sits at the bottom of its own bin — one golden pair measured at rank 10,687 of 10,949 within its bin, i.e. still hundreds of passes away. Fix: within-bucket order is `pairID ASC`, decorrelated from score entirely; since judged pairs drop out of the `thematicV2Score IS NULL` pool permanently, this guarantees every candidate in a bucket eventually gets a turn without needing a new "last considered" tracking column. **Final design (`ThematicV2BackgroundPass.swift`):** 9 fixed compositeScore bands (boundaries `[0.10, 0.15, 0.20, 0.25, 0.30, 0.35, 0.40, 0.50]`, i.e. bins narrower in the sparse 0.15–0.40 range) plus a 10th dedicated tail bucket for `accent_echo` pairs (preserving #100's tail-deprioritization intent under the new scheme, rather than folding them into the score bands). Weights `[1, 3, 25, 48, 10, 40, 25, 57, 36]` + tail weight `5` (sum 250, matching the typical non-role remaining-budget when the role pool is near its 500 cap) are scaled proportionally to whatever `remaining` budget actually is each pass (`nonRoleBucketSlots`), so behavior degrades gracefully as the role backlog drains and `remaining` grows toward 750. Implemented as a single CTE query (`CASE` bucket assignment → `ROW_NUMBER() OVER (PARTITION BY bucket ORDER BY pairID ASC)` → a per-bucket `WHERE` filter built from the computed slot counts) — no schema migration, no new DB columns. **Validated against the live DB:** every one of the 10 buckets returned exactly its allocated slot count on a real query against production data (1/3/25/48/10/40/25/57/36/5 = 250 rows, matching the design exactly). A full multi-pass drain simulation (Python, mirroring the exact SQL logic) found G14 at pass 40, G10 at pass 46, G8 at pass 56, G15 at pass 74 — solidly in a "tens of passes" horizon versus the pre-fix hundreds-of-thousands-of-ranks lockout. The dense 0.40–0.50 bucket kept the single largest weight (57, a clear plurality) throughout tuning, so previously-strong composite-ranked pairs are not starved in the other direction — confirmed by the same live-query bucket-count check. Constants are named and isolated (`nonRoleBucketBounds`, `nonRoleBucketWeights`, `nonRoleAccentEchoTailWeight`) for cheap re-tuning if a future live pass shows distribution drift. Closed 2026-07-07. Verified: live DB reproduction of #90 ranks before the change, live DB bucket-count query confirming exact per-bucket allocation after the change, full ConjunctEngine test suite green (102 tests), multi-pass simulation against the actual eligible pool for reachability and non-starvation. |
| Caption gaze-direction hallucination — impact on ThematicV2 diagnostic close-out (#98) | **Premise:** backlog #98 flagged 159 captions asserting a gaze target (`"looking at X"`) where X is described as being in the same frame — the caption-level cousin of #72/#108's directed-attention-hallucination concern, distinct from #109's geometric gaze pipeline (which never trusts caption text for gaze targets, precisely because #108 found captions unreliable for this — "91% two-dogs internal gaze"). Open question: is this hallucination actually shaping ThematicV2 judge (`qwen2.5:14b-instruct`) output, or a theoretical risk that hasn't materialized? **Derivation note:** no methodology for the original "159" figure was preserved in this log — it existed only as a bare claim in the CLAUDE.md backlog line. Re-derived per available judgment: captions containing `looking at/toward/down at/up at`, `looks at`, `gazing/gazes at`, excluding any caption that also mentions `camera` or `frame` (self-disclosed as camera-directed or explicitly off-frame — not the risky in-frame-target case #98 describes). This phrase list yields 166 images against the live DB (2026-07-07) — close enough to 159 to be confident it's the same population, just a slightly different phrase list; query at `diagnostics/98-gaze-hallucination-query.sql`. **Scale:** 166 flagged images → 162 entered the `pairs` table as a candidate → 131 entered at least one judged pair (`thematicV2Score IS NOT NULL`) → 686 judged pairs touch at least one flagged image. Of those 686 rationales, only 14 (~2%) contain any gaze/look/eye/watch/observe vocabulary at all. **Classification of the 14:** 12 are role-join "real-vs-depicted" or "sign enacts/subverts" pairs (`roleHypothesis` populated, `selectedFor='role'`) — the look/eye/observe word appears because the matched object is literally an eye (a van with cartoonish painted eyes, a t-shirt graphic), or as generic flavor text ("observing," "casual observation"); the connection's actual logic is driven by RoleProfile's `claims`/`enacts`/`subverts` extraction, not by the caption's gaze-target claim — structurally consistent with #112's finding that `directed_at` (the field that would carry a gaze claim) isn't wired into any join. 2 are rejected (`thematicV2Score = 0`, `relationshipType = 'none'`) — judge declined a connection. **0 clear "leans on it" cases** where a positively-judged connection's core logic depends on a caption's gaze-target claim. **Two borderline examples:** pair 607645 (507↔756, ironic, 0.85) references "gaze directed away," but caption 756 actually says "gaze is directed away from the camera" — an honest hedge with no named target, not a fabrication; the connection is really anchored by the literal sign text ("IGNORING IS IGNORANCE") in the other image, gaze commentary is decorative. Pair 607627 (829↔929, ironic, 0.85) claims the woman is "looking at signs," but caption 929 only says she's "looking down at the dog," never mentions the signs — the judge invented this detail itself, on top of an accurate caption; a judge-level fabrication, a cousin of #98's concern but a different failure surface than what #98 was scoped to check (caption hallucination, not judge hallucination) — logged as a one-off, not enough to scope a fix around (see #96). **Conclusion:** at current scale, non-issue for what #98 specifically worried about. Structural reason: RoleProfile extraction doesn't route raw "looking at X" verb phrases into anything the joins consume (joins key off `claims`/`enacts`/`subverts`/`phenomena`; `directed_at` is already known-unreliable per #112 and unused). No captioning prompt change or re-caption warranted. Closed 2026-07-07. Verified: live DB query cross-referencing all 166 re-derived flagged captions against judged `pairs` rows (686 pairs), full-text classification of the 14 rationales containing gaze-adjacent vocabulary, `roleHypothesis`/`selectedFor` lookup confirming the role-join origin of 12 of the 14. |
| ThematicV2 throughput — Ollama concurrency and KV-cache investigation (#93) | **Scope:** narrowly closes two of #93's five remaining axes — `OLLAMA_NUM_PARALLEL` concurrency and whether Ollama already reuses KV cache for the constant system prompt. Streaming JSON parsing and Q4/Q8 quantization stay open (explicitly out of scope this session; quantization needs a quality-regression pass, not just a speed benchmark). **Part A — `OLLAMA_NUM_PARALLEL`:** confirmed the server had no concurrency config set (`ps eww $(pgrep ollama) | grep OLLAMA` showed only `OLLAMA_FLASH_ATTENTION=1`). Benchmarked 10 real `qwen2.5:14b-instruct` judge calls (realistic captions sampled from the live DB, actual `kSystemPrompt`/`kValidatePrompt` text, temperature 0) three ways on the dev machine (M1 Max, 64GB, single GPU): sequential baseline 24.4s total (2.44s/call mean); 2 concurrent client requests against the *default* server config (no `NUM_PARALLEL` set) — 24.1s total, i.e. the server silently serialized them (per-call latency roughly doubled to ~4.6s, but throughput unchanged, confirming Ollama's default single-slot behavior queues concurrent requests rather than batching them); 2 concurrent requests after setting `OLLAMA_NUM_PARALLEL=2` (Homebrew plist edit + service restart, config backed up and reverted after the test) and genuinely enabling parallel decode — 25.7s total, **flat-to-slightly-worse than sequential**, not better. Output quality was checked (temperature 0, so a real regression would show as differing JSON): zero mismatches in `connected`/`relationship_type`/`confidence` across all 10 pairs between the sequential and concurrent-`NUM_PARALLEL=2` runs — no quality degradation, but also no speed gain to trade it for. **Conclusion: do not adopt `OLLAMA_NUM_PARALLEL`.** This empirically confirms #101's original caution ("one VLM can saturate the GPU") rather than overturning it — on this single-GPU Apple Silicon hardware the judge model is compute-bound, not queue-bound, so splitting requests across parallel slots doesn't reduce wall-clock time; it can even add scheduling overhead. `OLLAMA_NUM_PARALLEL` was not left enabled — the Homebrew plist was restored to its pre-benchmark state (`OLLAMA_FLASH_ATTENTION=1` only) and the service restarted. **Part B — KV-cache reuse for the constant system prompt:** researched Ollama's release notes (found "Improved prompt caching by decoupling it from context shift for better KV cache reuse," v0.30.8, and related llama.cpp-backend prompt-cache-hit accounting improvements in v0.30.2) confirming automatic prefix/prompt caching is a long-standing, on-by-default llama.cpp-server feature — not something requiring a flag. Verified empirically on the installed version (0.20.4): 6 calls with the real, unchanged system prompt (matching current `ThematicScorerV2` behavior — one `score()`-shaped call per pair, same `kSystemPrompt` every time) averaged 3.09s/call (2.4–2.7s once warm); 6 calls with the system prompt mutated by a random marker sentence each time (defeating any prefix match) averaged 4.77s/call — a clear, repeatable ~2× latency difference attributable to KV-cache reuse of the ~400-token constant system prompt. **Conclusion: Ollama already does this automatically; nothing to build.** Also checked whether the current code's call pattern is structured to actually benefit from this in production: `ThematicV2BackgroundPass.run()`'s candidate array is built role-candidates-first (all `validate()`/`kValidatePrompt` calls), then non-role-candidates (all `score()`/`kSystemPrompt` calls), and the `for candidate in candidates` loop iterates that array in order — so within each pass, the system prompt only changes **once**, at the role→non-role boundary, not per pair. The existing sequential loop is already cache-friendly by construction; no reordering needed. **Interaction with Part A:** since `NUM_PARALLEL` is not adopted, this doesn't need to be reconciled against multi-slot cache fragmentation — moot for now, but worth remembering if concurrency is revisited later (splitting requests across slots would likely fragment this cache benefit, an additional cost `NUM_PARALLEL` didn't need since it showed no throughput win anyway). No code changes required for either part — this was a measurement-and-config investigation. Closed 2026-07-07. Verified: three-way wall-clock benchmark (sequential / fake-concurrent-default-config / real-concurrent-`NUM_PARALLEL=2`) against real production captions and the actual system prompts, output-equivalence check at temperature 0, Ollama release-note research, same-vs-varying-system-prompt latency comparison, and direct code read of `ThematicV2BackgroundPass.fetchCandidates()`/`run()` confirming existing block ordering. See CLAUDE.md backlog #93 (two axes closed, two remain: streaming, quantization). |
| Join-2 synonym bridge — claim↔embodiment matching (#116) | **Problem:** join 2 (claim↔enact/subvert) matches via `rawMatch`, raw token-set overlap. #111 fixed the *content* side for two golden pairs (G14's `587` now `enacts:["hold hands","tenderness"]`; G8's `598` `subverts:["see","speak"]`), but the connections still didn't fire: `miss` (G14's claim, `645`) shares no token with `tenderness`/`hold hands`; `smile` (G8's claim, `259`) shares no token with `speak`/`see`. KNOWN_GOOD_PAIRS.md flagged this in advance: "a small curated synonym/concept bridge in join 2 would catch these — but it risks over-firing." **Fix:** added `RoleJoins.claimEmbodimentBridge: [String: Set<String>]`, a small curated table (currently one entry: `"miss" → ["tenderness"]`), checked via `bridgeMatch` alongside `rawMatch` in both join-2 directions (`rawMatch(ca,cb) || bridgeMatch(ca,cb)`). One-directional (claim key → embodiment concept only) and matched on the WHOLE claim/embodiment string (exact, case-insensitive) rather than token overlap. **Why exact-string, not tokens:** the first implementation tokenized both sides (mirroring `rawMatch`); harness validation caught a genuine false positive before this landed — image `958`'s claim is literally `"Miss Rodeo"` (a pageant sash), and token-level matching let the shared `"miss"` token spuriously bridge it to an unrelated `"tenderness"` embodiment in two other images (`174`, `587`) — pairs with zero real thematic connection. Switching to whole-string exact match eliminated both false fires while leaving the genuine G14 match (`claims:["miss"]` exactly) intact. **Why `smile↔speak` was tested and excluded:** added temporarily to see if it would surface G8. It does raw-fire (`259↔598`), confirming the semantic link is real, but never survives the cap-8 admission race — `598` still loses to other "smile" matches under the greedy-by-specificity ranking, exactly reproducing the prior finding in KNOWN_GOOD_PAIRS.md's join-side notes ("598 is id-ranked out of the cap-8"). It also added 145 extra raw join-2 candidates corpus-wide for zero net admission gain — a pure flood with no payoff, so it was removed. G8 remains unreached, consistent with #113's "structurally hub-limited popular concept" diagnosis — this is not a #116 regression, it's confirmation of a known limit. **Validation method:** offline harness (XCTest, deleted before merge) replicating `IndexingEngine.generateRoleCandidates`'s exact join/sort/cap logic against a live-DB profile dump (973 hero profiles, read-only, no production mutation). Ten golden/anchor images (`259,587,598,645,245,246,874,80,50,572`) were freshly re-extracted via direct Ollama calls with the current `OllamaRoleExtractionEngine` prompt, since their stored `roleProfile` predated #111 and didn't reflect current extraction behavior (`587`'s stored profile lacked `"tenderness"`; fresh extraction reproduced it, confirming #111's diagnostic was correct about current-prompt behavior, just not yet realized in the DB — tracked separately as #111/#114's "REALIZATION DEBT"). **Results:** content-patch-only baseline (10 fresh profiles, no bridge): 1221 admitted. With the final bridge (`miss`→`tenderness` only): 1223 admitted (+2: `587↔645` = G14 itself, `174↔645` = a second plausible "miss"-claim-to-genuine-tenderness-embodiment connection, not a false positive — image `174` depicts an actual tender embrace). **G14 now fires and is admitted.** **Anchors G4 (`245↔246`), G6 (`874↔80`), G7 (`259↔80`), G16 (`50↔572`) all remain admitted, undisturbed** — the addition is a new, disjoint cap-competitor only for the rare `miss` claim, not a change to any existing anchor's matching. 2 new unit tests added to `RoleJoinsTests.swift` (`testBridgeConnectsMissClaimToTendernessEmbodiment`, `testBridgeDoesNotFireOnSubstringWithinLongerClaim`); all 104 ConjunctEngine tests green. **Scope discipline:** the bridge table is small, named, and documented in-place with an explicit warning against widening it without re-running the eviction/flood check — not a general synonym/thesaurus system. G8 stays open, tracked under #113 (popular-concept recall); no cap/budget redesign attempted here, per scope. Closed 2026-07-07. Verified: offline harness (real `RoleJoins.join`/`generateRoleCandidates` cap logic) against live production profiles with targeted fresh re-extraction for validation accuracy; positive (G14) and negative (anchors + false-positive scan) checks both passed; full test suite green. |
| Double CLIP build on launch — diagnostic close-out and fix (#8) | **Task:** re-investigate backlog #8, originally filed as "race in `engineBuildTask?.cancel()` on some launches. No crashes recently — monitor" — an old, never-confirmed guess. Fresh console output showing `CLIP: loaded from app bundle (mlmodelc)` printed twice on one launch was the first concrete evidence in a while, so this session was diagnostic-first: find the actual mechanism before touching any cancellation logic. **Method:** grepped the one call site for the log line (`EngineController.buildCLIPEngine()`) and its only caller, `startEngineBuild(db:)`, which itself has exactly one call site, `EngineController.initialize()`. `initialize()` turned out to have *two* call sites: `CoupletApp.swift`'s launch `.task` (`engine.initialize()`, added by #106 on 2026-06-18 so the engine is ready while the setup flow runs) and `ContentView.swift`'s `.onAppear` (`engine.initialize()`, present unchanged since the very first commit, 2026-05-01 — predates #106's `AppPhase` gate entirely). Built the app with `xcodebuild` and ran the binary directly from Terminal (bypassing Xcode, ruling out a stale-debug-instance artifact) three times on a machine with Ollama/models already installed (the common dev-machine case, where `phase` goes `.checking → .ready` immediately): all three launches logged `CLIP: loaded` twice, plus a `CAPTION`/`ROLE` "not available" → "available" flicker (the two `startEngineBuild` calls each independently racing Ollama's `/api/tags` warm-up — explicitly noted as out-of-scope for #8 itself, but explained here since it's a direct symptom of the same root cause). Added temporary `ObjectIdentifier`-tagged diagnostic prints to both call sites and to `initialize()`'s entry; confirmed the SwiftUI `.task` itself fires exactly once (`windows=1`), but `EngineController.initialize()` is entered twice on the *same* engine instance — ruling out multi-window state restoration (also structurally implausible here since `CommandGroup(replacing: .newItem) {}` has removed the "New Window" menu item since the initial commit) and ruling out a genuine SwiftUI `.task` double-fire. **Diagnosis: two independent, always-executing call sites, not a race.** `engineBuildTask?.cancel()` was in fact working as designed — it correctly gates the final `self.indexingEngine = IndexingEngine(...)` assignment behind `guard !Task.isCancelled`, so only the second (uncancelled) call's engine ever gets wired up; there was no correctness bug, no crash risk. The cost was pure waste: a duplicate CLIP model load, duplicate Ollama availability probes for captioning/role extraction, and a duplicate `DatabaseManager`/GRDB `DatabasePool` construction (the first is deallocated moments later when `db` is reassigned) — on every single launch where the machine is already set up, not intermittently as the original "on some launches" phrasing suggested. **Fix:** removed the redundant `engine.initialize()` line from `ContentView.onAppear` (kept `libraryVM.loadCollections(engine:)`, which is unrelated), leaving `CoupletApp`'s launch `.task` as the sole call site — it already runs unconditionally before `ContentView` is ever shown, in both the healthy-machine and post-setup-flow paths. Reverted all temporary diagnostics before committing. Closed 2026-07-07. Verified: rebuilt and ran the binary directly three more times post-fix — each launch logged exactly one `CLIP: loaded` line and no CAPTION/ROLE flicker; all 104 ConjunctEngine tests green (`swift test --package-path ConjunctEngine`). See #106. |
| Grid page size / display limit — investigated, found moot, dead code removed (#110) | **Task:** backlog #110 asked to raise `pageSize = 150` and anchor `displayLimit = 750`, on the working assumption that `LazyVGrid` renders lazily so the cost is "mainly thumbnail memory" — cheap to raise. Instructed to verify that assumption before leaning on it, not just bump the constants. **What verification found: the premise doesn't hold, for a more fundamental reason than the lazy-rendering question.** Traced every call site of both constants. `pageSize = 150` (`PairsGridViewModel.swift`, `EngineController.fetchRepresentativePairs`) only governs `loadMorePairs`'s page-N slicing — but the main grid's actual load path, `streamPage0Pairs` → `QueryService.streamRepresentativePairs`, has **no SQL LIMIT** (confirmed in the function's own doc comment and by reading the SQL) and streams the *entire* `pairs` table into `allPairs` on every load, before `loadMorePairs` ever runs. `displayLimit = 750` / `dbLimit = 2000` (`EngineController.fetchPairs`) turned out to be **dead code**: a full-repo grep of call sites found `fetchPairs` is only ever invoked with `anchorImageID` set (`ContentView.swift`, the lightbox "other pairs for this image" strip), so the `if anchorImageID == nil` branch containing those two constants and the per-image cap-15 never executes in the running app — a leftover from the original "2000 DB fetch → cap → 750 display" decision, superseded when the #41 streaming/cap-2 rewrite gave the main grid its own unbounded path and never reached by any caller since. **Measurement (read-only harness — see below — against a copy of the production DB, 1,028 active images, 121,589 pair rows, no folder filter):** `streamRepresentativePairs` reads and processes all 121,589 rows every single grid load (first chunk at 0.59s, full drain 1.7s) to produce a **cap-2 result of 490 displayed pairs** — already below even the old dead 750 ceiling, confirming there was nothing to raise: the grid already shows everything cap-2 (explicitly out of scope for #110) allows. **Lazy-rendering assumption itself independently confirmed true:** `LazyVGrid`/`ScrollView` only materializes visible `PairTileView`s; each thumbnail loads via `ThumbnailView`'s `.task(id: url)`, gated by a 150-image/150MB `NSCache` (`ThumbnailCache`) and a 4-concurrent `LoadThrottle` — so cell/image cost genuinely doesn't scale with total pair count. It just isn't the relevant constraint here, since nothing gates total pair count in the first place. **Method — read-only measurement harness:** rather than drive the full macOS UI, built a throwaway SPM executable (`gridbench`, not committed) depending on `ConjunctEngine` by path, pointed at a **copy** of the production DB (never the live file — app was confirmed not running, but the copy was used regardless), replicating `PairSortOrder.axis`'s SQL expression and the exact `applyCap2Free` two-pass algorithm from `PairHelpers.swift`, timing `fetchRepresentativePairs`/`streamRepresentativePairs` directly. **Resolution (three actions, per explicit sign-off after presenting these findings):** (1) closed #110 as moot — no pageSize/displayLimit change, since raising either changes nothing observable; (2) removed the dead `anchorImageID == nil` branch from `EngineController.fetchPairs` (cap-15 + displayLimit trim + the 2000/750 ternaries) — the function now always takes the one path that's actually live (`dbLimit = 500`, no further cap/trim), with its doc comment updated to say so explicitly; (3) opened a new backlog item, #117, for the actual cost driver this investigation surfaced — every grid load unconditionally scans the full `pairs` table regardless of display count, a cost that scales with total scored pairs (not shown pairs) and will grow as the library grows. **Regression checks:** cap-2 algorithm untouched (not read from or written to during this session); anchor-strip `dbLimit = 500` behavior unchanged (same value, same query, dead branch removed only); full `swift test` suite green (104/104); `xcodebuild` release-config build succeeded. Closed 2026-07-07. Verified: live measurement against a production-DB copy (490 pairs, 121,589-row scan confirmed), full-repo call-site grep confirming `fetchPairs`'s non-anchor branch is unreachable, 104/104 ConjunctEngine tests green, Xcode build green. See CLAUDE.md backlog #117 (new). |
| Grid load — expression indexes replace full-table temp-b-tree sort (#117) | **Task:** #117 (opened by #110) asked whether the main grid's unbounded `streamRepresentativePairs` scan could be bounded with either (a) an early-exit once cap-2's greedy pass has covered every active image, or (b) a `PARTITION BY imageID` window-function LIMIT ahead of the Swift-side pass — with an explicit instruction to confirm the SQL ordering actually guarantees "best pairs first" per image before building either, since a false guarantee would silently serve worse pairs. **Diagnostic finding — the real mechanism was different from what the backlog assumed:** `EXPLAIN QUERY PLAN` on the exact query (folderID/collectionID both nil, the default "All Photos" view) showed `USE TEMP B-TREE FOR ORDER BY` for **every one of the five `PairSortOrder.dbColumn` expressions**, including `.axis` (the default "Best" sort) — none of them is a bare indexed column: `.axis`/`.composite` are `MAX(...)` formulas, `.thematic` is a `COALESCE`, and `.geometric`/`.aesthetic` are plain columns that were simply never individually indexed (only `compositeScore` was, via the original `idx_pairs_score` — but nothing sorts by raw `compositeScore` anymore since ThematicV2/axis scoring was added). A temp b-tree sort must materialize and sort the *entire* matching row set before SQLite can emit row 1 — so the doc comments claiming "SQLite walks idx_pairs_score in descending order... first chunk arrives within milliseconds" were stale/inaccurate for the default view; the measured 0.59s-to-first-paint from #110 was already paying for a full 121,589-row sort, not incremental streaming. **Both candidate fixes evaluated and rejected before building anything:** (a) *early-exit on cap-2 coverage* is only sound if pass-1 (the "both endpoints unseen" greedy accept) reaches full coverage of every pairable image on its own — measured against the production DB (1,028 images, 121,589 pairs), it doesn't: 966 of 973 pairable images win a pass-1 slot, the remaining 7 are only ever covered by pass-2, whose candidates are drawn from the *entire* rejected set in original score order, so proving "no more pass-2 candidates remain" requires having scanned to the point where each straggler's best-available row was seen — not knowable in advance without the equivalent of a full scan. The "safe" stopping condition (full pass-1 coverage) never fires on real data, so this fix would add complexity for zero measured benefit. (b) *`PARTITION BY imageID` window-function LIMIT* is provably unsound in general: cap-2 (`applyCap2Free`/`applyPass2Free`, `PairHelpers.swift`) is a **global greedy matching** over the whole score-sorted set, not a per-image top-K selection — whether a given pair is available to image X depends on whether some *other* image's higher-scoring pair already consumed X's partner, which is not visible from X's own partition. Truncating each image's candidate list to a fixed K could silently drop the row pass-2 needs for a straggler, changing which pairs win with no error surfaced. Rejected as unsafe, exactly the risk flagged going in. **Fix actually shipped — a third option neither candidate named: make the existing ORDER BY expressions indexed**, so SQLite streams in true index order instead of materializing a temp b-tree. SQLite supports indexes on arbitrary deterministic expressions, auto-maintained on every write with no application-level sync code (unlike a persisted/denormalized score column). Migration `v21_pairSortIndexes` (`DatabaseManager.swift`) adds one index per `PairSortOrder.dbColumn` case — `idx_pairs_axis`, `idx_pairs_compositeEffective`, `idx_pairs_thematicEffective`, `idx_pairs_geometricScore`, `idx_pairs_aestheticScore` — using the exact expression text from `AppModels.swift`. Confirmed via `EXPLAIN QUERY PLAN` that all five now produce `SCAN p USING INDEX ...` with no temp b-tree. Folder-scoped and collection-scoped queries were checked too: the planner correctly still prefers `idx_images_folder_date`/`idx_pairs_imageAID_score` for folder-narrowed queries (small result set, temp b-tree over a subset is already fast) and the new axis index for collection-scoped queries — no manual override needed. **Correctness validation:** three-way comparison (Python/raw-SQL for query-plan-level checks; a throwaway SPM executable, `bench117`, depending on `ConjunctEngine` by path per the established headless-driver pattern, for GRDB-level checks) between a `git worktree` build of `main` (pre-fix, migrations only through v20) and this branch (post-fix, auto-migrates to v21 on open — the real upgrade path a user hits on next launch) against fresh copies of the same production DB. Row-level order differs at 128 of 121,589 positions (0.1%), but every one of those is an *exact floating-point score tie* between two different pairs — SQLite doesn't guarantee stable tie order between a temp-b-tree plan and an index-scan plan, and this class of nondeterminism already existed pre-fix (same family as the documented Vision/ANE float nondeterminism in the "Parallel pair scoring" gotcha). **The final cap-2-selected display set is byte-identical pre- vs. post-fix**: same 490 pairs, same pass1(483)/pass2(7) split, same pair-ID order, confirmed via direct diff of the two runs' output. Anchor-strip (`EngineController.fetchPairs`, `dbLimit = 500`, always called with `anchorImageID` set) was separately confirmed unaffected — its `WHERE (p.imageAID = ? OR p.imageBID = ?)` filter is narrow enough that the planner already used per-image indices and ran in ~30ms both before and after, and its hardcoded axis-like ORDER BY expression (a distinct formula with a `roleHypothesis`/`thematicV2Score=0` fallback CASE, per the existing "two implementations must stay in sync" gotcha) was left untouched — same 500-pair result set pre/post. **Timing (production DB, 1,028 images, 121,589 pairs, default axis sort, folderID/collectionID nil), measured via `bench117` (mean of 2 runs, warm cache):** first-row latency 606–955ms → 0.6–1.8ms (~500–1000×); full drain 1,746–2,099ms → 1,318–1,455ms (~25–35%). **Synthetic 3× scale (3,084 images, 364,767 pairs, built by duplicating folders/images/pairs with an ID offset):** first-row latency 1,651–1,655ms (pre) → 0.6–5.3ms (post) — stays flat at the larger scale instead of growing with it, confirming time-to-first-paint is now decoupled from library size, which was the actual "gets slower as libraries grow" complaint in backlog #117; full drain 5,355–5,389ms → 4,344–4,479ms (still O(N), since drain time is dominated by per-row Swift-side conversion work in `streamPage0Pairs`, an orthogonal cost this fix doesn't touch). Pair-set identity re-confirmed at 3× scale (1,470 pairs both sides, same set) — row-order differences there are entirely an artifact of the synthetic duplication method creating literal triplicate score ties, not a real divergence. **Also fixed in passing:** the stale doc comments in `QueryService.swift` (`fetchRepresentativePairs`'s doc predated an `all_sides`/window-function/DISTINCT design that no longer exists in the current flat-join SQL) and `EngineController.swift`/`QueryService.swift` (both claimed incremental `idx_pairs_score` streaming that wasn't actually happening for any current sort order) — corrected to describe the actual mechanism and point at `v21_pairSortIndexes` so a future sort-order addition without a matching index doesn't silently regress. **Scope discipline:** cap-2 selection logic itself (`applyCap2Free`/`applyPass2Free`) was not touched; the anchor-strip `dbLimit = 500` path was verified, not modified; no broader query/schema restructuring beyond the five additive indexes. Full drain still scales ~linearly with total scored pairs (inherent to cap-2 needing to see the whole sorted candidate set in the general case, per the correctness analysis above) — not fully solved, but the dominant, worse-than-linear cost (materializing and sorting the whole table before any row could stream) is gone, and time-to-first-paint no longer depends on library size at all. Closed 2026-07-07. Verified: 104/104 ConjunctEngine tests green, `xcodebuild` Debug build green, byte-identical 490-pair cap-2 output confirmed via `diff` at both 1× and 3× scale (pre-fix `main` worktree vs. post-fix branch, both against copies of the production DB), anchor-strip output confirmed unchanged. See backlog #117 in CLAUDE.md (closed). |
| Lightbox thematic rationale — flickering cluster label fix (#118) | **Bug report:** in the fullscreen lightbox, the pair description text's cluster-label portion (e.g. "one image brings joy_celebration") appeared to swap between different valid values purely from cursor movement, no click or selection change. **Working hypothesis handed in:** hover-driven chrome show/hide forces the view body (and the rationale computation) to re-evaluate on every mouse move, and the cluster tie-break — `onlyA.first`/`onlyB.first` on a `Set<String>` in `DisplayPair.thematicRationale` (`AppModels.swift`) — isn't deterministic, so repeated re-evaluation could resolve the tie differently each time. Instructed to verify rather than assume. **Verification, in order:** (1) confirmed `LightboxView.swift`'s `.onContinuousHover` calls `vm.showControls()` on every hover event, which sets `@Published var controlsVisible`, forcing the whole `LightboxView` body — including `pair.thematicRationale` inline in `actionBar` — to re-evaluate; this part of the hypothesis held. (2) Built a throwaway XCTest calling the real `ConceptClusters.matchedClusters` + the `onlyA.first`/`onlyB.first` tie-break 2000 times in a single test process against fixed captions: **output was 100% stable within one process run** — disproving the specific claim that recomputation *within a session* changes the result (Swift's `Set` hash seed is randomized once per process launch and stays fixed for the process's lifetime, confirmed via Swift's own hashing design). (3) Re-ran the identical test as 5 separate `swift test` invocations (5 separate process launches, same captions): **got 5 different picks** (`community_gathering`, `joy_celebration`, `sensory_overwhelm`, `urban_street`, ...) for the exact same input — confirming the real mechanism is cross-launch nondeterminism, not intra-session recompute-driven flicker. The reported "seconds apart, different label" screenshots are explained by an app relaunch between them (routine during active dev/test iteration); the underlying defect — the description text is arbitrary per launch rather than a fixed function of the pair's content — is real and was the actual bug regardless of the exact trigger. Also confirmed a second, similar-looking spot (`LightboxInfoRail.thematicDetail`) was *not* affected: it renders all clusters via `.sorted()` in a `FlowLayout`, never picks a single one via `.first`. **Fix:** (1) added `ConceptClusters.representativeCluster(in:)` to `ConjunctEngine` — sorts candidates by weight descending, then name ascending, and takes the first; a content-based deterministic tie-break that can never vary by hash seed or launch. Exposed to the app via the existing `ConjunctConceptClusters` wrapper (`ConceptClustersWrapper.swift`). (2) Changed `DisplayPair.thematicRationale` from a computed property (recomputed on every access) to a stored `let`, built once via a new private static `buildThematicRationale(...)` called from `init` — so the hover-driven re-render no longer re-executes the caption-parsing/cluster-matching work at all, matching the "compute once when the pair is loaded" instruction; no `DisplayPair(...)` call site needed changes since all required inputs (`captionA`, `captionB`, `modality`, `rationale`) were already constructor parameters. Scoring/weighting logic (`PairScorer.swift`, `ConceptClusters` tier weights, axis bonuses) was not touched, per explicit instruction — the analogous `.first` usages there (`meaningfulShared.first`, decision #48) build the *stored*, once-per-index `rationale` field and are out of this bug's scope (display/caching, not scoring). **Regression test:** added `testRepresentativeClusterPicksHighestWeightThenAlphabetical` and `testRepresentativeClusterIsStableAcrossRepeatedCalls` to `ConceptClustersTests` (`ConjunctEngineTests.swift`); also re-ran the cross-launch check against the new `representativeCluster` function (5 separate `swift test` launches) and got the **same pick every time**, confirming the fix. **Verification:** 106/106 ConjunctEngine tests green (104 pre-existing + 2 new); `xcodebuild -scheme Couplet` Debug build green; app launched and ran successfully standalone (process confirmed alive via `ps`) — full manual mouse-hover GUI verification wasn't possible in this environment (no screen-capture/accessibility access to the sandboxed session), so the fix is verified via the automated cross-launch determinism proof above rather than a live visual check. Closed 2026-07-07. Verified: 106/106 tests green, Xcode build green, cross-launch determinism directly demonstrated (before: 5/5 launches differed; after: 5/5 launches matched) against real caption-derived cluster data. |
| ThematicV2-vs-cluster diagnostic — passes 1–4 + implementation (#96, CLOSED 2026-07-08) | **Task:** backlog #96 asks whether ThematicV2's relational judgments (`complementary`/`contrastive`/`echo`/`ironic`/`tonal`/`none`) can point at where the 29-cluster vocabulary needs work — this is pass 1 of 4 (over-broad vocabulary, recurring relational phrases, this distribution pass, judge-fabrication recheck), read-only, no scoring/cluster code touched. **Method:** reused `ConceptClusters.matchedClusters`/`.weights`/`.axisPairs` directly (no reimplementation) via a throwaway SPM executable depending on `ConjunctEngine` by local path — same headless-driver pattern as #117's `bench117` — against a JSON export (`diagnostics/96-cluster-relationship-distribution-query.sql`) of every judged pair (`thematicV2Score IS NOT NULL`) from a COPY of the production DB (2026-07-07 snapshot, 1,028 images / 121,589 pairs; harness preserved at `diagnostics/96-cluster-relationship-distribution-harness.swift`, not wired into the real build). Per pair: recomputed `clustersA`/`clustersB`, `meaningfulShared` (weight ≥ 0.75, the #49 gate), `meaningfulOnlyA`/`B`, and which `axisPairs` (#48) structurally fire (counted independent of production's `clusterScore ≤ 0.10` gate, since this pass asks about vocabulary/judge correlation, not exact score-gate replication) — a cluster counts as "fired meaningfully" for a pair if it's in `meaningfulShared` or is one of the two clusters in a fired axis pair. Segmented **non-role** (`roleHypothesis IS NULL`, cluster/composite/aesthetic/geometric-selected — the pool the cluster vocabulary itself is responsible for) from **role-hypothesis** (`roleHypothesis IS NOT NULL`, join-selected and judged via `validate()`) pairs per the task brief, since role candidates aren't selected by cluster overlap and would muddy the per-cluster read. **Sanity checks:** harness processed all 2,986 judged pairs with 0 dropped (matches `SELECT COUNT(*) WHERE thematicV2Score IS NOT NULL` exactly); G4/G6/G7/G16 from KNOWN_GOOD_PAIRS.md all recomputed under their documented relationshipType and score (contrastive 0.95, ironic 0.95 ×2, complementary 0.95 respectively) with plausible firing clusters (`devotion_belief`/`tenderness_care`/`bodily_gesture`/`tension_conflict` families); the #48-era musician(id 2045)/ears(id 1523) pair no longer resolves — those image IDs don't exist in the current (rebuilt) DB, consistent with #81's note that old preserve-list IDs are stale — searching by content instead confirms this pair is correctly **absent** from the judged set entirely, consistent with #46/#95/#113's finding that G5 never enters the `pairs` table at all (join-1 cap density on a popular phenomenon). **Headline finding — `echo` is extinct:** 0 of 2,986 judged pairs, role or non-role, are typed `echo` (direct SQL tally: none=2,281, contrastive=470, ironic=163, complementary=70, tonal=2, echo=**0**). This overturns the premise #96 inherited from #91's 2026-06-12 finding (`FINDINGS-cluster-vs-thematicv2.md`: 495/498 pairs, 99.4%, typed `echo`) — that finding was measured against the 3B-era judge, before #102 switched ThematicV2 to `qwen2.5:14b-instruct`; the catch-all-echo failure mode simply doesn't reproduce with the current judge. No cluster anywhere can trip an "echo-heavy" flag because the category the flag was built to catch no longer occurs — this should be dropped from the mental model for passes 2–4. **The real catch-all now is `none`:** 76.4% overall, but sharply bimodal by candidate origin — **97.3%** (1,749/1,797) among non-role pairs vs. **44.7%** (532/1,189, i.e. 55.3% non-none) among role-hypothesis pairs. This ~30-point-non-none gap is itself notable: the role pipeline's targeted join-based candidate generation continues to vastly outdo the general cluster-Dice-selected pool at producing judge-confirmed connections, consistent with #102's original design rationale — read the role-hypothesis breakdown below as context only, per the task brief, not as a verdict on cluster vocabulary. **Non-role per-cluster table (the primary deliverable; n = pairs where the cluster fired meaningfully; % of non-`none`/non-`echo` = complementary+contrastive+ironic+tonal, i.e. non-none since echo is always 0 here):** tension_conflict n=158, 16.4% non-none (most productive); devotion_belief n=374, 8.8%; skilled_performance n=486, 7.8%; power_dominance n=69, 5.8%; vulnerability_exposure n=96, 4.2%; joy_celebration n=129, 3.1%; tenderness_care n=1,133, 2.9%; movement_energy n=138, 2.8%; bodily_gesture n=1,617, 2.6%; looking_watching n=674, 2.2%; waiting_anticipation n=1,254, 1.4%; confinement_freedom n=731 and isolation_solitude n=166, 1.2% each; labor_effort n=455 and stillness_rest n=1,071, ~1.0% each; sensory_overwhelm n=124, 0.8%; youth_age n=466, 0.6%. **Flagged NEVER-RELATIONAL (n ≥ 15, zero complementary/contrastive/ironic/tonal — 100% `none` whenever meaningfully fired):** `sound_music` (n=59), `ritual_ceremony` (n=61), `transformation_change` (n=26). `sound_music` is a repeat offender: #48 already flagged it as catch-all-prone via the axis-bonus mechanism (musician+ears-woman both firing `sound_music`+`sensory_overwhelm` inflates Dice without genuine resonance); this pass finds the same cluster unproductive through the entirely separate Dice-shared-cluster mechanism too — two independent findings now converge on it. `grief_sorrow` (n=7) is also 100% `none` but too sparse to trust on its own (see below). **Flagged insufficient data (n < 15 — bimodal distribution, clusters sit either at 0–9 or 26–1,617 with nothing near the boundary, so 15 cleanly separates "well-sampled" from "structurally rare" and doesn't need tuning):** 8 of 29 clusters are n=0 in the non-role pool. Four are gate-structural, not a coverage gap: `urban_street`/`nature_landscape`/`community_gathering`/`animal_presence` are all ambient tier (weight 0.2), which by design can never clear the weight ≥ 0.75 meaningful-tier gate (#49) and aren't in any `axisPairs` entry — n=0 is exactly the intended behavior. The other four are genuinely sparse: `uncanny_ordinary`, `economic_precarity`, `solitude_in_crowd`, `domestic_intimacy` — the four two-signal "photographer-influence" clusters from #15, all weight 1.0 — have not meaningfully co-fired even once across ~1,800 non-role judged pairs; either genuinely rare in this library or the two-signal gating is too strict to ever clear in the judged sample so far. `humor_absurdity` (n=1) and `grief_sorrow` (n=7) round out the insufficient-data set. All should be revisited once judged-pool coverage grows — #93 puts full coverage at weeks-to-months out, so this isn't a quick recheck. **Role-hypothesis segment (context only, per task brief):** much higher non-none rates throughout (`sound_music` 61.2% non-none n=49; `sensory_overwhelm` 60.0% n=75; `devotion_belief` 58.6% n=558; most others 50–65%) — expected, since these pairs were pre-validated by the join system, not selected on cluster overlap; the contrast with the same clusters' near-zero non-role rates (e.g. `sound_music` 0% non-role vs. 61.2% role) illustrates the #102 role-pipeline win rather than saying anything about the clusters' own vocabulary quality. **Byproduct (lead for pass 2, not this pass's deliverable):** tabulated cross-side (`meaningfulOnlyA` × `meaningfulOnlyB`) cluster co-occurrence across non-role pairs, looking for a pair of clusters that co-occurs often under a consistent non-`none` relationshipType — a candidate new `axisPairs` entry. Result: **none found** — the 40 most frequent cross-cluster combinations (n=50–114 each) are all ≥93% `none`, including the 3 that are already-known `axisPairs` entries evaluated under this raw, ungated lens (`labor_effort`↔`stillness_rest` n=113, 100% none; `looking_watching`↔`skilled_performance` n=86, 98.8% none; `isolation_solitude`↔`tenderness_care` n=50, 100% none). Raw co-occurrence frequency alone does not appear to surface good axis-pair candidates this way — a caveat for pass 2 (whoever picked the existing 9 `axisPairs` entries in #48 was reasoning from semantic complementarity, not frequency) rather than a conclusion, since production's axis bonus only fires under the narrower `clusterScore ≤ 0.10` gate, not this pass's broader `onlyA`×`onlyB` proxy. **What warrants attention in passes 2–4:** `sound_music`, `ritual_ceremony`, `transformation_change` for over-broad-or-misdirected vocabulary (pass 2); the four rare #15 clusters for coverage, not quality (re-run this diagnostic once the judged pool grows); the null cross-co-occurrence result as a starting caveat for pass 2's axis-pair hunting. **Scope discipline:** no cluster weights, keyword lists, `axisPairs` entries, or scoring gates were changed. #96 is NOT closed — 3 passes remain. Diagnostic artifacts preserved at `diagnostics/96-cluster-relationship-distribution-query.sql` and `-harness.swift` for reproducibility (not committed to any build target). Dated 2026-07-07.<br><br>**━━ PART A ADDENDUM to pass 1 (image-level corpus firing, 2026-07-07, same DB snapshot) ━━** Pass 1 flagged the four #15 two-signal clusters (`uncanny_ordinary`/`economic_precarity`/`solitude_in_crowd`/`domestic_intimacy`) as n=0 in the judged pool and asked whether that's rare content or a too-strict gate. **Answer: it's the gate/content, not pool coverage** — reusing the same `matchedClusters` logic against ALL 1,028 active-captioned corpus images (not just judged-pair images), these clusters fire on **0, 0, 2, and 0 images respectively** across the entire library. For scale, in the same run `sound_music` fires on 100 images (9.7%), `ritual_ceremony` 142 (13.8%), `transformation_change` 96 (9.3%), `tenderness_care` 662 (64.4%), `bodily_gesture` 965 (93.9%). So these four clusters essentially never fire on this library's content at all — the two-signal `requiredGroups` gate from #15 (an eerie/dreamlike + concealment word co-present; a systemic-framing + material-evidence word co-present; a crowd + disconnection word co-present; an enclosed-space + domestic-register word co-present) is stricter than qwen's caption vocabulary ever satisfies here. This is a **design/gate finding, not a coverage gap** — pass 1's "revisit as coverage grows" read was too optimistic for these four; more judged pairs will never surface them because the images themselves don't match. Flagged as such; per task instruction NO gate fix attempted this session (the two-signal gates were deliberate anti-false-positive design in #15/#25 — loosening them is its own scoped decision, not a diagnostic byproduct). **━━ PASS 2 — rationale mining (2026-07-07) ━━** **Method note:** to attribute *which keyword* triggers a cluster (the engine's `tokenize`/`stem` are `internal`, not importable), the harness carries a COPY of that tokenizer, self-validated to reproduce the real `ConceptClusters.matchedClusters` on all 1,028 captions with **0 mismatches** — the finding's matching still uses the real function; the copy is only for keyword attribution. **BUCKET 1 (over-broad vocabulary — scoped by pass 1 to the 3 NEVER-RELATIONAL clusters):** read all non-role `none` pairs where `sound_music`/`ritual_ceremony`/`transformation_change` fired meaningfully — **143 distinct pairs** (validates against pass 1's 59+61+26=146 minus 3 pairs counted under two of the three). Category split: **~133/143 (a)** judge correctly found no connection, the cluster's presence incidental (rationales are near-universally "the images do not share any thematic connection…", many explicitly naming the incidental overlap — "beyond both featuring women," "beyond being urban scenes"); **0 (b)** cases where a real connection existed that the judge couldn't articulate in cluster terms (by construction the judge said `none` and, on caption re-read, was right every time); **~10/143 (c)** where the judge acknowledges a shared *mood/register* but declines to type it ("share a calm and focused atmosphere but… lacking any thematic connection beyond visual mood") — a mild Mode-3 (ambient register) near-miss neither the clusters nor the binary judge capture, out of scope. **Keyword attribution (the actionable core):** corpus-wide, `ritual_ceremony` fires on **`observ`:110 of 142** total firings — i.e. "observing/observation" (photographic watching), NOT "observance" (religious); genuine ceremony words trail far behind (kneel:14, bow:10, ceremoni:6, solemn:4). Within the 143 none-pairs, `observ` is the shared trigger in 46 of 61 `ritual_ceremony` cases. `transformation_change` fires on **`strip`:75 of 96** — "striped" clothing / "strip of X," NOT "strip away/transform"; genuine change words trail (reveal:11, end:4, born:2). `sound_music` is different: only 22 of its 59 none-pairs share `sound_music` on both captions (and only 4 via the *same* keyword — the rest are source-vs-receiver, e.g. one image "guitar", the other "ear"), while **37 of 59 fire only via the `sound_music`↔`sensory_overwhelm` axisPair** — i.e. `sound_music`'s noise here is the #48 axis-bonus mechanism + the known source/receiver ceiling (PAIRING_THEORY §Thematic), NOT a fresh over-broad keyword. **BUCKET 1 TAIL (weak-tail clusters — read the rare non-`none` verdicts for what's different):** `tenderness_care` 33 non-none of 1,133, `stillness_rest` 10/1,071, `confinement_freedom` 9/731, `isolation_solitude` 2/166, `sensory_overwhelm` 1/124. Consistent pattern: in almost every case the tail cluster is **incidental** and the relational verdict is carried by a *co-firing strong cluster* — overwhelmingly `devotion_belief`+`tension_conflict`+`skilled_performance` on protest/rodeo pairs (a sign-holder, a megaphone, a bucking horse). `tenderness_care` fires via "hold/lean/support" on people *holding protest signs*, not tenderness. So the weak tail isn't broken vocabulary — those clusters are simply low-signal riders, exactly pass 1's read; no edit indicated. **BUCKET 2 (recurring relational phrases — all non-role pairs typed complementary/contrastive/ironic/tonal with score ≥ 0.80, n=8):** the dominant recurring relational language is **"source ↔ receiver" / "individual ↔ broader crowd" / "call-and-response"** — 5 of 8 (and ~10 more in the tail at 0.70–0.75) are protest scenes where one image is a close-up sign-holder/megaphone-speaker and the other the receiving crowd, which the judge types complementary with explicit "source-receiver," "specific instance ↔ broader theme," "one holding a sign and the other a megaphone" language; the rest are rodeo "action-reaction" (lasso poised vs. thrown) and festival "call-and-response." **Crucially this is NOT a candidate new `axisPairs` entry:** in every one of these pairs BOTH images fire the *same* clusters (`devotion_belief`+`skilled_performance`+`tension_conflict`) — the source/receiver distinction lives only in the captions (individual vs. crowd, sign vs. megaphone), which cluster Dice architecturally cannot represent (both sides look identical). This is the same Mode-1 relational-position ceiling as G5 (sound source vs. receiver) that the role pipeline's **join-1 source/receiver (#102)** already exists to handle — and indeed G16's role-pair rationale in this very run is "source of sound ↔ block out sound." So the recurring-phrase finding points at the **role pipeline / an individual↔crowd role signal**, not at cluster vocabulary. (`community_gathering` is the natural "crowd/collective" end of such a relationship but is ambient-tier 0.2 and gated out — noted as a lead, not a candidate edit.) **PRIORITIZED CANDIDATE EDITS (do NOT implement here — land one at a time per #48/#92/#116):** **(1, high confidence)** remove `observ` from `ritual_ceremony`'s keyword set — it's 77% of that cluster's firings and is pure "observing"↔"observance" stem collision (identical class to #48's "play"→"play of light"); **#48 regression check: SAFE and net-positive** — `observ` already lives in `looking_watching`, so the watching semantic is fully retained there; removing it introduces no new cross-cluster collision and doesn't touch `sound_music`. **(2, high confidence)** remove `strip` from `transformation_change` — 78% of firings, almost all "striped" clothing; the transformation sense is carried by `reveal`/`shed`/`transform`/`born`/`die`; **#48 check: SAFE** — `strip` also appears in `vulnerability_exposure` (same collision risk, but out of scope for this pass and a separate cluster), removing it from `transformation_change` alone is clean, no `sound_music` impact. (Secondary: `reveal`:11 in `transformation_change` is a milder photographic-"revealing" collision — flag, don't act.) **(3, explicit non-edit)** do NOT touch `sound_music` vocabulary — per #48's standing warning and #25's prior prune, its residual none-noise is the axis-bonus + source/receiver ceiling, not a keyword; keyword-level nibbling (`ring`/`bow`/`string` are ambiguous but low-frequency) risks a #48-style regression for ~zero gain. **(4, lead for a later pass, not an edit)** the "individual↔crowd / source↔receiver" protest pattern is a role-pipeline signal (join-1 territory), not a cluster axisPair — both sides fire identical clusters. **Validation:** Part A firing counts reported raw (0/0/2/0). Bucket 1 read count (143) reconciles to pass 1's per-cluster n's. Bucket 2 golden-pair check: G4/G6/G7/G16 are all `roleHypothesis`-pairs, correctly EXCLUDED from the non-role bucket (their 0.95 scores clear the ≥0.80 threshold, so exclusion is by role-status not threshold — the intended segmentation); none of the proposed edits would be contradicted by them (they're role-join pairs, orthogonal to cluster vocabulary; G16's source/receiver rationale is *consistent* with candidate-edit finding (4), not contradicted). **Scope discipline:** still no cluster/scoring code changed. **Remaining #96 work: only pass 4 (judge-fabrication recheck, tied to #98's pair-607627 finding).** Passes 1's over-broad-vocabulary and recurring-relational-phrase explores are now both covered here. Diagnostic artifacts updated at `diagnostics/96-cluster-relationship-distribution-{query.sql,harness.swift}`. Dated 2026-07-07.<br><br>**━━ PASS 2c — cluster BLIND SPOTS (the mirror of bucket 1: judge found a real relationship the cluster system had nothing to contribute to, 2026-07-07) ━━** Pass 2's bucket 2 (recurring-phrase → new axis pairs) was scoped to non-role score-≥0.80 pairs, which was structurally the wrong population (non-role pairs are selected *because* they share cluster vocabulary — mining them for "language the clusters can't produce" is circular) and too small (n=8). Pass 2c targets the right population directly. **Definition (reuses the exact same `firing`/`relType` computations as passes 1–2, no drift):** a *blind-spot* pair = `relType != none` AND `meaningfulShared` empty AND no `axisPairs` entry fired — i.e. `firing.isEmpty` (firing = meaningfulShared ∪ axis-participant clusters) — the cluster Dice/axis-bonus mechanism had literally nothing to contribute. Run over BOTH role and non-role pools. **Headline: strict blind spots are essentially nonexistent — n=2** (both role-hypothesis, both `contrastive`; 0 non-role), out of 705 non-none judged pairs (0.3%). **The reason is itself the finding:** near-ubiquitous clusters guarantee incidental `meaningfulShared` on almost every pair — corpus firing rates: `bodily_gesture` 94%, `waiting_anticipation` 73%, `tenderness_care` 64%, `stillness_rest` 57%, `confinement_freedom` 54%, `community_gathering` 49% — so "meaningfulShared empty" almost never happens even when the shared cluster is *not* the basis of the connection. The cluster system almost always "contributes" *something*, but on relationally-judged pairs that something is overwhelmingly incidental high-frequency noise, not the relationship the judge named. **The two strict blind spots** are both role-pipeline **real-vs-depicted object** pairs (a tattoo, a truck) — join-3 territory (#102), the same mechanism as golden **G4** (real pigeon ↔ depicted peacock); the cluster system correctly has no representation for "same object, real vs. depicted register" because that's an object-register extraction signal, not a thematic concept. (Minor: on caption re-read both roleHyps were slightly imprecise — both trucks/tattoos are actually *real*, the "depicted" element is a stuffed Garfield toy in one truck — a join-precision nit, but the judge *rationale* stayed caption-grounded.) **Broader lens (since strict n=2 can't yield recurring patterns, per the deliverable's "stratify or say nothing" guidance):** tabulated cross-side meaningful-cluster combos (`meaningfulOnlyA` × `meaningfulOnlyB`) over ALL 705 non-none pairs, split role/non-role, flagging existing `axisPairs`. **Non-role (n=48, where clusters actually drive selection): ZERO recurring combos** — only 2 reach n≥5, both base-rate `contrastive`, nothing enriched. This re-confirms pass 2 bucket 2's null from the mirror direction: the cluster-selected pool produces no new axis-pair candidate. **Role (n=657): many high-count combos, but all confounded by base rate** — the role non-none pool is 68% contrastive / 25% ironic / 7% complementary by base rate, and the top combos (`skilled_performance×stillness_rest` n=96, `confinement_freedom×stillness_rest` n=82, etc.) sit right at that base rate with dominant relType shares ≈68% contrastive, i.e. pure high-frequency-cluster co-occurrence, same base-rate confound pass 1's byproduct hit. **A handful are genuinely enriched above base rate** — `tenderness_care×vulnerability_exposure` complementary 33% (vs 7% base) and `sensory_overwhelm×skilled_performance` complementary 23% — so I read all of them (step 5, caption-grounded). **Result: both enriched combos are 100% role pairs (12/12 and 10/10) with an identical roleHypothesis — the join-1 `source↔receiver` template** ("SOURCE of sound … RECEIVED or physically blocked"): a megaphone-speaker/drummer (`skilled_performance`/`sound_music`) paired with a distressed or ear-cupping woman (`vulnerability_exposure`+`sensory_overwhelm`+`tenderness_care`). They are not a distinct cluster-level relationship — they're the incidental cluster footprints of the role pipeline's existing source↔receiver join (conceptually the existing `sound_music`↔`sensory_overwhelm` axisPair; mechanically #102 join-1), and several share the *same* receiver hub image (the G5/G16 hearer #572, and the distressed woman #… `_R013838`). Proposing them as new *cluster* axisPairs would import role-pipeline structure into the clusters circularly. **So pass 2c's deliverable is a clean null for cluster edits: no blind-spot pattern recurs into a viable option-(a) new-axisPair, option-(b) added-vocabulary, or option-(c) new-cluster.** Everything the judge names in the blind/enriched zone is already role-pipeline territory (source↔receiver join-1, real-vs-depicted join-3) that cluster Dice architecturally cannot and should not represent — the same conclusion as pass 2 bucket 2, now confirmed with the role pool included. **One by-product LEAD logged (n=2, below the n≥3 recurrence bar — a lead to note, not a candidate to act on, per #98's n=1 discipline):** the judge productively *generalizes* the source↔receiver frame beyond sound to **light** (pair 607566: sunlit path ↔ woman shielding eyes) and **motion** (pair 607567: stunt rider ↔ figure lying still) — a natural extension of join-1's phenomenon taxonomy (which today folds speech/music into `sound` via `normalizePhenomenon`), so this is a **role-pipeline lead (#102/#113 territory), NOT a cluster edit**. **Validation:** (1) blind-spot filter provably reuses passes 1–2's field computations — `firing.isEmpty` is exactly `meaningfulShared`-empty ∧ no-axis-fired, computed in the same loop. (2) KNOWN_GOOD_PAIRS cross-ref: no golden pair is a *strict* blind spot (they all fire incidental shared clusters), but the enriched-combo read independently surfaced **G16** (pair 526763, megaphone ↔ ears-cupping #572) and the pass-2 protest source/receiver lead — confirming the method finds cases already known to matter, not noise. (3) Fabrication spot-check (narrow by-product of step 5 across the ~24 pairs read here — explicitly **NOT** a substitute for pass 4's systematic full-pool sweep): 0 fabrications of caption-absent detail; 1 borderline over-generalization (pair 592804 forces the sound source↔receiver frame onto a joyful-rickshaw + distressed-woman pair where the sound link is thin) plus the 2 real-vs-depicted roleHyp imprecisions noted above — all caption-grounded, none inventing objects/claims absent from the captions, so nothing rising to the 607627-class fabrication pass 4 will hunt. **Scope discipline:** still no cluster/scoring code changed; harness extended (blind-spot filter + cross-side combo aggregation + enriched-combo reader) at `diagnostics/96-cluster-relationship-distribution-harness.swift`. **#96 remains open — pass 4 (systematic judge-fabrication recheck across the full judged pool, tied to #98's pair 607627) is the only piece not yet started.** Dated 2026-07-07.<br><br>**━━ PASS 2d — specificity-corrected blind spots (2026-07-07, corrects 2c's undercount; 2c entry left intact per the no-retrofit norm) ━━** **Why 2c undercounted:** pass 2c's blind-spot test (`relType != none` AND `meaningfulShared` empty AND no axis fired) returned n=2 and was reported as a clean null. But that test treats *any* nonzero `meaningfulShared` as "the cluster system contributed" — too literal, since several meaningful-tier clusters fire on >half the corpus and so convey almost no information about why two *specific* images share them. 2d discounts near-ubiquitous clusters before rerunning. **(step 2) `CorpusFreq.build` (the #113 IDF specificity utility) does NOT fit** — it consumes `RoleProfile`s and buckets by phenomenon/concept/object/category, none of which are ConceptClusters; its `spec()` IDF formula `log((total+1)/(count+1))` was reused against cluster firing counts as a specificity column, but firing-rate % is the primary proxy. **(a) Full 29-cluster corpus-frequency table (denom = 1,028, matches Part A):** `bodily_gesture` 94% (0.75), `waiting_anticipation` 73% (0.75), `tenderness_care` 64% (1.0), `stillness_rest` 57% (0.75), `confinement_freedom` 54% (0.75) ‖ `community_gathering` 49% (0.2, ambient), `youth_age` 40%, `looking_watching` 39%, `devotion_belief` 36%, `urban_street` 36% (0.2), `skilled_performance` 34%, `labor_effort` 27%, `sensory_overwhelm` 19%, `vulnerability_exposure` 18%, `joy_celebration` 17%, `tension_conflict` 16%, `nature_landscape` 15% (0.2), `ritual_ceremony` 14%, `isolation_solitude` 13%, `sound_music` 10%, `transformation_change` 9%, `movement_energy` 8%, `animal_presence` 6% (0.2), `power_dominance` 6%, `grief_sorrow` 2%, `humor_absurdity` 1%, `solitude_in_crowd` 0.2%, `uncanny_ordinary`/`economic_precarity`/`domestic_intimacy` 0%. **(b) Cutoff + informative set:** among *meaningful-tier* clusters the descending firing counts are 965, 755, 662, 590, 550, then a **135-image gap** (54%→40%) down to `youth_age` 415 — the largest gap in the mid-range and the natural break (same bimodal-gap method as pass 1's n<15 cut). The >50%-firing set is exactly `{bodily_gesture, waiting_anticipation, tenderness_care, stillness_rest, confinement_freedom}` = the pre-identified 5, and it coincides with that gap. Cross-checked against pass-1 outcome-correlation: all 5 sit at ≤2.9% non-none (≤ the 2.7% non-role base rate) — high-frequency *and* uninformative, the double justification. These 5 are the **UBIQUITOUS** exclusion set; "informative firing" = `meaningfulShared ∪ axis-participant` minus these 5. **(c) Corrected blind-spot count: n=92** (91 role, 1 non-role), up from 2c's 2 — and **90 of the 92 had a non-empty `meaningfulShared` that was ENTIRELY ubiquitous**, i.e. exactly the pairs 2c miscounted as "cluster contributed." roleHyp-template tally of the 92: **~64 join-3 real-vs-depicted object** (the G4 class — real vs. depicted horse/cow/shirt/truck/katana/…), **16 join-2/4 claim↔embodiment/object**, **11 join-1 source↔receiver** (9 sound, 2 motion). The single non-role case (pair 489188, complementary 0.75) is a genuine "man supporting a woman from behind" comfort-rhyme where image B's caption simply doesn't use tender-holding vocabulary so `tenderness_care` fires only on A — a **caption-coverage** miss (the cluster would fire on a richer caption), not a vocabulary gap, and n=1 anyway. **So the corrected, 46×-larger blind-spot set changes the count but NOT the conclusion: every recurring class is an existing role-join mechanism (role-pipeline territory, #102, already built), and the lone non-role is caption coverage — no new cluster-edit (option a/b/c) candidate emerges.** KNOWN_GOOD_PAIRS cross-ref: the corrected filter now surfaces **G6** (ironic smile, pair 603760) and **G16** (sound source↔receiver, 526763) as blind spots — correctly, since both are role/extraction pairs the clusters can't represent — confirming the method finds known-relevant cases 2c's over-strict filter masked. **(d) Enriched cross-side combo scan, informative clusters only** (excludes the 5 ubiquitous from `meaningfulOnlyA/B`): **conclusion unchanged from 2c.** ROLE pool (n=657, base rate complementary 7% / contrastive 68% / ironic 25%): the only complementary-*enriched* family is `sound_music × {sensory_overwhelm[AXIS], vulnerability_exposure, transformation_change, tension_conflict, power_dominance, grief_sorrow}` — all the sound source↔receiver role pairs (the existing `sound_music↔sensory_overwhelm` axis shows the strongest enrichment, complementary 57% vs 7% base, validating the metric), so it's join-1 territory with the receiver variously tagged, NOT a new cluster axis; the many contrastive/ironic-skewed combos sit near the pool's own 68%/25% base rates (base-rate confound, same as pass 1's byproduct). NON-ROLE pool (n=48): only 3 combos reach n≥3, all n=3–4 and near the non-role base rate (complementary 52%) — **still zero axis candidate**, same as 2c and bucket 2. **(e) SCORING-GUARD FINDING (step 6 — kept separate from cluster-vocabulary findings, handed off as its own lead):** replicated PairScorer's axis-bonus guard (bonus fires only when the pair is neither saturated nor has `clusterScore > 0.10`) over ALL 527,878 corpus pairs. **347,108 pairs (66%) structurally fire ≥1 of the 9 `axisPairs`** (the axis clusters are common, so eligibility is broad); of these, the bonus FIRES on only 19,796 (5.7%) and is SUPPRESSED on 327,312. Of the suppressions: 17,457 by saturation, **115,565 (35% of suppressions, 33% of ALL axis-eligible pairs) purely by an incidentally-shared UBIQUITOUS cluster**, 194,290 by a genuine non-ubiquitous shared cluster. Mechanism: the 5 ubiquitous clusters are meaningful-*tier* (0.75/1.0) despite firing everywhere, so a single incidental shared `hand`/`hold`/`behind` lifts `weightedDice` above the 0.10 floor and trips the #48 guard, silently zeroing the complementary axis bonus. **This is a `clusterScore ≤ 0.10` guard-precision issue, explicitly NOT a #96 cluster-content edit** — #96 is scoped to vocabulary, not the guard. Handed off as a standalone lead (candidate fix: exclude ubiquitous clusters from the guard's `clusterScore`, or demote their tier — a separate scoped decision). **Caveat:** this is a structural count over all pairs; it does NOT prove 115k good pairs are lost (the judge says `none` to most such pairs) — it quantifies that the guard's trigger is polluted, so it cannot distinguish its intended target (pairs already scoring via genuine Dice) from incidental ubiquitous overlap; whether fixing it surfaces real pairs needs its own evaluation. **(f) Why the 5 ubiquitous clusters over-fire (reconnaissance for a FUTURE edit — no trim proposed here, per step 7):** each is dominated by one or two ultra-generic keywords — `bodily_gesture` ← **`hand`:827** (80% of the corpus; +back/tilt/turn/arm/lean) → fires on any described body, behaves like ambient context; `tenderness_care` ← **`hold`:565** ("holding a sign/phone/container", mostly objects, not tender holding); `confinement_freedom` ← **`behind`:345** (a spatial preposition — "behind her" — nothing to do with confinement; +wall/open); `stillness_rest` ← **`rest`/`relax`/`calm` (275/216/164)** (posture/mood description); `waiting_anticipation` ← **`look`/`gaze`/`observ`/`watch` (536/279/110/62)** (any looking verb — and note `observ`:110 is the *same* stem collision pass 2 flagged in `ritual_ceremony`, polluting a second cluster). Several of these are single over-broad keywords in the exact class of pass 2's `observ`/`strip` (notably `behind` in confinement_freedom, `hold` in tenderness_care, `observ` in waiting_anticipation); `bodily_gesture` is so dominated by `hand` that the whole cluster reads as ambient context — a candidate for #47-style tier demotion (heavier than a keyword trim). **This is input for a future edit pass, not a proposal.** **(g) What points where:** *viable cluster edits (option a/b/c)* — **none new from 2d**; the only actionable cluster-vocabulary edits remain pass 2's `observ`-from-ritual_ceremony and `strip`-from-transformation_change (both unaffected by 2d's reframing — neither cluster is ubiquitous, 14%/9%), plus 2d's (f) reconnaissance for a *later*, separately-scoped ubiquitous-cluster trim/demotion pass. *Role pipeline (#102/#113)* — the entire 92-pair informative blind-spot set (real-vs-depicted, claim↔embodiment, source↔receiver incl. the light/motion extension logged in 2c). *Scoring-guard change (not #96)* — the (e) axis-bonus-suppression finding. **Validation:** freq-table denom = 1,028 (= Part A); informative recompute does NOT flip pass 1/2's accepted `observ`/`strip` findings (both clusters are informative-tier, untouched); corrected blind spots re-cross-checked against KNOWN_GOOD_PAIRS (G6/G16 surfaced, correctly). **Scope discipline:** still no cluster/scoring code changed; harness extended (freq table + gap analysis + informative blind-spot recompute + informative combo scan + all-pairs axis-suppression counter + ubiquitous-cluster keyword reconnaissance). **#96 still open — pass 4 (judge-fabrication recheck) remains the only unstarted piece.** Dated 2026-07-07.<br><br>**━━ PASS 3 — caption-first vocabulary discovery, independent of pairing/judging (2026-07-07) ━━** Passes 1–2d all started from judged pairs and worked backward to clusters, so every actionable finding was a removal (`observ`, `strip`) or a role-pipeline lead — the search only ever looked at pairs the system already had partial machinery for. Pass 3 inverts direction: mine all 1,028 captions directly for recurring vocabulary the 29 clusters represent zero of, independent of any pair ever reaching a judge. **(1) Exclusion set:** `Set(ConceptClusters.all.flatMap { $0.keywords })` computed live from the imported module (not a stale copy) = **678 distinct keyword stems**. **(2) Tokenization:** reused the pass-2 tokenizer/stemmer copy (0 mismatches vs the real `matchedClusters` on all 1,028 captions) for unigrams; bigrams/trigrams built separately from lightly-cleaned (lowercased, punctuation-stripped, NOT stemmed) word sequences so phrases stay readable, with per-word stems checked against the exclusion set. **(4) Boilerplate stoplist** (~65 terms, built by inspecting the raw top-80 unigram frequency table and categorizing): generic-subject nouns (woman/man/person/people/someone/one/another/individual), clothing/color/appearance (wear/shirt/jean/pant/hat/hair + color names), prompt-v2 photographic-narration vocabulary (appear/suggest/background/scene/overall/possibly/visible/setting/slightly/casual/focus/atmosphere/camera/mood/moment/expression/seem/engaged, plus `stand`/`sits`/`captur`/`photograph`/`frame`/`sense`/`posture` added after a second look — `stand` alone was 59% of the corpus), generic spatial words (right/left/side/top/down/forward/toward/ground/front/beside/foreground/around/near/off/out/over/ahead/position/area/facing), generic body parts used for physical description not behavior (head/eyes/legs/feet/arms/body/face), and residual function words. Deliberately NOT stoplisted for individual read-through: `light` (287, 28%), `phone` (101), `convers`/`interac` (109/123), `downward` (147), `pattern`/`tattoo` (145/82) — each carries plausible thematic content. **(3+5) Leftover unigram ranking:** `light` stands alone at the top with a clean **133-hit gap** down to the next tier (154→147→142→126…, mostly smooth 1–20 gaps, no further clean break — a long tail of setting/texture nouns, same "no natural separator below the top item" shape as pass 2d's ubiquitous-cluster cutoff, just at the opposite end of the frequency spectrum). **Phrase-level (bigram/trigram) leftovers:** dominated by boilerplate the unigram stoplist didn't fully catch at phrase composition ("partially visible/obscured", "firmly planted", "light blue/colored", "twenties or thirties", "scene conveys a sense") — **except** `"in conversation"`/`"engaged in conversation"` (71/60 hits) and `"a phone"` (79 hits), which independently corroborate the `convers`/`interac`/`phone` unigram leads via a completely different extraction method (phrase vs. stem), raising confidence they're real recurring caption content, not a stemming artifact. **(6) Enrichment check — the decisive test:** for every surviving candidate (`phone`, `convers`/`interac`, `downward`, `light`, and two KNOWN_GOOD_PAIRS-motivated adds below), computed non-none rate when the term appears on either caption side vs. the pool's own base rate, role and non-role separately (pass 2d's method). **Result: every single candidate's NON-ROLE rate sits AT OR BELOW the 2.7% non-role base rate** (`phone` 0%, `convers`/`interac` 1%, `downward` 2%, `light` 2%, `pattern`/`stripe` 1%, `tattoo` 2% — vs. base 2.7%). None carries positive pairing signal in the pool that actually tests a new cluster's value (role-pool rates are all close to the 55.3% role base too, i.e. riding the role pipeline's own pre-selection, not a causal effect of the term itself). **(Validation) KNOWN_GOOD_PAIRS cross-reference surfaced two sharp, non-obvious hits:** **G2** (`375`/`864`) — KNOWN_GOOD_PAIRS.md already documents this pair's gap as "no pattern/texture signal exists anywhere in the system... capability gap, not a tuning issue," and pulling the actual captions confirms it's directly visible in caption text (`375`: "striped sweater"; `864`: "red patterned shirt") — `pattern`/`stripe` genuinely recurs (145/8%) and maps to a *documented* gap. **G15**-adjacent and pass-2c's tattoo blind-spot pair (607945) both surface `tattoo` (82 hits, 8%) as recurring content. **But in both cases the enrichment null is the right answer, not a contradiction:** pattern/stripe rhyme is a fundamentally *visual* concept (do the two patterns actually look alike?) that a caption cluster can only proxy noisily — two captions both saying "patterned" tells you nothing about whether the patterns rhyme, so the correct home is the **aesthetic axis** (already tracked, unmeasured texture/pattern signal, same category as backlog **#55**'s light-quality gap — `light`'s null result is the same story: a caption mention of "light" says nothing about actual light *quality*, which is a pixel-level aesthetic concern, not text). `tattoo` is different again: its real relational content (a tattoo *real* on one side, *depicted* on the other — pass 2c's pair 607945) is a **register distinction already owned by the role pipeline's join-3 object/register matching (#102)** — a text cluster that fires on "both captions mention a tattoo" would be blind to exactly the real-vs-depicted distinction that makes the pair interesting, so it would add noise, not signal, consistent with the null enrichment. **`phone`/`convers`/`interac`/`downward` read as genuinely inert corpus texture** — example captions confirm `phone` is neutral object-holding (like a bag or purse, no isolation/distraction charge visible in this corpus's usage) and `convers`/`interac` are overwhelmingly the caption model's *speculative hedge* language ("her posture suggests she is engaged in a conversation") rather than depicted action, explaining why they don't correlate with judged outcomes. **Conclusion for the main mandate: NO new cluster or new-vocabulary-for-an-existing-cluster candidate clears both the recurrence and enrichment bar.** Every recurring, plausible-looking leftover either shows null pairing signal outright, or maps to a capability gap whose correct fix lives outside the 29-cluster thematic system (aesthetic axis, or the already-built role pipeline) — a genuine, non-forced null that closes the loop passes 2/2c/2d left open, and *raises* confidence there's no easy missing-cluster win, since the search was run from the opposite direction and still came back empty. **BYPRODUCT (separate from the main mandate, but high-value): a keyword-reachability bug audit.** `matchedClusters` compares STEMMED caption tokens against RAW (not stemmed) keyword strings — most keywords are deliberately pre-truncated to stem form (`"cri"`, `"mourn"`), but a keyword written as a full dictionary word can silently fail to match its own common inflections (discovered via `uncanny_ordinary`'s `"obscure"`, which never matches caption stem `"obscur"` — `localStem("obscured")` = `"obscur"` ≠ `"obscure"`). Systematic check: **364 of 678 keywords (54%) have ZERO corpus stem support at all**; testing each dead keyword's common inflections (+ed/+s/+ing/+d/+es) against corpus-supported stems found **14 candidates** where a natural inflection IS reachable under a different stem. Manually verifying each against false positives and design intent (per step 8's regression-risk mandate): **3 are coincidental unrelated-word false positives from the naive inflection heuristic** (`crow`→`crowd`, `grin`→`grind`, `stare`→`star` — confirmed by reading example captions, none are actually about a crow/grin/stare), **1 is an intentional pre-existing duplicate, not a new bug** (`gaz` alongside `gaze` in `looking_watching` is a documented #118-era fix for a rare 7-char stemmer edge case that simply never fires in this corpus — working as designed), **1 carries a genuine collision-regression risk and should NOT be fixed** (`fix`→`fixed` in `labor_effort`: reading examples shows this corpus's `"fixed"` is overwhelmingly `"gaze fixed on X"` — an intent/stare sense, not labor_effort's intended repair sense; "fixing" the reachability would import a new false-positive collision, the same regression class as `#48`/`observ`/`strip`), **2 are real but too low-value to prioritize** (`hear`→`heard`, `mute`→`muted` in `sound_music`, n=4/3 — `sound_music` is already flagged catch-all-prone by #48 and pass 1/2, expanding its reach further isn't worth it), **1 is real but scoring-inert** (`paw`→`paws` in `animal_presence`, n=3 — ambient tier 0.2, can never clear the meaningful-tier gate regardless), leaving **2 clean, high-confidence, actionable fixes**: **`"raise"` → `"rais"`** (appears in BOTH `bodily_gesture` and `joy_celebration`, n=170 corpus hits — the single largest-impact fix found; 10 example captions read, all genuinely about raising a hand/arm/sign, zero collision risk) and **`"obscure"` → `"obscur"`** (`uncanny_ordinary`, n=86; verified this would NOT flip the cluster's corpus-firing outcome — 0 of the 86 `obscur`-containing images also satisfy `uncanny_ordinary`'s G1 eerie/dreamlike group — but it's a genuine latent defect independent of pass 2d's "gate too strict" diagnosis, i.e. both can be true at once). These are a distinct, lower-risk category from options (a)/(b)/(c) in pass 2c's framing — restoring intended reachability of vocabulary the cluster author already vetted, not adding new semantic territory. Framed for future action: land `raise`→`rais` and `obscure`→`obscur` as their own small, single-purpose fixes (same one-at-a-time discipline as #48/#92/#116), NOT the `hear`/`mute`/`paw`/`fix` set. **Scope discipline:** no cluster/scoring code changed this pass; harness extended (exclusion-set builder, boilerplate stoplist, unigram/bigram/trigram leftover ranking with gap analysis, enrichment-check helper, keyword-reachability audit + inflection-based bug/false-positive triage). **#96 still open — pass 4 (judge-fabrication recheck) is the only piece remaining.** Dated 2026-07-07.<br><br>**━━ PASS 4 — systematic judge-fabrication recheck across the FULL judged pool (2026-07-08) ━━** #98 (2026-07-07) checked this narrowly: 166 captions matching gaze-vocabulary phrases, 686 judged pairs touching them, 1 fabrication found (pair 607627) — logged as n=1, not enough to scope a fix. Pass 4 checks broadly: not a phrase-triggered subset, a **stratified sample across the full 2,986-pair judged pool** (relationshipType × role/non-role status), reading every sampled pair's rationale against BOTH source captions for any asserted object/action/target/attribute not stated or reasonably implied by that image's own caption — the same fabrication definition as #98's pair 607627. **Method:** built an automated lexical pre-filter first (rationale content-words absent from both captions, after excluding cluster vocabulary and an iteratively-calibrated judge-meta-vocabulary stoplist ~150 terms) — this converged slowly and was ultimately judged **not precise enough to gate manual reading**: even after heavy stoplisting it still flagged 1,600 of 2,986 pairs (54%), because it can't distinguish PARAPHRASE ("activism" for caption "protest") from FABRICATION (asserting a detail the image doesn't support at all) — any summarizing judge uses words absent from the source text as a matter of course. So the primary method became a **stratified DETERMINISTIC sample** (every Nth pair within each stratum, sorted by pairID — avoids both cherry-picking and true-randomness's reproducibility problem), with the lexical filter kept only as a secondary cross-reference during reading. **Sample composition, n=112:** non-role non-none — **ALL 48** (the full population, since this is the pool cluster vocabulary is directly responsible for and it's small enough to read exhaustively); role non-none — **40, stratified proportionally** by relType (27 contrastive of 446, 10 ironic of 161, 3 complementary of 68, the 1 tonal of the role pool); `none` pairs — **24, stratified** (12 role of 532, 12 non-role of 1,749; lighter sample here since a `none` verdict is a rejection, not an asserted connection, so the fabrication surface is structurally smaller). **Result: zero fabrications found in all 112 pairs read.** Every rationale's factual claims about each image trace to that image's own caption — including in cases where the *pairing itself* is weak or the judge's rejection reasoning is thin (e.g. "the proposed connection is superficial"), the judge never asserts a caption-absent detail to justify either a connection or a rejection. This **confirms #98's "not systemic" finding at a much broader and more representative sample** than the original phrase-triggered 686-pair check. **Honest limitation, stated per the task brief:** a sample cannot fully rule out a rare failure mode. #98 found the base rate at ~1-in-686; at that same rate, `P(0 fabrications observed in a fresh n=112 sample) ≈ (1 − 1/686)^112 ≈ 0.85` — i.e. there was already an ~85% chance of seeing zero even if the true corpus-wide rate exactly matches #98's finding. Pass 4 therefore does not prove the rate is zero; it adds a second, methodologically distinct, much-broader-coverage sample that is fully consistent with #98's low base rate and finds no new instance or recurring pattern. Per the task brief: since nothing new or recurring turned up, this is *not* folded into a separate item — #98's territory (judge-prompt fidelity) remains closed as "not systemic," and no new item is opened from pass 4. **━━ IMPLEMENTATION — four keyword edits landed (branch `fix/96-cluster-vocabulary-edits`, 2026-07-08) ━━** All four edits identified in passes 2–3 as high-confidence and low-risk were implemented in `ConceptClusters.swift`, each as a separately identifiable, independently-revertable hunk with an inline rationale comment: **(1)** removed `observ` from `ritual_ceremony` (stem collision with "observing," not "observance"); **(2)** removed `strip` from `transformation_change` (stem collision with "striped" clothing, not "strip away"); **(3)** `"obscure"` → `"obscur"` in `uncanny_ordinary`'s G2 group (both the flat `keywords` array and `requiredGroups`, kept in sync); **(4)** `"raise"` → `"rais"` in both `bodily_gesture` and `joy_celebration`. **Verification:** full `ConjunctEngine` test suite green — **106 tests, 0 failures** (up from the 104 noted at #116; the 2 additional tests are unrelated to this change). Re-ran the pass 1–3 diagnostic harness (keyword-only edit, so a re-score against the dev DB copy's existing captions is the correct verification — no re-index/re-extraction needed, caption data is untouched) and confirmed every predicted effect exactly: `ritual_ceremony` corpus firing **142 → 39** (−103); `transformation_change` **96 → 21** (**−75, exactly matching pass 3's predicted `strip`-attributable count**); `bodily_gesture` **965 → 968** (+3, small — the cluster already fires on 94% of the corpus via `hand`/`arm`/etc., so there's little room for `rais` to add new matches); `joy_celebration` **174 → 292** (+118, a much bigger jump — its other keywords are rarer, so `rais` reachability closes a real gap); `uncanny_ordinary` **confirmed still 0** (inert on this corpus, exactly as pass 3 predicted, since none of the images containing the `obscur` stem also satisfy the cluster's G1 eerie/dreamlike group). **One additional regression check performed before finalizing:** since `raise` (the literal, uninflected word — as opposed to `raised`/`raising`) stems to itself unchanged (`"raise"`, not `"rais"` — the stemmer's suffix rules don't touch it), replacing the keyword outright risked silently breaking a match for any caption that used the bare word "raise" without an `-ed`/`-ing` inflection. Checked directly: **0 of 1,028 captions contain the literal stemmed token `"raise"`** (qwen2.5vl's descriptive style in this corpus always inflects it) — so the swap is a pure gain with no regression on this corpus, though the same swap on a differently-worded corpus should re-check this. **KNOWN_GOOD_PAIRS cross-check:** recomputed `matchedClusters` on G4/G6/G7/G16's actual captions post-edit — **none of the four touches `ritual_ceremony`, `transformation_change`, or `uncanny_ordinary` at all**, so they are structurally unaffected by any of the four edits; their stored `thematicV2Score`/`relationshipType` (already-judged historical DB records, set by the LLM judge at judge-time, not derived from `matchedClusters`) are untouched regardless, as expected. **━━ #96 CLOSE-OUT ━━** Closed 2026-07-08. Passes 1–4 (all read-only diagnostics) plus this session's implementation constitute the full life of backlog #96. **Summary of the whole arc:** pass 1 found `echo` extinct under the current judge and `none` the real catch-all, flagging `sound_music`/`ritual_ceremony`/`transformation_change` as never-relational; pass 2 mined that into two real over-broad-keyword fixes (`observ`, `strip`) and confirmed `sound_music`'s noise is the #48 axis-bonus mechanism, not a keyword; pass 2c/2d (the blind-spot mirror, corrected for near-ubiquitous-cluster noise) and pass 3 (caption-first, direction-inverted vocabulary mining) both independently and rigorously concluded **no new cluster or new vocabulary for an existing cluster is warranted** — every recurring un-clustered concept either showed null pairing-enrichment signal or belongs to a different mechanism entirely (aesthetic axis: pattern/light; role pipeline: object-register, source/receiver); pass 3's byproduct keyword-reachability audit surfaced two more high-confidence fixes (`raise`→`rais`, `obscure`→`obscur`); pass 4 broadened #98's judge-fabrication check across the full pool and found nothing new, confirming "not systemic." **Four edits landed, verified (106/106 tests green, corpus-firing deltas match predictions exactly, golden pairs unaffected).** **Also surfaced and explicitly handed off elsewhere, not folded into this close-out:** a scoring-guard finding (pass 2d — 33% of axis-bonus-eligible pairs across the corpus have the bonus suppressed purely by an incidentally-shared ubiquitous cluster, a `clusterScore ≤ 0.10` guard-precision issue, not a vocabulary issue); a role-pipeline lead (pass 2c — the judge extends source↔receiver to light/motion, n=2, logged not scoped); a reconnaissance list for a future ubiquitous-cluster tier/vocabulary trim (pass 2d step f — `bodily_gesture`, `tenderness_care`, `waiting_anticipation`, `stillness_rest`, `confinement_freedom`, none acted on here). **The generative question #96 never asked — what new cluster concepts would make the system better, as opposed to whether current data already shows evidence of a missing one — is out of scope for this backlog item and is opened separately as #119.** Verified: 106/106 ConjunctEngine tests green; dev-DB-copy re-score confirms `ritual_ceremony` 142→39, `transformation_change` 96→21 (−75, exact), `bodily_gesture` 965→968, `joy_celebration` 174→292, `uncanny_ordinary` unchanged at 0; golden pairs G4/G6/G7/G16 confirmed structurally unaffected. |
| Pair-row lifecycle during re-index — thematicV2/roleHypothesis do NOT survive (2026-07-08, read-only investigation, no code changed) | **Question:** a full re-index of an already-captioned, already-judged folder — does it preserve `thematicV2Score`/`thematicV2RelationshipType`/`thematicV2Rationale`/`roleHypothesis` on existing pairs (UPDATE-style), or wipe them (DELETE+reINSERT)? This has been an implicit open question since before the v15 ThematicV2 migration; #102's role-candidate guard against "re-nulling and re-judging the same surviving pairs" hinted at it without settling it. Answered here definitively, traced end-to-end through `IndexingEngine.swift` (no method is literally named `reindex()`/`runIntraFolderScoring()` — those are doc-only names; the real entry point is `index()` → `runPipeline()`, with the pair-scoring block inlined as "Phase 4: Intra-folder pair scoring," `IndexingEngine.swift:494–744`). **Answer: they do NOT survive, for any pair whose row is rewritten during the re-index — mechanism is a scoped DELETE followed by a fresh INSERT, not an UPDATE or upsert.** Intra-folder: `DELETE FROM pairs WHERE imageAID IN (batch) AND imageBID IN (batch)` (`:626–629`) where `batch` = every image the scan returns for that folder (not a delta of new/changed images — "Scores only images in the current scan batch," `:495`, and the batch is unconditional regardless of whether any image actually changed). Cross-folder scoring applies the complementary one-inside-one-outside DELETE (`:866–869`). Both are followed by a fresh `PairRecord.insert(db)` (`:737`, `:972`). **Column-scope is the root cause, not any deliberate invalidation:** `PairRecord` (`Models/PairRecord.swift:5–33`) has **no properties at all** for `thematicV2Score`/`thematicV2RelationshipType`/`thematicV2Rationale` — they were never added to the struct when the v15 migration introduced the columns, so a freshly-inserted row cannot carry them forward even in principle; they land on SQLite's column default (NULL, no `DEFAULT` clause in the v15 migration). `roleHypothesis` **is** a `PairRecord` property (`:32`) but Phase 4's insert calls don't pass it (`:720–736`, `:955–971`), so it also comes back NULL on every rewritten row. **Concretely verified against the dev DB copy** (`conjunct-copy.db`, the same snapshot used in #96 passes 1–4): ran the literal `DELETE FROM pairs WHERE imageAID IN (297,578) AND imageBID IN (297,578)` from `:626` inside an explicit transaction against real pair 488338 (`thematicV2Score=0.95`, `relationshipType='contrastive'`, a populated `roleHypothesis`) — the row (with all judge data) is deleted (0 rows remain), confirming the DELETE's scope matches judged pairs with no special-casing; `ROLLBACK` afterward left the dev DB copy exactly as found, nothing altered. **The DELETE's scope has no carve-out for already-judged pairs** — it matches on `imageAID`/`imageBID` membership only, never checking `thematicV2Score`/`roleHypothesis`/`gazeJudgeScore`. This is the same "DELETE+reINSERT pairs on re-index" mechanism from the earliest (pre-numbering, pre-v15) decision-log entry — *"record.save() hit UNIQUE constraint silently; old scores were never updated"* — and it was never revised when v15 (thematicV2 columns) or v17 (`roleHypothesis`) landed; `PairRecord` simply never grew the three thematicV2 fields. **One real (partial, narrow) exception:** `generateRoleCandidates()` (Phase 8.5, runs after both scoring passes specifically so its own inserts survive the cross-folder DELETE — see the placement comment at `:90–95`) uses a genuine in-place `UPDATE` for pairs its deterministic joins re-match: `UPDATE pairs SET roleHypothesis = ?, thematicV2Score = NULL, ... WHERE id = ? AND (roleHypothesis IS NULL OR roleHypothesis != ?)` (`:1380–1386`). Because Phase 4 already nulled `roleHypothesis` moments earlier in the same run, this predicate is normally true and the UPDATE fires — restoring `roleHypothesis` (RoleJoins is deterministic over stable roleProfiles, so it recomputes the same hypothesis string) but explicitly *re-affirming* `thematicV2Score = NULL` rather than preserving any old value (there is nothing to preserve at that point — Phase 4 already cleared it). The guard's actual purpose (per its own comment, `:1374–1379`) is to stop *this function's own* repeated invocations from re-triggering judge work when a hypothesis is unchanged across multiple `generateRoleCandidates()` runs — it is not, and cannot be, a defense against Phase 4's earlier full-table rewrite within the same index() call, which has already happened by the time this function starts. **`generateGazeCandidates()`'s "(judged rows preserved)" comment (`:1440–1441`) is accurate only when this function is called in isolation** (e.g. standalone nominator re-tuning, without a preceding full Phase 4/cross-folder rewrite in the same session) — its own `DELETE FROM pairs WHERE selectedFor = 'gaze' AND gazeJudgeScore IS NULL` (`:1443`) only clears *unjudged* gaze rows, but during a genuine full re-index Phase 4's earlier unconditional DELETE has already destroyed any previously-gaze-judged row whose images are in the batch (gaze pairs are ordinary rows in the same `pairs` table, matched by `imageAID`/`imageBID` like any other `selectedFor` value) — there is nothing left for the narrower gaze-only DELETE to "preserve" by the time it runs. So `gazeJudgeScore`/`gazeJudgeRationale` are lost on full re-index exactly like `thematicV2*`, for the identical structural reason; the parenthetical is not wrong on its own terms, just easy to misread as "survives a full re-index," which it does not. **Is this logically necessary? No — checked and confirmed not.** `ThematicScorerV2.score(captionA:captionB:)` and `.validate(captionA:captionB:hypothesis:)` (`ThematicScorerV2.swift:122`, `:130`) take only caption strings (+ hypothesis for `validate`) — zero references to `ConceptClusters`/`matchedClusters`/`clusterScore` anywhere in the file, and `PairScorer.score()` never reads `thematicV2Score`/`thematicV2RelationshipType`/`thematicV2Rationale`/`roleHypothesis` as an input (confirmed by grep — zero hits). So a cluster-keyword-only change (like #96's four edits) changes nothing the judge actually consumes, and there is no code path that inspects "did clusterScore change" to decide whether to invalidate an existing thematicV2 verdict — **the wipe is pure collateral damage from Phase 4's blunt full-table DELETE+reINSERT of the touched batch, not a deliberate invalidation tied to cluster changes.** Concretely: #96's four keyword edits do not, by themselves, clear any existing `thematicV2Score` — only running an actual re-index of a folder does that, and it would do so regardless of what changed (or nothing changed) about the images. **Net practical consequence:** every full re-index of a folder resets `thematicV2Score`/`thematicV2RelationshipType`/`thematicV2Rationale`/`gazeJudgeScore`/`gazeJudgeRationale` to NULL for every pair whose endpoints are in the re-indexed batch, and `roleHypothesis` gets regenerated (not preserved) via `generateRoleCandidates()`'s guarded `UPDATE`; all previously-judged pairs must be re-earned by `ThematicV2BackgroundPass`/`GazeVisionBackgroundPass` on the next grid load. This has nothing to do with #96 specifically — it is the general, pre-existing behavior of any re-index, keyword-edit-triggered or not. Cross-referenced in CLAUDE.md's Known Gotchas. |
| #96 real-world validation gap — over-broad keywords far beyond the 4 fixed, confirmed with corpus-wide evidence (2026-07-08, opened backlog #120) | **Trigger:** Andre re-indexed the production library after #96's four keyword edits landed and reported the improvement was not appreciable, citing 3 concrete pairs whose shared clusters made no sense on caption/image re-read: (1) `20240907-L1000759.jpg` ↔ `20230101-L1004337.jpg`, shared `devotion_belief`; (2) `20250530-_R018695.jpg` alone tagged `devotion_belief`; (3) `20240818-L1000242.jpg` ↔ `20250603-R0019740.jpg`, shared `tenderness_care` + `confinement_freedom`. **Investigated against the live production DB (read-only, no writes) — every example confirmed as a genuine keyword collision, not user error:** pulled the 5 images' actual captions and ran the current (post-#96-edit) `ConceptClusters.matchedClusters` directly (throwaway ad hoc block in the same scratchpad harness used for #96 passes 1–4, so the real code, not a reimplementation). **Pair 1** (`sign`): both captions mention only a literal bus-stop/storefront sign ("The bus stop sign is prominently displayed," "a large, illuminated blue sign that reads 'main'") — zero religious or political content; `devotion_belief` fires purely on the word "sign." **Pair 2** (`cross`): the caption's only devotion_belief-relevant word is "her arms **crossed**" — a body-posture description, not a religious cross; `crossed` stems to `cross`, a literal `devotion_belief` keyword. **Pair 3** (`lean`/`behind`): both captions use "lean" for pure physical posture ("leaning against a black metal railing," "leans forward" / "a slight lean") — no emotional-support content — and both use "behind" as a plain spatial preposition ("Behind her, a man…") with zero confinement content; `tenderness_care` fires via `lean`, `confinement_freedom` via `behind`. **`behind` in `confinement_freedom` was already flagged** in #96 pass 2d (2026-07-07) as one of five near-ubiquitous, over-broad-keyword-driven clusters — logged there explicitly as "reconnaissance for a future edit pass," never acted on within #96's scope. **Quantified corpus-wide (full per-keyword frequency table, not just the top offender, for the 3 implicated clusters + the pass-2d ubiquitous set):** `devotion_belief` (weight 1.0, "rare when firing correctly," 373 total corpus firings) — `sign` 242 (65%), `flag` 93 (25%, lower-confidence — this library has heavy protest-photography content where flags are often genuinely patriotic/political, so not clearly a false positive the way `sign`/`cross` are), `cross` 87 (23%); genuine religious/devotional words (`pray`, `church`, `spirit`, `commit`) each land at 1–4 firings (≤1%). So a cluster explicitly designed to be rare and emotionally specific is, in practice, driven almost entirely by two structurally unrelated words (a generic sign, a crossed-arms posture) — arguably the single worst-polluted cluster in the system, worse than any #96 found. `tenderness_care` (weight 1.0, 662 firings) — `hold` 565 (85%, already known from pass 2d — mostly "holding a sign/phone/object," not tender holding), `lean` 101 (15%, newly confirmed here); combined these two account for effectively all firings, with genuine tenderness vocabulary (`touch`, `comfort`, `care`, `embrac`, `love`, `kiss`, `hug`, `nurtur`, `tender`) each under 5%. `confinement_freedom` (weight 0.75, 550 firings) — `behind` 345 (63%, pass-2d finding, still unfixed), `wall`/`open`/`wide`/`door`/`fence` more plausible but each a minority. **Conclusion — this is a methodology gap in #96, not a failure of the passes that ran:** #96's four passes mined vocabulary either FROM judged-pair rationale/relationshipType signal (passes 1–2, 2c–2d) or FROM caption vocabulary the clusters don't yet cover (pass 3) — neither approach exhaustively re-examines vocabulary the clusters ALREADY claim, keyword by keyword, against realistic caption phrasing. `sign`/`cross`/`lean` were never surfaced by any #96 pass because they weren't "leftover" vocabulary (pass 3) and didn't correlate with judged fabrication or blind spots in the specific ways passes 1/2/2c/2d tested for — they simply sat inside already-claimed cluster keyword lists, over-broad from day one, undetected until read directly against real captions. The 4 edits #96 landed (`observ`, `strip`, `obscure`→`obscur`, `raise`→`rais`) were real and correctly verified, but represent a small fraction of this class of problem — at minimum `sign`, `cross`, and `lean` are additional, comparably-sized collisions, and the corpus-wide frequency shape (a cluster's firings dominated by 1–3 keywords with genuine vocabulary in the low single digits) recurs across multiple clusters, suggesting more remain unaudited. **Secondary finding, not a #96 vocabulary issue but directly relevant to "why does the tag look wrong":** the pairs table's stored `rationale` for pair 719459 (340↔460) read "confinement freedom" while the live re-run of the exact same captions through the current code picked `devotion_belief` as the representative shared cluster — despite both being computed from the identical caption pair and the row having a `scoredAt` timestamp from *today's* re-index (i.e., not stale pre-#96-edit data). This is the known, previously-deferred sibling of decision #118: `PairScorer.swift`'s stored-at-index-time rationale builder still picks the representative cluster via `meaningfulShared.first` on a `Set` (non-deterministic across process launches, stable only within one run — #118's own finding), while `AppModels.swift`'s live lightbox recompute (fixed by #118) uses the deterministic `ConceptClusters.representativeCluster`. If the app was relaunched between indexing and viewing, the two surfaces can disagree on which of several tied shared clusters to display for the exact same pair — not a new bug, but now directly visible and worth fixing alongside the keyword audit since it independently makes "why was this cluster chosen" harder to reason about. **Scope discipline:** read-only investigation, no code changed, no cluster/keyword edits made in this session. Opened as backlog **#120** (see CLAUDE.md); #96's closed-backlog note amended (not rewritten — the original passes 1–4 write-up stands as an accurate record of what those specific methodologies found) to flag that real-world validation found the practical improvement inappreciable. Dated 2026-07-08. |
| Cluster keyword audit — exhaustive per-keyword collision review (#120, CLOSED 2026-07-08) | **Task:** the audit #96 never ran — read *every* keyword already inside all 30 clusters against corpus-wide caption usage and decide per-keyword keep/remove/narrow, rather than mining from judged-pair signal or uncovered vocabulary (both structurally blind to already-claimed over-broad keywords). **Method (reused #96's proven pipeline):** copied the production DB (2026-07-08, 1,028 active-captioned images), exported `corpus_captions.json` + `judged_pairs.json` via `diagnostics/96-…-query.sql`, and built a new throwaway `ClusterDiag` SPM executable (preserved as `diagnostics/120-keyword-audit-harness.swift`) that reuses the real `ConceptClusters.matchedClusters`/`weights` plus a self-validated local copy of the internal `tokenize`/`stem` (0 mismatches vs the real matcher over all 1,028 captions before any attribution was trusted). For every (cluster, keyword) it emits: corpus firing count, % of cluster, **sole-trigger count** (exact predicted firing delta on removal — captions where that keyword is the cluster's only trigger), the **raw-form inventory** (every distinct caption word that stems to the keyword, for collision detection), a **reachability sweep** (inflections that stem to something NOT in the keyword set — the #96 `raise`→`rais` class), and non-`none` thematicV2 enrichment vs the 2.4% non-role base rate. Above-threshold keywords (≥10 firings OR ≥5% cluster share OR ≥10 sole-trigger; 211 keywords) got up to 15 sampled caption sentences each, reviewed genuine-vs-artifact; `hold` got an enlarged 45-sentence review. **A consolidated verdict table (all 30 clusters) was signed off before any edit landed.** **Two matching-mechanics facts drove the findings:** (1) matching is exact set-membership of *stemmed* caption tokens against *raw* keyword strings, so a keyword only fires if it equals the stemmed form of a caption word — this creates both *collisions* (unrelated words landing on a keyword: `signal`→`sign`, `sweater`→`sweat`, `number`→`numb`, `larger`→`larg`) and *reachability gaps* (a keyword that can't match its own inflections: `gentl` never matched "gently" which stems to `gent`; `anticipat`/`solitud`/`solitary`/`empty`/`toddler`/`vulnerable` couldn't match their own base words). (2) The meaningful-tier gate in `weightedDice` is a per-cluster threshold (`contains(where: weight ≥ 0.75)`), NOT a sum — so demoting a cluster to 0.2 removes it from the gate without any ambient-sum risk. **Landed (one commit per cluster, #48/#92/#116/#96 discipline; each verified: exact predicted firing delta via harness re-run on the identical corpus JSON, zero collateral deltas in other clusters, 107 tests green, golden pairs G4/G6/G7/G16 `matchedClusters` recomputed and undisturbed):** 62 collision removals across 21 clusters + ~30 reachability fixes. Worst offenders cut: `waiting_anticipation` 755→76 (removed the entire look/gaze/observ/watch vocabulary that duplicated `looking_watching` and fired "waiting" on nearly every portrait), `confinement_freedom` 550→41 (12 removals — `behind` 63% bare preposition, `wall`/`open`/`wide`/`fence`/`door` scenery), `tenderness_care` 662→133 (`hold` 85% instrumental object-holding, `lean`/`shield`), `devotion_belief` 373→114 (`sign` 65%, `cross` 23%, `banner` — `flag` KEPT for 18% enrichment in a protest-heavy library), `stillness_rest` 590→285 (`rest`/`relax`/`calm` posture-idiom + captioner mood-boilerplate), `youth_age` 415→174 (`small` 63% size adjective), `labor_effort` 273→96 (`build` 53% background architecture — largest single collision found). Reachability adds *raised* genuine firings where the cluster had been blind: `joy_celebration` 292→330 (`celebr`/bare `smile`/bare `dance`), `movement_energy` 86→95 (`dynamic`/`runn`), `humor_absurdity` 12→17 (`humorou`). **Corpus-wide total firings 7,753 → 4,374 (−44%)** — the bulk is over-broad keyword artifacts removed, with genuine coverage preserved or improved. **Tier reconsideration:** `bodily_gesture` demoted 0.75→0.2 (ambient tier, à la #47's `animal_presence`) — it fires on 94% of the corpus (`hand` alone 85%) with ~3% enrichment ≈ base, so two people both gesturing is expected coincidence, not resonance; it keeps its Dice-denominator role but no longer clears the meaningful-tier gate, so a bodily_gesture pair must also share a genuine emotional cluster to score above the ambient floor (not in `axisPairs`, so no bonus interaction). `looking_watching` (39% post-edit) considered for demotion but **kept at 0.75** per Andre — it's a genuine attention register, not mere setting. `devotion_belief`/`tenderness_care`/`confinement_freedom`/`grief_sorrow` kept at their tiers: post-cleanup they're rare-but-correct (~114/133/41/6 firings), which is the tier-1.0 design intent, not grounds for demotion. **Phase 0 (landed first):** fixed the deferred #48/#118 sibling — both stored-at-index-time rationale sites in `PairScorer.swift` (`meaningfulShared.first` in the `score()` block, `shared.first!` in `captionRationale`) now use the deterministic `ConceptClusters.representativeCluster`, so the DB-stored rationale can no longer disagree with the lightbox's live recompute across app relaunches; a stability test was added. **Deliberately NOT done:** adding `contempl`/`introspective`/`peace`/`festive`/`speak` (captioner mood-summary boilerplate — would recreate the calm/relax problem), `blurr` (depth-of-field, not motion), `settl`→`sett` (would match 490 "setting" captions), `ears`→sound_music (8/13 animal ears); ambient-tier (0.2) clusters were left unedited since their collisions are structurally contained below the meaningful gate — **with one post-re-index exception:** `public` removed from `community_gathering` (122/504 firings, 50 sole-trigger — "public space"/"public transportation stop"/"public expression") after Andre's re-index surfaced it as a visibly wrong tag on a lone couple at a bus stop; 504→454, harness-verified. **Residual mixed keyword noted, not fixed:** `rais` in `joy_celebration` (from #96's raise→rais fix) also catches non-celebratory raised arms (a megaphone speaker's raised hand), so a protest speaker can fire `joy_celebration` — genuinely mixed (raised arms ARE often celebratory), left as-is pending a clearer signal. Follow-ups from this re-index review opened as backlog #121 (caption accuracy log), #122 (role-pair third-meaning precision), #123 (quiet-photo surfacing). **Verification note:** all deltas confirmed exact against the production-DB-copy harness; `matchedClusters` is a pure function of the caption, so these edits change stored pair *scores* only on Andre's next re-index (~14 min, he runs it) — the DECISIONS/CLAUDE close-out stays **amendable pending his post-merge re-index spot check**, which is the real-world acceptance test #96 failed. See `diagnostics/120-keyword-audit-harness.swift`, KNOWN_GOOD_PAIRS.md, decisions #47/#48/#96/#118. Dated 2026-07-08. |
| Event-proximity thematic discount — same-event taxonomic pairs (#122, PARTIAL 2026-07-09) | **Task:** backlog #122 — the ThematicV2 `validate()`/`score()` judge confirms taxonomic / same-event pairs (two skate tricks from one session, two protesters holding signs) at 0.75–0.95, failing PAIRING_THEORY's third-meaning test. Design direction (settled 2026-07-10): *discount, don't reject* — same-event most, cross-event same-category less, no category overlap unaffected. **Diagnosis (read-only, against a production-DB copy):** (1) the existing `temporalPenalty` is a burst-scale (≤300s) whole-composite guard that **cliffs to 1.0 at 300s** — the two skateboard pairs sit at 567s / 3261s, just past the cliff, so they leak through at full score; a new event-scale signal is justified and not redundant. (2) The inflation is **entirely on the thematic (V2) axis** — aesthetic (0.35–0.48) and geometric (0.41–0.63) are legitimately moderate; the high `axisScore` is driven by `effectiveThematic = thematicV2Score` (0.75–0.95). So the discount belongs on `effectiveThematic` at the display layer (where the #82 V2 override already lives), *not* on the whole composite (that would wrongly punish aesthetic/geometric and worsen #123). (3) **The two bad-pair classes are fundamentally different and only one is safely fixable.** *Same-event* (the 2 skateboard pairs, 9.5 min / 54 min apart) is cleanly separable by temporal proximity. *Cross-event same-category* (the 5 protest + 1 pet pairs, 32–1796 days apart) is **not separable by any cheap signal**: temporal can't distinguish them from good cross-event pairs (both 30–1800d — G7=150d, G5=119d, G8=348d…), and the `ConceptClusters` "shared meaningful cluster" signal (measured over all 973 hero captions) **fails in both directions** — it misses 4 of 6 cross-event bad pairs (they share no meaningful cluster; "protest sign" content was de-collided out of clusters in #96/#120) *and* false-fires on golden pairs G6/G13/G15 (which share devotion_belief/joy_celebration despite being genuine Mode-1 pairs). Building a cross-event category discount on clusters would damage golden pairs while missing most targets — the only thing that "knows" the protests are taxonomic is the LLM judge itself, so their correct lever is the judge rubric, not a proximity discount. (4) **Safety margin is enormous:** nearest KNOWN_GOOD_PAIRS golden pair by capture time is G16 at 21.2 days; every other is 38d–7.8y. The same-event window (minutes–hours) contains zero golden pairs. **Fix (primary lever):** `eventProximityThematicFactor(captureDateA:captureDateB:captionA:captionB:)` in `ConjunctEngine/Scoring/EventProximity.swift` (pure, unit-tested) returns a multiplier on `effectiveThematic`: temporal guard first (`300s < gap ≤ 6h`, else 1.0), then a category gate (`ConceptClusters.sharesMeaningfulCluster` — ≥1 shared ≥0.75-weight cluster; ambient-tier excluded), then graduated 0.70 (300s–30min) / 0.80 (30min–2h) / 0.90 (2h–6h). Called from BOTH synced display sites (`PairHelpers.convertToPairFree`, `EngineController.convertToPair`) via the one shared engine function so they can't diverge (cf. the #26/#85/#86 temporalPenalty sync gotcha). The temporal guard short-circuits before caption parsing, so the cluster match only fires for the small in-window population. **Verified (production-DB-copy replay of the display-layer axisScore, weights 0.40/0.20/0.40):** P7 skateboard axis 0.792→0.589 (rank 156→1987), P8 0.774→0.639 (rank 167→874) — both drop out of top ranks but survive (discount, not reject); all 5 cross-event protest pairs + pet pair correctly unchanged (factor 1.00, out of window); **every golden pair unchanged (factor 1.00 — zero golden pairs discounted)**; blast radius 1,356 of 118,931 pairs (1.1%), the targeted same-event same-category population. 117/117 ConjunctEngine tests green (10 new `EventProximityTests`); Xcode app build green. **PARTIAL close:** the primary lever ships and fixes the mandated-minimum skateboard pairs safely. The cross-event same-category class (5 protest pairs) is **not addressable via proximity+cluster** per the diagnosis — deferred to the secondary rubric lever (a `validate()`/`score()` clause requiring any claimed contrast to be traceable to caption text), opened as backlog #124 because validating an LLM-behavior change requires a full ThematicV2 re-run (~45 min, non-deterministic) out of this session's scope. Harnesses: `diagnostics/122-category-signal-harness.swift`, `diagnostics/122-rescore-validation-harness.swift`. See PAIRING_THEORY.md §Taxonomy of Bad Pairs, decisions #26/#82/#85/#86, backlog #122/#123/#124. Dated 2026-07-09. |

| Third-meaning rubric in the ThematicV2 judge — cross-event taxonomic pairs (#124, 2026-07-09) | **Task:** backlog #124 (split from #122) — the `qwen2.5:14b-instruct` judge confirmed cross-event same-category pairs at 0.75–0.95, either restating shared content ("people with pets") or **fabricating a contrast neither caption supports** ("real vs. theatrical", "nuanced discussion vs. unified protest"). #122's diagnosis showed these are unreachable by temporal/cluster signals — only the judge itself can tell taxonomy from resonance. **Phase-1 diagnosis (production-DB copy, bench-swap harness replaying the real `score()`/`validate()`; temp-0 baseline reproduced all 28 curated verdicts exactly):** the failure has THREE mechanisms, not one. (M1) **False-premise hypotheses rubber-stamped** — 3 of 5 protest targets (105/220, 339/670, 363/695) carry a join-3 hypothesis "a sign appears REAL in one image and as a depicted version in the other" that is factually wrong: role extraction registers printed sign text/graphics as `depicted` objects, join 3 matches the register flip, and the judge never checks the premise — it confirms and *elaborates* it into "real protest vs. staged depiction." (M2) **Definitional embodiment scored as ironic** — 18/126, 486/804: a sign claims "protest"/"solidarity", the other image "embodies" it — but both images are protests, where the claimed idea is inherent (vs. golden G6/G7 where a smile-sign's embodiment crosses contexts). (M3) **Cold-path relational fabrication** — the pets pair (238/811, 0.75) and both skateboard pairs (729/1009, 278/643, 0.89): the model invents a source-receiver story ("one action leads to the next") for shared-category content. **Fix (7 prompt iterations against the harness; each rebuilt + re-run, ~2 min/iteration):** `kValidatePrompt` restructured into ordered checks — CHECK 1 verifies every FACTUAL claim of the hypothesis against captions (real-vs-depicted needs an actual depiction in a caption; sign text is not one) while keeping the proposed LABEL permissive (facts strict, label lenient — v4 showed conflating these re-admits fabrications); CHECK 2 protects genuine **text-vs-world** pairs (a sign stating an idea + an unrelated image living it is cross-register, embodiment alone suffices, contradiction NOT required — without this clause the model's lexical prior for "ironic" rejected golden G6/G7 as "aligns rather than contradicts"); CHECK 3 applies the restatement test, requires the link to run through MAIN subjects (not an incidental flag in two political scenes — kills 267/384-class pairs), and keeps the superficial-link rejections. `kSystemPrompt` (cold) gains the third-meaning test + evidence rule, a leading `"together"` chain-of-thought JSON field (write RESTATEMENT → reject; the parser ignores unknown fields; this was the only lever that killed the pets pair — prose self-checks alone did not), a **cross-the-pair** requirement on `complementary` (each image containing its own self-contained caretaker↔animal relation is a category), and a SAME-subject requirement on `contrastive`. Iteration history worth keeping: v1 (evidence rule + restatement alone) over-fired — killed G6/G7 and every embodiment pair; v3's "together" field flipped a previously-rejected cold pair (37/135) to confirmed until the field description itself became the gate (v7). **Design constraint honored:** same-category pairs are NOT rejected as a class — the prompts state "a shared category does not by itself disqualify a pair" and the harness's category-positive goldens (G4 real-vs-mural birds) still confirm. Restatement-only pairs are rejected outright (0), not soft-scored (Andre's sign-off); a rejected role hypothesis still falls back to cluster `thematicScore` in `effectiveThematic` (#102), so rejection never zeroes an otherwise-good pair. **Verified (28-case harness, temp-0):** all 8 targets reject with honest category/premise rationales (zero fabrication); goldens G4/G6/G7/G16 at 0.95/0.95/1.00/0.95; all 4 previously-rejected sample pairs stay rejected. **Full precision sweep (all 399 previously-confirmed pairs from the production-DB copy re-judged under the new rubric):** all 399 previously-confirmed pairs re-judged: **75 survive / 324 drop (81%)** — validate path 71/368, cold 31→4; zero parse failures; survivor scores essentially unchanged (mean Δ +0.01; types: 28 contrastive / 24 ironic / 18 complementary / 5 echo). Drop-rationale breakdown (keyword classification): ~45% restatement/category, ~33% hypothesis premise failure, ~6% superficial-link, ~16% other (mostly embodiment-does-not-hold and premise variants). The drop size reflects how permissive the old `validate()` was — it confirmed 55% of all role hypotheses, many riding the M1 fabricated real-vs-depicted premise. The full survive/drop list with baseline vs. new verdict + rationale is the human-review artifact: `diagnostics/124-confirmed-rejudge-results.tsv`. Known marginal class for that review: smile-poster embodiment hub pairs beyond the goldens can still drop with "does not contradict" reasoning (e.g. 4/874, a sibling of golden G6 80/874 which survives) — the text-vs-world clause protects the canonical cases but the model see-saws on hub siblings; these are the first recall-tuning candidates if Andre's review finds the drop too aggressive. 117/117 ConjunctEngine tests green; Xcode app build green. **Realization caveat:** prompt changes only take effect when the pass re-judges — existing DB verdicts stand until re-judged (null `thematicV2Score` or re-index, then re-run the V2 pass). The stored `thematicV2Rationale` of surviving pairs also refreshes only then. The M1 root cause (extraction registering sign text as `depicted` objects, feeding join 3 false premises) is upstream of the judge and remains — the judge now catches it, but join-3 candidate quality is an extraction issue worth watching. Harness preserved as `diagnostics/124-judge-rubric-harness.swift` + `diagnostics/124-judge-rubric-cases.json`. See PAIRING_THEORY.md §Third Meaning Test / §Taxonomy of Bad Pairs, decisions #102/#122, KNOWN_GOOD_PAIRS.md. Dated 2026-07-09. |


## Backlog

Open backlog items are tracked in [CLAUDE.md](CLAUDE.md#open-backlog). Completed items are in the Closed Backlog section of the same file. Individual decision entries in the log above are the authoritative record of what was built and why.
